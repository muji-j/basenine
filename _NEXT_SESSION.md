# 다음 세션 인계 (2026-09-03 · 지침 정합성 복구 **완료**)

## 0. ⚠먼저 — 메인 체크아웃의 `_NEXT_SESSION.md` 는 전제가 반대다 (아직 그대로 있다)

메인 체크아웃(`bb-app/` 루트 · 로컬 `main` 소속)의 `_NEXT_SESSION.md` 는 이렇게 적는다:

> ⚠워크트리에 **정리 전** 273행 `CLAUDE.md` 가 남아 있다. 그 브랜치를 살릴 계획이면
> 머지할 때 **현재 `main` 에 맞춰라**.

**그 방향이 반대다.** 로컬 `main`(07cf86b)은 `8647cb6`(**2026-08-15 14:31**)에서 갈라진 가지이고
`origin/main` 대비 **ahead 3 / behind 549**, 그 549 커밋이 **365파일 · +94,646행**이다.
그래서 그쪽 `CLAUDE.md` 에는 8월 하순 실측이 없다 — `Cache-Control` **0건** ·
`BB_ALL_SEASONS` **0건** · `2026-08-2*` 날짜 **0건**.

**사용자 결정: 「최신 판 위에 정리를 다시 적용」.** 이 브랜치가 그 결과다.
⚠**메인 체크아웃의 낡은 판을 어떻게 할지는 아직 사용자에게 확인받지 않았다.**

---

## 1. 이 브랜치 (`guidelines-realign` · 기준 `origin/main` = f5f9120)

**PR #90** — https://github.com/muji-j/bb-app/pull/90 (push 완료 · 머지 전)

⚠**push 하려면 `gh` 활성 계정을 `muji-j` 로 바꿔야 한다.** 원격 소유자가 `muji-j` 인데
기본 활성이 `jwlee-muji` 라 그대로 밀면 **「Repository not found」**(비공개 저장소라 404)가 난다.
**전환 → push → 되돌리기**로 처리했다(순 변화 0). ⚠**계정 차이는 실수가 아니라 설계다 — 통일하지 마라.**

| 커밋 | 내용 |
|---|---|
| `8381799` | 감사 스킬 로컬 사본 제거(5파일) |
| `32fbe45` | 에이전트 5종 제거 + 11종 루트 참조 정리 |
| `a8a50a9` | 중단 지점 인계(이 파일의 앞 판) |
| `fe0ffd3` | **`CLAUDE.md` 재정리** — 낡은 사실 8건 + 루트 중복 제거 |
| `8fccb74` | **에이전트 낡은 사실 7건** |
| `920c65f` | 루트의 3중 검토 층 반영 + 「공통 8종」 숫자 제거 |
| `5ee83e6` | 인계를 결과판으로 재작성 |
| `3c7f54a` | **RO 3종에 `Write` 명시** + §0 제약2 판정 기준을 이름에서 풀기 |

**검증**: `npm test` **2,123본 중 2,123본 통과**(fail 0 · ERROR 0 · skipped 0 · exit 0) ·
`npm run typecheck` exit 0 · 마지막 문서 수정 후 `doc-figures` **14본 중 14본**.

### 조사 방법 (재현하려면)

울트라코드 2라운드. **1차** 6축 전수 조사(39에이전트) → 발견 **61건** · 반증 판정 33건 →
**26 통과 / 7 기각(21%)**. **2차** 상한에 걸렸던 19건 반증(19에이전트) → **12 통과 / 7 기각(37%)**.
전문은 저널에 남아 있다:
```
~/.claude/projects/C--Users-...-npb-foundation/047b805b-.../subagents/workflows/
  wf_94ffae69-bf3/journal.jsonl   (1차)
  wf_3ac38c51-cbf/journal.jsonl   (2차)
```
스크립트: 같은 세션 디렉터리의 `workflows/scripts/guidelines-staleness-*.js`

---

## 2. ⚠기각된 것 — **고치지 마라** (같은 지적이 또 올라온다)

반증이 **14건을 기각**했다. 그중 다음은 「결론이 그럴듯한데 사유가 틀린」 것들이라
다음 사람이 또 고치려 들 가능성이 높다.

| 대상 | 왜 고치면 안 되나 |
|---|---|
| **§3 작업 규칙 12항** | 루트와 겹치지만 **다른 문서가 「작업규칙 N」으로 인용하는 주소 공간**이다(`packages/parser/test/fixtures/README.md` 가 실제로 그렇게 가리킨다). 지우려면 인용하는 쪽을 먼저 옮겨라 |
| **§6 의 「(작업규칙 11)」 류 불릿** | 자백이 아니라 **금지 색인**이다. §6 불릿 21개 중 **6개**가 같은 형식이고, 기준을 일관 적용하면 M6 의 취소선 정정까지 지워야 한다 |
| `design-graphics-engineer:23` **「라이브 중 값」** | 스키마에 **`status IN ('final','live')`** 가 실재하고 주석이 「v2 라이브가 DDL 변경 없이 얹히게」라고 적는다. 지우면 M9 의 근거를 지운다 |
| `senior-engineer:7` **「주니어가 막힌 작업」** | 에이전트 이름이 아니라 **보통명사**다(`od -c` 실측 · 백틱 없음). 같은 파일은 에이전트를 지목할 때 백틱을 친다 |
| `i18n-engineer:28` | 「키 체계 0건」이 **거짓**이다 — `glossary.ts` 가 키 53개를 시험 4본으로 고정한다 |
| `data-source-scout:43` 라이브 지연 | 「이미 끝난 조사」가 아니라 **「일부러 재지 않기로 한 것」**이다(결정문 원문) |
| `design-auditor:43` `contrast_sweep.py` | exit 2 인 것은 맞지만 ⑴ 도구는 `--target`·`SHIRO_AUDIT_TARGET` 도 본다 ⑵ 명령부는 「기계로 잴 수 있는 것은 재라」이고 도구는 **「~ 등」 예시**다 ⑶ 전제조건이 이미 **세 곳**에 있어 네 번째 사본은 부트스트랩한 순간 거짓이 된다 |

⚠**맨이름 문제는 반대로 뒤집혔다.** 1차 반증이 「roster 가 인정한 별칭」이라며 기각했지만,
roster 를 직접 읽으니 그 표는 **「별칭 매핑 (기존 프로젝트에서 옮겨올 때)」**이고 열이
**「기존 이름들 | 처리」**이며 값이 **「→ `shiro-core:X`」**다 — **마이그레이션 지시표이지 허용표가 아니다.**
그래서 §0-8·§4 에 접두사를 붙였다. **반증도 틀릴 수 있다 — 1차 출처를 직접 읽어라.**

---

## 3. 남은 일

1. **PR #90 머지** — 사용자 결정. 그 뒤 이 브랜치를 정리한다.
2. **메인 체크아웃의 낡은 `_NEXT_SESSION.md`** 와 549 커밋 뒤처진 로컬 `main`.
   ⚠**PR 을 머지해도 그 낡은 판은 대체되지 않는다** — 파일이 서로 다른 가지에 있다.
   사용자가 「지금은 둘다」로 보류했다(2026-09-03).
3. **DeepSWE 원수치를 `_common/docs/agent-roster.md` 로 옮기기** — 지금 이 저장소 `CLAUDE.md` §4 에만
   있고 루트 §4 는 규칙·사유만 담는다. ⚠**`_common/` 은 상위 자산이라 읽기 전용**이므로(루트 §10)
   **별도 작업**이다. 여기서 할 수 없다.
4. **`.claude/audit/` 오버레이** — 넓은 감사를 하려면 먼저 `shiro-core:project-bootstrap`.
   ⚠**D8 은 정본이 있을 때만 만든다**(§5 에 사유를 적어 뒀다). 지금 감사 계획이 없으면 만들지 마라 —
   지원 작업이 제품 작업보다 커지지 않게 한다(루트 §4).
5. **`.claude/settings.json` 의 `permissions.allow`** — 로컬 `main` 정리는 이것을 지웠지만
   **옮기지 않았다.** 유저 레벨에 대체가 없어 중복 제거가 아니고, 지우면 편의만 잃는다.

### ✅ 처리 완료 (앞 판에서 「남은 일」이었던 것)

- **push·PR** → PR #90.
- **§0 제약2 의 떠 있던 판정 기준** → 「ADR-001에」를 **「인프라 결정문(`docs/decisions/`)에」로 풀었다.**
  ⚠약화가 아니라 **이름에서 푼 것**이다 — 요구(3섹션 · 월 추정액 · 상한 초과안은 참고로만)는 그대로다.
- **RO 에이전트의 쓰기 권한** → 명단을 전수로 다시 재서 **RO 5종 중 3종**(`data-source-scout` ·
  `planning-architect` · `sabermetrics-analyst`)에 `Write` 를 명시했다.
  ⚠**권한을 넓힌 게 아니다** — `Bash` 가 있어 `cat >` 로 원래부터 쓸 수 있었고, `Edit` 를 빼도
  `Write` 로 덮어쓸 수 있다. **「RO 니까 파일을 못 만든다」는 한 번도 참이 아니었다.**
  경계는 도구가 아니라 §4 의 문장이 지킨다.

---

## 4. 이 작업에서 배운 것 (다음에도 쓴다)

- **문서가 주장하는 것을 코드·git·설정으로 재라.** 이번에 나온 가장 무거운 결함
  (§1 「미확정 · 선택 전 구현 착수 금지」인데 이미 배포 중)은 **읽기로는 안 잡히고 실측이 잡았다.**
- **반증을 붙여라.** 조사 발견의 **21%·37%가 기각**됐다. 반증 없이 반영했으면
  「라이브 중 값」·「주니어」·「키 체계 0건」을 잘못 고쳤을 것이다.
- ⚠**반증도 틀린다.** 맨이름 건은 반증이 기각했지만 1차 출처(roster)를 직접 읽어 뒤집었다.
- **자기 수정을 다시 읽어라.** 이번에도 내가 만든 결함 2건이 그 단계에서 나왔다
  (①「전 이력 0건」을 남의 실측으로 적음 ②DeepSWE 원수치를 지워 워크스페이스에서 소실시킴).
- ⚠**숫자를 문서에 박지 마라.** 「공통 8종」이 **같은 날 9종이 됐다.**
- ⚠**캐시 디렉터리를 「무엇이 있는가」의 근거로 쓰지 마라** — 소스가 정본이다.
