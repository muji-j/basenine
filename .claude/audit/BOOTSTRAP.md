# 부트스트랩 게이트 — bb-app

```
BOOTSTRAP_GATE=PASS
날짜: 2026-09-04
스킬: shiro-core:project-bootstrap
```

## 무엇을 만들었나

| 파일 | 줄수 | 요약 |
|---|---|---|
| `.claude/audit/target.json` | 106 | 감사 단위 13 · dev-server 렌더 · 위험어 15(`ja`) · 제외/적용불가 목록 |
| `.claude/audit/dimensions.local.md` | 175 | D8 5축 + D1~D7 구체값 + 자동 스크린 + 미확인 슬롯 |

## 무엇을 **만들지 않았나** (범위를 좁힌 것)

- **`CLAUDE.md`** — 이미 있고 **2026-09-03~04 에 정리를 마쳤다.** 스킬 0단계 규칙(덮어쓰기 금지)과
  사용자 지시 양쪽에 따라 **건드리지 않았다.** `.proposed` 초안도 만들지 않았다 — 대체할 이유가 없다.
- **`AGENTS.md`** — **없다.** 스킬은 「Codex 가 루트를 상속하지 않으므로 만들라」고 하지만
  이번 작업은 **오버레이 생성으로 범위가 제한**돼 있었다. ⚠**남은 일이다**(만들지 않은 것이지 불필요한 것이 아니다).
- **프로젝트 특화 에이전트** — 새로 만들지 않았다. 이미 11종이 있고 같은 세션에서 정리를 마쳤다.
  ⚠공통 9종과 겹치는 것은 이미 제거됐다(16종 → 11종).

## 게이트 확인 방법 (실측)

**`target.json`**
- JSON 파싱 통과 — `units` 13 · `danger.words` 15 · `danger.lang` `ja` · `render.mode` `dev-server`
- ⚠**도구가 그 값으로 실제로 UI 를 띄웠다** — 이것이 「파일이 있다」와 다른 유일한 증거다.

**대비 스윕 1회 실행**(`contrast_sweep.py` · `npm run preview` 로 서버를 띄운 상태 · `index.html` **HTTP 200** 확인)

도구가 스스로 찍은 것:
- `target.json` 경로와 **리포 루트를 정확히 해석**
- 화면 **13개** 인식 · 테마 **light/dark** 둘 다
- 파괴어 가드 배너 `lang=ja · 어휘 15개 · 대상외 셀렉터 4종`
- **상태 마커 7종** — 내장 6종에 우리가 넣은 `.on` 이 **실제로 더해졌다**
- 조사량이 **0 이 아니다**: `index` 픽셀 실측 **1,872** · `ranking` **4,706** · `players` **59,216** ·
  `starters` **20,160** · `matchup` **1,654** · `compare` **1,632** · `today` **640** · `teams` **916** · `days` **1,082**

→ **「0건」이 아니라 실측**이다. 오버레이가 읽히고 도구가 이 프로젝트의 화면을 실제로 열었다.

## ⚠미확인으로 남긴 것 (비워 두면 다음 라운드가 커버리지를 오판한다)

| 무엇 | 왜 확인 못 했나 |
|---|---|
| 화면 **4개**(`postseason` · `glossary` · `log` · `players/01005157`)의 조사량 | 도구는 13개를 다 쟀지만 **실행할 때 출력을 `head -25` 로 잘랐다.** 도구 탓이 아니라 측정자 탓이다. 다음 실행에서 전체 출력을 받아라 |
| **대비 위반 건수** | 조사량만 확인했고 **위반 목록은 읽지 않았다.** 이건 게이트가 아니라 **첫 감사의 일**이다 |
| `forced_colors_sweep.py` · `state_driver.py` | **돌리지 않았다.** 대비 스윕 하나가 통과했으므로 `target.json` 해석 경로는 공유되지만, **그 둘이 돈다는 증거는 없다** |
| `selectors.dataColor` · `selectors.floating` | 셀렉터를 실측하지 않아 **빈 배열**이다. ⚠비어 있으면 강제 배색·부유면 검사가 **0회**가 된다 — 첫 감사에서 채워라 |
| **과거 시즌 화면** | `units` 는 최신 시즌 12화면 + 선수 1장뿐이다. `dist/2018/` 등은 **측정 범위 밖**이다 |
| 에이전트 리로드 검증 | 이 부트스트랩은 **에이전트를 만들지 않았다.** 같은 세션에서 7개를 고쳤지만 그건 이 게이트의 대상이 아니다 |

## ⚠다음 감사 전에 반드시 할 것

1. **`npm run build:web` 으로 `dist` 를 다시 만든다.** 이번 측정은 **기존 `dist`** 를 썼고 그것이 최신이라는
   보장이 없다. 재생성 전 측정은 「고쳤는데 안 고쳐졌다」의 전형적 오독을 만든다.
2. **`npm run preview` 로 서버를 띄운다.** ⚠**`file://` 로 열지 마라** — 색인 `fetch` 가 막혀
   **검색이 죽은 화면**을 재게 된다(`scripts/preview.ts` 머리말).
3. **D8 지적에는 정본 인용을 붙인다** — `docs/metrics/` 의 검증 케이스나 NPB 공식 규칙.
   인용 없는 D8 지적은 그 자체가 결함이다(오버레이 D8 절).
