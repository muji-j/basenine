/**
 * 용어집 — 화면의 지표 이름에 붙는 설명.
 *
 * ⚠**정의는 여기 한 벌만 둔다**(M1). 같은 지표를 화면마다 다르게 설명하면
 * 어느 쪽이 맞는지 알 수 없고, 통계 서비스에서 그건 값이 틀린 것과 같다.
 *
 * ⚠**「무엇을 재지 않는가」를 함께 쓴다.** 지표의 오독은 대개 「이건 그것도 재는 줄 알았다」에서
 * 나온다. 짧게라도 경계를 적는다.
 *
 * 문장은 일본어다(제품 UI 언어 · CLAUDE.md §7).
 */

export interface Term {
  /** 화면에 나오는 표기 */
  label: string;
  /** 한 줄 설명 */
  short: string;
  /** 계산식이나 분모. 없으면 생략 */
  how?: string;
  /**
   * **값 옆에 찍는 분모의 단위**(`414出塁機会`). ⚠**여기가 그 단위의 정본이다**(M1).
   *
   * ⚠**단위는 화면이 정하는 것이 아니라 지표가 정한다.** 화면마다 문자열을 적었더니
   * 같은 지표가 화면에 따라 다른 단위로 나갔다(2026-08-20 감사 P2 · 배포물 실측):
   * BABIP 는 선수 페이지가 `打球`·`compare/*.json` 이 `打数` 로 **342명** 어긋났고,
   * SRP 는 `対戦打者` 대 `打者` 로 **356명** 어긋났다. 값은 전 항목 일치였다 —
   * **틀린 것이 말뿐이라 타입도 린트도 못 잡았다.**
   *
   * ⚠**`how` 바로 옆에 둔다.** 분모의 이름과 분모의 정의가 떨어져 있으면 또 갈린다.
   * 비율인데 이 값이 없으면 `denUnit()` 이 **던진다** — 순서(`metric-order.ts`)와 달리
   * 분모는 장식이 아니라 M2 그 자체라 조용히 넘어가면 안 된다.
   */
  den?: string;
  /** ⚠오독을 막는 한마디 */
  caveat?: string;
}

/**
 * ⚠**키는 화면 라벨과 1:1로 이어진다.** 라벨을 바꾸면 여기도 바꾼다 —
 * 어긋나면 툴팁이 조용히 안 뜬다(테스트가 이를 고정한다).
 */
export const GLOSSARY: Readonly<Record<string, Term>> = {
  avg: {
    label: "打率",
    short: "安打の出る割合。もっとも古くからある指標です。",
    how: "安打 ÷ 打数（四死球・犠打は打数に入りません）",
    den: "打数",
    caveat: "四球で出塁しても上がりません。出塁の全体像は出塁率を見てください。",
  },
  obp: {
    label: "出塁率",
    short: "アウトにならずに出塁した割合。",
    how: "(安打+四球+死球) ÷ (打数+四球+死球+犠飛)",
    /**
     * ⚠**打席ではない.** 分母は打席から犠打を引いた数だ。
     * 実測(2026-08-20 · dist/players/41445153.html 中野拓夢): 打席 434 · 犠打 20 · 出塁率の分母 414。
     */
    den: "出塁機会",
    caveat:
      "母数の「出塁機会」は 打数+四球+死球+犠飛 です — 打席から犠打を引いた数なので、" +
      "同じ行の打席数より少なくなります。打率より得点との関係が強い指標です。",
  },
  slg: {
    label: "長打率",
    short: "1打数あたりに稼いだ塁打の数。",
    how: "塁打 ÷ 打数（単打1・二塁打2・三塁打3・本塁打4）",
    den: "打数",
    caveat: "「率」という名前ですが割合ではありません。最大4.000です。",
  },
  ops: {
    label: "OPS",
    short: "出塁率と長打率を足したもの。打撃の総合力の目安です。",
    how: "出塁率 + 長打率",
    /** ⚠**여기는 정말 打席이다** — `ops()` 가 `denominator: line.pa` 를 낸다(metrics/batting.ts) */
    den: "打席",
    caveat:
      "分母の違う2つを足しているので厳密な指標ではありませんが、簡便で広く使われます。" +
      "母数には打席数を載せています（出塁率の母数とは別の数です）。",
  },
  woba: {
    label: "wOBA",
    short: "打席の結果ごとに得点への貢献度で重みをつけた出塁率。",
    how: "本塁打2.065・三塁打1.725・二塁打1.334・単打0.865・四球0.692 などで加重（1.02 の NPB 用係数）",
    /** ⚠**出塁機会からさらに敬遠を引く** — `wobaDenominator()`(metrics/woba.ts)가 정본이다 */
    den: "wOBA機会",
    caveat:
      "母数の「wOBA機会」は 打数+四球−敬遠+死球+犠飛 です — 出塁機会から敬遠を引いた数で、" +
      "打席数とも出塁機会とも違います。出塁率と同じ目盛りで読めます。.320 前後がリーグ平均です。",
  },
  wrcPlus: {
    label: "wRC+",
    short: "打撃でどれだけ得点を生んだかを、リーグ平均100として指数化したもの。",
    how: "wRAA/打席 と リーグの打席あたり得点 から算出",
    den: "打席",
    caveat: "⚠球場補正は入れていません（当サイトはまだ球場係数を出していません）。",
  },
  wraa: {
    label: "wRAA",
    short: "リーグ平均の打者と比べて、何点多く（少なく）生んだか。",
    how: "(wOBA − リーグwOBA) ÷ 1.24 × 打席",
    den: "打席",
    caveat: "0が平均です。打席数が多いほど絶対値は大きくなります。",
  },
  src: {
    label: "SRC",
    short: "状況得点貢献。打席ごとに得点期待値をどれだけ動かしたかの合計です。",
    how: "Σ(打席後の得点期待値 − 打席前 + その打席の得点)",
    den: "打席",
    caveat:
      "⚠守備とポジション補正は含みません。WARではなく、WARと比較できません。" +
      "⚠その打席の「間に」起きた走塁（暴投・盗塁など）で入った点も含まれます — " +
      "元の記録が打席単位でしか公表されておらず、打撃と走塁を分けられないためです。",
  },
  srcPer600: {
    label: "SRC/600",
    short: "SRC を600打席あたりに換算したもの。打席数の違う選手を並べるときに使います。",
    den: "打席",
    caveat: "打席が少ないほど換算の振れ幅が大きくなります。",
  },
  caughtStealing: {
    label: "盗塁刺",
    short: "盗塁を試みてアウトになった回数。",
    caveat:
      "⚠**牽制でアウトになった分は含みません。** NPBの記録では牽制死は盗塁刺ではないためです。" +
      "当サイトは牽制死を別に数えて表示しています。",
  },
  stealRate: {
    label: "盗塁成功率",
    short: "盗塁の企図のうち、成功した割合。",
    how: "盗塁 ÷ (盗塁 + 盗塁刺)",
    // ⚠**牽制死は入らない**(NPB の記録で盗塁刺と別) — 入れると全員の成功率が下がる
    den: "企図",
    caveat:
      "⚠企図が0のときは値を出しません（「走らなかった」と「走って全部失敗した」は違う事実です）。" +
      "⚠この2つは試合経過（打席ログ）から数えた値です。公表値との照合では" +
      "2026年レギュラーシーズンで盗塁611・盗塁刺254が一致しています。",
  },
  pickoff: {
    label: "牽制死",
    short: "牽制球でアウトになった回数。",
    caveat: "⚠盗塁成功率の分母には入れていません（NPBの記録で盗塁刺と別のためです）。",
  },
  hitStreak: {
    label: "連続安打",
    short: "安打を打った試合が何試合続いているか。",
    caveat: "⚠打席のなかった試合（代走・守備固めだけ）は数えません。数えると記録が理不尽に途切れるためです。",
  },
  onBaseStreak: {
    label: "連続出塁",
    short: "安打・四死球のどれかで出塁した試合が何試合続いているか。",
    caveat: "連続安打より途切れにくく、選球眼のある打者ほど長くなります。",
  },
  hitlessStreak: {
    label: "連続無安打",
    short: "安打の出ていない試合が何試合続いているか。",
    caveat: "⚠良い記録ではありませんが、いまの状態を知るには必要な数字です。",
  },
  pitches: {
    label: "投球数",
    short: "投げた球の総数。1試合100球前後が先発の目安です。",
    caveat: "⚠登板数が多いほど増えるので、投手どうしを比べるときは球数/アウトを見てください。",
  },
  pitchesPerOut: {
    label: "球数/アウト",
    short: "アウトを1つ取るのに何球かかったかの平均。少ないほど効率が良い指標です。",
    how: "投球数 ÷ アウト数",
    caveat:
      "⚠少ないほど良い指標です。ほかの多くの指標と向きが逆なので、色も逆向きにしています。" +
      "また三振を多く取る投手は球数が増えやすく、効率が悪い＝悪い投手という意味ではありません。",
  },
  wildPitch: {
    label: "暴投",
    short: "捕手が捕れない投球で走者が進んだ回数。",
    caveat: "⚠捕逸（捕手の記録）とは別ものです。ここに出るのは投手に付いた分だけです。",
  },
  balk: {
    label: "ボーク",
    short: "投球動作の反則。走者が1つ進みます。",
  },
  srp: {
    label: "SRP",
    short: "状況失点抑制。投げた場面ごとに、失点の期待値をどれだけ抑えたかの合計です。",
    how: "−Σ(打席後の得点期待値 − 打席前 + その打席の失点)",
    /**
     * ⚠**アウトではない.** 打席ごとに付ける指標なので母数は投球回ではなく相手打者の数だ
     * (2026-08-20 이전에 `打者` 와 `対戦打者` 로 갈려 **356명**이 어긋나 있었다).
     */
    den: "対戦打者",
    caveat:
      "⚠打者のSRCと同じ計算を裏返したものです。0が平均で、プラスなら平均より失点を抑えたことを表します。" +
      "母数は投球回ではなく「対戦打者」の数です。" +
      "守備の影響は切り離せません。またWARではなく、WARと比較できません。",
  },
  srpPer9: {
    label: "SRP/9",
    short: "SRP を9イニングあたりに換算したもの。登板数の違う投手を並べるときに使います。",
    den: "対戦打者",
    caveat: "⚠先発と救援では投球回が大きく違うので、役割の違う投手を直接くらべないでください。",
  },
  inheritedRunner: {
    label: "引き継ぎ",
    short: "前の投手が残した走者。SRPと自責点で扱いが正反対になります。",
    caveat:
      "⚠自責点は「走者を出した投手」に付きますが、SRPは各打席をその打席を投げた投手に付けます。" +
      "つまり残して降りた投手はSRPでは軽く、自責点では重くなります。二つが食い違うのは不具合ではありません。",
  },
  iso: {
    label: "ISO",
    short: "長打力だけを取り出した指標。",
    how: "長打率 − 打率",
    den: "打数",
    caveat: "単打の多さに影響されません。.200 を超えると長距離打者です。",
  },
  babip: {
    label: "BABIP",
    short: "本塁打を除く、フェアゾーンに飛んだ打球が安打になった割合。",
    how: "(安打−本塁打) ÷ (打数−三振−本塁打+犠飛)",
    /**
     * ⚠**打数ではない.** `compare/*.json` 이 `打数` 라고 쓰고 있었는데,
     * 실측(2026-08-20 · 中野拓夢)으로 그 사람의 打数는 **380** 이고 BABIP 분모는 **317** 이었다 —
     * 즉 그 라벨은 그냥 거짓이었다. 342명이 같은 상태였다.
     */
    den: "インプレー打球",
    caveat:
      "母数の「インプレー打球」は 打数−三振−本塁打+犠飛 です — 打数とは違う数で、犠打は含みません。" +
      "⚠運の影響が大きく、良し悪しの色をつけていません。平均は .300 前後です。",
  },
  kRate: {
    label: "K%",
    short: "三振になった打席の割合。低いほど良い指標です。",
    how: "三振 ÷ 打席",
    den: "打席",
  },
  bbRate: {
    label: "BB%",
    short: "四球で出塁した打席の割合。選球眼の目安です。",
    how: "四球 ÷ 打席",
    den: "打席",
  },
  era: {
    label: "防御率",
    short: "9イニングあたりの自責点。低いほど良い指標です。",
    how: "自責点 × 27 ÷ アウト数",
    /**
     * ⚠**계산은 아웃 카운트로, 표기는 이닝으로.** `format.ts` 의 `denominator()` 가
     * `回` 를 받으면 아웃을 이닝으로 바꾼다 — 여기 `アウト` 라고 쓰면 415가 그대로 나가
     * 분모가 3배로 부풀어 보인다(`parts.ts` 의 `statRateOuts` 가 적어 둔 그 함정이다).
     */
    den: "回",
    caveat: "守備の巧拙や失策の影響を受けます。",
  },
  whip: {
    label: "WHIP",
    short: "1イニングあたりに出した走者の数。低いほど良い指標です。",
    how: "(被安打 + 与四球) ÷ 投球回",
    den: "回",
    caveat: "死球は含みません。",
  },
  fip: {
    label: "FIP",
    short: "本塁打・四死球・奪三振だけから、防御率の目盛りに換算した値。",
    how: "(13×被本塁打 + 3×(与四球+与死球) − 2×奪三振) × 3 ÷ アウト数 + リーグ定数",
    den: "回",
    caveat: "⚠守備の影響を切り離す代わりに、打球の質は測っていません。",
  },
  k9: { label: "K/9", short: "9イニングあたりの奪三振数。", how: "奪三振 × 27 ÷ アウト数", den: "回" },
  bb9: { label: "BB/9", short: "9イニングあたりの与四球数。低いほど良い指標です。", how: "与四球 × 27 ÷ アウト数", den: "回" },
  hr9: { label: "HR/9", short: "9イニングあたりの被本塁打数。低いほど良い指標です。", how: "被本塁打 × 27 ÷ アウト数", den: "回" },
  pa: {
    label: "打席",
    short: "打席に立った回数。四死球・犠打も含みます。",
    caveat: "率の指標を読むときは、この数（母数）を必ず一緒に見てください。",
  },
  ab: { label: "打数", short: "打席のうち、四死球・犠打・犠飛などを除いた数。打率の分母です。" },
  sample: {
    label: "母数",
    short: "その率がいくつの機会から出た数字か。当サイトは率のとなりに必ず出します。",
    caveat: "⚠10打席の .400 と 500打席の .300 は同じ「打率」ではありません。母数のない率は読まないでください。",
  },
  innings: { label: "投球回", short: "投げたイニング数。6.2 は6回3分の2を表します。" },
  qualifiedBatter: {
    label: "規定打席",
    short: "率の順位がつく最低ライン。チーム試合数 × 3.1（端数は四捨五入）です。",
    caveat: "⚠届かない選手にも成績はあります。順位がつかないだけです。",
  },
  qualifiedPitcher: {
    label: "規定投球回",
    short: "率の順位がつく最低ライン。チーム試合数 × 1回です。",
  },
  scoring: {
    label: "得点圏",
    short: "二塁または三塁に走者がいる状況。",
    caveat: "⚠サイトによって数え方が違う指標です。当サイトはこの定義です。",
  },
  runExpectancy: {
    label: "得点期待値",
    short: "その走者・アウト状況から、イニング終了までに入る平均得点。",
    how: "当サイトのアーカイブからリーグ・シーズンごとに算出",
    caveat: "他リーグ（MLB）の値は使っていません。得点環境が違うためです。",
  },
  allowedAvg: {
    label: "被打率",
    short: "投手が打たれた割合。打者の打率と同じ計算で、立場が逆です。",
    caveat: "⚠低いほど良い指標です。打者の打率と同じ色で読まないでください。",
  },
} as const;

export function termOf(key: string): Term | undefined {
  return GLOSSARY[key];
}

/**
 * 그 지표의 **분모 단위**. 화면은 이 함수 말고 다른 데서 단위를 얻지 않는다(M1).
 *
 * ⚠**모르는 키에 던진다** — `metricRank()` 와 반대다. 순서는 틀려도 값이 안 변하지만
 * 분모는 **값의 의미 자체**라(M2), 빈 문자열로 흘리면 「.341 317」 같은 무의미한 글자가 나가거나
 * 더 나쁘게는 옆 지표의 단위를 물려받는다. 새 비율을 만들면 용어집에 `den` 을 적어라.
 *
 * ⚠**여기 없는 비율이 정확히 하나 있다 — `pitchesPerOut`**(2026-08-20 실측 · 배포물의 비율 20종 중).
 * 화면이 둘로 갈려 있는데(선수·比較는 `138.1回`, 구단 표는 `415アウト`) **둘 다 참인 같은 수**라
 * 어느 쪽이 정본인지는 표시 판단이고, 이번 수정(라벨이 **사실과 다른** 것만 고친다)의 범위 밖이다.
 * 정하면 여기에 `den` 을 적고 `compare.ts`·`team-page.ts` 의 그 두 자리에서 인자를 지워라.
 */
export function denUnit(key: string): string {
  const den = GLOSSARY[key]?.den;
  if (den === undefined) {
    throw new Error(`분모 단위가 정해지지 않은 지표: ${key} — glossary.ts 의 den 에 적어라(M2)`);
  }
  return den;
}

export function glossaryKeys(): string[] {
  return Object.keys(GLOSSARY);
}

/**
 * 화면 라벨 → 용어집 키.
 *
 * ⚠**이 역인덱스가 있어서 렌더러가 키를 따로 넘기지 않아도 된다.**
 * 라벨을 바꾸면 툴팁이 조용히 사라지므로, 「모든 용어가 라벨로 도달 가능한가」를 테스트가 고정한다.
 * ⚠`打率`와 `被打率`처럼 **뜻이 반대인 라벨을 같은 키로 묶지 마라.** 투수 화면이 타자 설명을 낸다.
 */
const BY_LABEL: Readonly<Record<string, string>> = Object.fromEntries(
  Object.entries(GLOSSARY).map(([key, t]) => [t.label, key]),
);

export function termKeyForLabel(label: string): string | undefined {
  return BY_LABEL[label];
}
