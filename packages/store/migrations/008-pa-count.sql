-- 타석이 끝난 시점의 볼카운트(`B-S`).
--
-- ⚠**파서는 처음부터 이 값을 뽑고 있었다.** `playbyplay.ts` 가 `1-2より` 를 읽어 `count` 로
-- 돌려주는데, 적재가 그것을 받는 자리를 만들지 않아 **매번 버려졌다**(2026-08-17 확인).
-- 재수집이 필요 없다 — 아카이브 1,575경기를 다시 읽으면 채워진다.
--
-- ⚠**「투구 단위 데이터」가 아니다.** 우리는 각 투구를 모른다. 아는 것은 **타석이 끝난 카운트**뿐이고,
-- 그것으로 「2스트라이크 이후」·「초구 결착」·「풀카운트」를 말할 수 있다.
-- 헛스윙 유도율(SwStr%)은 여전히 불가능하다 — 분모인 스윙 수가 없다.
--
-- ⚠**원문 그대로 넣는다**(M4). `1-2` 로 정규화해 저장하면 소스가 표기를 바꿨을 때
-- 「원본이 뭐라고 썼는가」로 되돌아갈 수 없다. 해석은 읽는 쪽에서 한다.
--
-- ⚠**NULL 을 허용한다**(M11). 0-0 으로 채우면 「초구에 끝났다」는 거짓이 된다.
--
-- ⚠**분모를 정정한다(2026-08-17).** 처음 이 파일에 적은 「115,724행 중 113,405행(98.0%)」은
-- **다른 모집단**(playbyplay 의 행)을 가리켰다. 이 표의 실측은 **178,420행 중 NULL 0건**이고
-- 고유 값은 12종(전부 `N-Nより`)이다 — 즉 **지금 NULL 분기는 사실상 죽은 코드**다.
-- 이유: `align.ts` 가 `completed` 타석만 담으므로, 카운트 표기가 없는 행
-- (`（途中終了）`·`（途中交代）`)은 애초에 이 표에 오지 않는다.
-- 그래도 NOT NULL 로 조이지 않는다 — 소급 시즌에서 표기가 없을 수 있고,
-- 그때 「0-0」으로 때우는 것이 이 주석이 막으려는 바로 그 거짓말이다.

ALTER TABLE pa_event ADD COLUMN ball_count TEXT;
