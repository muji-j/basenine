-- 드래프트 지명. `2000年ドラフト5位` **원문 그대로**(M4).
--
-- ⚠**우리가 이미 받아 두던 선수 페이지에 있던 것이다** — 새로 받는 페이지가 아니다.
--   `pc_bio` 를 파서가 이미 읽고 있었는데 이 칸만 버리고 있었다(2026-08-17).
--   실측: 아카이브 980장 중 **980장**에서 읽힌다.
-- ⚠**연도와 순위로 쪼개지 않는다.** `育成ドラフト` 가 섞여 있어 쪼개면 그 구별이 사라진다.
-- ⚠**NULL 은 「지명 없음」이 아니라 「그 칸을 못 읽었다」**(M11).
ALTER TABLE player ADD COLUMN draft TEXT;
