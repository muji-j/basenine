-- 004 타석별 득점
--
-- 「이 타석에서 몇 점이 났는가」. 득점기대치(RE) 행렬의 재료다.
--
-- ⚠**파생값이지만 저장한다.** 유도에 라인스코어(두 번째 소스)가 필요하므로
-- `pa_event`만으로는 다시 만들 수 없다. 재계산 가능한 집계와는 성질이 다르다.
--
-- 유도 방식과 검증은 `src/runs.ts` 참조. 실측: 632경기 총 4,557점이
-- 라인스코어 총점과 **정확히 일치**(2026-08-15).

ALTER TABLE pa_event ADD COLUMN runs_scored INTEGER NOT NULL DEFAULT 0;
