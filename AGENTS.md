# AGENTS.md — bb-app (NPB 정보 서비스 · BaseNine)

> ## 공통 규약 (요약 — 정본은 워크스페이스 루트 `../CLAUDE.md`)
>
> **충돌하면 정본이 이긴다.** 이 요약은 낡을 수 있다. 판단이 갈리면 정본을 읽어라.
> ⚠**Claude 는 루트 `CLAUDE.md` 를 자동 상속하지만 Codex 는 루트 `AGENTS.md` 를 상속하지 않는다** —
> 그래서 이 블록이 있다. 이 저장소 고유 규약의 정본은 같은 폴더의 **`CLAUDE.md`** 다.
>
> 1. **검증** — 변경 1건마다 검증하고 **무엇을 확인했는지** 보고한다. 실행하지 않은 검사를 통과했다고
>    말하지 않는다. **FAIL(떨어짐)과 ERROR(돌지도 않음)를 분리**해 세고, **분모를 항상 표기**하며
>    **「0건」과 「안 쟀음」을 구별**한다.
> 2. ⚠**Windows 함정** — PowerShell 문자열 매칭으로 통과/실패를 판정하지 마라(`node`/`py` 로 실제
>    실행하고 출력을 읽는다). **파이프가 `$LASTEXITCODE` 를 덮어쓴다.** 경로에 일본어가 있어 자식 셸
>    인코딩이 깨질 수 있고, cp932 콘솔에서는 유니코드 출력이 죽어 **「결과 0건」처럼 보인다.**
> 3. **설계 우선** — 신규 기능은 요구·데이터 모델·계획을 먼저 확정. 소규모·정형은 바로 진행.
> 4. **질문** — 선택지 형식, 추천안 먼저. **제품 동작·안전·비용·외부 작업**이 걸린 것만 묻는다.
> 5. **보안** — 비밀키는 시크릿 스토어에만. **인가는 서버(또는 엣지)에서 매 요청 검증**한다 —
>    UI 숨김은 인가가 아니다. 실데이터를 git·데모·커밋에 넣지 않는다.
> 6. **코드** — **틀린 값을 조용히 보여주는 것이 예외를 던지는 것보다 나쁘다.** 파서·계산식은 1벌만
>    두고, 시각·난수는 주입해 결정론화한다.
> 7. **경계** — 형제·상위 프로젝트는 **읽기 전용**(⚠**스크립트 실행 포함**). 공통 자산 `_common/` 을
>    프로젝트 작업 중에 고치지 않는다. ⚠**`.claude/` 와 `.codex/` 는 상호 불가침** —
>    **Codex 는 `.claude/`(agents·audit·settings)를 만들거나 고치거나 지우지 않는다.**
> 8. **Git·배포** — 커밋 신원은 **저장소별** 설정(전역이 비어 있다). `main` 직접 push·force push·
>    merge 금지. **배포·원격 push·PR·외부 게시는 사용자 승인 뒤에만**, 승인은 그 작업 직전에 받는다.
> 9. **검토** — 중요 수정은 **서로 다른 두 모델로 이중 검토**. 배포 직전·고위험 변경은 **3중 검토**.
> 10. **언어** — 대화·보고·내부 문서는 **한국어**. **제품 UI 는 일본어**. 업무·법령 데이터는 **원문 유지**.

일본 프로야구(NPB)의 경기 기록과 파생 지표를 일반 유저에게 보여주는 **공개 웹 서비스**(브랜드 **BaseNine**).
**정적 다페이지 생성**이라 서버가 없다 — 빌드가 9시즌을 `dist/` 로 굽고 **Cloudflare Pages** 가 낸다.
지금 단계는 **지인 한정 비공개**(Cloudflare Access 허용목록). 형상관리는 git(원격 `muji-j/bb-app` · 비공개).

⚠**이 저장소의 규약 정본은 `CLAUDE.md`** 이고, 특히 **§2-3 도메인 의무 M1~M12** 와
**§2-5 데이터 권리 L1~L7 / 단계 전략 S1~S3** 가 이 프로젝트의 헌법이다. 작업 전에 읽어라.

## 작업 단위

npm 워크스페이스 · 패키지 **7개**.

| 단위 | 역할 |
|---|---|
| `packages/domain` | 팀·시즌·대회 구분 등 도메인 원시 개념 |
| `packages/parser` | npb.jp HTML 파싱. 고정 픽스처는 `packages/parser/test/fixtures/` |
| `packages/store` | SQLite 스키마·마이그레이션·적재 |
| `packages/aggregate` | 스플릿·순위·상대전적 집계 |
| `packages/metrics` | 지표 계산(정의서는 `docs/metrics/`) |
| `packages/archiver` | 외부 수집과 원본 아카이브 |
| `packages/web` | 정적 사이트 생성 |

## 기술 스택

**Node ≥24 + TypeScript** · **SQLite 1파일**(`data/bb.sqlite`) · 원본은 `data/archive` 의 `.html.gz`
(⚠둘 다 `.gitignore` — 저장소에 없다) · **정적 생성** → `dist/` · **Cloudflare Pages + Access** ·
배치는 **GitHub Actions**(`daily.yml` · `access-config.yml`) · 배포 도구 `wrangler`.
프레임워크 없음 · 런타임 의존성 없음(devDependencies 3개).

## 아키텍처 급소

⚠**정본은 `CLAUDE.md` §2-3(M1~M12)과 §6 이다. 여기는 그 요약이고, 두 파일이 갈라지면 그 자체가 결함이다.**

- **파싱 실패의 침묵**(M7) — 구조가 바뀌었을 때 빈 배열·0 을 반환하면 **「성적 급락」이라는 거짓말**이
  유저에게 나간다. 파싱은 실패해야 할 때 실패해야 한다.
- **분모**(M2) — 비율을 반환하는 함수는 표본 수도 함께 반환한다. **분모 없는 비율은 렌더링 금지.**
  분모의 **단위 이름**은 `packages/web/src/glossary.ts` 가 정본이다(화면이 정하지 않는다).
- **멱등**(M5) — 수집·적재는 재시도·크론 중복 기동을 전제로 설계한다.
- **선수 동일성**(M10) — 이름 문자열 조인 금지. 자체 안정 ID + 소스별 매핑.
- **시각**(M6) — JST 고정. 인자 없는 `new Date()`/`Date.now()` 직접 호출 금지, 주입된 `clock` 하나.
  ⚠**린트가 아니라 시험이 강제한다**(`scripts/test/clock-injection.test.ts`).
- **결측 3분류**(M11) — 미수집 / 0 / 없음을 섞지 않는다.
- **서버가 없다** — 폴링·SSR·요청 시 계산·서버 집계를 전제한 설계는 여기서 성립하지 않는다.

## 검증 명령

| 목적 | 명령 | 합격 판정 |
|---|---|---|
| 전체 시험 | `npm test` | **2,123본 중 2,123본 통과**가 기준선(2026-09-03 실측) · fail 0 · ERROR 0 |
| 타입체크 | `npm run typecheck` | exit 0 (tsc 무출력) |
| 사이트 빌드 | `npm run build:web` | 9시즌을 `dist/` 로 생성 |
| 화면 확인 | `npm run preview` | `http://127.0.0.1:4173` · ⚠**`file://` 로 열지 마라**(색인 `fetch` 가 막혀 검색이 죽는다) |
| 데이터 신선도 | `npm run freshness` | 2일 이상 낡으면 빨개진다 |

⚠**FAIL(떨어졌다)과 ERROR(돌지도 않았다)를 분리해 센다.**
⚠**CI 는 `npm test` 만 돌린다.** 타입체크는 로컬이고 **린트는 이 저장소에 존재한 적이 없다.**

## ⚠ 금지·주의 (이 리포에서만 참인 것)

- **라이브 취득을 하지 마라**(2026-08-15 확정). 필요한 폴링이 수집 규약의 약 100배이고 NPB 규정에
  정면으로 걸린다. 대체는 유저가 대전 상대를 고르는 `matchup.html`. ⚠**라이브 「표시」는 v2 로 살아 있다** —
  취득과 표시를 섞지 마라.
- **선수 사진·구단 로고·엠블럼을 쓰지 마라.** 사진은 촬영자의 저작물이고 초상권이 붙으며 로고는 상표다.
- **외부 사이트 실접속은 조사·검증 단계에서만.** 개발 루프는 `packages/parser/test/fixtures/` 의
  고정 픽스처로. ⚠수집은 **1req / 2~5초 · 동시 1커넥션 · 식별 가능한 UA**(L1).
- **저장소 밖에 파일을 흘리지 마라.** 조사·디버깅용 임시 파일을 바탕화면·홈에 두지 않는다(실제로 쌓였다).
- **CI 무료 분이 유한하다** — 스케줄만으로 월 한도의 대부분을 쓴다. **수정을 모아 한 번에 배포한다.**
- ⚠**「배포했다」를 「보인다」로 읽지 마라.** 사람에게 주는 주소는 `https://bb-app-7mk.pages.dev` 하나이고,
  `wrangler` 가 찍는 **배포 해시 주소는 그 배포의 영구 스냅샷**이라 영원히 안 바뀐다.

## Codex 에이전트 라우팅

- ⚠**이 저장소에 `.codex/` 는 없다.** 만들게 되면 그것은 **Codex 전용**이고,
  **`.claude/`(agents 11종 · audit 오버레이 · settings)는 out of scope** — 생성·수정·삭제하지 않는다.
- Claude 쪽에는 프로젝트 특화 에이전트 11종과 공통 `shiro-core:*` 가 있지만 **Codex 는 그것을 쓸 수 없다.**
  대신 그 에이전트들이 참조하는 **문서를 직접 읽어라**: `CLAUDE.md` §2(도메인) · `docs/metrics/`(지표 정의) ·
  `docs/sources/`(소스 판정표) · `docs/operations/`(배포·수집 런북) · `docs/decisions/`(결정문).
- 기본 흐름: **설계(문서 확정) → 실패하는 시험 → 구현 → `npm test`·`typecheck` → 보고**.
  고위험 영역(수집 멱등 · 지표 산식 · 마이그레이션 · 배포)은 **바꾸기 전에 근거 문서를 먼저 갱신**한다.
- 배포·원격 push·PR 은 **사용자 승인 없이 수행하지 않는다.**
- ⚠**`docs/metrics/` 에 없는 산식을 즉흥으로 만들지 마라.** 정의서가 정본이고, 없으면 만들기 전에 물어라.
  자체 계산 지표는 **표준 지표와 다른 이름**을 쓴다(같은 이름에 다른 값이면 거짓말이 된다).

## 인계

- Codex 가 바꾸거나 추가한 내용은 **워크스페이스 루트 `../CODEX_HANDOFF.md`** 에 기록해 Claude 가 이어받게 한다.
  반대도 같다.
- ⚠**핸드오프와 dirty 파일은 상대 에이전트의 작업물일 수 있다** — 읽기 전용으로 취급하고,
  덮어쓰기 전에 확인한다.
- 이 저장소의 진행 상황과 「남은 일」은 **`_NEXT_SESSION.md`** 에 있다.
