# CLAUDE.md — bb-app (NPB 정보 서비스 · Lunomel)

일본 프로야구(NPB) 각종 정보를 **일반 유저**가 쉽게 확인할 수 있는 **공개 웹 서비스**.
프로듀스 브랜드는 **Lunomel** — `../x-scraper`(Trovune) · `../google-chat`(KAWAII GChat) · `../google-calendar` 와 동일 패밀리.

> **작업 규약의 뿌리** = `../PPS_NEW_PORTAL/CLAUDE.md`. 거기서 검증된 방침(검증 후 보고 · 감사↔구현 분리 ·
> 모델/에포트 기준 · 분모 표기 · 침묵 오류 금지)을 이 프로젝트 도메인에 맞게 옮겼다.
> **스택·DB·호스팅·제품명 미확정** — §1 · §7의 결정 절차를 따른다.

---

## 0. 프로젝트 제약 (모든 작업이 만족해야 함)

| # | 제약 | 판정 기준 |
|---|---|---|
| 1 | **일반 유저 접근성 최우선** — 설치·계정·설정 없이 URL만으로 핵심 정보에 도달 | 첫 방문 → 원하는 선수 성적까지 **3클릭 이내**, 로그인 요구 0 |
| 2 | **비용 2트랙 제시** — 유료 최선안 / 무료 최선안 / 두 안의 비교를 항상 병기. **상한 = 월 1,000엔**(2026-08-14 확정) | ADR-001에 3섹션 전부 존재 + 월 추정액 명시 · 상한 초과안은 「참고」로만 |
| 3 | **데이터는 자동 최신화가 기본** — 수동 입력은 「자동이 불가능함을 입증한 항목」만 | 수동 의존 항목 목록 + 각각의 「왜 자동이 안 되는가」 |
| 4 | **라이브 대응은 v2**(2026-08-14 확정). 단 **v1 설계가 라이브를 막지 않을 것** | 라이브 도입 시 스키마·타입 변경 없이 얹을 수 있는가(M9 타입 분리 · 타석 로그 보존) |
| 5 | **지표 깊이** — 일반 지표(타율·출루·장타·방어율·홈런·도루) + **리그 순위** + 세이버메트릭스 + 스플릿(투수유형별/타자유형별) + 선수 간 상대전적 | §2 지표 카탈로그의 전 항목이 데이터에 존재 |
| 6 | **UI 독창성** — 「AI가 만든 티」 회피. Human Interface Guidelines 참조 | `design-auditor` 「AI틱함」 축 확정 지적 0건 + 목업 선택 이력 |
| 7 | **분모 없는 비율 렌더링 금지** | 스플릿·파생 지표 전부에 표본 수 병기 (M2) |
| 8 | **정합성** — 수집 멱등성·정정 반영·부분 실패 | `concurrency-correctness-auditor` 확정 P0/P1 0건 |
| 9 | 보안 — **공개 데이터 기반이므로 경량 검토**. 단 인젝션·시크릿·공급망·남용은 예외 없음 | `security-auditor` 확정 P0 0건 (P1은 근거와 함께 보류 가능) |
| 10 | **출처 추적성** — 모든 수치가 「어디서 · 언제 · 몇 번째 판」인지 답할 수 있을 것 | M4 필드 4종이 전 수집 테이블에 존재 |
| 11 | **법적 안전** — robots.txt·이용약관 준수, 출처 명기, 사실(수치)과 표현물 구분 | `data-source-scout` 소스별 판정표에서 「미확인」 0건 |
| 12 | 비용 — 무과금 선호, 필수 비용은 지불 가능 (**월 1,000엔 이내**) | 제안마다 월 추정액 병기 |
| 13 | **v1 = 지인 공유**, 시장 배포·수익화는 별도 단계 | v1 범위에 결제·과금·계정 기능 넣지 않기 |

### 단계 정의 (2026-08-14 확정)

```
v1   확정 데이터    선수·구단 성적 / 리그 순위 / 세이버메트릭스 / 스플릿 / 선수 간 상대전적
v2   라이브        경기 중 현재 투수 × 현재 타자 기준 표시 (v1 스키마 위에 증축)
v3+  시장 배포·수익화 검토
```
⚠**「라이브를 v2로 미룬다」는 「라이브를 고려하지 않는다」가 아니다.**
확정/잠정 타입 분리(M9)와 **타석 단위 원시 로그 보존**은 v1부터 지킨다. 안 지키면 v2가 전면 재작성이 된다.

---

## 1. 기술스택 · 아키텍처 (⚠미확정 — ADR-001에서 결정)

확정된 것이 없다. 아래 절차로만 확정한다.

```
ADR-001  스택·호스팅·DB
         A안: 유료 최선안   (성능·확장성 우선 · 월 추정액 명시)
         B안: 무료 최선안   (GAS 포함 후보 · 무료 한도와 한도 초과 시 거동 명시)
         C안: A vs B 비교표 (성능·비용·운용부하·확장한계·뒤집힐 조건)
         → 사용자가 선택. 선택 전 구현 착수 금지.
```

**GAS를 무비판으로 채택하지 않는다.** 구 PPS 프로젝트가 GAS에서 탈출한 이력이 바로 옆에 있다
(`../PPS_NEW_PORTAL/docs/decisions/ADR-001-stack.md`). 동시 실행·실행시간 상한·락·트리거 중복 기동에서
**침묵 실패**가 나는 것이 실증되어 있으므로, GAS를 후보로 올릴 때는 **그 실패 모드를 어떻게 피하는지**를 함께 쓴다.

ADR에 반드시 들어갈 항목: 후보 · 근거 · **뒤집힐 조건** · 월 추정액 · 라이브 갱신 지연 목표치 · 데이터 총량 추정.

---

## 2. 도메인 (NPB) — 반드시 지킬 규칙

### 2-1. 시간·경기 식별
- **JST 고정.** 로컬 타임존 의존 금지.
- 경기일(game date)과 실제 시각은 다르다 — **연장·서스펜디드·더블헤더·우천 노게임·재편성**을 식별자가 감당해야 한다.
- 시즌 경계(정규/CS/일본시리즈/오픈전/이스턴·웨스턴)를 데이터에 명시. **섞어서 집계하지 마라.**

### 2-2. 지표 카탈로그 (§0-5의 실체)
```
타자 기본   타율 출루율 장타율 OPS 안타 2루타 3루타 홈런 타점 득점 도루 도루자 사사구 삼진 병살
            ⚠**도루자(盗塁刺)는 박스스코어에 없다** — `盗塁` 컬럼 하나뿐이다.
              출처는 **타석 로그의 주자 행**이고(2026-08-17 구현), 대조는 이렇다:
              경기별 도루 수 대 박스 `盗塁` 열 **2,395경기 중 어긋남 0건**(2024~2026) ·
              2026 정규시즌 도루 **611=611** · 도루자 **254 = 공표 `盗塁刺` 254**.
              ⚠**견제사(牽制死)는 도루자가 아니다** — 성공률의 분모에 넣으면 전원이 낮아진다.
              ⚠**대회를 섞으면 틀린다**: 올스타를 넣으면 2026 도루가 620이 되어 9 어긋난다.
투수 기본   방어율 승 패 세이브 홀드 이닝 피안타 피홈런 사사구 탈삼진 WHIP 퀄리티스타트
세이버      wOBA wRAA wRC+ ISO BABIP K% BB% · FIP WHIP K/9 BB/9 HR/9 · RE24 WPA(타석 로그 기반)
            ⚠**xFIP는 산출하지 않는다**(**사유 정밀화 2026-08-17**). 「FB%가 소스에 없다」보다
              정확한 사유: **타구 종류가 「안타에만」 없다.** npb.jp는 아웃이 된 타구에만 ゴロ/フライ/ライナー를
              붙이고 안타는 방향만 붙인다 — 실측 타석 로그 113,019행에서 인플레이 타구의 **30%가 미분류이고
              그게 전부 안타**다. 안타는 뜬공 비중이 높으므로 이 결손은 **FB를 체계적으로 과소계상**한다.
              **수집을 늘려도 안 채워진다**(기록 방식의 구조적 성질). 다른 무료·약관 클린 소스 0건.
            ⚠**WAR는 표시하지 않는다**(2026-08-14 확정 · **사유 정정 2026-08-17**).
              ~~타구 좌표·존·타구속도가 공개 기록에 존재하지 않는다~~ → **틀린 서술이었다. 존재한다.**
              Data Stadium이 보유하고 `hc_x`/`hc_y`(타구 좌표)·`plate_x`/`plate_y`·구속·수비 배치 9명까지
              실재한다(2026-08-17 실측 확인). 정확한 사유는 **「우리가 정당하게 접근할 수 없다」**이다 —
              문서화된 클린 경로는 NPB BIP 하나뿐이고 **월 3만엔~ · 기업/연구기관 한정**이라
              우리 상한(월 1,000엔)의 30배다. 무허락 경로는 상류가 명시적으로 스크레이핑을 금지한다.
              타 사이트 계산값을 빌려오지도 않는다(갱신·정의 추적 불가).
              ⚠**화면에 「데이터가 없습니다」라고 쓰지 마라 — 이제 사실이 아니다.**
            → 대체: **SRC(状況得点貢献)** 확정(2026-08-15). 타격 상황가중만. 정의는 docs/metrics/ §3.5
              가진 입력(타석 로그)으로 정직하게 계산 가능하고, 무엇을 재고 무엇을 안 재는지 말할 수 있다.
              ⚠자체 지표는 반드시 **WAR과 다른 이름**을 쓴다. 이름이 같으면 값이 다른 것이 거짓말이 된다.
순위        위 전 지표의 **리그 내 순위** (센트럴/퍼시픽 구분 · 자격 기준 명시 · 동률 처리 규칙 명시)
스플릿      타자: 좌투/우투 상대 · 홈/원정 · 주자상황 · 월별
            투수: 좌타/우타 상대 · 홈/원정 · 주자상황 · 월별 · 타순별
            ⚠구종별은 제외 — 무료·약관 클린 소스 0건(Yahoo가 2026-02에 앱 전용으로 이관).
상대전적    투수 × 타자 개별 매치업 통산 성적
소급 범위   보유는 **2020~2026 7시즌**(2023·2024를 2026-08-17에, 2022·2021·2020을 2026-08-18에 공개).
            ⚠**이 줄이 낡으면 판정 기준 자체가 거짓이 된다**(2026-08-18 감사 P2에서 정정 —
              「2023~2026 4시즌 · 6,816파일」로 남아 있었다). 백필할 때 **여기도 같이 고쳐라.**
              지금 값은 실측이다: `dist` **11,928파일** · DB 보유 시즌 2020~2026.
            ⚠**2021 은 공개 전에 품질을 쟀다**(CLAUDE.md 가 요구하는 절차 · 2026-08-18):
              투수 귀속 **100.0%**(64,749타석) · 타자 귀속 100.0% · 볼카운트 100.0% ·
              12팀 전부 정확히 143경기 · 대회 구분 정상(정규 890 · CS 10 · 일본시리즈 6 · 올스타 2) ·
              적재 실패 0 · **격리 0**(도루 대조 통과) · 투타 미상 0명.
            ⚠**2020 은 143경기 시즌이 아니다** — 코로나로 **120경기**였고(실측: 12팀 전부 120),
              그래서 `REGULAR_SEASON_GAMES` 를 **시즌별 표**(`regularSeasonGames(season)`)로 바꿨다.
              143 고정이면 2020 화면에 「잔여 −23경기」·「143試合換算」이 나온다.
              부수 검산도 맞았다: 일본시리즈 **4경기**(소프트뱅크 스윕) ·
              클라이맥스 **2경기**(그해 센트럴은 CS 를 열지 않았다) · 타석 귀속 54,612/54,612.
            ⚠**Pages 파일 상한이 다음 벽이다** — 배포당 20,000개(유료 100,000).
              7시즌이 **11,928파일**(60%)이고 시즌당 약 1,700씩 는다.
              **11시즌(2016~)이면 약 18,700으로 94%** — 들어가지만 여유가 없다.
              ⚠**比較 데이터를 선수마다 파일로 두던 것을 ID 첫 글자 10개로 묶어** 29%를 줄인 뒤의
              수치다(2026-08-17). 다음 지렛대는 **소급 시즌의 경기 페이지**(시즌당 878개)다
              (docs/operations/deploy.md).
            ⚠**백필은 「경기를 더 넣는 것」으로 끝나지 않는다.** 2024를 넣자 **투타 미상 122명**이
              생겼다 — 선수 페이지는 **현재 등록 선수만** 받을 수 있는데 소급 시즌에는
              NPB를 떠난 선수가 대거 들어오기 때문이다. 투타는 좌우 스플릿의 근거라
              그 선수들의 스플릿이 통째로 깨진다. **경기별 명단(`roster.html`)이 그것을 메운다**
              (§2-2 아래 「받고 있는데 안 읽던 것」 참조).
            취득 가능 범위는 **2016 시즌~**(2026-08-17 정정).
            ⚠**「2025년분부터만 존재」는 틀린 판정이었다.** 근거였던 「2024-08-14 = HTTP 404」는
              **존재하지 않는 경기 슬러그를 찍은 것**이다. 일정 페이지에서 실제 슬러그를 뽑아
              다시 치면 200이 돌아온다. `https://npb.jp/games/backnumber.html`이 2016~2026을 나열한다.
              **실측(2026-08-17): 우리 파서를 고치지 않고 2016·2020·2024년 playbyplay가 그대로 파싱된다**
              (타석 88·78·74건). 2015년 일정 = 404이 하한이다.
            ⚠**그렇다고 「11시즌을 얻었다」고 말하지 마라.** 오래된 시즌은 투수 귀속이 얇을 수 있다.
              ⚠**근거는 연도당 1경기뿐이다**(표본 3경기) — 일반화하지 마라.
              그 표본에서 2016년은 투수 ID가 **34/88(39%)**, 2020·2024년은 **100%**였다.
              투수 귀속이 빠지면 **상대전적·SRP·타순 순회가 그만큼 성립하지 않는다.**
              **어느 해부터 온전한지는 미확인이다 — 백필 전에 시즌마다 여러 경기로 재라.**
            ⚠**백필은 공짜가 아니다.** 9시즌 ≈ 16,389요청 · L1 준수 시 13.7~22.8시간 · 149MB(gz).
              실측(2026-08-17): **2024 한 시즌 = 3,636페이지 · 약 4시간 · 43MB**.
              적재는 아카이브 전체를 매번 다시 훑으므로 3시즌이면 **27만 행**이다.
            구형(2016~2018) 분기는 **넣었고 실물로 검증했다**(2026-08-17).
              ⚠**합성 픽스처로는 검증되지 않는다** — 처음 만든 픽스처에 **타석 결과 칸이 0개**여서,
              구형 경로의 `plateAppearances` 가 항상 빈 배열이어도 테스트가 전부 초록이었다.
              그 상태로 백필하면 여기 적힌 **「3시즌이 조용히 0건」**이 그대로 난다.
              → 실물 2016년 박스를 `packages/parser/test/fixtures/2016-box.html.gz` 로 고정했다.
              실측: 결과 칸 **88건** · 어휘 해석 6/6 · `チーム計` 2행 구별 · 도출 타수/안타 **격리 0건**.
            ⚠**롤오프 여부는 여전히 모른다**(미결). 2015년이 404인 이유가 「2016년에 기능이 시작됐다」인지
              「N시즌 지나면 사라진다」인지 확인되지 않았다. 후자면 2016년분이 언젠가 사라진다.
```

### 2-2-1. ⚠「받고 있는데 안 읽던 것」 — 하루에 세 번 나왔다 (2026-08-17)

새 데이터가 필요하다고 판단하기 전에 **이미 받아 둔 페이지를 먼저 열어 봐라.**
2026-08-17 하루에 셋이 나왔고, 셋 다 **외부 요청 0회**로 해결됐다.

| 무엇 | 어디에 있었나 | 왜 안 읽혔나 |
|---|---|---|
| **볼카운트** | `playbyplay` — 파서가 이미 `1-2より` 를 뽑고 있었다 | 적재에 **받는 자리가 없어** 매번 버려졌다 |
| **도루·도루자·견제사** | `playbyplay` 의 주자 행 | 주자 행도 칸이 5개라 타석 행과 모양이 같은데 **이름 칸만 비어** 있어서 「선수 링크 없는 행」으로 걸러졌다 |
| **투타·배번(전 선수)** | `roster.html` — 경기마다 받고 있었다 | 적재가 **이 파일을 한 번도 열지 않았다** |

⚠**받는 페이지와 읽는 페이지가 다르다는 것 자체를 정기적으로 확인해라.**
지금 경기마다 4장(`index` · `playbyplay` · `box` · `roster`)을 받는데
적재가 여는 것은 **`box` 와 `playbyplay` 둘뿐**이다.
`index`(試合TOP)는 확인 결과 **バッテリー · 本塁打 · 最新のオーダー** 셋뿐이고
전부 박스에서 이미 얻는 것이라 안 읽어도 된다(2026-08-17 확인).

⚠**출처가 둘이 되면 반드시 규칙을 적어라**(M1). 투타는 선수 페이지가 권위 있는 출처이고
명단은 **빈 자리만 채운다**(`WHERE throws IS NULL`). 대조는 858/858 일치였지만,
일치한다고 두 곳에서 쓰면 어느 날 갈린다.
⚠**같은 이름이라도 뜻이 다를 수 있다.** 배번은 선수 페이지가 **현재** 값,
명단이 **그 경기 시점** 값이다 — 실측 782명 중 10명이 어긋났고 전부 실제 변경이었다.

### 2-3. ⚠이 도메인이 만드는 의무 (M1~M12 · 어기면 조용히 틀린 숫자가 나간다)

| # | 의무 | 왜 |
|---|---|---|
| **M1** | **지표 계산식은 단일 패키지에 1벌** — 서버·클라·배치 중복 구현 금지 | 구 PPS에서 파서가 3중 구현이었고 **서로 값이 달랐다**(O13). 통계 서비스에서 같은 일이 나면 신뢰가 끝난다 |
| **M2** | **분모(표본 수)를 데이터와 UI 양쪽에 동반.** 분모 없는 비율은 렌더링 금지 | 10타석 .400을 「리그 1위」로 보여주는 순간 이 서비스는 거짓말을 한다 |
| **M3** | **자격 기준(규정타석·규정투구회)과 동률 처리 규칙을 명시하고 테스트로 고정** | 순위는 규칙이 곧 값이다. 규칙이 코드에만 있으면 아무도 검증할 수 없다 |
| **M4** | 전 수집 레코드에 `source` · `fetched_at` · `as_of`(데이터 기준시각) · `revision` | 「어제 본 숫자와 다른데?」에 답하지 못하면 버그와 정정을 구별할 수 없다 |
| **M5** | **수집기는 멱등** — 같은 경기를 두 번 수집해도 결과가 한 번과 같다 | 재시도·크론 중복 기동·수동 재수집이 전부 일어난다 |
| **M6** | **시계는 주입 가능한 `clock` 1개.** `new Date()`/`Date.now()` 직접 호출 금지(린트) | 구 PPS는 시계를 2번 읽어 자정 경계에 날짜가 어긋났다(연 1~2회). 라이브 경기는 자정을 넘긴다 |
| **M7** | **파서는 소스별 1벌 + 스키마 변화 감지.** HTML 구조가 바뀌면 **빈 값이 아니라 실패**로 | 스크레이핑 프로젝트 최대 사망 원인. 조용히 0을 넣으면 「성적 급락」으로 표시된다 |
| **M8** | 외부 접근은 **robots.txt · rate limit · 캐시 우선 · 지수 백오프** | 차단당하면 서비스가 죽는다. 예의는 가용성 문제다 |
| **M9** | **확정값과 잠정값을 타입 수준에서 구분** | 라이브 중 기록은 정정된다. 같은 타입이면 반드시 섞인다 |
| **M10** | **선수는 자체 안정 ID + 소스별 외부 ID 매핑 테이블.** 이름 문자열 조인 금지 | 동명이인 · 등록명 변경 · 한자/카나 표기 흔들림 · 개명 · 외국인 선수 표기 요동 |
| **M11** | **미수집 · 0 · 결측을 구별한다.** `0`과 `null`을 섞지 마라 | 「도루 0개」와 「도루 데이터 없음」은 다르다 |
| **M12** | 빈 상태는 **4상태**(로딩 / 데이터없음 / 수집실패 / 시즌외) | 셋이 같은 화면이면 그것 자체가 결함 |

### 2-4. 데이터 출처 원칙
- **신뢰 등급**을 소스마다 부여: 공식(NPB/구단) > 대형 집계 > 개인 집계. 충돌 시 상위 우선, **충돌 사실을 기록**한다.
- **수치(사실)와 표현물(기사·해설·이미지)을 구분.** 이 서비스가 다루는 것은 수치다.
- 각 소스의 robots.txt·이용약관 판정을 `data-source-scout`가 표로 남기고, **판정 없는 소스는 쓰지 않는다.**
- 자동 취득이 불가능한 항목은 **「왜 불가능한가」를 적고** 수동 입력 경로를 만든다(§0-3).

### 2-5. 데이터 권리 — 조사 결과와 대응 (2026-08-14)

전수 조사 결과는 `docs/sources/2026-08-14-feasibility-report.md`. **이 프로젝트의 병목은 기술이 아니라 데이터 권리다.**

**⚠아래는 법률 자문이 아니라 리스크 정리다.** 조사 자체가 근거의 일부를 2차 인용에 의존했고(경산성 문서가 비브라우저 UA에 403), 판례 원문 대조를 완료하지 못했다. **공개 전환을 결정하는 시점에는 일본 변호사 확인을 거친다.**

혼동하기 쉬운 4개 층을 분리해서 다룬다. **「공개 정보다」는 1층에만 답하는 말이다.**

| 층 | 무엇이 걸리는가 | 우리 대응 |
|---|---|---|
| **1. 사실 자체** | 개별 기록(안타·홈런·타율)은 **사실이며 저작물이 아니다.** 여기까지는 유저 직관이 맞다 | 수치를 쓰는 것 자체는 문제의 본체가 아니다 |
| **2. 편집물로서의 DB** | 저작권법 §12-2 — **선택·체계적 구성에 창작성**이 있으면 보호. 즉 「표를 통째로 베끼는 것」이 걸린다 | **원본 표의 구성·컬럼 순서를 재현하지 않는다.** 원시 기록을 받아 우리 스키마로 재구성한다 |
| **3. 일반 불법행위** | 저작권이 없어도 성립할 수 있다(翼시스템·YOL 판시 계열). 위험 인자 = **영리목적 · 정보의 신선도 이용 · 데드카피 · 경합관계** | 이 4개를 동시에 피하는 것이 설계 목표. **비영리·비경합·저빈도·자체계산**이 그 대응이다 |
| **4. 계약·규정** | npb.jp 푸터 「二次利用および無断転載を固く禁じます」 · NPB 撮影/配信규정 제4조1항(2)의 **리얼타임 배신 금지** | 재배포 형태를 취하지 않는다. **라이브는 규정이 정면으로 금지하므로 무허락 구현 대상에서 제외**(§0-4) |

**⚠robots.txt가 404인 것은 「허용」이 아니라 「지시 없음」이다.** 허용의 근거로 쓰지 마라.

#### 코드로 강제할 안전장치 (구현자는 이것을 지킨다)

| # | 규칙 | 근거 |
|---|---|---|
| **L1** | 수집은 **1req / 2~5초 · 동시 1커넥션 · 식별 가능한 UA + 연락처**. 하루 1회 배치가 기본 | 岡崎 사건은 1초 1요청 크롤러로 체포까지 갔고 **사법판단이 남지 않아 정량 기준이 존재하지 않는다.** 안전측 설계밖에 할 수 없다 |
| **L2** | **원본 표 레이아웃을 재현하지 않는다.** 화면은 우리가 계산한 값 위주로 구성 | 2층·3층(데드카피) 회피 |
| **L3** | 전 화면에 **출처 명기 + 원본 링크**. 원본을 대체하는 게 아니라 가리킨다 | 3층(경합) 회피 |
| **L4** | **삭제·정정 요청 창구를 처음부터** 둔다 | 개인정보 논점의 최소 대응. 나중에 붙이면 늦다 |
| **L5** | 취급 대상은 **공개된 직업활동 성적만.** 비공개 개인정보는 수집·저장하지 않는다 | 개인정보 논점 |
| **L6** | **원시 아카이브는 내부 보관, 외부 노출은 파생값 위주** — 이 경계를 저장소 계층에서 나눈다 | 2·3층의 핵심 경계선 |
| **L7** | 조건부 요청(ETag/If-Modified-Since)과 캐시를 우선한다 | L1의 연장 — 안 보내는 요청이 가장 안전한 요청 |

#### 단계 전략 (2026-08-14 확정)

```
S1 지인한정 비공개   **로그인 + 허용목록**. 재배포 형태를 취하지 않는다
                     ⚠**요구는 「엣지에서 매 요청 허용목록 검증」이지 특정 로그인 수단이 아니다.**
                     2026-08-15 확정: Cloudflare Access의 One-time PIN(이메일 코드)을 쓴다 —
                     보증 수준이 같고 지인이 Google 계정을 갖지 않아도 된다(deploy.md §4-2)
                     → 3·4층 위험 인자 대부분이 성립하지 않음
                     이 단계의 목적은 「보여주기」가 아니라 **아카이브 축적**(소급 불가 자산)
S2 공개 검토        「우리가 계산한 것」의 비중이 충분히 커진 뒤에 판단 + 변호사 확인
S3 수익화 검토      ⚠영리목적이 붙는 순간 3층 판단요소가 강해진다 → 반드시 재평가
```

**⚠S1이 안전한 것은 「공개」 축뿐이다. 수집 행위 자체(L1·L7)는 비공개여도 그대로 남는다.**
**⚠허용목록은 UI 숨김이 아니라 서버(또는 엣지)에서 매 요청 검증한다.** 인증을 통과한 것과 이용이 허가된 것은 다르다.

---

## 3. 작업 규칙

1. **업데이트 1건마다 검증 필수** — 변경 후 타입체크/테스트/동작을 확인하고 *무엇을 확인했는지* 보고. 「완료」는 검증 뒤에만.
2. **신규 기능은 설계부터** — `superpowers:brainstorming` → `writing-plans` → 구현. 소규모·정형(문구·라벨·단순 픽스)은 바로 진행.
3. **질문은 AskUserQuestion 선택지 형식**(추천안 먼저·「(추천)」 표기). 자유서술 질문 지양. 진행보고·결과전달은 텍스트.
4. **표시 언어는 한국어**로 통일(내부 작업 언어는 자유). ※ 제품 UI 언어는 별개 — §7.
5. **최종 검토는 항상 sonnet + opus 이중 검토** — 1차 `review-specialist`(기본 sonnet high) + 2차 동일 에이전트를 `model:"opus"` 오버라이드로. **모델 다양성이 핵심**(도구 비교에서 발견의 93.4%가 정확히 한 도구에서만 검출됐다).
6. **설치 스킬 능동 활용** — `frontend-design`·`dataviz`·`auditing-multi-dimensionally`·`superpowers` 계열. 남용 금지.
7. **분모를 항상 표기** — 「대부분」·「전부 통과」 금지. 「23본 중 23본」. **「0건」과 「안 쟀음」을 구별해 쓴다.**
8. **FAIL(떨어짐)과 ERROR(돌지도 않음)를 분리해서 센다** — 구 프로젝트에서 E2E 5본이 전부 실행 불가인 채 「0건=합격」으로 오독된 전례.
9. **새 테스트는 「고치기 전 코드에서 실패하는가」를 확인**한 뒤 채택한다(뮤테이션 검사).
10. ⚠**자기 수정을 다시 읽는다** — 구 감사에서 *수정이 새 결함을 만든 사례가 한 라운드에 4건*. 전부 읽기가 아니라 **실측**이 잡았다.
11. **외부 사이트 실접속은 소스 조사·검증 단계에서만.** 개발 루프에서 반복 히트 금지 — 고정 픽스처(저장된 HTML/JSON)로 테스트한다.
12. **울트라 코드(Workflow) 사용 허가됨** — 소스 전수 조사·스택 비교·다각 감사처럼 **팬아웃이 이득인 작업**에 한해 자체 판단으로 사용. 단건 구현에는 쓰지 않는다.

---

## 4. 에이전트 편대

모델·에포트 기준은 **DeepSWE 실측**(2026-08 시점, `https://deepswe.datacurve.ai/`):
`claude-opus-5[max] 74% / $11.84` · `claude-fable-5[max] 70% / $21.63` · `claude-sonnet-5[max] 54% / $26.40 · 268스텝`.

- **opus는 effort `max`** — 1위. 놓치면 비싼 모든 일.
- **sonnet은 effort `high`** — ⚠**sonnet에 `max` 금지**: opus보다 점수가 낮은데 비용은 2배 이상이고 스텝이 폭주한다(268 vs 99).
- **haiku는 effort `medium`** — 판단이 필요 없는 기계적 정형작업 한정.
- **fable 미사용** — opus보다 점수는 낮고(70 vs 74) 비용은 1.8배. 부활 금지.

| 에이전트 | 모델/에포트 | 역할 |
|---|---|---|
| `data-source-scout` | opus / max · RO | NPB 데이터 소스 발굴·취득 가능성·robots/약관·갱신주기·라이브 지연 조사 (**최대 리스크 담당**) |
| `planning-architect` | opus / max · RO | 요구·아키텍처·ADR·비용 2트랙 비교·로드맵·작업분해 |
| `sabermetrics-analyst` | opus / max · RO | 지표 정의·계산식·자격 기준·스플릿/상대전적 데이터 모델 |
| `senior-engineer` | opus / max | 고난도 구현·라이브 동기화·정합성·성능 |
| `junior-engineer` | sonnet / high | 범위 명확한 정형 구현 |
| `data-pipeline-engineer` | opus / max | 스키마·수집기·파서·스키마 변화 감지·ETL·대조검증 |
| `frontend-architect` | opus / max | SPA 구조·상태·페칭·라이브 갱신·렌더 성능 |
| `design-graphics-engineer` | opus / max | 목업·디자인 토큰·모션·데이터 시각화 **구현** |
| `design-auditor` | opus / max · RO | UI/UX·a11y·대비·모션·「AI틱함」 감사 |
| `security-auditor` | opus / max · RO | 인젝션·시크릿·공급망·남용 방지 감사 (공개 데이터 전제로 경량) |
| `concurrency-correctness-auditor` | opus / max · RO | 멱등성·레이스·재시도·부분실패 감사 |
| `review-specialist` | sonnet / high · RO | 1차 코드리뷰 (2차는 `model:"opus"` 오버라이드) |
| `qa-test-engineer` | sonnet / high | 테스트 작성·E2E·회귀 스위트·픽스처 관리 |
| `devops-platform-engineer` | sonnet / high | 호스팅·CI/CD·비용 관측·배포·백업 |
| `i18n-engineer` | sonnet / high | 로케일·선수명 표기(한자/카나/로마자)·숫자·날짜 포맷 |
| `chore-runner` | haiku / medium | 스크립트 실행·린트 자동수정·수집 (판단 불요 작업만) |

`RO` = 읽기 전용(코드 수정 금지).

### 감사 → 구현 분리
`design-auditor` → `design-graphics-engineer` / `security-auditor`·`concurrency-correctness-auditor` → `senior-engineer`.
감사자는 findings만 내고, 구현자가 소비한다. **한 에이전트에 감사와 구현을 같이 맡기면 자기 산출물을 승인한다.**

---

## 5. 등록된 플러그인·스킬

플러그인은 **유저 레벨**(`~/.claude/settings.json`)에서 전역 활성:
`superpowers` · `understand-anything` · `example-skills` · `document-skills` · `jp-power` · `frontend-design`
프로젝트 `.claude/settings.json`은 마켓플레이스 등록만 담당(PPS_NEW_PORTAL과 동일 구성).
프로젝트 로컬 스킬: `.claude/skills/auditing-multi-dimensionally` (PPS_NEW_PORTAL에서 승계).

---

## 6. ⚠금지·주의

- ⚠**분모 없는 비율을 렌더링하지 마라**(M2). 스플릿은 표본이 작다 — 그게 이 도메인의 1급 함정이다.
- ⚠**파서 실패를 빈 값으로 흘리지 마라**(M7). 조용한 0은 「기록 급락」이라는 거짓말이 된다.
- ⚠**이름 문자열로 선수를 조인하지 마라**(M10).
- ⚠**`new Date()` / `Date.now()` 직접 호출 금지** — 주입된 `clock` 경유(M6). 린트로 강제한다.
- ⚠**모듈 전역에 가변 상태를 두지 말 것** — 요청 스코프는 `AsyncLocalStorage`(런타임이 Node인 경우). 다중 인스턴스에서 침묵 실패.
- ⚠**수집·통지 등 변경 경로에 멱등키 없이 만들지 말 것**(M5).
- **개발 루프에서 외부 사이트를 반복 히트하지 마라** — 픽스처로 테스트(작업규칙 11).
- 예외를 삼켜 빈 값으로 렌더링하지 마라(침묵 오류의 주된 발생원).
- **라벨 강조 원칙에 도메인 예외**(2026-08-15 확정). PPS의 원칙은 「항목명이 값보다 잘 보이게」였고,
  뜻은 **「의미를 모르는 값을 보여주지 마라」**였다. 그런데 이 제품은 **숫자가 곧 내용**이라
  항목명을 값보다 크게 하면 읽을 수 없는 화면이 된다.
  → **값을 주역으로 두되 라벨과 분모를 반드시 인접**시킨다. 원칙의 정신은 M2로 지키고 형식은 버린다.
  ⚠**분모 없는 값은 여전히 금지**다. 예외는 크기 위계에만 적용된다.
- ⚠**선수 사진·구단 로고·엠블럼을 쓰지 마라**(2026-08-15 확정). §2-5 1층(「사실은 저작물이 아니다」)의
  논리가 **여기엔 닿지 않는다** — 사진은 촬영자의 저작물이고 선수의 초상권이 붙으며, 로고는 상표다.
  L5(공개된 직업활동 성적만)·L6(외부 노출은 파생값)에도 걸린다. 핫링크도 같다.
  → 대체: **우리가 계산한 값으로 만든 우리 그림**(구단 색 마크 · 월별 추이 꺾은선).
- ⚠**라이브 취득을 하지 마라**(2026-08-15 조사 확정 · `docs/decisions/2026-08-15-live-matchup-feasibility.md`).
  npb.jp가 경기 중 현재 투수를 공표하는 것은 **실측으로 확인**됐지만, 필요한 폴링이 L1의 약 100배이고
  3층(정보의 신선도 이용)·4층(규정)에 정면으로 걸린다.
  → 대체: **유저가 대전 상대를 고르는 화면**(`matchup.html`). 취득이 0이면 3·4층이 성립하지 않는다.
- `background-attachment: fixed` 재도입 금지(GPU 래스터 렉).
- 「AI틱함」 금지 목록: 보라-파랑 그라데이션 · 균질한 카드 그리드 · 이모지 아이콘 · 목적 없는 glassmorphism · 중앙정렬 히어로.
- 비밀키를 코드·리포·로그·클라 번들에 두지 말 것.
- **형제 프로젝트를 수정하지 말 것** — `../PPS_NEW_PORTAL` · `../PPS需給管理ポータル` · `../x-scraper` · `../google-chat` · `../google-calendar` 는 **읽기 전용 참조**. 「읽기 전용」에는 **스크립트 실행도 포함**된다(파일을 생성·덮어쓰는 스크립트가 실재한다).

---

## 7. 브랜드 · 계정

| 항목 | 값 |
|---|---|
| 패런트 브랜드 | **Lunomel** (`LOO-no-mel` / 루노멜 / ルノメル) |
| 제품 브랜드 | **BaseNine**(2026-08-15 확정) · 표기 `ベースナイン`. Lunomel 하위 endorsed 모델(Trovune 선례와 동일). `Nine`은 일본어권에서 **팀 자체**를 가리키는 야구어(「甲子園のナイン」)이고 9이닝·9명의 근거도 겹친다 |
| 이름의 적용 범위 | ⚠**화면에 보이는 이름만 제품명이다.** 리포명 `bb-app` · 패키지명 `@bb-app/*` · Pages 프로젝트명은 **내부 식별자**라 그대로 둔다 — 바꾸면 이력과 링크만 끊긴다. 화면 문자열은 `BB_SITE_NAME`(기본값이 제품명) 한 곳에서 나온다 |
| 서명 형식 | `<제품명> by Lunomel` (endorsed 모델 · `by Lunomel`은 시각적으로 더 조용하게) |
| 표기 규칙 | Latin 워드마크 고정 — 번역·합성(`Lunomel <제품명>`) 금지 |
| 메인 계정 | Google: `307930238+muji-j@users.noreply.github.com` |
| GitHub | `muji-j` |
| 제품 UI 언어 | **일본어 우선**(2026-08-14 확정). 데이터 원본과 표기가 일치해 구현이 단순하고, NPB 팬 주류 시장에 맞는다. 다른 로케일은 v3 이후 검토 — 단 **문자열 하드코딩 금지**(i18n 키 필수)로 확장 여지를 남긴다 |

브랜드 상세 선례: `../x-scraper/brand/README.md` · `../x-scraper/docs/superpowers/specs/2026-08-13-lunomel-brand-architecture-design.md` · `../google-chat/BRAND.md`.
**Lunomel은 브랜드 결정이지 법인이 존재한다는 선언이 아니다.** 개인정보처리방침·약관·스토어 게시자 필드는 실제 운영자를 표시한다.

---

## 8. 문서 배치

```
docs/decisions/ADR-XXX-*.md          아키텍처 결정 기록
docs/superpowers/specs/YYYY-MM-DD-*-design.md   brainstorming 산출 스펙
docs/superpowers/plans/YYYY-MM-DD-*.md          writing-plans 산출 실행계획
docs/sources/                         데이터 소스 판정표·수집 규약
docs/metrics/                         지표 정의서(계산식·자격 기준·동률 규칙)
```
