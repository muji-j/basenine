# CLAUDE.md — bb-app (NPB 정보 서비스 · Lunomel)

일본 프로야구(NPB) 각종 정보를 **일반 유저**가 쉽게 확인할 수 있는 **공개 웹 서비스**.
프로듀스 브랜드는 **Lunomel** — `../x-scraper`(Trovune) · `../google-chat`(KAWAII GChat) · `../google-calendar` 와 동일 패밀리.
설계 선례로 읽는 형제: `../PPS_NEW_PORTAL` · `../PPS需給管理ポータル`. ⚠**형제·상위는 읽기 전용이다**(루트 §10).

> **공통 규칙은 워크스페이스 루트 `CLAUDE.md`(§0~§12)에서 자동 상속된다** — 검증·완료 판정 · 설계 우선 ·
> 질문 형식 · 언어 · 에이전트 편성과 모델·에포트 기준 · 보안 · 코드 규율 · UI 원칙 · git·배포 · 경계 ·
> 문서 배치 · 스킬과 울트라코드 사용 기준. **이 파일에는 bb-app 고유한 것만 적는다.**
> 공통 에이전트 명단과 별칭 매핑은 `../_common/docs/agent-roster.md`.
>
> ⚠**스택도 제품명도 이제 확정이다** — 스택은 §1(확정돼 배포 중) · 제품명은 §7(**BaseNine** · 2026-08-15).
> ~~스택·DB·호스팅·제품명 미확정~~ 은 **2026-09-03 까지 남아 있던 거짓 서술**이다.

---

## 0. 프로젝트 제약 (모든 작업이 만족해야 함)

| # | 제약 | 판정 기준 |
|---|---|---|
| 1 | **일반 유저 접근성 최우선** — 설치·계정·설정 없이 URL만으로 핵심 정보에 도달 | 첫 방문 → 원하는 선수 성적까지 **3클릭 이내**, 로그인 요구 0 |
| 2 | **비용 2트랙 제시** — 유료 최선안 / 무료 최선안 / 두 안의 비교를 항상 병기. **상한 = 월 1,000엔**(2026-08-14 확정) | **인프라 결정문**(`docs/decisions/`)에 3섹션 전부 존재 + 월 추정액 명시 · 상한 초과안은 「참고」로만. ⚠~~ADR-001에~~ 였는데 **그 파일은 존재한 적이 없어 만족시킬 대상이 없었다**(§1) — 기준을 **이름에서 풀었다**(2026-09-03) |
| 3 | **데이터는 자동 최신화가 기본** — 수동 입력은 「자동이 불가능함을 입증한 항목」만 | 수동 의존 항목 목록 + 각각의 「왜 자동이 안 되는가」 |
| 4 | **라이브 대응은 v2**(2026-08-14 확정). 단 **v1 설계가 라이브를 막지 않을 것** | 라이브 도입 시 스키마·타입 변경 없이 얹을 수 있는가(M9 타입 분리 · 타석 로그 보존) |
| 5 | **지표 깊이** — 일반 지표(타율·출루·장타·방어율·홈런·도루) + **리그 순위** + 세이버메트릭스 + 스플릿(투수유형별/타자유형별) + 선수 간 상대전적 | §2 지표 카탈로그의 전 항목이 데이터에 존재 |
| 6 | **UI 독창성** — 「AI가 만든 티」 회피. Human Interface Guidelines 참조 | `design-auditor` 「AI틱함」 축 확정 지적 0건 + 목업 선택 이력 |
| 7 | **분모 없는 비율 렌더링 금지** | 스플릿·파생 지표 전부에 표본 수 병기 (M2) |
| 8 | **정합성** — 수집 멱등성·정정 반영·부분 실패 | `shiro-core:concurrency-correctness-auditor` 확정 P0/P1 0건 |
| 9 | 보안 — **공개 데이터 기반이므로 경량 검토**. 단 인젝션·시크릿·공급망·남용은 예외 없음 | `security-auditor` 확정 P0 0건 (P1은 근거와 함께 보류 가능) |
| 10 | **출처 추적성** — 모든 수치가 「어디서 · 언제 · 몇 번째 판」인지 답할 수 있을 것 | `provenance.test.ts` 통과 — **어디서·언제**는 전 수집 표가(자기 컬럼이든 부모를 타고든) · **몇 번째 판**은 정정이 성립하는 표가. ⚠~~M4 필드 4종이 전 수집 테이블에 존재~~ 는 **거짓이었다**(2026-08-27 · 아래) |
| 11 | **법적 안전** — robots.txt·이용약관 준수, 출처 명기, 사실(수치)과 표현물 구분 | `data-source-scout` 소스별 판정표에서 「미확인」 0건 |
| 12 | 비용 — 무과금 선호, 필수 비용은 지불 가능 (**월 1,000엔 이내**) | 제안마다 월 추정액 병기 |
| 13 | **v1 = 지인 공유**, 시장 배포·수익화는 별도 단계 | v1 범위에 결제·과금·계정 기능 넣지 않기 |

### 단계 정의 (2026-08-14 확정)

```
v1   확정 데이터    선수·구단 성적 / 리그 순위 / 세이버메트릭스 / 스플릿 / 선수 간 상대전적
v2   라이브        경기 중 현재 투수 × 현재 타자 기준 표시 (v1 스키마 위에 증축)
v3+  시장 배포·수익화 검토
```
⚠**「라이브를 v2로 미룬다」는 「라이브를 고려하지 않는다」가 아니다.**
확정/잠정 타입 분리(M9)와 **타석 단위 원시 로그 보존**은 v1부터 지킨다. 안 지키면 v2가 전면 재작성이 된다.

---

## 1. 기술스택 · 아키텍처 (**확정** — 아래는 추정이 아니라 실측이다)

⚠**~~미확정 — ADR-001에서 결정 · 선택 전 구현 착수 금지~~ 는 2026-09-03 까지 남아 있던 거짓 서술이다.**
스택은 확정됐을 뿐 아니라 **완성돼 배포되고 있다.**

| 층 | 실제 | 근거 |
|---|---|---|
| 런타임·언어 | **Node ≥24 + TypeScript** npm 워크스페이스 · 패키지 **7개**(`domain`·`parser`·`store`·`aggregate`·`metrics`·`archiver`·`web`) | `package.json` (`engines.node` = `>=24`) · `packages/` |
| 저장 | **SQLite 1파일**(`data/bb.sqlite`) · 원본은 `data/archive` 의 `.html.gz` | ⚠**둘 다 `.gitignore`** — 저장소에 없다 |
| 화면 | **정적 다페이지 생성 · 서버가 없다.** `npm run build:web` 이 9시즌을 `dist/` 로 굽는다 | `packages/web/tools/build.ts` |
| 호스팅 | **Cloudflare Pages** + **Access**(One-time PIN · 세션 1개월) | `docs/operations/deploy.md` |
| 배치 | **GitHub Actions** — `daily.yml`(수집·빌드·배포·반영 확인) · `access-config.yml` | `.github/workflows/` |
| 배포 도구 | `wrangler` | `package.json` devDependencies |

⚠**「서버가 없다」가 이 표에서 가장 무거운 줄이다.** 폴링·SSR·요청 시 계산을 전제한 설계는
이 제품에서 성립하지 않는다. 라이브도 그래서 §6 이 **취득**을 금지한다(표시는 v2 · §0 제약4).

### ⚠ADR-001 은 존재한 적이 없다

**전 브랜치 595커밋 전수**에서 `ADR-` 이름 규칙에 맞는 파일이 추가된 적이 **0건**이다
(2026-09-03 실측 · `git log --all --diff-filter=A`). 남아 있는 것은
**유료 트랙 설계안**(`docs/decisions/2026-08-14-paid-track-verified.md` — VPS + PostgreSQL +
Fastify + React SSR)뿐인데, **그건 채택된 안이 아니다.** 실제로 선 것은 무료 트랙이고
**그 전환을 기록한 문서가 없다.**

→ 지금 필요한 것은 「선택 절차」가 아니라 **「왜 이 구성이 됐는지」의 사후 기록**이다.
⚠**그래서 §0 제약2의 판정 기준(「ADR-001에 3섹션 전부 존재」)이 공중에 떠 있다.** 다음 인프라
결정에서 그 형식을 실제로 쓰든지, 판정 기준을 현행 문서 관례(날짜 접두사 · §8)에 맞추든지
**둘 중 하나를 골라야 한다.** 지금은 만족시킬 대상이 없다.

**다음 인프라·배치 결정에서 GAS를 무비판으로 채택하지 않는다.** 구 PPS 프로젝트가 GAS에서 탈출한
이력이 바로 옆에 있다(`../PPS_NEW_PORTAL/docs/decisions/ADR-001-stack.md` · **읽기 전용**).
동시 실행·실행시간 상한·락·트리거 중복 기동에서 **침묵 실패**가 나는 것이 실증되어 있으므로,
후보로 올릴 때는 **그 실패 모드를 어떻게 피하는지**를 함께 쓴다.

인프라 결정문에 반드시 들어갈 항목: 후보 · 근거 · **뒤집힐 조건** · 월 추정액 · 데이터 총량 추정.

---

## 2. 도메인 (NPB) — 반드시 지킬 규칙

### 2-1. 시간·경기 식별
- **JST 고정.** 로컬 타임존 의존 금지.
- 경기일(game date)과 실제 시각은 다르다 — **연장·서스펜디드·더블헤더·우천 노게임·재편성**을 식별자가 감당해야 한다.
- 시즌 경계(정규/CS/일본시리즈/오픈전/이스턴·웨스턴)를 데이터에 명시. **섞어서 집계하지 마라.**

### 2-2. 지표 카탈로그 (§0-5의 실체)
```
타자 기본   타율 출루율 장타율 OPS 안타 2루타 3루타 홈런 타점 득점 도루 도루자 사사구 삼진 병살
            ⚠**도루자(盗塁刺)는 박스스코어에 없다** — `盗塁` 컬럼 하나뿐이다.
              출처는 **타석 로그의 주자 행**이고(2026-08-17 구현), 대조는 이렇다:
              경기별 도루 수 대 박스 `盗塁` 열 — **완결 8시즌 6,875경기** 전수에서 **어긋남 0건**.
              ⚠**실질 분모는 도루가 있던 4,229경기**다 — 도루 0인 경기는 0=0 으로 자동 일치하므로
              그것까지 세면 분모가 부푼다.
              **2025 정규시즌 도루 921 · 도루자 381.**
              ⚠**세는 범위는 완결 시즌이다**(2026-08-22 정정). ~~2026 진행 시즌의 611/254~~ 를 적고 있었고
              **경기가 들어올 때마다 낡아 이미 618/255 였다** — 시즌 중에는 매일 그렇게 된다.
              ⚠**공표값 대조는 「그날의 사건」이다**: 2026-08-17 에 2026 도루 611 = 공표 611 ·
              도루자 254 = 공표 `盗塁刺` 254 로 일치했다. **그 일치는 이력이고,
              오늘 값이 다른 것은 틀려서가 아니라 경기가 늘었기 때문이다.**
              ⚠**견제사(牽制死)는 도루자가 아니다** — 성공률의 분모에 넣으면 전원이 낮아진다.
              ⚠**대회를 섞으면 틀린다**: 2025 도루는 정규 921 인데 전 대회를 섞으면 **943 이 되어 22 어긋난다.**
              ⚠**이 수들은 `scripts/test/doc-figures.test.ts` 가 DB 로 다시 센다** — 손으로 고치지 마라.
투수 기본   방어율 승 패 세이브 홀드 이닝 피안타 피홈런 사사구 탈삼진 WHIP 퀄리티스타트
세이버      wOBA wRAA wRC+ ISO BABIP K% BB% · FIP WHIP K/9 BB/9 HR/9 · RE24 WPA(타석 로그 기반)
            ⚠**아래 두 판정은 사유를 이미 네 번 고쳤다**(1차 2026-08-14 · 2차 08-17 · 3차 08-20 ·
              **4차 08-20 · 같은 날 · 정보원 전수 조사에서**).
              **결론은 한 번도 안 바뀌었고 네 번 다 사유가 틀렸다.** 4차에서 무너진 것 넷:
              ⑴ **NPB BIP 가 UZR 의 경로였던 적이 없다**(운영사 카탈로그에 打球·座標·トラッキング **0건** ·
              실측 grep) — 「비싸서 못 쓴다」가 아니라 **「그 문은 그 방이 아니었다」**.
              ⑵ **1.02 인용에서 두문 `当社の許諾のない` 가 빠져 있었다** — 금지가 아니라 **「허락 없이는」**이고,
              약관이 스스로 허락 창구를 안내한다. **게다가 스크레이핑 조항이 `手作業で複製や蓄積` 까지 포함해
              현행 운용도 문언상 닿는다.** → ⚠**사용자가 「허락을 얻는」 대신 「의존을 끊는다」를 골랐다**(아래).
              ⑶ **Positional 의 사유가 「데이터가 없다」에서 「권리가 없다」로 바뀌었다** —
              타석마다 수비 9명이 든 데이터가 실재한다.
              ⑷ **「뒤집힐 조건」의 방향이 반대였다** — NPB 데이터는 개방이 아니라 **상품화**로 가고 있다.
              조사 전문 둘: **`docs/sources/2026-08-20-blocked-metrics-source-survey.md`**(소스 **판정 20건** ·
              4차) · `docs/sources/2026-08-20-war-xfip-recheck.md`(실측 수치 · 3차).
              ⚠**「이번엔 다 맞다」고 읽지 마라. 다섯 번째가 없다고 가정하지 마라.**
            ⚠**xFIP는 산출하지 않는다**(**사유 3차 정정 2026-08-20**). ~~FB%가 소스에 없다~~ ·
              ~~인플레이의 30%가 미분류(113,019행)~~ → 둘 다 낡았다. **완결 8시즌(2018~2025) 516,038타석 전량 실측**:
              인플레이 타구 **369,214** 중 종류 미분류 **101,292(27.4%)** 이고 그 내역이
              単打 79,809 / 二塁打 19,417 / 三塁打 2,066 = **100% 안타**. 종류가 붙은 비홈런 안타는
              **815건이고 전부 バントヒット**다. → **비율보다 강한 전칭 명제로 적어라:
              「번트를 뺀 안타는 보유 전 시즌 전건이 타구 종류 없음」.** 시즌별 미분류율 **27.0% ~ 27.8%**(완결 8시즌 폭 0.8%p) —
              **「소급해도 안 채워진다」가 이제 추론이 아니라 실측이다.**
              ⚠**세는 범위는 완결 시즌이다**(2026-08-21 정정). 진행 중 시즌을 넣으면 **경기가 하나 들어올 때마다
              이 줄이 낡고** 시즌 중에는 매일 그렇게 된다 — 走塁 각주에 이미 적용한 규칙과 같다.
              **전칭 명제 쪽은 안 좁힌다** — 그건 2026 까지 포함해 검사한다(`scripts/test/doc-figures.test.ts`).
              방향으로 메울 수 있는 것은 **22.5%**뿐이고 **「前」 68.1%는 원리적으로 안 갈린다.**
              ⚠**「크기」로 막는 게 아니다** — 두 극단(前·線을 전부 땅볼/전부 뜬공)의 xFIP 차이는
              중앙값 **0.040** · 최대 **0.169**이고, 같은 집단 SD **0.475**의 각각 **8% · 36%**다
              (2025 · 100이닝 이상 47명 · 리그 상수는 두 경우 각각 다시 잡았다).
              막는 것은 ⑴**정답 표본이 0건**이라 T1도 T2도 아니라는 것 · ⑵**그건 xFIP가 아니라는 것**
              (이름을 달면 거짓말이고, 이름을 바꾸면 비교 가능성이라는 존재 이유가 사라진다).
              ⚠「오차가 투수 유형과 상관돼 있다」는 근거는 **약하다**(r=−0.300은 분모를 나눠 갖는
              두 비중이라 산수가 강제한다 · 분모를 독립시키면 −0.119) — **기각을 여기에 얹지 마라.**
              ⚠**4차(08-20 소스 조사)에서 위 수치는 하나도 안 바뀌었다.** 바뀐 것은 **구멍의 소유자**다 —
              **Yahoo 계열 공개 미러도 안타에는 방향만 붙인다**(`bb_type` 실측). 즉 이 구멍이
              **npb.jp 만의 성질이 아니라 일본 스코어링 관행 자체의 성질**일 가능성이 올라갔다.
              타구 4분류를 정당하게 주는 소스는 **판정한 20건 중 0건**이다:
              ⚠**~~전수 조사~~ 가 아니다**(2026-08-24 정정 · 감사 [50]). 집계 사이트 목록을
              **어떤 질의로 만들었는지가 조사 문서에 적혀 있지 않고**, 명단 밖에서 최소 2건이 나왔다.
              **「그 20건이 전부」가 아니라 「판정한 20건에서 0건」이다** — 결론은 안 바뀌고 **분모의 뜻이 바뀜다**.
              한계는 그 문서 §6 표의 7번 행에 있다.
              1.02 Premium은 **화면 전용 · 野球関係者 한정**, NPB+는 **앱 전용 + 리버스 엔지니어링 명시 금지**,
              구단 Hawk-Eye는 **선수별 시즌 「割合」 · 홈구장 한정 · 打球角度 기준(우리 스코어러와 다른 수) ·
              투수 피타구 없음**. ⚠**구단 공개처는 야쿠르트 1곳만 확인됐고 나머지 11구단은
              「없었다」가 아니라 「못 찾았다」**다 — 톱페이지·사이트맵 스캔은 **야쿠르트 자신에게도 위음성**이
              난다는 것을 실측했다. **음성 결과를 「없다」의 근거로 쓰지 마라.**
            ⚠**WAR는 표시하지 않는다**(2026-08-14 확정 · **사유 3차 정정 2026-08-20 · 4차 정정 같은 날**).
              ~~타구 좌표·존·타구속도가 공개 기록에 존재하지 않는다~~ → **틀렸다. 존재한다**(2026-08-17).
              ~~그러니 「정당하게 접근할 수 없다」가 사유다~~ → **그것도 투수에 대해서는 거짓이다**(2026-08-20):
              **FIP 계열 투수 WAR은 정의상 수비를 배제한다.** 그 사유가 애초에 적용되지 않는다.
              **투수 WAR** — 부품은 거의 다 있다: FIP·cFIP **있음** · **RPW 실측**(2025 센트럴 8.485 ·
              퍼시픽 8.667 · 각 리그내 375경기) · **파크팩터 실측**(docs/metrics/ §3.3) · 레버리지 불필요.
              없는 것은 **대체수준 하나이고 그건 데이터가 아니라 우리가 고르는 값**이다.
              진짜 사유는 **이름**이다 — 일본 기준 구현(1.02/DELTA)이 **tRA 기반**이라 FIP로 낸 값은
              구조적으로 다른 수이고, 대체수준을 고르는 순간 그 값은 우리 것이 된다.
              ⚠~~그래서 투수 WAR은 오늘도 뒤집을 수 있다 — 데이터가 아니라 결정이다~~
              ⚠**사용자가 결정했다(2026-08-27) — 그 결정이 이 자리를 닫는다.**
              「대체수준을 우리가 정한다 + **WAR이 아닌 이름을 쓴다**」가 유일한 길인데,
              **이름을 바꿔야 하는 세이버는 무조건 패스**가 방침이다(아래 §2-2-2).
              → **투수 WAR도 안 낸다. 「데이터가 아니라 결정」이었고, 그 결정이 났다.**
              ⚠**다시 열리는 조건은 「이름을 그대로 쓸 수 있게 되는 것」뿐이다** —
              대체수준이 정해지는 것으로는 안 열린다. 1.02/DELTA 와 같은 산식 기반을
              **정당하게** 얻어 tRA 로 낼 수 있게 될 때다(= xFIP 를 막는 그 구멍이 열릴 때).
              ⚠**tRA가 요구하는 4종 분류(ゴロ/ライナー/内野フライ/外野フライ)는 xFIP를 막는 그 구멍과 같다 —
              하나가 열리면 둘 다 열린다.** 따로 재검토하지 마라.
              **타자 WAR은 여전히 불가**(이건 사실이다). 하드 블록 셋:
              ⑴ **UZR** — 존·타구 좌표는 **존재한다**(Data Stadium이 `hc_x`/`hc_y`·`plate_x`/`plate_y`·구속·
              수비 배치 9명까지 보유 · 2026-08-17 확인). 막는 것은 **접근**이다.
              ⚠**~~문서화된 클린 경로는 NPB BIP 하나뿐이고 월 3만엔~ 이라 우리 상한의 30배다~~ →
              4차 정정(08-20): 두 군데가 틀렸다.** ①**BIP 는 그 데이터를 판 적이 없다** —
              운영사 공개 카탈로그 원문이 「球団・選手情報、試合結果、打者・投手・守備・走塁成績、
              セイバーメトリクス指標」이고 **打球·座標·トラッキング·球種·球速이 0건**이다(샘플 2건 실측 grep).
              「월 3만엔」은 실재하는 문구지만 **그 값은 좌표가 안 들어간 상품의 값**이라
              UZR 을 막는 근거가 못 된다. ②**서비스 페이지가 지금 404**이고 웨이백 마지막 200이 2023-04-25다 —
              ⚠**종료 공지는 없다. 「종료됐다」고 쓰지 마라.**
              **정본 판매 주체는 따로 있다: JBD(Japan Baseball Data)** — NPB·12구단이 허락한 주체이고
              1.02 약관 말미가 그 사실을 적는다(실측). **B2B 문의 전용 · 가격 비공개.**
              ⚠**그러므로 지금 우리가 말할 수 있는 것은 「비싸다」가 아니라 「가격을 물어보지 않았다」**다.
              무허락 경로는 상류가 명시적으로 스크레이핑을 금지한다.
              ⑵ **UBR** — `bases`가 **점유만** 말하고 **주자 신원을 안 말한다.** 신원이 붙은 것은
              `runner_event`뿐이고 그건 도루·도루자·견제사만 담는다 — 「단타에 1루에서 3루까지 갔는가」를 물을 수 없다.
              ⚠**셋 중 이것만이 「어느 소스에도 못 찾았다」**이다 — 그것도 **「없다」가 아니라 「못 찾았다」**로 적는다.
              ⑶ **Positional** — ⚠**사유가 4차에서 바뀌었다: 「데이터가 없다」가 아니라 「권리가 없다」다.**
              **우리 소스에 대해서는** 여전히 참이다 — 박스는 위치를 **문자열 순서**로만 주고 **이닝 수가 없다.**
              실측(완결 8시즌): 정규 타자-경기 **21,027/189,870 = 11.1%**가 복수 표기 ·
              2025 타자 **206/642(32%)**가 수비 2포지션 이상이고 **指 포함이면 252명(39%)**, 최대 7.
              ⚠**32%와 39%를 가르는 것은 DH를 세는가 하나다 — 이닝이 없으면 포지션 보정은
              「정의를 고르는 일」이 된다.** 수비 교대를 로그로 메울 수도 없다:
              `playbyplay`의 특수행은 2025-08-16 6경기 전수 **48개가 전부** `（先発投手）`·`（投手交代）`이고
              수비 교대 **0건**이다 — ⚠**분모는 하루 6경기다. 전 시즌은 안 쟀다.**
              **그런데 타석마다 수비 9명이 든 데이터가 실재한다** — 공개 데이터셋의 타석 CSV가
              `fielder_2`~`fielder_9`를 담는다(헤더 실측 · ⚠**칸은 8개이고 투수는 `pitcher` 열이다** —
              둘을 합쳐야 9명). **그러면 이닝은 근사가 아니라 계산이 된다.**
              못 쓰는 이유는 **상류가 Yahoo**이고 LINEヤフー 이용규약 14가 「제공 목적을 넘는 이용」을
              정면 금지하기 때문이다(영리 요건 없음). ⚠**게다가 그 데이터셋은 출처·라이선스를 한 줄도 안 밝힌다** —
              상류가 Yahoo라는 것은 **매우 강한 정황이지 확정이 아니고, 확정이 아니라서 더 위험하다.**
              → **UZR과 Positional이 이제 같은 모양이다: 「없다」가 아니라 「못 쓴다」.**
              **UBR만 「못 찾았다」**이고 셋을 뭉뚱그리지 마라.
              타 사이트 계산값을 빌려오지도 않는다(갱신·정의 추적 불가).
              ⚠**참고**: `npbbasement.com`이 `GB%/LD%/OFFB%/IFFB% · tRA · TZR · Inn · Positional · batWAR`를
              무료로 낸다(번들 실측 · **1군 2020~2026 · 2군 2021~2026 = 13개 시즌-리그** ·
              ⚠**「2020~2026 1군2군 전부」가 아니다 — `2020_2g`는 없다**).
              **「일본에서 아무도 못 한다」가 아니라 우리가 안 하는 것이다** —
              **출처 표기 0건**이라 상류 권리를 추적할 수 없어서 채용하지 않는다.
              ⚠**1.02 — 의존을 끊는다**(**사용자 결정 2026-08-20** · docs/metrics/ §6-C).
              지금까지는 「산식만 읽고 값은 대조용으로도 안 쓴다」였다. **4차 정정에서 그 전제가 흔들렸다:**
              제6조의 두문이 **「当社の許諾のない」**이고(우리 인용에서 이 두 글자가 빠져 있었다)
              그 뒤에 「以下のデータ利用（個人・法人問わず）（１）商用利用
              （２）分析、研究、またはその結果を第三者に提供、開示を目的とした利用」이 온다 —
              **절대 금지가 아니라 「허락 없이는」**이고 **약관이 스스로 허락 창구를 안내한다.**
              **영리 요건이 없다는 것은 그대로 맞다**(商用利用은 별개 항목).
              ⚠**더 무거운 것**: 같은 조에 「機械的に、**もしくは手作業で**複製や蓄積、転送する行為やスクレイピング行為」가
              별개 불릿으로 있다 — **현행 운용(「산식만 손으로 옮긴다」)도 문언상 여기에 닿는다.**
              ⚠**「robots가 Allow니까」로 지탱하지 마라** — robots는 크롤 지시이고 §2-5 **4층(계약·규정)에 답하지 않는다.**
              → **그래서 허락을 얻는 대신 「의존 자체를 끊는다」를 골랐다.**
              **wOBA·FIP는 원래 공개 정의가 있다**(학술 논문·서적·공개 해설) — **1.02를 인용할 이유가 없다.**
              ⚠**「위반이었다」고 쓰지 마라** — 우리는 법률 판단을 할 위치가 아니고, 이건 **회색지대를 안 밟기로 한 선택**이다.
              ⚠**「이제 깨끗하다」고도 쓰지 마라 — 문서 5곳이 남았다**(docs/metrics/ §6-C 의 표에서 ☐).
              **코드·시험·화면 문자열과 정의서 §3.1·§3.2 는 2026-08-20 에 끊었다.**
              ⚠**대체 출처를 「찾은」 게 아니라 「없어도 되게」 했다** — wOBA 계수와 wOBAscale 을
              **우리 RE 행렬에서 리그×시즌마다 유도**한다(`scripts/woba-weights-derive.ts` ·
              `packages/aggregate/src/woba-weights.ts`). **선형가중치는 원래 RE 에서 나오고
              우리는 RE 를 이미 만들고 있었다** — 베낄 이유가 애초에 없었다.
              ⚠**값이 움직였다**: 자격 도달 타자 459명(18 리그-시즌)의 `|Δ wRC+|` 중앙 **1.01** · 최대 **10.00** ·
              **1위가 바뀐 리그-시즌 0/18**(wOBA·wRC+ 둘 다). 적용 기준과 수는 docs/metrics/ §4.
              ⚠**계수를 화면에 박지 마라** — 리그·시즌마다 다르므로 박는 순간 다른 시즌에서 거짓이 된다.
              ⚠**화면에 「데이터가 없습니다」라고 쓰지 마라** — 타자 쪽은 「우리가 못 얻는다」이고
              투수 쪽은 **「우리가 정하지 않았다」**다.
              ⚠**뒤집힐 조건의 방향도 4차에서 반대로 밝혀졌다.** 지금까지 「NPB가 DMP/PitchNet 계열에서
              데이터를 공개하면」을 조건으로 적었는데, **NPB+ 앱이 2026-02-26 정식 개시**(앱 전용 +
              리버스 엔지니어링 명시 금지 · **실측**)됐고, **2025 시즌부터 NPB가 12구단 데이터를 일괄 관리하며
              외판(外販)을 모색**한다(⚠**보도 인용 · 미대조** — 저장 샘플에 근거가 없다) —
              **개방이 아니라 상품화다. 그 조건의 확률은 올라간 게 아니라 내려갔다.**
              **PitchNet의 실체도 4차에서 확정됐다.** ⚠**우리 문서는 지금까지 「DMP/PitchNet 계열」이라고만 적고
              실체를 안 적었다**(「SPA라 못 봤다」고 적은 적은 없다 — 그건 데이터스타디움 쪽 서술이다).
              실체는 **Okta SSO 사내 툴**(issuer `npb.okta.com` · 인프라는 MLB Advanced Media)이고
              **투구 분류(pitch classification)용**이다 — 번들에 타구 쪽 필드가 0건이다.
              **즉 인증 담장이고, 애초에 우리가 찾던 물건이 아니다. 담장은 넘지 않는다.**
              ⚠**BIP·JBD 가격을 「현재의 사실」처럼 쓰지 마라.** 1차 사실은 **카탈로그에 그 데이터가 없다**는 것이고,
              가격에 대해 우리가 말할 수 있는 것은 **「물어보지 않았다」**뿐이다 — **「비싸다」는 추측이다.**
              **문의하면 정해진다**(뒤집힐 조건에 넣었다 · docs/metrics/ §6 3′).
              ⚠**언제 물을지는 사용자가 「나중에 정한다」로 보류했다**(2026-08-20).
              ⚠**1.02 허락 창구는 이 목록에서 빠졌다** — 허락을 얻는 대신 **의존을 끊기로 정했다**(위).
            → 대체: **SRC(状況得点貢献)** 확정(2026-08-15). 타격 상황가중만. 정의는 docs/metrics/ §3.5
              가진 입력(타석 로그)으로 정직하게 계산 가능하고, 무엇을 재고 무엇을 안 재는지 말할 수 있다.
              ⚠자체 지표는 반드시 **WAR과 다른 이름**을 쓴다. 이름이 같으면 값이 다른 것이 거짓말이 된다.
순위        위 전 지표의 **리그 내 순위** (센트럴/퍼시픽 구분 · 자격 기준 명시 · 동률 처리 규칙 명시)
스플릿      타자: 좌투/우투 상대 · 홈/원정 · 주자상황 · 월별 · 구장별 · **상대 구단별**
            투수: 좌타/우타 상대 · 홈/원정 · 주자상황 · 월별 · 타순별 · 구장별 · **상대 구단별**
            ⚠구종별은 제외 — 무료·약관 클린 소스 0건(Yahoo가 2026-02에 앱 전용으로 이관).
            ⚠**상대 구단별은 타자와 투수에서 뜻이 반대다** — 타자에게는 수비하는 쪽,
              투수에게는 치는 쪽이다. 한쪽 식을 베끼면 **조용히 반대 팀이 나오고**
              값도 합계도 그럴듯해서 눈으로는 못 잡는다(`aggregate/src/splits.ts` 에 두 식을 나란히 둔다).
            ⚠**「今季 / 通算」 전환은 표본이 무너진 축에만 붙인다**(2026-08-27 확정).
              「전환할 수 있으니 전부 전환한다」는 **탭만 늘리고 정보를 안 더한다**
              (스플릿을 전부 두 벌로 두면 6탭 → 13탭이다).
              실측(2025 · 규정타석급 40명 · **30타석 미만 칸의 비율** 今季→通算):
              **対左右 0.0%→0.0%** · **走者状況 0.0%→0.0%** · **本拠地 0.0%→0.0%**(한 시즌으로 충분) ·
              打順 53.2%→32.2%(안 서는 타순은 영영 얇다 · **구조적 잔여**) ·
              ⚠**球場別 54.0%→62.2%**(**통산이 더 나쁘다** — 9시즌이면 지방구장이 더 는다) ·
              **対戦球団別 52.7%→32.3%** · **カウント別 33.8%→14.0%** ← 이 둘은 붙였다.
              ⚠**「안 한다」의 사유가 셋 다 다르다**(정보를 안 더한다 / 구조적 잔여 / 더 나빠진다) —
              뭉뚱그리면 다음 사람이 잘못된 것을 다시 시도한다.
              ⚠**그런데 이 잣대(얇은 칸의 비율)가 모든 축에 맞는 것은 아니다 — 실제로 틀렸다.**
              **対戦成績**(투수×타자)은 이 잣대로 **91.6%→82.0%**(대전 타석 중앙값 3으로 불변)이라
              「통산이 못 고친다」로 판정할 뻔했다. **그 표는 분모를 달고 얇은 행을 그대로 싣는 표라
              「얇은 행의 비율」이 애초에 결함이 아니었다.** 물어야 할 것은 **「시즌 화면에
              원리적으로 존재할 수 없는 행이 생기는가」**였고, 다시 재니
              **30타석 이상인 대전이 今季 0행 · 通算 382행**(今季는 **한 쌍 최대가 27타석**이라
              30을 넘을 수가 없다) — **붙였다.**
              ⚠**축마다 무엇이 결함인지가 다르다. 잣대를 그대로 옮겨 쓰지 마라.**
              ⚠**통산에는 「지금 없는 사람」이 섞인다 — 붙일 때마다 나오는 함정이다.**
              실측(2026 화면): 통산 대전 상대 중 **그 시즌 페이지가 없는 투수 523명 · 타자 651명**.
              그대로 두면 ⑴**링크가 404** 이고 ⑵**구단 칸이 빈다**(`teamOfPlayer` 는 그 시즌 소속만 담는다).
              → 구단은 **마지막으로 뛴 시즌** 것을 쓰고, 페이지가 없으면 **링크만 빼고 이름은 남긴다.**
              ⚠**행을 지우지 마라** — 그 대전은 실제로 있었다.
              ⚠**`careerMatchups` 자체를 메우지 마라** — 선발예고 화면이 **빈 구단으로 은퇴 선수를
              거르는 데 의존**한다. 선수 페이지 경로에서만 갈아 끼운다.
              ⚠**月別은 또 다르다** — 키가 `2025-04` 라 통산이면 54행이 되고,
              「4月에 약하다」를 보려면 **연도를 지운 月** 로 다시 정의해야 한다.
              **그건 새 축이지 같은 축의 통산이 아니다.**
              ⚠**통산을 만들기 전에 그 재료의 보유율을 먼저 재라**(M11) —
              볼카운트는 **9시즌 전 시즌 100%**(**결측 0건** · 보유 전 시즌·전 대회)라 안전했다.
              안 재고 만들면 **미수집을 0으로 메우는** 쪽이 된다.
              ⚠**그 보유율을 「N/N 실측」으로 적지 마라 — 첫 판에서 그렇게 적었다가 두 번 틀렸다**
              (2026-08-27). ⑴ 그 총계는 **진행 중 시즌을 포함해 매일 늘어난다**(doc-figures 가
              막으려는 바로 그 모양) · ⑵ **손으로 더한 수 자체가 틀렸다**(482,563 이라고 적었는데
              정규 타석은 552,563 이다). **전칭 명제로 적어라 — 「결측 0건」은 자라지 않고,
              틀리면 바로 거짓이 되므로 검사할 수 있다.**
              판정표 전문: `docs/superpowers/specs/2026-08-27-season-career-toggle-survey.md`
              (선수 페이지 블록 **14개** 전부에 대한 가능/불가와 **그 사유**).
            ⚠**「通算」이라고만 쓰면 거짓이다** — 우리 통산은 **보유 범위 안**의 통산이고
              NPB 가 같은 화면에 공표하는 `通算成績` 은 **그 선수의 실제 전 경력**이다.
              **화면이 「2018〜2026年」이라고 말한다**(`火消し` 가 정본 · 시험이 고정한다).
              ⚠**통산의 끝은 언제나 「보고 있는 시즌」**이다 — 2022년 화면이 2026년을 말하지 않는다.
상대전적    투수 × 타자 개별 매치업 통산 성적
소급 범위   보유는 **2018~2026 9시즌**(2023·2024를 2026-08-17에, 2022~2018을 2026-08-18에 공개).
            ⚠**이 줄이 낡으면 판정 기준 자체가 거짓이 된다**(2026-08-18 감사 P2에서 정정 —
              「2023~2026 4시즌 · 6,816파일」로 남아 있었다). 백필할 때 **여기도 같이 고쳐라.**
              지금 값은 실측이다: 배포물 **9,514파일**(2026-08-27 · CI 배포 실측) · DB 보유 시즌 2018~2026.
              ⚠**이 값은 ドラフト 화면(시즌마다 1장 · 9장)을 얹기 전이다**(2026-09-05).
                **CI 로는 아직 안 쟀다** — 로컬 전 시즌 빌드는 **9,549파일**이다(2026-09-07 · §2-2 아래).
                ⚠**둘을 같은 수로 맞추지 마라** — 하나는 배포된 파일 수, 하나는 빌드 산출 파일 수다.
            ⚠**드래프트만은 보유 범위가 다르다**(2026-09-07). DB 가 가진 드래프트는 **2005~2025 21시즌**이고
              이제 **그 전부에 화면이 있다** — 경기가 없는 13시즌(2005~2017)은 `draft.html` **한 장씩**만 굽는다.
              ⚠**「시즌을 늘렸다」고 읽지 마라** — 선수·구단·순위는 그 해에 여전히 없다.
                상단 내비가 그 화면들을 **가장 최신 시즌으로** 보내고, `→` 와 `aria-label` 이 그 사실을 말한다
                (`pathsFor` 의 `navTo`). 조용히 보내면 사용자가 해가 바뀐 것을 모른다.
              ⚠~~15,500파일~~ 은 **경기 페이지를 날짜별로 묶기 전**의 값이다(2026-08-24 배포 로그).
              ⚠**이 수는 매일 는다** — 경기가 들어오면 페이지가 늘어난다. 날짜와 함께 읽어라.
            ⚠**2018 은 슬러그가 다르다** — 오릭스가 **`bs`**(2019부터 `b`)다.
              모른 채 적재해서 **148경기가 「모르는 팀 코드」로 실패**했다. 그대로 뒀으면
              2018 오릭스 성적이 화면에서 사라진 채 「그 시즌은 원래 그렇다」로 읽혔을 것이다.
              → `TEAM_CODE_ALIASES`(domain/teams.ts)에 넣었고 **적재 시점에 정규화**한다.
              ⚠**조용히 넘기지 않는 설계가 이걸 잡았다**(M7) — 0으로 흘렸으면 못 봤다.
              ⚠**다음 소급 시즌에서도 먼저 이걸 의심하라**: 실측으로 2018 이전은 아직 안 쟀다.
            ⚠**2018 품질 실측**(2026-08-18): 12팀 전부 **143경기** · 투수 귀속 **100.0%**(67,352타석) ·
              타자 100.0% · 볼카운트 100.0% · 적재 실패 **0** · 격리 **0** · 투타 미상 **0/1,640** ·
              대회 구분 정상(정규 858 · CS 13 · 일본시리즈 6 · 올스타 2).
            ⚠**2021 은 공개 전에 품질을 쟀다**(CLAUDE.md 가 요구하는 절차 · 2026-08-18):
              투수 귀속 **100.0%**(64,749타석) · 타자 귀속 100.0% · 볼카운트 100.0% ·
              12팀 전부 정확히 143경기 · 대회 구분 정상(정규 858 · CS 10 · 일본시리즈 6 · 올스타 2) ·
              ⚠**~~정규 890~~ 이었다**(2026-08-24 정정 · 감사 [45]). 890 은 **실시 858 + 미실시 32** 이고,
              같은 문단의 2018(858)·2020(720)은 **실시만** 센다 — **같은 이름이 시즌마다 다른 것을 셌다.**
              ⚠**이 파일에서 「정규 N」은 실시 경기다**(미실시는 따로 적는다). 2021 미실시는 32건.
              적재 실패 0 · **격리 0**(도루 대조 통과) · 투타 미상 0명.
            ⚠**2020 은 143경기 시즌이 아니다** — 코로나로 **120경기**였고(실측: 12팀 전부 120),
              그래서 `REGULAR_SEASON_GAMES` 를 **시즌별 표**(`regularSeasonGames(season)`)로 바꿨다.
              143 고정이면 2020 화면에 「잔여 −23경기」·「143試合換算」이 나온다.
              부수 검산도 맞았다: 일본시리즈 **4경기**(소프트뱅크 스윕) ·
              클라이맥스 **2경기**(그해 센트럴은 CS 를 열지 않았다) · 타석 귀속 54,612/54,612.
            ⚠**빌드 메모리가 먼저 벽이었다**(2026-08-18 실측). 9시즌에서 Node 기본 힙(약 2GB)을 넘어
              **8시즌까지 만들고 마지막에서 OOM**으로 죽었다(CI run 32126443819 · exit 134).
              지금은 `--max-old-space-size=6144` 로 고정(실측 필요량 3GB 미만 · 러너 16GB).
              쓰는 곳 둘: ⑴ 링크 검사가 전 시즌을 모아야 해서 15,434장을 들고 있었다(약 900MB) —
              **색인만 남기게 고쳤다**. ⑵ **전 시즌 SiteData 를 미리 다 읽는다**(시즌 전환 링크 때문) —
              **남은 덩어리는 이쪽이고 다음 백필 전에 손봐야 한다.**
            ⚠**Pages 파일 상한** — 배포당 20,000개(유료 100,000).
              ⚠**~~9시즌이 15,434파일(77%)이고 2시즌만 더 넣으면 벽이다~~** — **벽을 물렸다**(2026-08-26).
              **경기 페이지를 날짜별로 묶었다**: 경기 하나에 파일 하나였던 것을 **경기일 하나에 파일 하나**로.
              9시즌 전 시즌 빌드 실측 **9,549파일 / 1478.2MB = 47.7%** ·
              링크 **9,392장 검사(앵커 포함) · 깨진 것 0** · 여유 **약 6.0시즌**
              (2026-09-07 · 로컬 전 시즌 빌드 실측 · ⚠**이 수는 매일 는다**).
              ⚠**~~9,536파일 / 1477.9MB~~ 는 드래프트를 보유 전 시즌으로 넓히기 전이다**(2026-09-07).
              **경기가 없는 시즌 13개(2005~2017)에 드래프트 한 장씩**이 늘었다 —
              ⚠**시즌은 늘었는데 화면은 안 늘었다**: 그 시즌들은 `draft.html` 하나만 굽는다.
              경기 데이터를 소급하면 그때 비로소 시즌당 약 1,000장이 붙는다.
              ⚠**같은 날 같은 DB 로 잰 앞뒤다**(변경 전 9,536 / 1477.6MB → 변경 후 9,549 / 1478.2MB) —
                차이는 **새 13장과 CSS 한 줄뿐**이고, 기존 9,536장 중 **9,526장이 바이트까지 같다**
                (달라진 10장 = 드래프트 9장 + `assets/site.css`).
              ⚠**~~9,473파일 / 1417.6MB = 47.4%~~ 는 ドラフト 화면을 얹기 전이다**(2026-09-05).
              **드래프트가 시즌마다 한 장(9장 · 합 306.1 KiB)** 늘었고, 내비 항목이
              **전 화면 9,379장에 40 B/장 = 0.36MB** 를 더한다.
              ⚠**「여유 약 6.0시즌」은 시즌당 1,743파일**(경기 페이지를 날짜별로 묶기 전의 평균)
              **로 나눈 값이라 보수적이다** — 지금 실측 시즌당 파일 수는 **981~1,105(평균 1,059.6)**
              이고, 그 최대치로 나누면 여유는 **약 9.5시즌**이다.
              ⚠**보수적인 쪽을 남긴다** — 2018 이전 시즌이 어느 규모인지는 **안 쟀다.**
              ⚠**바이트가 또 늘었다**(1364.7 → **1417.6MB** · 2026-08-28) — 스플릿을 표로 바꾸고
              **세부 성적(홈런·삼진·사사구)과 분모**를 넣었기 때문이다. **파일 수는 그대로다.**
              ⚠**~~878.8MB~~ 는 통산 대전 표를 얹기 전이다**(2026-08-27 정정).
              **파일 수는 안 늘고 바이트만 +55%** 늘었고, **그래서 파일 수만 재던 시험이 초록이었다** —
              바이트 검사를 `doc-figures.test.ts` 에 같이 붙였다(허용 10%).
              ⚠**「무게」의 무게를 압축 전으로 재지 마라** — 가장 큰 선수 페이지가
              **403.3 KB 원본 → gzip 약 10배 압축**이고, 중앙은 **188.4 KB**다.
              걸리는 것은 전송량이 아니라 **DOM 크기**다.
              ⚠**총 바이트도 줄었다** — 공통 머리와 각주가 경기마다가 아니라 날짜마다 한 번 나가기 때문이다
              (2026 한 시즌 실측: `games/` 11.5 → **9.6 MiB**, 파일 641 → **122**).
              ⚠**한 장이 무거워졌다**: 경기 상세 중앙 17.8 → **87.5 KiB**(최대 114.9) —
              선수 페이지(114 KiB)와 같은 급이라 받아들였다. 날짜당 경기는 **중앙 6 · 최대 6**이다.
              설계와 **기각한 안**(날짜 페이지 합치기 · 월별 묶기 · 선수 페이지 샤드)은
              `docs/superpowers/specs/2026-08-26-game-pages-by-date-design.md`.
              ⚠**날짜 페이지(`days/`)에 합치지 않은 이유를 기억하라** — 경기 날짜 1,531개 중
              **105개에 그 페이지가 없다**(포스트시즌 + 최신 경기일). 그대로 옮겼으면 **151장이 조용히 404**였다.
              ⚠**比較 데이터를 선수마다 파일로 두던 것을 ID 첫 글자 10개로 묶어** 29%를 줄인 것이 그 앞 단계다
              (2026-08-17). 다음 지렛대는 **선수 페이지 6,207장(배포물의 65%)** 이다
              (docs/operations/deploy.md).
            ⚠**백필은 「경기를 더 넣는 것」으로 끝나지 않는다.** 2024를 넣자 **투타 미상 122명**이
              생겼다 — 선수 페이지는 **현재 등록 선수만** 받을 수 있는데 소급 시즌에는
              NPB를 떠난 선수가 대거 들어오기 때문이다. 투타는 좌우 스플릿의 근거라
              그 선수들의 스플릿이 통째로 깨진다. **경기별 명단(`roster.html`)이 그것을 메운다**
              (§2-2 아래 「받고 있는데 안 읽던 것」 참조).
            취득 가능 범위는 **2016 시즌~**(2026-08-17 정정).
            ⚠**「2025년분부터만 존재」는 틀린 판정이었다.** 근거였던 「2024-08-14 = HTTP 404」는
              **존재하지 않는 경기 슬러그를 찍은 것**이다. 일정 페이지에서 실제 슬러그를 뽑아
              다시 치면 200이 돌아온다. `https://npb.jp/games/backnumber.html`이 2016~2026을 나열한다.
              **실측(2026-08-17): 우리 파서를 고치지 않고 2016·2020·2024년 playbyplay가 그대로 파싱된다**
              (타석 88·78·74건). 2015년 일정 = 404이 하한이다.
            ⚠**그렇다고 「11시즌을 얻었다」고 말하지 마라.** 오래된 시즌은 투수 귀속이 얇을 수 있다.
              ⚠**근거는 연도당 1경기뿐이다**(표본 3경기) — 일반화하지 마라.
              그 표본에서 2016년은 투수 ID가 **34/88(39%)**, 2020·2024년은 **100%**였다.
              투수 귀속이 빠지면 **상대전적·SRP·타순 순회가 그만큼 성립하지 않는다.**
              **어느 해부터 온전한지는 미확인이다 — 백필 전에 시즌마다 여러 경기로 재라.**
            ⚠**백필은 공짜가 아니다.** ⚠**경기 하나가 4장이다**(`GAME_PAGES` = index·playbyplay·box·roster).
              **그 「4장」을 문장에 박아 둔다** — 옛 값 ~~16,389요청 · 149MB~~ 가 죽은 원인이 정확히 이것이다
              (roster 를 더한 뒤 아랫줄만 고치고 이 줄을 안 고쳤다 · 2026-08-24 정정).
              **완결 8시즌 7,130경기 × 4장 = 28,520요청**이고, 보유분 전체(2018~2026)는
              **31,220요청 · 250.8 MiB**다(아카이브 `scores` 의 `.gz` 실측과 **정확히 같다**).
              ⚠**시간은 요청 수 × L1 간격이다** — 문서 실측 3.96초/장이면 31,220장 ≈ **34시간**.
              ~~13.7~22.8시간~~ 은 절반짜리 요청 수 위에 서 있던 값이다.
              실측(2026-08-17): **2024 한 시즌 = 3,636페이지 · 약 4시간**
              (3,636 ÷ 4 = 909 = 그 시즌 경기 수 — 여기는 처음부터 4장 기준이었다).
              ⚠**같은 줄의 「43MB」는 원인 미확정이라 뺐다** — 2024 gz 실측(28.5MiB)과 안 맞고
              `du` 할당 크기일 가능성이 있다. **근거로 쓰지 마라.**
              적재는 아카이브 전체를 매번 다시 훑으므로 3시즌이면 **27만 행**이다.
              ⚠**이 수들은 `scripts/test/doc-figures.test.ts` 가 DB×`GAME_PAGES` 로 다시 센다** — 손으로 고치지 마라.
            구형(2016~2018) 분기는 **넣었고 실물로 검증했다**(2026-08-17).
              ⚠**합성 픽스처로는 검증되지 않는다** — 처음 만든 픽스처에 **타석 결과 칸이 0개**여서,
              구형 경로의 `plateAppearances` 가 항상 빈 배열이어도 테스트가 전부 초록이었다.
              그 상태로 백필하면 여기 적힌 **「3시즌이 조용히 0건」**이 그대로 난다.
              → 실물 2016년 박스를 `packages/parser/test/fixtures/2016-box.html.gz` 로 고정했다.
              실측: 결과 칸 **88건** · 어휘 해석 6/6 · `チーム計` 2행 구별 · 도출 타수/안타 **격리 0건**.
            ⚠**롤오프 여부는 여전히 모른다**(미결). 2015년이 404인 이유가 「2016년에 기능이 시작됐다」인지
              「N시즌 지나면 사라진다」인지 확인되지 않았다. 후자면 2016년분이 언젠가 사라진다.
```

### 2-2-1. ⚠「받고 있는데 안 읽던 것」 — 하루에 세 번 나왔고, 열흘 뒤 또 나왔다 (2026-08-17 · 08-27)

새 데이터가 필요하다고 판단하기 전에 **이미 받아 둔 페이지를 먼저 열어 봐라.**
2026-08-17 하루에 셋이 나왔고, 셋 다 **외부 요청 0회**로 해결됐다.
⚠**그리고 2026-08-27 에 네 번째가 나왔다 — 그 셋을 고친 바로 그 파일에서.**

| 무엇 | 어디에 있었나 | 왜 안 읽혔나 |
|---|---|---|
| **볼카운트** | `playbyplay` — 파서가 이미 `1-2より` 를 뽑고 있었다 | 적재에 **받는 자리가 없어** 매번 버려졌다 |
| **도루·도루자·견제사** | `playbyplay` 의 주자 행 | 주자 행도 칸이 5개라 타석 행과 모양이 같은데 **이름 칸만 비어** 있어서 「선수 링크 없는 행」으로 걸러졌다 |
| **투타·배번(전 선수)** | `roster.html` — 경기마다 받고 있었다 | 적재가 **이 파일을 한 번도 열지 않았다** |
| **포지션(전 선수)** | `roster.html` — **같은 파일의 구획 머리**(`<th colspan="3">投手</th>`) | 위 줄에서 이 파일을 열게 만들면서 **그때 필요했던 칸만 읽었다.** 「이 파일은 이미 읽고 있다」가 다음 사람의 눈을 가렸다 |

⚠**네 번째가 말하는 것은 앞의 셋과 다르다.** 앞의 셋은 「안 여는 파일」이었지만
이것은 **「열고 있는데 안 보는 칸」**이다 — 훨씬 찾기 어렵다.
**받는 페이지 목록을 확인하는 것만으로는 안 잡힌다.**

⚠**대가는 컸다**(2026-08-27 실측): 선수 페이지의 역할 판정이 `position === "投手"` 를 먼저 보므로,
미상이면 **투수가 타자로 판정되어 투수 스플릿이 화면에서 통째로 사라졌다.**
투타 양쪽 기록을 가진 **2,374 선수-시즌** 중 **917** 이 그 상태였고, 사라진 투구가
**상대한 타자 합 163,631 · 한 명 최대 847**이다. 소급 시즌일수록 심했다 —
**2018년 191명 · 2026년 0명**. ⚠**에러가 아니라 빈 화면이라 아무도 결함으로 못 읽었다.**
포지션 보충 실측: 미상 **829 → 0** · 선수 페이지에도 값이 있던 **811명 중 808 일치**
(불일치 3은 전부 전향·그 경기 시점 표기).
⚠**포지션만으로도 부족하다** — `position` 은 **현재**를 말해 전향을 못 따라간다.
다 메운 뒤에도 **4건이 남고 전부 한 사람**이다(西純 · 2021~2024). 그래서 판정의 마지막 갈래는
**그 시즌 데이터**다(상대한 타자 > 자기 타석이면 투수 · `roleOf` in `web/src/query.ts`).
⚠**야수 등판을 뒤집지 않는다** — 실측 5건은 전부 자기 타석이 훨씬 많다.

⚠**받는 페이지와 읽는 페이지가 다르다는 것 자체를 정기적으로 확인해라.**
지금 경기마다 4장(`index` · `playbyplay` · `box` · `roster`)을 받고,
적재가 여는 것은 **`box` · `playbyplay` · `roster` 셋**이다(실측: `load-archive.ts` 의 `.html.gz` 참조 전건).
⚠**~~둘뿐~~ 이라고 적혀 있었다**(2026-08-24 정정 · 감사 [43]). **바로 위 표가 말하는 그 결함을
고친 커밋(`efbf207` · 2026-08-17 roster 추가)이 이 줄을 낡게 만들었고, 그 줄을 안 고쳤다** —
**고침이 문서를 낡게 하는** 전형적인 모양이다.
`index`(試合TOP)는 확인 결과 **バッテリー · 本塁打 · 最新のオーダー** 셋뿐이고
전부 박스에서 이미 얻는 것이라 안 읽어도 된다(2026-08-17 확인) — **안 여는 것은 `index` 하나다.**
⚠적재기는 이와 별개로 **월간 일정 페이지**(`schedule_MM.html.gz`)도 연다 — 구장 조회표 때문이고,
경기별 4장 밖이라 위 셋과 층이 다르다.
⚠**이 문장은 `scripts/test/doc-figures.test.ts` 가 소스와 대조한다** — 손으로만 고치지 마라.

⚠**출처가 둘이 되면 반드시 규칙을 적어라**(M1). 투타는 선수 페이지가 권위 있는 출처이고
명단은 **빈 자리만 채운다**(`WHERE throws IS NULL`). 대조는 858/858 일치였지만,
일치한다고 두 곳에서 쓰면 어느 날 갈린다.
⚠**같은 이름이라도 뜻이 다를 수 있다.** 배번은 선수 페이지가 **현재** 값,
명단이 **그 경기 시점** 값이다 — 실측 782명 중 10명이 어긋났고 전부 실제 변경이었다.

### 2-2-2. ⚠세이버 지표의 채택 기준 — **이름을 바꿔야 하면 패스** (사용자 결정 2026-08-27)

> 「세이버는 **이름을 바꿔서 표현할 수밖에 없는 상황**(제대로 된 수치를 뽑을 수 없는 상황)에서는
> **무조건 패스**로 생각하고 있어.」

**이것이 지금까지 흩어져 있던 판단들의 뿌리다.** WAR·xFIP·SRC 를 각각 다른 문단에서
다른 말로 설명해 왔는데, 실은 전부 이 한 줄의 적용이었다.

| 지표 | 판정 | 이 기준으로 읽으면 |
|---|---|---|
| **xFIP** | 안 낸다 | 정답 표본이 **0건**이라 낼 수 없고, 이름을 바꾸면 **비교 가능성이라는 존재 이유**가 사라진다 |
| **투수 WAR** | **안 낸다**(이 결정으로 닫힘) | 부품은 거의 다 있지만 일본 기준(1.02/DELTA)이 **tRA 기반**이라 FIP 로 낸 값은 다른 수다 → **이름을 바꿔야 한다** |
| **타자 WAR** | 안 낸다 | 이름 이전에 **데이터가 없다**(UZR·UBR·Positional) — 이 기준을 적용할 자리도 아니다 |
| **SRC** | **낸다** | ⚠**「이름을 바꾼 WAR」이 아니다.** 가진 입력으로 **정직하게 끝까지 계산되는 다른 지표**이고, 무엇을 재고 무엇을 안 재는지 말할 수 있다 |

⚠**「이름을 바꾼다」와 「다른 지표를 만든다」를 구별하라.** 전자는 같은 것을 다르게 부르는
것이라 **거짓말**이고, 후자는 처음부터 다른 것을 재는 것이라 **정직**하다. SRC 가 후자다.

⚠**이 기준은 「통산으로 접는」 경우에도 그대로다.** wRC+·득점기대값을 여러 시즌에 걸쳐 접으면
**무엇에 상대적인가**를 우리가 고르게 되고, 그 순간 그 값은 「wRC+」가 아니다 —
**그래서 세이버·득점기대값의 통산 전환도 패스**다(조사표
`docs/superpowers/specs/2026-08-27-season-career-toggle-survey.md` §3).

⚠**다시 열리는 조건은 하나뿐이다: 이름을 그대로 쓸 수 있게 되는 것.**
대체수준·기준선을 우리가 정하는 것으로는 **안 열린다.**

### 2-3. ⚠이 도메인이 만드는 의무 (M1~M12 · 어기면 조용히 틀린 숫자가 나간다)

| # | 의무 | 왜 |
|---|---|---|
| **M1** | **지표 계산식은 단일 패키지에 1벌** — 서버·클라·배치 중복 구현 금지 | 구 PPS에서 파서가 3중 구현이었고 **서로 값이 달랐다**(O13). 통계 서비스에서 같은 일이 나면 신뢰가 끝난다 |
| **M2** | **분모(표본 수)를 데이터와 UI 양쪽에 동반.** 분모 없는 비율은 렌더링 금지 | 10타석 .400을 「리그 1위」로 보여주는 순간 이 서비스는 거짓말을 한다 |
| **M3** | **자격 기준(규정타석·규정투구회)과 동률 처리 규칙을 명시하고 테스트로 고정** | 순위는 규칙이 곧 값이다. 규칙이 코드에만 있으면 아무도 검증할 수 없다 |
| **M4** | 전 수집 레코드가 **「어디서 · 언제 · 몇 번째 판」에 답할 수 있을 것** — 자기 컬럼이든 **부모를 타고든**. ⚠아래 정정 참조 | 「어제 본 숫자와 다른데?」에 답하지 못하면 버그와 정정을 구별할 수 없다 |
| **M5** | **수집기는 멱등** — 같은 경기를 두 번 수집해도 결과가 한 번과 같다 | 재시도·크론 중복 기동·수동 재수집이 전부 일어난다 |
| **M6** | **시계는 주입 가능한 `clock` 1개.** 인자 없는 `new Date()`/`Date.now()` 직접 호출 금지 — **시험으로 강제**(`scripts/test/clock-injection.test.ts`) | 구 PPS는 시계를 2번 읽어 자정 경계에 날짜가 어긋났다(연 1~2회). 라이브 경기는 자정을 넘긴다 |
| **M7** | **파서는 소스별 1벌 + 스키마 변화 감지.** HTML 구조가 바뀌면 **빈 값이 아니라 실패**로 | 스크레이핑 프로젝트 최대 사망 원인. 조용히 0을 넣으면 「성적 급락」으로 표시된다 |
| **M8** | 외부 접근은 **robots.txt · rate limit · 캐시 우선 · 지수 백오프** | 차단당하면 서비스가 죽는다. 예의는 가용성 문제다 |
| **M9** | **확정값과 잠정값을 타입 수준에서 구분** | 라이브 중 기록은 정정된다. 같은 타입이면 반드시 섞인다 |
| **M10** | **선수는 자체 안정 ID + 소스별 외부 ID 매핑 테이블.** 이름 문자열 조인 금지 | 동명이인 · 등록명 변경 · 한자/카나 표기 흔들림 · 개명 · 외국인 선수 표기 요동 |
| **M11** | **미수집 · 0 · 결측을 구별한다.** `0`과 `null`을 섞지 마라 | 「도루 0개」와 「도루 데이터 없음」은 다르다 |
| **M12** | 빈 상태는 **4상태**(로딩 / 데이터없음 / 수집실패 / 시즌외) | 셋이 같은 화면이면 그것 자체가 결함 |

⚠**M4 의 문구를 고쳤다**(2026-08-27 · 감사 P3 #9). 옛 문구는 「전 수집 레코드에
`source`·`fetched_at`·`as_of`·`revision`」이었고 §0-10 의 판정 기준도 「M4 필드 4종이 전 수집 테이블에 존재」였다.
**그 기준은 거짓이었다** — `as_of` 는 **표 11개 중 1개**(`player_season_name`)에만 있다.

⚠**그런데 거짓인 방식이 감사가 적은 것과 다르다.** 자식 넷(`batting_line`·`pitching_line`·
`pa_event`·`runner_event` · 합 **851,072행**)은 `game_id` 로 부모를 타고 답한다 —
부모를 통째로 다시 넣을 때 자식도 같이 갈리므로(`replacePaEvents`) **출처가 갈릴 자리가 없다.**
글자대로 지키려면 **같은 값을 851,072행에 복사**해야 하는데, 그건 **목적이 아니라 기준을 위한 일**이다.

⚠**컬럼 이름으로 세면 틀린다.** 처음에 그 네 글자만 찾아서 `player` 를 「아무것도 없음」으로 셌는데
실제로는 `first_seen_at`·`last_seen_at`·**`profile_fetched_at`** 을 갖고 있다.
**물어야 할 것은 이름이 아니라 「답할 수 있는가」다.**

⚠**약하게 바꾼 것이 아니다.** 바뀐 것은 **무엇을 요구하는가**다 —
**「어디서·언제」는 전 수집 표가 반드시**(자기든 부모든) 답하고,
**「몇 번째 판」은 정정이 개념적으로 성립하는 표만** 답한다(실측 6/11).
통째로 지우고 다시 넣는 표(`upcoming_game`·`player_season_name`)에서 `revision` 은 뜻이 없고,
**면제에는 「왜 없는 것이 맞는가」를 적는다.**
⚠**`career_*` 의 판은 DB 가 아니라 아카이브 사이드카(`*.meta.json`)의 `revision` 이 답한다** —
DB 만으로는 못 답하는 유일한 축이므로 여기 적어 둔다.

⚠**시험이 그것을 지킨다**(`packages/store/test/provenance.test.ts` · 7본).
**빈 DB 에 마이그레이션을 적용해 스키마만 보므로 데이터 없이도 돈다** —
새 표가 출처 없이 생기면 **그날 바로** 붉어진다.

### 2-4. 데이터 출처 원칙
- **신뢰 등급**을 소스마다 부여: 공식(NPB/구단) > 대형 집계 > 개인 집계. 충돌 시 상위 우선, **충돌 사실을 기록**한다.
- **수치(사실)와 표현물(기사·해설·이미지)을 구분.** 이 서비스가 다루는 것은 수치다.
- 각 소스의 robots.txt·이용약관 판정을 `data-source-scout`가 표로 남기고, **판정 없는 소스는 쓰지 않는다.**
- 자동 취득이 불가능한 항목은 **「왜 불가능한가」를 적고** 수동 입력 경로를 만든다(§0-3).

### 2-5. 데이터 권리 — 조사 결과와 대응 (2026-08-14)

전수 조사 결과는 `docs/sources/2026-08-14-feasibility-report.md`. **이 프로젝트의 병목은 기술이 아니라 데이터 권리다.**

**⚠아래는 법률 자문이 아니라 리스크 정리다.** 조사 자체가 근거의 일부를 2차 인용에 의존했고(경산성 문서가 비브라우저 UA에 403), 판례 원문 대조를 완료하지 못했다. **공개 전환을 결정하는 시점에는 일본 변호사 확인을 거친다.**

혼동하기 쉬운 4개 층을 분리해서 다룬다. **「공개 정보다」는 1층에만 답하는 말이다.**

| 층 | 무엇이 걸리는가 | 우리 대응 |
|---|---|---|
| **1. 사실 자체** | 개별 기록(안타·홈런·타율)은 **사실이며 저작물이 아니다.** 여기까지는 유저 직관이 맞다 | 수치를 쓰는 것 자체는 문제의 본체가 아니다 |
| **2. 편집물로서의 DB** | 저작권법 §12-2 — **선택·체계적 구성에 창작성**이 있으면 보호. 즉 「표를 통째로 베끼는 것」이 걸린다 | **원본 표의 구성·컬럼 순서를 재현하지 않는다.** 원시 기록을 받아 우리 스키마로 재구성한다 |
| **3. 일반 불법행위** | 저작권이 없어도 성립할 수 있다(翼시스템·YOL 판시 계열). 위험 인자 = **영리목적 · 정보의 신선도 이용 · 데드카피 · 경합관계** | 이 4개를 동시에 피하는 것이 설계 목표. **비영리·비경합·저빈도·자체계산**이 그 대응이다 |
| **4. 계약·규정** | npb.jp 푸터 「二次利用および無断転載を固く禁じます」 · NPB 撮影/配信규정 제4조1항(2)의 **리얼타임 배신 금지** | 재배포 형태를 취하지 않는다. **라이브는 규정이 정면으로 금지하므로 무허락 구현 대상에서 제외**(§0-4) |

**⚠robots.txt가 404인 것은 「허용」이 아니라 「지시 없음」이다.** 허용의 근거로 쓰지 마라.

#### 코드로 강제할 안전장치 (구현자는 이것을 지킨다)

| # | 규칙 | 근거 |
|---|---|---|
| **L1** | 수집은 **1req / 2~5초 · 동시 1커넥션 · 식별 가능한 UA + 연락처**. 하루 1회 배치가 기본 | 岡崎 사건은 1초 1요청 크롤러로 체포까지 갔고 **사법판단이 남지 않아 정량 기준이 존재하지 않는다.** 안전측 설계밖에 할 수 없다 |
| **L2** | **원본 표 레이아웃을 재현하지 않는다.** 화면은 우리가 계산한 값 위주로 구성 | 2층·3층(데드카피) 회피 |
| **L3** | 전 화면에 **출처 명기 + 원본 링크**. 원본을 대체하는 게 아니라 가리킨다 | 3층(경합) 회피 |
| **L4** | **삭제·정정 요청 창구를 처음부터** 둔다 | 개인정보 논점의 최소 대응. 나중에 붙이면 늦다 |
| **L5** | 취급 대상은 **공개된 직업활동 성적만.** 비공개 개인정보는 수집·저장하지 않는다 | 개인정보 논점 |
| **L6** | **원시 아카이브는 내부 보관, 외부 노출은 파생값 위주** — 이 경계를 저장소 계층에서 나눈다 | 2·3층의 핵심 경계선 |
| **L7** | 조건부 요청(ETag/If-Modified-Since)과 캐시를 우선한다 | L1의 연장 — 안 보내는 요청이 가장 안전한 요청 |

#### 단계 전략 (2026-08-14 확정)

```
S1 지인한정 비공개   **로그인 + 허용목록**. 재배포 형태를 취하지 않는다
                     ⚠**요구는 「엣지에서 매 요청 허용목록 검증」이지 특정 로그인 수단이 아니다.**
                     ⚠**UI 숨김은 검증이 아니다**(인가 원칙 자체는 루트 §5).
                     2026-08-15 확정: Cloudflare Access의 One-time PIN(이메일 코드)을 쓴다 —
                     보증 수준이 같고 지인이 Google 계정을 갖지 않아도 된다(deploy.md §4-2)
                     ⚠**2026-08-24: 세션을 24시간 → 1개월로 늘렸다**(사용자가 「매일 코드를 받는 게
                     너무 불편하다」고 보고). 로그인 수단은 그대로 두고 **Google 을 선택지로 더할 수 있게** 했다.
                     ⚠**대가가 있다** — 허용목록에서 뺀 사람이 **즉시 차단되지 않는다.**
                     지울 때는 **세션도 폐기**해야 한다(deploy.md §4-2-A).
                     ⚠**「엣지에서 매 요청 검증」은 그대로다** — 매 요청 검증되는 것은
                     「그 토큰이 유효한가」이지 「지금도 명단에 있는가」가 아니다. **둘을 같은 말로 쓰지 마라.**
                     ⚠**설정은 이제 코드가 붙든다**(`scripts/access-config.ts` · 매일 배포가 대조)
                     → 3·4층 위험 인자 대부분이 성립하지 않음
                     이 단계의 목적은 「보여주기」가 아니라 **아카이브 축적**(소급 불가 자산)
S2 공개 검토        「우리가 계산한 것」의 비중이 충분히 커진 뒤에 판단 + 변호사 확인
S3 수익화 검토      ⚠영리목적이 붙는 순간 3층 판단요소가 강해진다 → 반드시 재평가
```

**⚠S1이 안전한 것은 「공개」 축뿐이다. 수집 행위 자체(L1·L7)는 비공개여도 그대로 남는다.**

---

## 3. 작업 규칙

1. **업데이트 1건마다 검증 필수** — 변경 후 타입체크/테스트/동작을 확인하고 *무엇을 확인했는지* 보고. 「완료」는 검증 뒤에만.
2. **신규 기능은 설계부터** — `superpowers:brainstorming` → `writing-plans` → 구현. 소규모·정형(문구·라벨·단순 픽스)은 바로 진행.
3. **질문은 AskUserQuestion 선택지 형식**(추천안 먼저·「(추천)」 표기). 자유서술 질문 지양. 진행보고·결과전달은 텍스트.
4. **표시 언어는 한국어**로 통일(내부 작업 언어는 자유). ※ 제품 UI 언어는 별개 — §7.
5. **이중 검토의 이 프로젝트 배선** — 1차 `shiro-core:review-specialist`(sonnet) → 2차 `shiro-core:deep-reviewer`(opus). ⚠**~~동일 에이전트를 `model:"opus"` 로 오버라이드~~ 가 아니다**: 공통 에이전트은 모델·`effort` 를 명시하지 않고 기본치를 쓴다(`../_common/docs/agent-roster.md` — 「프로젝트마다 스키마가 갈리던 원인이었다」). **모델 다양성이 핵심**(도구 비교에서 발견의 93.4%가 정확히 한 도구에서만 검출됐다).
   ⚠**배포 직전·고위험 변경은 층이 다르다** — 루트 §4 가 **3중 검토**를 요구한다. 이 배선으로 대체하지 마라.
6. **설치 스킬 능동 활용** — `frontend-design`·`dataviz`·`shiro-core:auditing-multi-dimensionally`·`superpowers` 계열. 남용 금지.
7. **분모를 항상 표기** — 「대부분」·「전부 통과」 금지. 「23본 중 23본」. **「0건」과 「안 쟀음」을 구별해 쓴다.**
8. **FAIL(떨어짐)과 ERROR(돌지도 않음)를 분리해서 센다** — 구 프로젝트에서 E2E 5본이 전부 실행 불가인 채 「0건=합격」으로 오독된 전례.
9. **새 테스트는 「고치기 전 코드에서 실패하는가」를 확인**한 뒤 채택한다(뮤테이션 검사).
10. ⚠**자기 수정을 다시 읽는다** — 구 감사에서 *수정이 새 결함을 만든 사례가 한 라운드에 4건*. 전부 읽기가 아니라 **실측**이 잡았다.
11. **외부 사이트 실접속은 소스 조사·검증 단계에서만.** 개발 루프에서 반복 히트 금지 — 고정 픽스처(저장된 HTML/JSON)로 테스트한다.
12. **울트라 코드(Workflow) 사용 허가됨** — 소스 전수 조사·스택 비교·다각 감사처럼 **팬아웃이 이득인 작업**에 한해 자체 판단으로 사용. 단건 구현에는 쓰지 않는다.

---

## 4. 에이전트 편대

편성 규칙(기본 프로파일 · 감사↔구현 분리 · 병렬 상한 · 역할 중복 금지)과 **모델·에포트 기준**
(opus=`max` / sonnet=`high` / haiku=`medium` · ⚠sonnet 에 `max` 금지 · fable 미사용)은
**루트 `CLAUDE.md` §4** 가 소유한다. 여기서 다시 적지 않는다.
공통 에이전트의 명단과 별칭 매핑은 `../_common/docs/agent-roster.md` 가 정본이다.
⚠**몇 종인지를 여기에 박지 마라** — 2026-09-03 에 **8종에서 9종으로 늘었다**(`gpt-reviewer` 추가).
숫자를 복사한 문서는 그날 전부 거짓이 된다.
⚠**각 에이전트의 모델 정본은 정의 파일 자체**(`.claude/agents/*.md` frontmatter)다 — 여기에 복사하지 않는다.
복사하면 한쪽만 고쳐졌을 때 **어느 쪽이 도는지 알 수 없다.**

⚠**그 기준의 근거 수치는 지금 워크스페이스에서 이 줄에만 있다** — 루트 §4 는 규칙과 사유만 담고
원수치를 담지 않는다. **DeepSWE 실측**(2026-08 시점 · `https://deepswe.datacurve.ai/`):
`claude-opus-5[max] 74% / $11.84 · 99스텝` · `claude-fable-5[max] 70% / $21.63` ·
`claude-sonnet-5[max] 54% / $26.40 · 268스텝`. **옮길 자리는 `_common/docs/agent-roster.md` 이지만
공통 자산은 프로젝트 작업 중에 고치지 않는다**(루트 §10) — 별도 작업으로 옮기기 전까지 여기서 지키자.

### 프로젝트 특화 11종 (`.claude/agents/`)

공통 에이전트으로 대체할 수 없는 것만 남겼다 — **판정 기준 자체가 이 도메인에서만 성립**하기 때문이다.

| 에이전트 | 공통 에이전트과 무엇이 다른가 |
|---|---|
| `data-source-scout` · RO | **이 프로젝트 최대 리스크 담당.** 대상이 외부 사이트이고 산출물이 **법적 판정표**다(§2-5). `shiro-core:scout`(코드 검색)와 층이 다르다 |
| `planning-architect` · RO | 계획의 합격 기준(월 1,000엔 상한 · §0 제약2)이 이 프로젝트의 헌법이다 |
| `sabermetrics-analyst` · RO | 지표 정의·산식·자격 기준·동률 규칙. 도메인 지식 그 자체 |
| `senior-engineer` | 고위험 영역 목록이 곧 이 프로젝트의 **사고 이력**이다 |
| `data-pipeline-engineer` | 스키마·수집기·파서·스키마 변화 감지·백필 대조검증 |
| `frontend-architect` | **정적 다페이지 생성**(§1)의 구조·페이지 무게·DOM 크기·4상태 배선 |
| `design-graphics-engineer` | 목업·토큰·데이터 시각화 **구현**. 브랜드(Lunomel)는 프로젝트 자산 |
| `design-auditor` · RO | 도메인 표시 규칙(분모 M2 · 잠정값 M9 · 자격기준 M3)이 P0 축이라 공통화 불가 |
| `security-auditor` · RO | 위협 모델이 **공개 데이터 전제** — 기밀 축은 경량이고 남용·비용 폭주가 주 위험(§0 제약9) |
| `devops-platform-engineer` | Pages·Access·Actions 배선과 **월 비용·CI 분 실측**(§6) |
| `i18n-engineer` | 선수명 표기(한자/카나/로마자)·야구 숫자 포맷·용어집 |

`RO` = 읽기 전용(코드 수정 금지).

⚠**RO 5종 중 3종은 `Write` 를 갖는다** — `data-source-scout`(판정표) · `planning-architect`(결정문) ·
`sabermetrics-analyst`(정의서)는 **산출물이 파일인 것이 존재 이유**이기 때문이다.
쓰기는 **각자의 산출 경로에만** 한다(`docs/sources/` · `docs/decisions/` · `docs/metrics/`).
`design-auditor` 와 `security-auditor` 는 산출이 **발견 목록**이라 `Write` 가 없다.

⚠**이 경계를 도구가 막아 주지 않는다.** `Edit` 를 빼도 `Write` 로 기존 파일을 덮어쓸 수 있고,
**애초에 `Bash` 가 있어 `cat >` 로 쓸 수 있었다** — 즉 「RO 니까 파일을 못 만든다」는 **한 번도 참이 아니었다.**
2026-09-03 에 `Write` 를 준 것은 **권한을 넓힌 게 아니라 이미 있던 것을 정직하게 적은 것**이다.
**경계를 지키는 것은 도구가 아니라 이 문장이고, 어기면 감사↔구현 분리가 무너진다.**

### 공통 에이전트에 넘기는 일 — ⚠**함께 전달할 도메인 조건이 있다**

⚠**호출 이름은 `shiro-core:<이름>` 이다.** 접두사 없는 맨이름은 roster 의 「**기존 이름들**」 칸에 있는
**옛 이름**이고, 그 표는 「→ 이렇게 처리하라」는 마이그레이션 지시다(별칭 허용표가 아니다).

| 하는 일 | 에이전트 | 이 도메인에서 함께 전달할 것 |
|---|---|---|
| 머지·배포 전 diff 검수 | `shiro-core:review-specialist` | §2-3 M1~M12 — 특히 **M2 분모 없는 비율 · M7 파싱 실패가 기본값으로 · M10 이름 문자열 조인 · M6 시계 직접 호출** |
| 브랜치 전체 종합 + 적대적 반증 | `shiro-core:deep-reviewer` | 이중 검토의 2차(§3-5) |
| 타입체크·테스트·빌드 실행과 증거 보고 | `shiro-core:test-runner` | 분모 표기 · FAIL/ERROR 분리 · **이닝 1/3 단위 · 더블헤더 · 결측 3분류(M11) · 파서 실패는 실패로** |
| 멱등성·레이스·재시도·부분실패 감사 | `shiro-core:concurrency-correctness-auditor` | M5 멱등 · M4/M9 정정 순서 · **부분 실패 후 재실행으로 복구되는가** |
| 외부 사실을 1차 출처로 대조 | `shiro-core:factcheck-frame` | 1차 출처 목록은 `docs/sources/` |
| 판단 없는 정형작업 | `shiro-core:chore-runner` | ⚠**형제 프로젝트에서 스크립트를 실행시키지 마라**(루트 §10) · **외부 사이트에 대량 요청을 보내게 하지 마라**(L1·M8) |

⚠**`qa-test-engineer` 의 절반은 아직 주인이 없다.** roster 의 「QA 계열 분리 규칙」은 **실행·보고만**
공통(`shiro-core:test-runner`)으로 넘기고 **프리뷰 파이프라인 조작·스택 고유 하네스는 프로젝트에
남기라**고 한다. 지금 그 몫을 받을 프로젝트 에이전트가 **없다.** 필요해지면 그때 만든다 —
**빈 껍데기를 미리 만들지 않는다.**

### 감사 → 구현 분리

`design-auditor` → `design-graphics-engineer` / `security-auditor` · `shiro-core:concurrency-correctness-auditor` → `senior-engineer` · `data-pipeline-engineer`.

---

## 5. 등록된 플러그인·스킬

플러그인은 **유저 레벨**(`~/.claude/settings.json`)에서 전역 활성 — **7개**:
`superpowers` · `understand-anything` · `example-skills` · `document-skills` · `jp-power` ·
`frontend-design` · **`shiro-core`**. ⚠마지막 것의 마켓플레이스 `shiro-common` 은 GitHub 이 아니라
**워크스페이스 `_common/` 디렉터리**가 소스다.
프로젝트 `.claude/settings.json` 은 **마켓플레이스 등록 + 이 저장소에서 자주 쓰는 읽기·검증 명령의
`permissions.allow`(10건)** 를 담는다.

**`.claude/audit/` 오버레이가 있다**(2026-09-04 생성 · 게이트 기록은 같은 폴더 `BOOTSTRAP.md`):
`dimensions.local.md`(감사 기준) · `target.json`(도구가 **무엇을 열고 무엇을 눌러도 되는가**).
**D1~D8 전부 활성**이고 측정 대상은 **§2-3 M1~M12** 다.

⚠**D8 정본은 NPB 공식 기록 규칙 + `docs/metrics/` 의 검증 케이스이고, 판정에 그 인용을 붙인다.**
`docs/metrics/` 는 **우리가 쓴 문서**라 그것으로 「산식이 학술적으로 옳은가」는 판정할 수 없다(순환 참조) —
판정할 수 있는 것은 **「코드가 정의서와 일치하는가」**와 **「검증 케이스가 공개 수치와 맞는가」**까지다.
**인용 없는 D8 지적은 그 자체가 결함이다.**

⚠**감사 전에 `npm run build:web` 으로 `dist` 를 다시 만들고 `npm run preview` 로 띄운다.**
`file://` 로 열면 색인 `fetch` 가 막혀 **검색이 죽은 화면**을 재게 된다(`scripts/preview.ts` 머리말).

구 로컬 사본을 제거한 이유: 다른 프로젝트 전용 내용이었고, 측정 도구 3개가 이 저장소에서
첫 줄부터 실행 불가였는데도 「이 도구로 재라」고 지시하고 있었다.

---

## 6. ⚠금지·주의

- ⚠**분모 없는 비율을 렌더링하지 마라**(M2). 스플릿은 표본이 작다 — 그게 이 도메인의 1급 함정이다.
- ⚠**파서 실패를 빈 값으로 흘리지 마라**(M7). 조용한 0은 「기록 급락」이라는 거짓말이 된다.
- ⚠**이름 문자열로 선수를 조인하지 마라**(M10).
- ⚠**인자 없는 `new Date()` / `Date.now()` 직접 호출 금지** — 주입된 `clock` 경유(M6).
  ⚠**린트가 아니라 시험으로 강제한다**(`scripts/test/clock-injection.test.ts` · 2026-08-21).
  ~~린트로 강제한다~~ 고 적혀 있었지만 **린트는 존재한 적이 없다**(실측: 설정 파일 0개 · `lint` 스크립트 없음).
  ⚠**`new Date("2026-08-14")` 는 금지가 아니다** — 주어진 문자열을 해석할 뿐 시계를 읽지 않는다.
  ⚠**진입점(`tools/` · `scripts/`)은 목록에 적힌 만큼만 예외**다. 라이브러리(`packages/*/src`)는
  `archiver/src/clock.ts` 하나 말고 **0건**이고, 그 0을 시험이 따로 지킨다.
- ⚠**모듈 전역에 가변 상태를 두지 말 것** — 요청 스코프는 `AsyncLocalStorage`(런타임이 Node인 경우). 다중 인스턴스에서 침묵 실패.
- ⚠**수집·통지 등 변경 경로에 멱등키 없이 만들지 말 것**(M5).
- **개발 루프에서 외부 사이트를 반복 히트하지 마라** — 픽스처로 테스트(작업규칙 11).
- ⚠**저장소 밖에 파일을 흘리지 마라.** 조사·디버깅용 일회성 스크립트와 저장한 페이지를
  **바탕화면이나 홈 디렉터리에 두지 않는다.** 실제로 쌓였다 — 2026-08-14 소스 조사가
  바탕화면에 **10개(591KB)**를 남겼고 **10일 동안 아무도 몰랐다**(2026-08-24 사용자 지적으로 발견·삭제).
  ⚠**저장소가 참조하지 않는 파일은 아무 시험도 안 지킨다** — 낡아도 알 수 없고, 지워도 알 수 없다.
  - **남길 소스 표본** → `docs/sources/samples/` (출처·취득일과 함께).
    ⚠**`.gitignore` 로 무추적이다 — 클론한 사람에게는 0파일이다.** 그러니 그 자리는
    「다음 사람에게 전달되는 곳」이 아니다. **재현에 필요한 것은 `docs/sources/*.md` 에
    URL·기준일·인용문으로 적어라**(`.gitignore` 주석이 같은 말을 한다)
  - **버릴 임시 파일** → 세션 임시 디렉터리(`$CLAUDE_JOB_DIR/tmp` 등). 저장소 안에도 두지 않는다
  - ⚠**상대 경로로 파일을 만들기 전에 `cwd` 를 확인해라** — 바탕화면에서 스크립트를 돌리면
    바탕화면에 떨어진다. 그게 그때 일어난 일이다
- **라벨 강조 원칙에 도메인 예외**(2026-08-15 확정). PPS의 원칙은 「항목명이 값보다 잘 보이게」였고,
  뜻은 **「의미를 모르는 값을 보여주지 마라」**였다. 그런데 이 제품은 **숫자가 곧 내용**이라
  항목명을 값보다 크게 하면 읽을 수 없는 화면이 된다.
  → **값을 주역으로 두되 라벨과 분모를 반드시 인접**시킨다. 원칙의 정신은 M2로 지키고 형식은 버린다.
  ⚠**분모 없는 값은 여전히 금지**다. 예외는 크기 위계에만 적용된다.
- ⚠**선수 사진·구단 로고·엠블럼을 쓰지 마라**(2026-08-15 확정). §2-5 1층(「사실은 저작물이 아니다」)의
  논리가 **여기엔 닿지 않는다** — 사진은 촬영자의 저작물이고 선수의 초상권이 붙으며, 로고는 상표다.
  L5(공개된 직업활동 성적만)·L6(외부 노출은 파생값)에도 걸린다. 핫링크도 같다.
  → 대체: **우리가 계산한 값으로 만든 우리 그림**(구단 색 마크 · 월별 추이 꺾은선).
- ⚠**라이브 취득을 하지 마라**(2026-08-15 조사 확정 · `docs/decisions/2026-08-15-live-matchup-feasibility.md`).
  npb.jp가 경기 중 현재 투수를 공표하는 것은 **실측으로 확인**됐지만, 필요한 폴링이 L1의 약 100배이고
  3층(정보의 신선도 이용)·4층(규정)에 정면으로 걸린다.
  → 대체: **유저가 대전 상대를 고르는 화면**(`matchup.html`). 취득이 0이면 3·4층이 성립하지 않는다.
- ⚠**「배포했다」를 「보인다」로 읽지 마라**(2026-08-30 사고 · docs/operations/deploy.md §7-A).
  사용자가 **8월 18일자 화면**을 보고 있는데 우리 증거는 **전부 초록**이었다 — DB(경기 7,574→7,580 ·
  나이 1일 · 구멍 0) · 予告先発(8/30 · 12/12) · 빌드(최신 8/29) · 업로드(3,134+1 파일).
  `wrangler` 의 `✨ Deployment complete!` 는 **「업로드가 끝났다」까지만** 말한다.
  ⚠**판정을 가른 것은 「띠가 초록이었다」**이다 — `isStale` 는 `lagDays > 3` 에서 켜지므로
  「まで反映」이었다는 것은 **그 화면이 만들어질 당시엔 신선했다**는 뜻이고, 렌더 버그면 초록일 수 없다.
  그리고 사용자가 본 **8/18·8/27 이 둘 다 실재했던 빌드**였다(수집 로그 3회·8회) — **낡은 사본이다.**
  ⚠**`_headers` 에 `Cache-Control` 이 한 줄도 없었다** — 재사용 기간을 **플랫폼 기본값에 맡겨 놓고
  그 값을 아무도 재 본 적이 없다.** 신선도가 곧 상품인 사이트가 할 일이 아니다.
  → 이제 명시하고(`max-age=0, must-revalidate` · `no-store` 아님), **배포 반영을 매일 잰다**
  (`scripts/verify-deploy.ts`).
  ⚠⚠**그런데 사인이 틀렸다**(같은 날 확정 · 사용자 확인). **PC 는 캐시가 아니라 「주소」였다** —
  **배포 해시 주소**(`https://<hex8>.bb-app-7mk.pages.dev`)로 들어가고 있었고, 그건
  **그 배포 하나의 영구 스냅샷이라 캐시를 지우든 헤더를 고치든 영원히 안 바뀐다.**
  ⚠**내가 만든 함정이다** — 배포 보고에 `wrangler` 가 찍는 그 주소를 그대로 옮겨 적어 왔다.
  **사람에게 주는 주소는 `https://bb-app-7mk.pages.dev` 하나다.**
  ⚠**틀리게 읽은 것 둘**: 「비단조 관측 = 낡은 사본 여러 벌」은 실은 **기기 두 대**였고
  (PC 동결 8/18 · 폰 캐시 8/27), 「8/24 Access 세션 연장이 캐시를 털어 주던 것을 없앴다」는
  **근거가 시점 하나뿐이라 이제 가설로도 약하다** — 그것으로 무엇을 결정하지 마라.
  ⚠**`Cache-Control` 은 그대로 두되 「이 사고의 원인」이라고 쓰지 마라** —
  폰이 나은 것이 그 수정 덕인지 캐시가 저절로 밀려난 것인지 **구별 못 한다.**
  남기는 근거는 따로다: **신선도가 곧 상품인 사이트가 재사용 기간을 재 본 적 없는 기본값에 맡기고 있었다.**
  ⚠**이 사고의 모양을 기억하라: 증거가 전부 초록인데 사용자만 틀린 것을 본다.**
  거기까진 맞게 읽었고 **그다음이 틀렸다** — 초록인 이유를 **전달 경로에서만** 찾았고
  **「사용자가 우리와 같은 주소를 보고 있는가」를 묻지 않았다. 다음엔 그것부터 물어라.**
- ⚠**CI 무료 분은 유한하고, 스케줄만으로 이미 88% 를 쓴다**(2026-08-31 사고 ·
  docs/operations/deploy.md §7-C). Actions **2,000분이 소진**돼 배포가 **스텝 0개 · 러너 미배정 ·
  과금 0ms** 로 죽었다. ⚠**「그날 많이 돌려서」가 아니다** — 스케줄 전체 실행이 **하루 평균 61.9분**
  (실측 11일)이라 **월 약 1,750분**이고, 수동 배포가 0이어도 닿는다. **여유는 월 254분 = 수동 배포 13회분.**
  → **수정을 모아 한 번에 배포한다.** 커밋마다 배포하지 마라.
  ⚠**「배포할 때만 잠깐 공개로」는 안 된다** — 워크플로의 L6 게이트가 `exit 1` 이고,
  `data/archive` 의 npb.jp 원본 250.8 MiB 가 §2-5 2·3층에 걸린다.
  ⚠**~~git 이력에 남아 되돌릴 수 없다~~ 는 틀렸다**(2026-09-03 실측): `data/` 는 `.gitignore` 2행이고
  **그 경로를 건드린 커밋이 0건**이다. 노출 경로는 git 이력이 아니라 **배포물과 릴리스 자산**이고,
  되돌릴 수 없는 것은 이력이 아니라 **이미 받아 간 사본**이다. **결론은 그대로다 — 사유만 바뀐다.**
- ⚠⚠**로컬 시간으로 CI 병목을 판단하지 마라 — 실제로 엉뚱한 것을 고칠 뻔했다**(2026-08-31).
  로컬 최장이 `배포물에 HTML 주석이 없다`(144초)라 그것을 최적화했는데 **CI 에서는 6.0초**다.
  진짜 상위는 `woba-weights-wired` **150.7초** · `ranking-min-sample` **145.0초**로
  **둘이 시험 490초의 60%** 였다. **CI 로그에 본별 시간 2,112개가 그대로 들어 있다 — 그걸 읽어라.**
  ⚠**측정 지표가 질문에 답하는지 먼저 확인해라**: 질문은 「CI 분을 무엇이 먹는가」인데
  잰 것은 「내 기계에서 무엇이 느린가」였다.
  ⚠**그 조사에서 `BB_FULL_SCAN` 한 값이 두 가지를 켜고 있던 것이 드러났다** —
  **페이지 표본**(근거가 적혀 있다)과 **시즌 범위**(딸려 왔고 근거가 없다). 비용은 후자에서 났다.
  → 시즌 쪽을 **`BB_ALL_SEASONS`** 로 갈라 **하루 한 번**만 돈다. **완결 시즌은 얼어 있으므로**
  하루 세 번 다시 재는 것은 검사가 아니라 반복이다. **수동 실행은 언제나 전수.**
- ⚠**수집 창이 「딱 하루」였다 — 이틀 멈추면 가운데 날이 영구히 빈다**(2026-08-31 · 위 사고에서 드러남).
  `targetDates` 가 `[어제]`만 봐서, 실행이 하루 걸러지면 그날 경기가 **다음 실행의 창 밖**으로 밀려났다.
  ⚠**이번엔 손해가 0이었는데 그건 8/31 이 NPB 휴식일이었기 때문이지 설계가 막은 게 아니다.**
  → DB 의 `MAX(game_date)` 를 기준점으로 **빠진 날을 메운다**(`MAX_CATCHUP_DAYS = 7`).
  ⚠**정상·휴식일에는 요청이 1건도 안 는다**(시험이 고정한다) · ⚠**상한을 넘으면 안 메운다**
  (비시즌에 매일 수백 일을 훑지 않기 위해서 · `freshness.ts` 가 2일에 이미 빨개진다) ·
  ⚠**모르면 안 넘긴다**(M11 — 모르는 것을 「어제」로 메우면 빈 날이 있어도 안 메운다).
- ⚠**「AI틱함」 목록은 루트 §7 이 소유한다.** 이 제품이 **더하는** 것은
  `design-graphics-engineer.md` 가 갖는다(모든 요소에 같은 border-radius · 근거 없는 그림자).

---

## 7. 브랜드 · 계정

| 항목 | 값 |
|---|---|
| 패런트 브랜드 | **Lunomel** (`LOO-no-mel` / 루노멜 / ルノメル) |
| 제품 브랜드 | **BaseNine**(2026-08-15 확정) · 표기 `ベースナイン`. Lunomel 하위 endorsed 모델(Trovune 선례와 동일). `Nine`은 일본어권에서 **팀 자체**를 가리키는 야구어(「甲子園のナイン」)이고 9이닝·9명의 근거도 겹친다 |
| 이름의 적용 범위 | ⚠**화면에 보이는 이름만 제품명이다.** 리포명 `bb-app` · 패키지명 `@bb-app/*` · Pages 프로젝트명은 **내부 식별자**라 그대로 둔다 — 바꾸면 이력과 링크만 끊긴다. 화면 문자열은 `BB_SITE_NAME`(기본값이 제품명) 한 곳에서 나온다 |
| 서명 형식 | `<제품명> by Lunomel` (endorsed 모델 · `by Lunomel`은 시각적으로 더 조용하게) |
| 표기 규칙 | Latin 워드마크 고정 — 번역·합성(`Lunomel <제품명>`) 금지 |
| 메인 계정 | Google 계정 하나. ⚠**주소를 여기 적지 않는다**(2026-09-06) — **이 저장소를 공개할 계획**이라 적어 두면 그대로 노출된다(`docs/decisions/2026-09-06-repo-public-and-data-store.md`). 쓰는 자리는 **`BB_ARCHIVER_CONTACT`**(수집기 UA · L1 이 요구하는 **닿는** 연락처)와 **`BB_CONTACT`**(화면의 삭제·정정 창구 · L4)이고 **둘 다 시크릿이다**. ⚠**GitHub 의 `users.noreply.github.com` 을 수집기 연락처로 쓰지 마라 — 수신함이 없다.** 커밋 신원용이지 「닿는 연락처」가 아니다 |
| GitHub | `muji-j` |
| 제품 UI 언어 | **일본어 우선**(2026-08-14 확정). 데이터 원본과 표기가 일치해 구현이 단순하고, NPB 팬 주류 시장에 맞는다. 다른 로케일은 v3 이후 검토. ⚠**~~문자열 하드코딩 금지(i18n 키 필수)로 확장 여지를 남긴다~~ 는 지켜진 적이 없다**(2026-09-03 실측): **로케일 카탈로그 0건**이고 화면 문자열은 일본어로 코드에 직접 있다. ⚠**그렇다고 「키 체계 0건」은 아니다** — 지표 라벨·설명·**분모 단위**는 `packages/web/src/glossary.ts` 에 **한 벌로** 모여 있고(M1 · 시험이 고정한다), v3 에서 뽑아내기 시작할 자리가 거기다. **그 비용을 알고 시작한다** |

브랜드 상세 선례: `../x-scraper/brand/README.md` · `../x-scraper/docs/superpowers/specs/2026-08-13-lunomel-brand-architecture-design.md` · `../google-chat/BRAND.md`.
**Lunomel은 브랜드 결정이지 법인이 존재한다는 선언이 아니다.** 개인정보처리방침·약관·스토어 게시자 필드는 실제 운영자를 표시한다.

---

## 8. 문서 배치 (루트 §11 의 `docs/superpowers/**` 에 더해)

```
docs/decisions/YYYY-MM-DD-<주제>.md   아키텍처·정책 결정 기록
docs/sources/                          데이터 소스 판정표·수집 규약 (⚠`samples/` 는 무추적 — §6)
docs/metrics/                          지표 정의서(계산식·자격 기준·동률 규칙)
docs/operations/                       배포·수집 런북
```

⚠**`ADR-XXX-*.md` 라는 이름 규칙은 쓰인 적이 없다** — `docs/decisions/` 의 실재 3건이 **전부**
날짜 접두사다(전 이력 실측 0건 · §1). 새 결정문도 날짜 접두사로 쓴다.
