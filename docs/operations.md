# 운용 — 일일 갱신과 감시

## 매일 하는 일

```bash
node scripts/update.ts --contact <닿는-연락처>
```

1. **어제** 경기 페이지 아카이브
2. DB 적재 (멱등)
3. 새로 등장한 선수 프로필 아카이브·적재
4. 신선도 보고

소요 약 2분(요청 25건 × 3초 + 적재). 계정·클라우드 없이 로컬에서 돈다.

### ⚠왜 「오늘」이 아니라 「어제」인가

진행 중인 경기를 받으면 **미확정 성적이 `final`로 저장된다**(M9 위반).
그런데 npb.jp 경기 페이지에는 **그 경기 자신의 종료 여부를 가리키는 안정된 표시가 없다** —
헤더의 「試合終了」는 그날 전 경기 목록이라 구별되지 않는다.

감지기를 추측으로 만드는 대신 **구조적으로 안전한 시각**을 쓴다.
자정 이후에 돌리면 어제 경기는 확실히 끝나 있다.

만약 진행 중에 받아버려도 **수집은 멱등이고 내용 해시로 판정하므로** 다음 실행에서
확정판으로 갱신된다. 낡은 값이 몇 시간 보일 뿐 영구 오염은 아니다.

## 감시

```bash
node scripts/freshness.ts data/bb.sqlite 2
```

낡았으면 **종료 코드 1**. 크론이 그대로 실패로 잡는다.

### ⚠~~주 감시는 제품 화면이다~~ — **그 문장이 거짓이었다** (2026-09-09 정정)

**주 감시는 `heartbeat.yml` 이다.** 화면의 신선도 띠는 **감시 장치가 아니다.**

⚠**왜 거짓인가**: `packages/web/src/layout.ts:160` 이 `lagDays` 를 **`builtOn`**(빌드 시각) 기준으로
계산하고 이 사이트는 **정적 생성물**이다 — **파이프라인이 멈추면 페이지가 다시 안 구워지므로
그 값이 얼어붙는다.** 마지막 빌드 당시가 신선했으면 **영원히 초록**이다.
**정지를 그 배지로는 원리적으로 탐지할 수 없다.**
⚠**CLAUDE.md §6 이 2026-08-30 사고에서 같은 함정을 이미 적어 뒀다**
(「초록 띠는 그 화면이 만들어질 당시엔 신선했다는 뜻」) — **읽고도 이 문단을 그대로 뒀다.**
⚠**띠가 쓸모없다는 말이 아니다** — **「지금 보고 있는 화면이 며칠짜리인가」**는 여전히 말한다.
**「파이프라인이 살아 있는가」를 못 말할 뿐이다. 둘을 섞지 마라.**
⚠**임계도 둘이다**: 화면 배지는 `STALE_AFTER_DAYS = 3`(`layout.ts:125`),
`scripts/freshness.ts` 는 인자 기본 **2일**. **다른 장치의 수를 섞어 적지 마라.**

원래 조사가 무료 외부 감시 3종을 **전부 시한부**라며 탈락시킨 표는 그대로 유효하다:

| 수단 | 만료 |
|---|---|
| Cloudflare 헬스체크 | Free 플랜에 **없다** (Pro $20/월) |
| GitHub Actions 스케줄 | **60일 무활동 시 자동 비활성화** |
| healthchecks.io | **1년 무로그인 시 계정 삭제** (ping은 활동으로 안 침) |

⚠**그런데 그 표의 2행을 다시 채택했다**(`heartbeat.yml`). **판단이 바뀐 이유를 적어 둔다:**
그때 물음은 「제품이 낡은 것을 감시할 수 있는가」였고 답이 「제품 화면이 한다」였다 —
**그 답이 거짓이었다는 것이 이번 발견이다. 시한부라도 없는 것보다 낫다.**
⚠**대가**: `daily.yml` 이 멈추면 저장소 활동도 멈추고 **60일 뒤 둘이 함께 비활성화된다.**
`heartbeat.yml` 은 아무것도 커밋하지 않으므로 **활동을 만드는 것은 `daily.yml` 의 `ops/` 커밋뿐**이고,
**그것이 멈추는 것이 바로 이 감시기가 알리려는 상황이다** — **자기가 감시하는 것에 자기 수명이 걸려 있다.**

**`heartbeat.yml` 이 하는 일과 못 하는 일**

- 묻는 것: **「최근 24시간 안에 `collect` 잡이 한 번이라도 성공했는가」**. 아니면 실패 → **메일**.
- **탐지 지연**: 창 24h + 주기 6h = **30시간**이 하한이고, **스케줄 지연(실측 81~385분)을 얹으면
  약 36시간**, 한 회 탈락하면 **약 42시간**이다. ⚠**「최악 30시간」이라고 쓰지 마라 — 그건 하한이다.**
- **못 잡는 것**: 부분 장애(슬롯 하나만 죽는 것) · `deep` 의 영구 정지 · 予告先発 실취득 실패 ·
  지연 · 탈락(탐지만 된다) · **자기 자신의 정지**.
- ✅**휴식·오프시즌 소음은 판정 변경으로 막았다**(2026-09-11) — ⚠12월(월간 일정이 404 면)~3월 새 시즌 일정 미공표 기간은 아직이다(아래 3).

### ⚠감시기를 사람이 확인한다 (자동으로는 못 한다)

1. **살아 있는가** — 가끔 `gh run list --workflow heartbeat.yml` 로 본다.
   **침묵이 「정상」과 「감시기 사망」 양쪽을 뜻하므로** 이건 자동화할 수 없다.
2. **메일이 실제로 오는가** — ✅**확인됐다**(2026-09-11 · 사용자가 수신을 확인).
   끝에서 끝까지 전부 실측으로 이어졌다: **스케줄 → 진짜 API 판정 → `stopped` → exit 1 →
   잡 실패 → GitHub 알림 → 메일 수신.** 「탐지가 생겼다」를 **그날부터** 쓴다.
   ⚠**수신자가 누구인지도 확인했다.** GitHub 문서: 스케줄 워크플로의 알림은
   **「워크플로를 처음 만든 사용자」, 다른 사용자가 cron 을 고쳤다면 그 사용자**에게 간다.
   `heartbeat.yml` 은 **커밋 작성자·커밋한 사람·PR 작성자·머지한 사람이 전부 `muji-j`** 다.
   ⚠**이걸 확인한 이유**: 개발 중 `gh` 계정이 **여러 번 다른 계정으로 되돌아갔다.**
   그 상태에서 누가 cron 을 고쳐 푸시하면 **알림이 그 계정으로 조용히 옮겨 간다.**
   → **`heartbeat.yml` 의 cron 을 고칠 때는 푸시 전에 `gh auth status` 로 계정을 확인하라.**

   **다시 확인하는 법(부작용 0)** — 코드·커밋·브랜치를 새로 만들 필요가 없다:
   ```
   gh workflow run heartbeat.yml --repo muji-j/basenine --ref <main 이 아닌 이미 푸시된 브랜치>
   ```
   하트비트는 `BRANCH` 를 `github.ref_name` 에서 받으므로, **`main` 이 아닌 브랜치**에서 돌리면
   그 브랜치엔 `daily.yml` 실행이 0건이라 **진짜 API 응답으로 `stopped` → exit 1** 이 난다 —
   **운영 경보와 똑같은 경로**를 실제 리눅스 러너에서 탄다. 로그에
   `⚠최근 실행 중 collect 가 성공한 것이 하나도 없다` 가 찍히고 실패 메일이 와야 한다.
   ⚠**그 성질 자체는 함정이기도 하다** — 하트비트를 `main` 밖에서 **손으로** 돌리면 **언제나 경보가 난다.**
   스케줄은 언제나 기본 브랜치에서 돌므로 운영에는 영향이 없다. **「경보가 왔는데 파이프라인은 멀쩡하다」면
   먼저 그 실행의 브랜치를 보라.**
3. ✅**~~11월 초 전에 정해야 할 것~~ — 정했고 고쳤다**(2026-09-11 · 사용자 결정 「일정과 대조해 판정」).
   ⚠**그 기한부터 틀렸다** — 첫 헛경보는 11월이 아니라 **10월 초**(정규시즌 종료 → CS 사이)였다.
   보유 완결 8시즌에 시즌 도중 3일 넘게 빈 구간이 **23개**였다. 판정을 「최신 경기가 며칠 전인가」에서
   **「NPB 가 치렀다고 표시한 경기(월간 일정의 점수 링크)를 못 받았나」**로 바꿨고, 같은 기간에 함께 실패하던
   예고 맥박 · 빈 달 가드 · 빌드 게이트도 같이 고쳤다.
   설계: `docs/superpowers/specs/2026-09-11-offseason-collection-verdict-design.md`

   **⚠실물로 못 잰 전제 — 이 날짜에 수집 로그(`gh run view` · `ops/collection-log.jsonl` 의 `period`)를 사람이 본다**

   | 날짜 | 무엇을 본다 | 틀렸으면 |
   |---|---|---|
   | **리그 우승 확정 직후 첫 실행**(9월 말~10월 초) | 10월 월간 일정의 **대진이 반쯤 정해진 행**이 어떤 모양인가 — 적재 로그에 `못 읽은 행이 있는 달` 이 없는가. ⚠**10·11월의 링크·숫자 없는 행은 모르는 표기를 예정 표기로 받는다**(3중 검토 2차 N1) — 그래도 멈췄다면 그 행 원문을 보고 `packages/parser/src/upcoming.ts` 의 분류를 고친다. ⚠**적재 요약의 `대진 미정 팀 칸 N행(표기: …)` 을 본다** — `CS勝者` 같은 **진짜 미정 표기**인가, 아니면 **약칭이 깨진 경기**(`ＤｅＮＡ` 전각 등)인가. 후자면 그 경기가 치러지는 날 적재기가 멈추니 미리 약칭 표를 고친다(수정분 재검토 1차 R1) | §6 P10 |
   | **10월 CS 첫 주** | 정규시즌 종료 → CS 휴식에 `collect` 가 성공하고 `period` 가 `rest` 인가 · CS 예비일에 `starters-behind` 가 안 우는가 · CS 경기에도 점수 링크가 붙는가 | 설계서 §6 P3·P4 |
   | **11월 첫 주** | 일본시리즈 뒤 `period` 가 `offseason` 인가 · 予告先発 페이지가 계속 200 + 「試合が予定されていません」인가(맥박이 전진하는가) · ⚠**그 페이지가 마지막 일본시리즈 날짜를 계속 보이는가** — 그렇다면 `load-starters.ts` 의 연도 추정이 1월에 받은 「11-01」을 **다음 해** 11월로 만들어 `starters-behind` 가 1년 동안 조용할 수 있다(3중 검토 2차 · 범위 밖 관찰 · 미확인) | §6 P1 |
   | **12월 첫 실행** | 경기 없는 달의 월간 일정이 **공백 행만 있는 페이지**인가(적재기가 「경기가 없는 달」로 통과하는가) 아니면 **404** 인가 | §6 P2 — 404 면 단계 B 가 필요하다 |

   **⚠아직 남은 것(후속 작업 · 단계 B·C)** — 새 시즌 일정 공표 전(1월~)의 **월간 일정 404** 와 **개막 전 공표표 대조 0명**.
   그 전까지 **12월(월간 일정이 404 면)~3월에 하트비트가 울릴 수 있다.** 설계는 끝났다(설계서 D7 · D8).

   **`game-missed` 가 울렸을 때** — 출력이 A(일정표가 치렀다고 표시했는데 경기 행이 없다 · 경기 단위)와
   B(예고됐는데 받지 못했다 · 그 달 일정 사본이 그 날 뒤에 안 받아졌다 · 구단 단위)를 나눠 적는다.
   - **A** 는 수집·적재가 그 경기를 못 받은 것이다 — 그 날짜의 수집 로그를 본다.
   - **B** 는 그 달 일정 페이지를 못 받고 있다는 뜻이다(경기 발견 경로 고장). 원인을 고치면 다음 정기 실행이 받는다.
     ⚠**7일 넘게 멈췄으면** 따라잡기가 안 넓어지므로 **소급 절차**(`node scripts/update.ts --date <D>` · 경기당 4장 · 그 달과 다음 달 일정 ·
     予告先発 1장 · 낡은 선수 프로필 상한 400명)로 그 날짜를 다시 받는다 — ⚠워크플로 수동 실행에는 날짜 입력이 없다.
     ⚠`packages/archiver/src/cli.ts --date` **만** 돌리면 **풀리지 않는다** — DB 의 사본 기록(`schedule_month`)은 적재기가 쓴다.

   **적재기가 `날짜가 빠진 달 MM(없는 날 … · 근거: …)` 으로 멈췄을 때**(설계서 D5) — 적재기는 되돌렸고 기존 증거는 그대로다.
   **메시지의 근거부터 읽는다** — 넷 중 하나다:
   - **`날짜 행 없음`** → 그 파일에 그 달의 날짜 행이 하나도 없다(예: `schedule_10` 에 9월 날짜만 있다). **다른 달 페이지가 왔거나 형식이 바뀐 것**이다 —
     npb.jp 페이지를 확인하고 사본 재취득 또는 **파서·적재기 수정**으로 올린다(4라운드 재검토 2차 F4).
   - **`중간·끝 날짜가 빠짐`** → 페이지 중간이나 끝이 없다. 잘린 응답이거나 **페이지 형식이 바뀐 것**이다.
     다음 정기 실행이 풀지 않으면 npb.jp 의 그 달 페이지를 사람이 연다(요청 1회 · L1). 페이지는 온전한데 사본만 잘렸으면 소급 절차로 다시 받고,
     페이지 형식 자체가 바뀌었으면 **파서·적재기 수정**으로 올린다(데이터를 고치지 않는다).
   - **`경기 행·치러짐 표시가 YYYY-MM-DD 에 있다`** → 그 날 치러진 사실이 있는데 사본이 그 날보다 늦게 시작한다 — **사본이 잘렸다**(치러진 날은 페이지에서 안 사라진다).
     npb.jp 페이지로 확인하고 소급 절차(`node scripts/update.ts --date <그 달 안의 오늘 이전 날>`)로 사본을 다시 받는다.
     ⚠**`game` · `schedule_played` 행을 지우지 마라** — 그게 치러진 사실이고, 지우면 누락 판정이 조용히 풀린다.
   - **`전 사본이 YYYY-MM-DD 부터 내용을 실었고 그 날이 이미 왔다`** → 받아들였던 사본이 그 날부터 경기·예정 표기를 실었는데 새 사본에는 그 날이 없다.
     1. 다음 정기 실행이 다시 받아 풀리는지 본다(잘린 응답은 대개 한 번이다).
     2. 안 풀리면 **npb.jp 의 그 달 페이지를 사람이 연다.** 그 날짜가 **페이지에 있으면** 사본이 잘린 것이다 — 소급 절차로 다시 받는다.
     3. 페이지에 **정말로 없으면** NPB 가 **온 날을 일정에서 지운 것**이다(설계서 §6 P11 · 실측 표본 없음 · 밤 슬롯은 평소 자정 뒤에 돌므로
        전날 밤 공표된 연기도 여기에 걸릴 수 있다). 그 달 **기준선만 비운다** — ⚠**실행과 실행 사이에** 한다(도는 실행은 시작에서 받은 DB 를
        끝에서 `--clobber` 로 다시 올려 **이 수정을 덮는다** · 4라운드 재검토 2차 F3):
        ```sh
        gh auth switch --user muji-j                     # ⚠계정이 되돌아가 있을 수 있다
        gh run list --repo muji-j/basenine --workflow daily.yml --status in_progress   # 비어 있어야 한다
        gh release download data-store --repo muji-j/bb-app-data --pattern bb.sqlite.gz --dir <작업 폴더>
        gunzip -c <작업 폴더>/bb.sqlite.gz > <작업 폴더>/bb.sqlite
        node -e "const {DatabaseSync}=require('node:sqlite');const d=new DatabaseSync(process.argv[1]);console.log(d.prepare('UPDATE schedule_month SET first_content = NULL WHERE season = ? AND month = ?').run(Number(process.argv[2]), Number(process.argv[3])).changes);d.close()" <작업 폴더>/bb.sqlite <시즌> <월>   # 1 이 찍혀야 한다
        gzip -c <작업 폴더>/bb.sqlite > <작업 폴더>/bb.sqlite.gz
        gh release upload data-store --repo muji-j/bb-app-data <작업 폴더>/bb.sqlite.gz --clobber
        ```
        다음 실행 로그에서 그 달이 받아졌는지(`날짜가 빠진 달` 이 없는지) 본다. ⚠`--clobber` 는 **지운 뒤 올린다** — 업로드가 실패하면 자산이 사라지고
        다음 실행이 DB 를 아카이브에서 다시 만든다(모든 기준선이 NULL → 사실 근거만 남는 기간 · 설계서 §5). 실패하면 바로 다시 올린다.
        ⚠**이 칸은 적재기만 쓴다** — 다른 적재기가 되살리지 않는다. ⚠기준선이 NULL 인 동안 그 달은 사실 근거로만 막으므로, **페이지를 확인하기 전에 비우지 마라.**
        ⚠~~그 날짜의 `upcoming_game` · `probable_pitcher` 행을 지운다~~ 는 **안 된다** — 예고 행은 같은 실행의 `load-starters` 가 아카이브에서
        되살려 **다음 날 다시 멈췄다**(3라운드 재검토 2·3차 · 실행 재현). ⚠**아카이브(예고 원본)를 지우지 마라** — 소급 불가 자산이다.
   - 사이드카가 깨져 **사본 시각을 모르면** 「이미 왔다」고 보고 멈춘다 — 사이드카(`schedule_MM.meta.json`)부터 복구한다.

## 쓰기 예산

⚠**지금 운영은 D1 이 아니다**(SQLite 1파일 · 정적 사이트 — CLAUDE.md §1 · 2026-09-11 정정 · 4라운드 재검토 1차 Q1). 아래는 **D1 로 옮길 때를 대비해
코드에 남은 장치**(`load-archive.ts` 의 `D1_DAILY_WRITE_LIMIT` 기본 100,000 · 일일 배치의 `scripts/update.ts --max-writes` 기본 5,000,000)의 근거다.

D1 무료는 **하루 10만 행**에서 **차단**된다(과금이 아니다).

| 작업 | 행 수 | 한도 대비 |
|---|---|---|
| 2026 시즌 전량 적재 | 70,821 | 70.8% |
| 일일 증분(6경기) | 약 700 | 0.7% |

한 시즌은 하루에 들어가고 **두 시즌은 안 들어간다.**
`--from` / `--to` / `--max-writes`로 나눠 넣는다 — 상한에 도달하면 **넘기기 전에 멈추고 재개 날짜를 알린다.**

## 알아둘 것

### 경기 페이지는 경기와 무관하게 매일 바뀐다
8/14 페이지에 8/15 경기 링크가 들어간다(헤더 내비). 그래서 하루 지나 다시 받으면 「신규」로 판정된다.
**파싱 결과는 그대로**이고, 「어제 본 숫자와 다른데?」에 답할 때는 **DB의 `revision`**을 보면 된다.

### 격리는 버그가 아니라 판단 요청이다
`quarantine` 표에 뭔가 쌓이면 원문을 보고 규칙을 늘릴지 정한다.
2026 시즌 전량에서 현재 **0건**이다.

## 화면 생성

```
npm run build:web            # data/bb.sqlite → dist/  (2026 현행 + 2025·2024 는 /{연도}/)
BB_CONTACT="..." npm run build:web
npm run preview              # http://127.0.0.1:4173
```

⚠**`dist/index.html`을 파일로 직접 열면 검색이 안 된다.** 선수 색인을 `fetch`로 받는데
`file://`에서는 브라우저가 막는다. 볼 때는 `npm run preview`를 쓴다.

| 항목 | 실측 (2026 시즌, 630경기 시점) |
|---|---|
| 출력 | 700파일 · 25.8MB · 선수 695명 |
| 집계 시간 | 1.8초 |
| 검색 색인 | `players.json` 48KB |
| D1 읽기 | 배포 때 1회. 요청마다 계산하지 않는다 |

**서버가 없다.** 데이터는 하루 1회만 바뀌므로 정적 파일로 낸다.
낡은 데이터로 생성하면 **종료 코드 1**과 함께 화면 상단에 경고 띠가 나온다.

⚠**`BB_CONTACT`를 설정하지 않으면 삭제·정정 요청 창구가 화면에 나오지 않는다**(L4).
가짜 주소를 넣지 않도록 비워 두면 화면이 「미설정」이라고 말하고, 빌드가 경고한다.
**공개 전 필수**다.

## 보관소

아카이브와 DB는 **비공개 리포의 릴리스 자산**(`data-store` 태그)에 둔다.
R2가 아닌 이유와 실측은 `docs/operations/deploy.md`에 있다 — 요약하면
Actions 캐시는 7일이면 지워지고, R2는 시크릿이 늘고, 릴리스는 추가 계정·시크릿이 0이다.

```bash
node scripts/archive-guard.ts count data/archive              # 지금 몇 건인가
node scripts/archive-guard.ts check data/archive ops/archive-manifest.json
```

⚠**줄어들면 멈춘다.** 복원이 조용히 실패한 채 덮어쓰면 소급 불가능한 자산이
한 번에 사라지고 로그에는 「정상 종료」가 남는다. 워크플로가 복원 직후와 수집 직후
**두 번** 검사하고, 통과하지 못하면 보관소에 올리지 않는다.

## 아직 안 된 것

- **Cloudflare Pages 배포** — 런북은 `docs/operations/deploy.md`. 시크릿 2개(`CLOUDFLARE_API_TOKEN`·
  `CLOUDFLARE_ACCOUNT_ID`)가 생긴 뒤에 워크플로에 붙인다
- **Access 허용목록** — ⚠**데이터를 올리기 전에** 건다. 순서가 바뀌면 그 사이가 공개다
- **경기 진행 중 판정** — 위의 「어제」 규칙으로 우회하고 있다
- **화면 배포** — `dist/`를 만드는 데까지. Cloudflare Pages + Access 연결은 증분 H
