# UI 설계 — 名鑑(명감)을 기준으로 · 조립 가능한 선수 페이지

**작성일** 2026-08-15 · **상태** 사용자 검토 대기 · 선행 `2026-08-14-npb-v1-design.md`

목업(동작): https://claude.ai/code/artifact/d94fe4d8-3c90-4593-a9ea-3949d53780c0
방향 3안 비교(기록): https://claude.ai/code/artifact/1073d826-c7a4-4ac3-bef0-f94a2a24c499

---

## 1. 결정

**C안 名鑑을 기준으로 삼는다**(2026-08-15 사용자 확정).

그리고 **A안·B안을 경쟁안에서 블록으로 강등한다.** 셋 중 하나를 고르는 문제가 아니라,
名鑑이라는 그릇 안에 무엇을 담을지의 문제로 다시 놓는다.

| 원래 | 이후 |
|---|---|
| A 記録票 (경쟁안) | **打席記録 블록** — 켜면 명감 안에 들어온다 |
| B 状況盤 (경쟁안) | **得点期待値 블록** — 동상 |
| C 名鑑 (경쟁안) | **페이지의 정체성** — 배색·타이포·괘선·밀도 |

## 2. 名鑑이 정체성으로 가져오는 것

| 요소 | 내용 | 왜 |
|---|---|---|
| **배면(背表紙)** | 왼쪽 세로 띠가 **선수의 소속 구단 색**을 입는다 | 페이지가 누구의 것인지를 색 하나로 말한다. 12구단이 각자 다른 페이지가 된다 |
| 타이포 | 작고 촘촘한 본문 + 등번호·이름만 크게 | 야구 팬은 숫자를 많이·빠르게 본다. 여백만 넓은 화면은 이 제품에서 실패다 |
| 괘선 | 머리카락 굵기의 밑줄로 항목을 가른다 | 카드·그림자를 쓰지 않는다. 인쇄물의 질감 |
| 숫자 | `tabular-nums` 고정폭, 우측 정렬 | 세로로 훑을 수 있어야 한다 |
| 순위 배지 | 구단 색 바탕의 작은 사각 | 「1位」가 색을 얻어 눈에 걸린다 |

⚠**구단 색이 12벌 필요하다.** 阪神의 노랑처럼 강한 색은 글자색 대비를 따로 잡아야 한다.
이건 이 방향이 지불하는 비용이고, 지금은 阪神만 구현돼 있다.

## 3. 블록 모델

선수 페이지 = **블록의 나열**. 사용자가 무엇을 켜고 어떤 순서로 볼지 정한다.

| 블록 | 내용 | 조작 |
|---|---|---|
| `standard` | 기본 성적 | — |
| `advanced` | 세이버 (wOBA·wRC+·SRC) | — |
| `splits` | 스플릿 | **축 전환**: 대투수좌우 / 주자상황 / 홈원정 / 월별 |
| `scorebook` | 타석 기록 (A안 유래) | 기간 |
| `situation` | 득점기대치 24칸 (B안 유래) | — |
| `matchup` | 대전 투수별 | — |
| `ranking` | 리그 순위 | **지표 전환**: wRC+ / SRC / OPS / 타율 / 홈런 |

### 프리셋 — **대부분의 사용자는 커스터마이즈하지 않는다**

| 프리셋 | 구성 | 대상 |
|---|---|---|
| 標準 | 기본 + 세이버 + 스플릿 + 순위 | 기본값 |
| 記録 | 기본 + 타석기록 + 스플릿 + 대전 | 기록을 읽는 사람 |
| 分析 | 세이버 + 득점기대치 + 스플릿 + 대전 + 순위 | 지표를 파는 사람 |
| 簡易 | 기본만 | 빨리 확인만 |

⚠**프리셋이 먼저고 조립은 나중이다.** 설정 화면부터 보여주는 도구는 아무도 안 쓴다.
「組み替え」 버튼을 눌러야 편집 UI가 나온다.

### 조립 UI

명감의 **색인 탭**에 가깝게 만든다 — 톱니바퀴 설정 아이콘을 쓰지 않는다.
체크박스로 켜고 끄고, ↑↓로 순서를 바꾼다.

## 4. ⚠사용자가 끌 수 없는 것

**비율 옆의 분모(타석 수·타수)는 항상 나온다.** 설정에서 지울 수 없다.

10타석 .400을 순위로 보여주지 않기 위한 규칙이고(M2), **취향이 아니다.**
편집 화면에 이 사실을 명시해서, 없는 기능을 찾게 만들지 않는다.

같은 이유로:
- **대전 성적에 순위를 매기지 않는다.** 대부분 10타석 미만이라 나열하는 순간 「이 투수에 강하다」로 읽힌다.
  타석 수 순으로 놓고, 10타석 미만은 색을 낮춘다.
- **스플릿 막대는 표본이 얇으면 칠을 약하게** 한다. 값은 그대로 보이되 시각적 무게를 뺀다.
- **득점기대치 칸도 표본 100 미만이면 흐리게.**

## 5. ⚠상위 규약과의 충돌 — 확인 필요

`CLAUDE.md` §6: 「값 강조 금지 — **라벨 강조 원칙**(항목명이 값보다 잘 보이게)」

이 규칙은 PPS(업무 포털)에서 확립됐고, 뜻은 **「의미를 모르는 값을 보여주지 마라」**였다.
그런데 통계 서비스에서는 **숫자가 곧 내용**이다. 항목명을 값보다 크게 하면 읽을 수 없는 화면이 된다.

**목업에서 내린 판단**: 값을 주역으로 두되, **분모와 라벨을 반드시 인접**시킨다.
규칙의 정신(모르는 값 금지)은 M2로 지키고, 형식(라벨을 더 크게)은 이 도메인에서 버린다.

→ **사용자 확인이 필요하다.** 승인되면 `CLAUDE.md` §6을 도메인 예외와 함께 고친다.

## 6. 설정의 보존

| 단계 | 방식 | 한계 |
|---|---|---|
| 지금(목업) | `localStorage` | 단말이 바뀌면 사라진다 |
| v1 | 동상 | Access로 신원은 알지만 저장소를 붙이는 비용이 있다 |
| 이후 | 사용자별 서버 저장 | Cloudflare Access가 이메일을 주므로 키가 이미 있다 |

⚠**저장에 실패해도 화면은 동작해야 한다.** 목업도 `try/catch`로 감싸 두었다.

## 7. 구현 상태 (2026-08-15 갱신)

목업이 아니라 **실제 DB에서 생성되는 정적 사이트**로 구현됐다 — `packages/web`.
`node packages/web/tools/build.ts data/bb.sqlite dist 2026` → 700파일 / 25.8MB / 선수 695명 / 집계 1.8초.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 구단 색 12벌 | **해결.** `packages/domain/src/colors.ts`에 12벌. **WCAG AA 4.5:1을 테스트가 고정**한다(12/12 통과). 阪神 노랑에 흰 글자를 넣는 뮤테이션에서 1.61:1로 잡히는 것을 실측 확인 |
| 2 | 스플릿 최소 표본 임계값 | **잠정 확정 30타석**(`THIN_SPLIT_PA`). 축마다 다르게 잡을 근거가 아직 없어 하나로 두고 코드가 아니라 상수와 문서에 적었다. 대전 성적·득점기대치는 10 |
| 3 | 투수 페이지 | **부분 해결.** 기본성적·세이버·타석기록·대전·순위는 나온다. **스플릿과 득점기대치는 제공하지 않는다** — 타석에 선 쪽의 이야기라 투수용 집계가 따로 필요하다. 빈 블록을 내는 대신 **목록에서 뺐다** |
| 4 | 검색·이동 | **해결.** **전역 헤더에 검색창**(키보드 ↑↓·Enter)과 一覧/順位/対戦 이동. 어느 화면에서나 2클릭 |
| 5 | 4상태 표시 | **부분 해결.** 신선도 띠(3일 초과 시 경고색) · 데이터없음 · 수집실패 · 시즌외 문구가 각각 다르다. 「로딩」은 정적 생성이라 페이지 단위로는 존재하지 않고 **검색 색인 fetch에만** 있다 |
| 6 | 모바일 | **CSS 해결 · 실기 미확인.** 900/680/420px 분기점, 배면 44→7px, 레일 고정 해제, 단·막대·표 재배치, `pointer:coarse` 확대. **실제 단말에서는 아직 안 봤다** |

### 2026-08-15 2차 (사용자 지적 8건)

| # | 지적 | 처리 |
|---|---|---|
| 1 | 순위표를 나열하지 말고 선택식으로 | **해결.** 리그 탭 × 지표 탭. 지표 그룹을 리그 간 공유해 리그를 바꿔도 보던 지표가 남는다 |
| 2 | 선수 사진 | ⚠**하지 않는다.** 사진은 사실이 아니라 저작물이고 초상권이 붙는다(CLAUDE.md §6에 규칙으로 기록). 대체로 **월별 추이 꺾은선**을 표제에 넣었다 — 사진보다 정보량이 많다 |
| 3 | 이동 버튼이 최하단에만 있다 | **해결.** 전역 헤더 |
| 4 | 반응형 | **해결(CSS).** 실기 확인은 남음 |
| 5 | 모션 | **해결.** 120~420ms · ease 1종 · `prefers-reduced-motion`에서 전부 정지 |
| 6 | 전반 개선 | 월별 정렬 버그(10月<4月) · 잘라낸 것 전부 표기 · `positionMark` 통합 · 스킵링크·`<main>`·탭 시맨틱·테마 전환 |
| 7 | 대전 성적 검색·선택 | **해결.** 전량(19,769쌍) 게재 + 이름 좁히기 + 정렬 3종. ⚠타율순은 10타석 이상 제한(탭 이름에 명시) |
| 8 | 라이브 시점 표시 | **조사 완료 · 취득은 하지 않는다.** `docs/decisions/2026-08-15-live-matchup-feasibility.md`. 대신 `matchup.html`(유저가 고르는 대전 화면) |

### 2026-08-15 3차

| # | 결정 | 상태 |
|---|---|---|
| 1 | **식별 마크 = B안 成績の紋 확정**(사용자) | 선수 페이지 표제(52px)와 색인 명단(18px)에 **같은 마크**를 쓴다. 투수는 축이 다르다 — 奪三振·制球·被弾·抑制·失点이고 ⚠**네 축이 「낮을수록 좋다」라 뒤집어** 넣었다. 안 뒤집으면 좋은 투수가 작은 도형이 되어 타자와 뜻이 정반대가 된다 |
| 2 | 표본이 없는 선수 | ⚠**아주 작은 도형으로 그리지 않는다** — 「나쁘다」로 읽힌다. 포지션 한 글자 마크로 대체(M11) |
| 3 | 마크의 분모 | 이미 사람이 읽는 문자열(`442打席`·`138.1回`)로 받는다. **숫자와 단위를 따로 받지 않는다** — 투수 표본은 아웃 카운트라 `415アウト`가 되어 사이트의 다른 분모와 어긋난다. 어긋나는 경로를 타입에서 없앴다 |
| 4 | 予告先発을 대전 카드 버튼으로 | 경기 수만큼 버튼이 생기고 `auto-fit` 그리드라 폭에 맞춰 6→3→2→1열로 접힌다. 버튼 키는 **팀 코드 쌍**(`d-g`) — 순번이나 구장이면 다음날 엉뚱한 경기를 가리킨다. 「すべて」도 남겨 두었다 |
| 5 | 페이지 무게 | 색인에 695개 마크가 들어가 500KB(원본) / **43KB(gzip)**. 선수 페이지 101KB / 12KB |

### 남은 것

| # | 항목 | 왜 남았나 |
|---|---|---|
| A | **`BB_CONTACT` 미설정** | 삭제·정정 요청 창구(L4). 가짜 주소를 넣지 않으려고 비워 뒀고, **화면이 「미설정」이라고 말한다.** 공개 전 필수 |
| B | wRC+ 외부 대조 | 내부 정합성은 확인(리그 상수로 역산해 일치). **공표값과의 대조는 안 했다** — 개발 루프에서 외부 사이트를 치지 않기로 했으므로 검증 단계에서 별도로 한다 |
| C | 투수 스플릿 | 위 3번. `pa_event`에 필요한 입력은 다 있고, `pitchingSplits` 집계만 없다 |
| D | 설정의 서버 저장 | 지금은 `localStorage`. §6 그대로 |

### 확인한 것

- **T1 재현**: 佐藤(41045153) `through=2026-08-13`에서 .317 / .404 / .622 — 이전 검증값과 **정확히 일치**.
  사이트 기본 생성(08-14)은 .317 / .403 / .620이고, 차이는 버그가 아니라 **하루치 경기**다.
- **M2 집행**: 화면의 모든 `<dd>` 안 소수는 분모 span을 동반한다(테스트가 고정). 분모 span을 지우는
  뮤테이션에서 2건이 실패하는 것을 실측.
- ⚠**분모를 붙이는 것만으로는 M2가 아니다** — 투수 지표의 `Rate.denominator`는 **아웃 카운트**인데
  초판이 그걸 「投球回」로 찍었다(40아웃 → `40投球回`). 분모가 3배로 부풀어 보였다. `statRateOuts`로 고쳤다.
