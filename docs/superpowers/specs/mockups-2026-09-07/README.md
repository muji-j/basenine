# 목업 3안 (2026-09-07) — 방향 선택의 정본

`../2026-09-07-design-direction.md` 가 **C안을 바탕으로** 정했다. 그 판단의 실물이 여기다.

⚠**이 파일들이 저장소 밖(세션 임시 폴더)에만 있었다** — 콜드 리뷰가 P1 으로 잡았다:
「저장소만 받은 구현자는 채택된 C안의 실제 CSS/DOM 을 볼 방법이 없다」. **옳은 지적이라 옮겼다.**

| 파일 | 안 | 입장 |
|---|---|---|
| `a-space.html` | **A** | 선을 걷어내고 **여백과 옅은 층**으로 나눈다 |
| `b-almanac.html` · `b-almanac-notes.md` | **B** | 선을 줄이는 대신 **선에 위계를 준다**(괘선 6단·활자 7단) |
| `c-modern.html` · `c-modern-notes.md` | **C** ⭐**채택** | 화면을 **조작층/내용층** 두 장으로 자르고 모션은 그 경계에만 |

⚠**A 의 노트는 없다** — 담당이 네트워크 단절로 죽어 못 썼다. **「안 만든 것」이 아니라 「못 만든 것」**이다.

## 여는 법

자체 완결 HTML 이라(외부 의존 0) 브라우저로 그냥 열면 된다.
⚠**본 사이트는 `file://` 로 열면 안 된다**(색인 fetch 가 막힌다 · CLAUDE.md §5). **이 목업은 예외다.**

## ⚠이것은 목업이지 구현이 아니다

- **홈 화면 1장씩만** 있다. 선수 페이지(블록 14개)·순위 화면(42,703노드)에서 성립하는지는 **모른다.**
- 내용은 **2025년 실제 데이터를 옮겨 적은 것**이라 갱신되지 않는다.
- 채택안의 값(색·간격·활자·괘선·모션 시간)을 **토큰으로 뽑는 것이 다음 단계**다.
