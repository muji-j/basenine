# 디자인 2단계 — 값을 바꾼다 (2026-09-08)

정본: `../specs/2026-09-07-design-direction.md`(방향·콜드 리뷰 반영) ·
`../specs/2026-09-07-design-audit.md`(무엇이 문제인지) · `../specs/mockups-2026-09-07/`(채택안 실물).

**1단계(토큰)는 끝났고 화면을 한 픽셀도 안 바꿨다**(산출물 9,548/9,549 · HTML 9,392/9,392 바이트 동일 ·
스크린샷 72/72 동일 · 펼침 등가 67,048자). **여기서부터 화면이 바뀐다.**

---

## 0. ⚠조각으로 나눈다 — 각 조각은 **독립적으로 배포 가능**해야 한다

이 사이트는 **매일 자동 배포**되고 화면이 **9,624장**이다. 절반만 적용된 상태로 며칠 서 있어도
화면이 성립해야 한다(방향서 §6).

| 조각 | 무엇 | 왜 이 순서 |
|---|---|---|
| **2a** | **값 척도 정리** — 간격·활자를 척도에 앉힌다 | 나머지 전부가 이 위에 선다. ⚠**`ranking.html` 을 여기서 먼저 재라**(방향서 §7-5) |
| **2b** | **괘선 위계** — 구획 2px / 행 1px 로 가른다 · **겹친 선 3겹을 푼다** | 감사가 짚은 「투박함」의 정체 |
| **2c** | **모바일 크롬** — 내비→버튼 · 시즌 띠→드롭다운 | ⚠**가로 스크롤 4 → 1.** 감사가 「진짜 UX 문제」로 꼽은 것 |
| **2d** | **표 열 묶음 + 세로 레일** | 눈의 횡단거리 · 구조 안내 |
| **2e** | **모션** | 값이 다 선 뒤에 얹는다 |

⚠**조각마다 굽고 실기 확인한다.** 이 세션에서 「시험도 타입도 못 잡고 굽고서야 보인」 결함이
**두 번** 났다(홈 표가 10행 중 7행 투수 · 이적 투수 구단 오배정).

---

## 2a. 값 척도 정리

**1단계가 이탈을 세어 뒀다**(`design-tokens.test.ts` 의 이탈 예산):

- **간격 339건** — ⚠**현행은 4px 격자가 아니라 1px 단위**다(5px×53 · 6px×49 · 9px×37 · 10px×37 …).
- **활자 57건** — ⚠**9~16px 사이에 0.5px 단으로 13종.** 계단이 아니라 연속이다.
- `inset box-shadow` 로 그린 괘선 **29건**이 척도 밖.

**할 일**: 이탈을 척도로 접는다. **예산이 줄어드는 것이 이 조각의 지표다.**

⚠**한 축만 따라오는 자리를 조심하라** — 1단계가 `padding:5px var(--s2)` 같은 혼합을 **341곳** 만들었다.
`--s2` 를 움직이면 **세로/가로 중 한쪽만 따라온다.** 접을 때 **두 축을 같이 정하라.**

⚠**`ranking.html`(42,703노드)을 여기서 먼저 굽고 재라.** 방향서 §7-5 가 「거기서 먼저 재고
전역에 얹으라」고 정했는데 목업은 홈 1장뿐이었다. **바이트·DOM·렌더 시간을 앞뒤로 보고하라.**

**게이트**: 시험 통과 · 링크 검사 0 · **이탈 예산이 줄었을 것** ·
⚠**행이 두 줄 되는 곳 0**(스크립트로 앞뒤 대조 — 서체 측정에서 쓴 그 방법).

## 2b. 괘선 위계

현행은 **행 구분과 구획 경계가 둘 다 1px** 이고 잉크로만 갈린다(1단계 보고).
감사가 짚은 것: **카드 테두리 + 안쪽 세로줄 + 표 테두리가 몇 px 안에 선 3겹.**

**할 일**: `--rw-sect`(2px)와 `--rw-row`(1px)를 **실제로 가른다.** **겹친 3겹 중 둘을 없앤다.**
⚠**선을 늘리지 마라** — B안의 통찰은 「선을 많이 쓴다」가 아니라 **「굵기에 뜻을 준다」**이다.

**게이트**: 대비 시험 통과 · 앞뒤 스크린샷에서 **선이 줄어든 것이 보일 것**.

## 2c. 모바일 크롬 — ⚠**이 조각이 UX 상 가장 크다**

감사 실측: **390px 첫 화면에 가로 스크롤 영역이 4개**(내비 · 시즌 띠 · 탭 줄 · 표).
**사용자가 어느 것을 밀어야 하는지 알 수 없다.** 카드는 화면 끝에 붙어 **바깥 여백이 거의 0**이고
내비의 `他大…` 는 **단어 중간에서 잘린다.**

**할 일**(A안에서 빌려온다): **내비 → 버튼** · **시즌 띠 → 드롭다운** · **탭 줄 → 줄바꿈** ·
**표 하나만 남기고 「옆으로 흐른다」고 말하게 한다.**
⚠**C 의 세로 레일은 넓은 화면의 것이다** — 좁은 화면에서 무엇을 대신할지 정하라(2d 와 같이).

**게이트**: ⚠**390px 첫 화면의 가로 스크롤 영역이 1개**여야 한다(스크립트로 센다) ·
바깥 여백 > 0 · **내비 항목이 단어 중간에서 안 잘릴 것.**

## 2d. 표 열 묶음 + 세로 레일

- **묶음은 C안의 3그룹**(`成績` / `勢い` / `この先`) — 방향서 §7-1 이 정했다.
  ⚠**열→그룹 매핑표를 이 문서에 적고 나서 구현하라.**
- **세로 레일**은 C안의 것(상시 남고 **현재 위치도 말한다**).
  ⚠**선수 페이지는 블록이 14개다** — 홈(7)에서 되는 것이 거기서도 되는지 **먼저 재라**.

## 2e. 모션

방향서 §7-2 가 정한 대로:
- **연출은 화면이 명시적으로 켠다**(기본 꺼짐). **순위 화면은 켜지 않는다.**
- **뷰포트 첫 진입 1회**만(`IntersectionObserver`). 재진입 시 재생 안 함.
- ⚠**상한을 빌드가 검사한다.** 한 화면의 연출 대상이 상한을 넘으면 **빌드 실패.**
  ⚠**상한값은 실측으로 정한다 — 고른 수를 박지 마라.**
- **`prefers-reduced-motion` 에서 최종 상태로 즉시** 그린다(1단계가 배선을 세워 뒀다).

---

## ⚠전 조각 공통 — 깨면 개악

- **표 한 줄이 두 줄이 되면 안 된다.**
- **각주를 지우지 않는다** — 읽기 쉽게 만드는 것이 과제다.
- **색은 도메인에서** — iOS 기본 파랑으로 수렴 금지.
- **상태를 색만으로 말하지 않기** · **키보드** · **WCAG AA** · **M12 4상태**.
- ⚠**`design-auditor` 확정 지적 0건**이 §0 제약6 의 합격선이다.

## 0-A. ⚠사용자 결정 (2026-09-08 · 밤 작업 전에 미리 받음)

| # | 물은 것 | **정해진 것** | 어느 조각 |
|---|---|---|---|
| ⑴ | 모바일 상단 내비 | **버튼으로 접는다**(A안) | **2c** |
| ⑵ | 좁은 화면의 구조 안내 | **접힌 목차**(기본은 접힘 · 펼치면 절 목록) | **2c·2d** |
| ⑶ | 2단계의 배포 단위 | **조각마다 배포** | 전 조각 |
| ⑷ | 서체 초기값 | **IBM Plex** | 3단계 |

⚠**⑴ 의 대가를 알고 고른 것이다** — 내비가 **한 탭 뒤로** 가고 **지금 어느 화면인지 안 보인다**
(C 담당이 직접 지적한 약점). **⑵ 의 접힌 목차가 그 구멍을 메우는 자리**이므로 **둘을 같이 설계하라.**

⚠**⑵ 는 B 담당의 자기 지적을 그대로 푼 것이다** — 「목차가 모바일 첫 화면 290px 를 매번 먹는데
매일 오는 사람에겐 두 번 다시 필요 없다」. **접어 두면 처음 온 사람만 펼친다.**

⚠**⑶ 때문에 각 조각은 「검토 → 배포」를 따로 돈다.** 사이트가 며칠에 걸쳐 조금씩 변한다 —
**그게 의도다**(문제가 나면 어느 조각 탓인지 바로 안다).

## ⚠미확정 (조각 착수 시 정하고 여기 적는다)

1. **열→그룹 매핑표**(2d).
2. **모션 상한값**(2e) — 실측으로.
3. ~~좁은 화면에서 세로 레일의 대체~~ → **정해졌다: 접힌 목차**(§0-A ⑵).
4. **폼 컨트롤 테두리 대비** — `design-auditor` 판정 대기(2b 의 입력).
