# 인계 — 디자인 전면 개선 (2026-09-07 밤 ~ 09-08)

**이어받는 사람이 먼저 알아야 할 것부터 적는다.**

## 0. 지금 어디까지 왔나 — **1~3단계가 전부 끝났다**

| 단계 | 상태 | PR |
|---|---|---|
| 감사(실측) · 방향 확정 · 목업 3안 · 콜드 리뷰 | 끝 | — |
| **1단계 토큰**(화면 무변경) | 배포됨 | #4 |
| **접근성·대비 수정**(감사 P0×2) + 서체 파이프라인 | 배포됨 | #5 |
| **2a 값 척도** | 배포됨 | #6 |
| **2b 괘선 위계** | 배포됨 | #7 |
| **2c 모바일 크롬** | 배포됨 | #8 |
| **2d 표 · 2e 모션 · 문서 간 전환** | 배포됨 | #9 |
| **3단계 서체 배선** | 머지됨 | #10 |

⚠**PR 번호는 전부 `muji-j/basenine` 의 것이다.** 이 워크트리의 `origin` 은 아직
**은퇴한 `muji-j/bb-app`**(RETIRED.md)을 가리키고, 개발 원격은 **`pub` = `basenine`** 이다.
⚠**`git push origin` 을 습관으로 치지 마라** — 실제로 은퇴 저장소에 브랜치를 하나 만들었고
**아직 지우지 못했다**(원격 삭제가 분류기에 막혔다 · 사용자 승인 필요).

정본 문서:
- **방향** `../superpowers/specs/2026-09-07-design-direction.md`
- **감사** `../superpowers/specs/2026-09-07-design-audit.md`
- **목업 3안** `../superpowers/specs/mockups-2026-09-07/` (**채택은 C**)
- **2단계 계획** `../superpowers/plans/2026-09-08-design-step2-values.md`
- **3단계 계획** `../superpowers/plans/2026-09-08-design-step3-fonts.md`

## 1. ⚠사용자가 정한 것 — 다시 고르지 마라

| 무엇 | 정해진 것 |
|---|---|
| 방향 | **C 기반 + A·B 차용** |
| 애니메이션 | **진입 연출까지**(막대 차오름) |
| 서체 | **IBM Plex Sans JP + Plex Sans** · **초기값도 IBM Plex** |
| 서체 선택 | **3종**(기본(標準) / Plex / Noto) |
| 모바일 내비 | **버튼으로 접는다** |
| 좁은 화면 구조 안내 | **접힌 목차** |
| 배포 단위 | **조각마다** |

⚠**「초기값도 IBM Plex」의 뜻이 좁아졌다 — 확인이 필요하다.**
지금은 **「스크립트가 있으면 Plex」**다. CSS 의 기본은 시스템이고 스크립트가 Plex 를 켠다.
그래야 **고른 대로만 받는다**(안 그러면 「기본」을 고른 사람이 Plex 를 받고 버린다 · 3단계 §6-B).
**스크립트가 없으면 시스템 글꼴로 남는다.**

## 2. ⚠이번 작업이 남긴 가장 값진 것 — **되풀이된 실패의 모양**

### 2-1. 캐스케이드 — **같은 실수를 여섯 번 했다**

| # | 어디 | 모양 |
|---|---|---|
| 1 | 2b `.tabs.seg` 복원 | 고른 탭의 테두리를 덮었다 |
| 2 | 2b `:not()` 특이도 | 호버를 눌렀다 |
| 3 | 2b seg 호버 | 다시 고른 탭을 덮었다 |
| 4 | 2c `.mfind .tab` | 굽기 전에 자수 |
| 5 | 2c 펼친 내비 padding | **손가락 보정을 눌렀다**(8px → 6px) |
| 6 | 2d 호버 box-shadow | **이긴 쪽이 나머지를 지웠다** |

⚠**여섯 번째만 모양이 다르다.** 앞의 다섯은 「누가 이기나」였는데 이번 것은
**「이긴 쪽이 나머지를 지운다」**다 — `box-shadow`·`transform` 처럼 **값이 통째로 갈리는 속성**에서는
특이도를 맞추는 것만으로 부족하다.

### 2-2. ⚠**「보이는가」를 셀 때는 `checkVisibility` 를 써라 — 하루에 세 번 밟았다**

`offsetParent` 로 세면 **`hidden=until-found` 패널 안의 요소가 「보인다」로 세어진다**
(`content-visibility:hidden` 이라 배치는 되지만 안 그려진다).
겹침 무리의 「과잉 적용」 오탐 · 블록 수 오계수 · 그 둘을 잡은 것도 같은 함정이었다.

### 2-3. ⚠**이 팔레트에서 「면」으로는 표식을 만들 수 없다 — 세 번 확인했다**

`--page : --panel` = **라이트 1.044 · 다크 1.084**, `--panel-2` 도 1.08~1.19.
2b 에서 두 번(패널의 「층」 · `.legend` 의 면), 2d 에서 한 번(**표 호버가 1.044**) 걸렸다.
**표식은 선으로 만든다** — 2b 가 세운 어휘: **칠해진 테두리 = 지금 고른 것이거나 손가락이 얹힌 것.**

### 2-4. ⚠**구단색을 「선」에 쓰지 마라 — 이 저장소가 네 번 같은 판정을 내렸다**

`.tbar i` · `.mf-shape`/`.mf-dot` · `.seasons a[aria-current]` · `.tnav`/`.brand`.
12구단 전수(`--panel` 대비): **라이트 4/12 · 다크 8/12 · 합집합 12/12** 가 3:1 미달.
**네 번 났다는 것은 기억으로는 안 막힌다는 뜻**이라 이제 **시험이 묶는다**
(「지금 여기 표식 셋이 한 잉크이고 구단색을 쓰지 않는다」).

### 2-5. ⚠**게이트가 설계를 고쳤다 — 두 번**

- **2c**: `<details>` 를 CSS 로 못 편다는 것을 **재고 나서** 체크박스로 갔다.
  JS 로 정하면 **defer 라 100ms 깜빡인다**(FCP 504ms · DCL 607ms · 실측).
- **3단계**: 「기본은 요청 0건」을 재 보니 **저장 noto 8개 · 저장 system 4개** 였다 —
  **기본이 아닌 것을 고른 사람이 손해**를 보고 있었다. 그래서 CSS 기본을 뒤집었다.

**둘 다 안 재 봤으면 그대로 배포했을 것이다.**

## 3. ⚠내가 틀렸던 것 (다음 사람이 같은 길을 안 가게)

- **「감사가 말한 선 3겹」은 실측으로 0건**이다. 실재한 것은 **2겹 무리 62개**였다.
- **「C 의 `この先` 그룹에 넣을 열이 없다」는 틀렸다** — **순위 화면의 표**를 보고 내린 결론인데
  **C 의 목업은 홈 화면**이었다. 홈 표에는 그 열이 **둘**(`残り`·`全勝〜全敗の勝率`) 있다.
  ⚠**목업의 판정을 다른 표에 옮기기 전에 「그 목업이 무엇을 그린 것인지」를 확인하라.**
- **「선수 페이지 블록 14개」는 12~13개**이고 **동시에 보이는 것은 5~6개**다.
- **`npm run build:web` 은 데이터가 낡으면 폰트 단계를 조용히 건너뛴다**
  (`build.ts` 가 exitCode 1 → `&&` 가 끊긴다). ⚠**파이프를 물리면 그 종료코드가 가려진다.**
- **CI 무료 분 제약이 낡았다** — `basenine` 은 **PUBLIC** 이라 표준 러너 분이 무제한이다.
  ⚠**결론은 남고 사유가 바뀐다**: 매 실행이 **npb.jp 를 치므로**(L1·M8) 여전히 모아서 배포한다.

## 4. ⚠공정에서 배운 것

- **시험을 빌드보다 먼저 돌려라.** `assets.ts` 는 통째로 템플릿 리터럴이라 **주석의 역따옴표가
  파일을 깬다** — 하루에 세 번 깼다. 시험은 그것을 **1초 만에** 잡는데 빌드는 2분을 쓴다.
- **배포물에 나가는 글자는 ASCII 로 적어라.** CSS 표식 주석을 한국어로 썼다가
  **커버리지 게이트가 한글을 잡아 멈췄다**(`assets.ts` 의 주석은 `stripJsComments` 가 걷지만
  `build-fonts.ts` 가 붙이는 것은 그 경로를 안 지난다).
- **`gh` 활성 계정이 세션 도중 `jwlee-muji` 로 되돌아간다** — 이 작업 중 **네 번**.
  「권한 없음」이 뜨면 분류기가 아니라 **계정을 먼저 의심하라.**

## 5. ⚠사람이 봐야 하는 것 (계측기로 못 하는 것)

1. **문서 간 전환** — 실제로 도는지, **순위 화면(42,703노드)에서 걸리는 느낌**이 있는지.
   ⚠**발동을 증명하지 못했다**(계측기를 세 번 고쳐도 못 잡았다). 걸리면 CSS 두 줄만 지우면 원상복구다.
2. **서체** — `Plex` / `Noto` / `標準` 을 눌러 비교. 실회선에서 297 KiB 가 어떤지.
3. **모바일** — 내비·목차 접기가 손에 맞는지.
4. **낭독기** — 접기를 체크박스로 만들어서 「체크박스」로 읽힌다. `aria-controls` 만 줬고
   `aria-expanded` 는 **일부러 안 줬다**(스크립트 없이는 갱신이 안 돼 **틀린 값이 고정된다**).
5. **Safari · Firefox · Mac** — 전부 미확인이다. 특히 **문서 간 전환은 그 둘에서 무변화**여야 한다.

## 5-A. 2f — 사용자가 「거의 그대로다」라고 한 뒤에 한 것 (2026-09-08)

### 무엇을 했나

| 항목 | 무엇 | 근거 |
|---|---|---|
| **모서리 척도** | `--r-pill/-panel/-field/-data/-thumb` 를 만들고 자리에 앉혔다 | 목업 C 에 `border-radius` 가 **34곳**인데 배포물엔 **선언 2건**(둘 다 스크롤바 손잡이) — **2b 가 넘긴 「모서리 반지름」을 내가 조용히 빠뜨렸고 빠뜨렸다고 말하지도 않았다** |
| **시즌 띠 접기** | 9시즌이면 ≤680px 에서 한 줄만 보이고 `すべての年` 로 편다 | 사용자 요청. 실측: 390px 2줄(56.6) · 320px 3줄(84.4) · 한 줄 28.8 |
| **목차 전면 개편** | `目次` 라벨을 눈에 보이게 · 기본 펼침 · 접기 가능 · **칩 테두리 복원** | 사용자 지적(「가시성이 나쁘다」) |
| **판정 영역** | `.hjlab`·`.snbtn` 22.3 → **30.3px**(손가락 34.3) | WCAG 2.2 SC 2.5.8 = 24×24 · **포인터 면제 없음** |
| **시즌 손잡이를 띠 안으로** | `.snwrap` 으로 띠와 손잡이를 한 상자에 | 사용자 지적(「모든 연도 버튼이 잘 안 어울린다」) |

### ⚠이번에 배운 것 — **재기 전에 쓴 문장이 틀렸다**

`@media (pointer:coarse)` 주석에 **「.hjlab·.snbtn 은 기본값이 이미 손가락에 맞는다」**고 적었다.
**재 보니 둘 다 22.3px 이었다**(마우스·손가락, 390·1280px 네 지점 전부). 근거 없이 쓴 문장이었고,
그 문장이 그대로 남았으면 **다음 사람이 「이미 쟀다」고 읽었을 것**이다.
→ 그 자리에 **틀렸다는 사실과 실측값**을 남겼다.

### ⚠고정(sticky)을 걷어낸 이유 — 상수를 박을 수 없어서다

2f 전 `.hjump` 는 **한 줄을 가로로 굴리는 띠**라 높이가 **52px 로 고정**이었고,
그래서 `scroll-padding-top` 사다리에 52 를 박을 수 있었다. 2f 가 **줄바꿈 + 접기**로 바꾸자
높이가 **43.3 ~ 122.5px** 로 움직였다(684~1456px step 28 · 전 시즌 홈 9장 · 최대는 684px).

- ⚠**폭으로 구간을 나누는 우회도 못 쓴다** — **서체가 3종**이라 줄바꿈 지점이 사용자 선택에 따라 움직인다.
- → **고정을 버렸다.** 사다리는 기본 규칙(`--topbar + --s4`)으로 돌아갔고 **척도 이탈이 2 → 1 로 줄었다.**
- 실측 확인: 앵커 **35회**(항목 7 × 폭 5)에서 가장 나쁜 여유 **+7.5px** — 가려지는 것 0.

### 시험이 세 번 붙잡았다 (전부 진짜였다)

1. `sticky-anchor` — `.hjbar` 를 sticky 로 만들고 사다리에 안 넣었다 → 위 결론으로 이어졌다.
2. `css-tables` — 반지름 규칙이 `.scroller{` 리터럴을 **앞에서 가로채** 시험이 엉뚱한 규칙을 읽었다.
   → **첫 일치**가 아니라 **전건**을 모으게 고쳤다(뮤테이션 검사로 확인: `overflow-x` 를 빼면 붉어진다).
3. `design-tokens` — `border-bottom:0` 이 척도 밖으로 잡혔다 → `none` 으로 바꿨다(굵기가 아니라 「안 그린다」).

### 실측 요약

- 시험 **2,667본 중 2,666 통과 · 실패 0 · 건너뜀 1** · 타입체크 통과
- 빌드 **9,549파일 / 1491.8MB** · 링크 **9,392장 검사 · 깨진 것 0**
- 판정 영역(≥24px): `.hjlab` 30.3/34.3 · `.snbtn` 30.3/34.3 · `.hjump a` 32.6/36.6 · `.navbtn` 28.6/32.6
- `--topbar` 96/88/46 — **바꾸기 전후로 토큰과 일치**(둘 다 `.topbar` 밖이라 안 건드린다)
- 대비: `--tx-3` 테두리가 `--page` 위에서 **4.910(라이트) / 5.499(다크)** — 1.4.11 의 3:1 통과

### ⚠검토 두 건이 다시 잡은 것 (2026-09-08 · 확정 9건)

⚠**여기서 배운 것 하나: 「사용자 요청대로 접었다」가 곧 「접어도 된다」는 아니다.**

| # | 무엇 | 왜 진짜였나 |
|---|---|---|
| **F1** | 접힌 시즌 띠에서 **「지금 여기」가 사라진다** | 칩이 최신 내림차순이라 2018년 화면에서 현재는 9개 중 **9번째** · 2005 드래프트는 **22칩 중 17개가 가려진다**. 가로 스크롤용 보정은 줄바꿈 상태에서 안 돌고 **그걸 지키는 시험도 가로 축만 봐서 초록이었다** |
| **F2** | 잘린 링크가 **초점을 그대로 먹고** 띠가 스스로 굴러 굳는다 | `overflow:hidden` 은 「프로그램으로 스크롤 가능한 상자」다. **같은 커밋의 목차는 `display:none` 이라 이 문제가 없었다 — 한 커밋 안에서 두 접기가 다른 규율을 썼다** |
| **F3** | 알약 반지름이 **`aria-current` 밑줄을 호로 깎았다** | 직선 구간 = `w − h` 라 `.tnav a` 가 40 → **13.4px**(모바일 ≈5px). `.brand` 는 반지름이 없어 **2b 가 통일한 셋이 갈라졌다** |
| **F4** | 「배포물엔 4곳」이 실측이 아니었다 | grep 4줄 중 **둘이 주석**이다 — 선언은 **2건**이고, 지운 문장이 이미 정확히 그렇게 적고 있었다 |
| **F5** | `.snbtn` 대비 근거가 **다른 바탕**의 수 | 바탕은 `--page` 가 아니라 `--panel-2` → **4.541 / 4.611**. 통과하므로 결론은 그대로, **근거만 틀렸다** |
| **F6** | `border-bottom:none` 의 사유가 거짓 | `--rw-none` 이 그 자리를 위해 있고 이 파일에서 15회 쓰인다 |
| **F7** | **2f 가 만든 계약 넷을 붙드는 시험이 0본** | 시험 파일 셋을 만졌지만 전부 「실측에 맞춘 것」이고 **새로 지킨 것은 0건**이었다 |
| **F8/F9** | 면제 목록과 낡은 서술 | `.hjump a` 는 테두리가 돌아와 면제가 필요 없고, `.hjump` 를 sticky 로 적은 자리가 넷 남아 있었다 |

**고친 뒤**: 접기를 **높이가 아니라 개수**로 자르고 `[aria-current]` 를 예외로 뒀다.
`.tnav a`·`.seasons a` 는 알약 목록에서 뺐다(**표식이 장식보다 무겁다**).
새 시험 **9본**을 붙였고 **뮤테이션 9건 중 9건**을 잡는 것을 확인했다.

⚠**반증돼 안 고친 것 7건**도 기록해 둔다 — 481~680px 클램프 · `.tabs.seg` 단일 탭(dist 9,392장 전수:
2탭 11,746 · 3탭 18 · 4탭 1 · 5탭 2 · **1탭 0건**) · `display:contents` 기하 · `.qhits`/`#tip` 반지름 ·
`.seasons a` 23.8px(Spacing 예외로 **위반이 아닐 가능성이 높다**) · sticky-anchor 캐너리 · 빌드 수치.

⚠**2차 검토가 sticky 판단에 남긴 한 줄**: 「정직한 상수가 없다」는 *정확한* 상수를 뜻할 때만 참이다 —
**최댓값(122.5px)을 박아도 가려지지는 않는다.** 버린 것에는 동의하되 사유는
**「안전한 상수는 있지만 그 값이 화면을 먹는다」**(684px 에서 노트북 세로의 약 27%)가 정확하다.

### ⚠아직 안 고친 것

- **`.seasons a` 가 23.8px** 로 SC 2.5.8 의 24px 에 **0.2px 모자란다.** 2f 가 만든 것이 아니고
  손대면 띠 높이가 전부 움직여서 이번 범위 밖으로 뒀다. **「0건」이 아니라 「안 고쳤다」로 읽어라.**

## 6. 남은 일

- **`preload` 미적용** — HTML 이 해시보다 먼저 쓰여서 9,549장을 다시 써야 한다. **비용을 안 쟀다.**
- **순위 화면의 12열 표는 안 묶었다** — 앞을 말하는 열이 없어 `この先` 이 빈다(제안은 계획서에).
- **`.tnav a[aria-current="true"]`**(`--hair-2` · 1.580/1.557)와 **`.hjump a[aria-current]`**(구단색) —
  같은 결함이지만 이번 작업이 만든 것이 아니라 손대지 않았다.
- **선수 화면 탭줄 1개**가 아직 옆으로 굴러간다(주 조작이라 접으면 더 나쁘다 · 그늘로 말하게 했다).
- **681px 에서 목차가 59px 넘친다** — 접기 분기를 680px 로 뒀기 때문. 폭 관례를 바꿀지는 따로 정할 일.
- **은퇴 저장소(`bb-app`)의 `design-2a` 브랜치 삭제** — 사용자 승인이 필요하다.
- ⚠**`$CLAUDE_JOB_DIR/tmp` 가 계속 자란다**(8월 16일부터 누적 · 한때 6.5GB).
  **자기가 만든 것만 이름을 적어 지워라** — 다른 세션 것이 섞여 있다.
