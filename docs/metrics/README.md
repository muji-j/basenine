# 지표 정의서

**이 문서가 정본이다.** 구현은 `packages/metrics`에 **1벌만** 존재한다(CLAUDE.md M1).
여기에 없는 산식을 즉흥으로 만들지 마라 — `sabermetrics-analyst`로 올려라.

최종 갱신 2026-08-14 · 대응 구현 `packages/metrics` · 대응 계획 `docs/superpowers/plans/2026-08-14-increment-c-metrics.md`

---

## 0. 이 문서를 읽는 법

### 확신도
| 표기 | 뜻 |
|---|---|
| **공식** | 산출 주체의 공개 정의를 인용했다. URL을 병기한다 |
| **관례** | 널리 쓰이는 정의지만 단일 권위가 없다. 채택한 변형을 명시한다 |
| **우리 정의** | 우리가 정했다. 근거를 명시한다 |

### 검증 등급
| 등급 | 뜻 |
|---|---|
| **T1** | npb.jp가 **공표하는 값**과 직접 대조할 수 있다 |
| **T2** | 무료·약관 클린한 NPB 공표값이 없다. **성질 테스트**로 검증한다 |

⚠**T1과 T2를 섞어 말하지 마라.** 「검증됨」의 의미가 다르다.

### 공통 규약
- 모든 비율은 **값 + 분모**를 함께 반환한다(M2). 분모 없는 비율을 렌더링하지 않는다.
- **분모가 0이면 `null`.** 0으로 대체하지 않는다(M11) — 「10타수 무안타」와 「0타수」는 다르다.
- 이닝은 **아웃 카운트(정수)**로 계산한다. `6.2`는 표시용 문자열이며 계산에 쓰지 않는다.
- 리그·시즌·경기구분(정규/CS/일본시리즈/오픈전)을 섞어 집계하지 않는다.

---

## 1. 타자 기본 지표

| 지표 | 산식 | 분모 | 확신도 | 검증 |
|---|---|---|---|---|
| **타율 AVG** | `安打 ÷ 打数` | 타수 | 공식 | **T1** |
| **출루율 OBP** | `(安打+四球+死球) ÷ (打数+四球+死球+犠飛)` | 좌동 | 공식 | **T1** |
| **장타율 SLG** | `塁打 ÷ 打数`, 塁打 = 単打+2×二塁打+3×三塁打+4×本塁打 | 타수 | 공식 | **T1** |
| **OPS** | `OBP + SLG` | **타석**(분모가 둘이라 하나로 줄일 수 없다) | 관례 | T1 파생 |
| **ISO** | `SLG − AVG` | 타수 | 관례 | T1 파생 |
| **BABIP** | `(安打−本塁打) ÷ (打数−三振−本塁打+犠飛)` | 좌동 | 관례 | **T2** |
| **K%** | `三振 ÷ 打席` | 타석 | 관례 | **T2** |
| **BB%** | `四球 ÷ 打席` — **고의사구를 포함한다** | 타석 | 관례 | **T2** |
| **ROE** | 박스스코어 결과 셀의 `失` 계열을 센다 | — | 우리 정의(집계 방법) | **T2** |

⚠**BB%는 고의사구를 포함하고 wOBA는 제외한다.** 관례가 다른 것이지 어느 한쪽이 틀린 게 아니다. 그래서 입력 타입이 `bb`와 `ibb`를 **둘 다** 보관한다 — 하나로 합치면 어느 한쪽이 반드시 틀린다.

**표시**: 타율·출루율·장타율은 소수 3자리, 선행 0을 쓰지 않는다(`.301`). OPS·ISO는 3자리. K%·BB%는 1자리 백분율.

---

## 2. 투수 기본 지표

| 지표 | 산식 | 분모 | 확신도 | 검증 |
|---|---|---|---|---|
| **방어율 ERA** | `自責点 × 27 ÷ アウト数` | 아웃 수 | 공식 | **T1** |
| **WHIP** | `(被安打+四球) × 3 ÷ アウト数` — **사구를 포함하지 않는다** | 아웃 수 | 관례 | **T1** |
| **K/9** | `奪三振 × 27 ÷ アウト数` | 아웃 수 | 관례 | **T2** |
| **BB/9** | `四球 × 27 ÷ アウト数` | 아웃 수 | 관례 | **T2** |
| **HR/9** | `被本塁打 × 27 ÷ アウト数` | 아웃 수 | 관례 | **T2** |

⚠**9이닝 = 27아웃**이므로 `× 27 ÷ outs`다. `÷ (outs/3) × 9` 로 쓰면 같은 값이지만 부동소수 오차가 두 번 생긴다.

**경계값**: 아웃 0이면 전부 `null`. **ERA 0.00과 「미등판」을 같은 화면에 그리지 마라** — 전자는 훌륭한 성적이고 후자는 성적이 없는 것이다.

**표시**: ERA·WHIP은 소수 2자리. K/9 계열은 2자리.

---

## 3. 세이버메트릭스

### 3.1 wOBA

```
wOBA = {0.692×(四球−故意四球) + 0.73×死球 + 0.966×失策出塁 + 0.865×単打
        + 1.334×二塁打 + 1.725×三塁打 + 2.065×本塁打}
       ÷ (打数 + 四球 − 故意四球 + 死球 + 犠飛)
```

- **확신도: 공식** — 1.02 Essence of Baseball Glossary
  https://1point02.jp/op/gnav/glossary/gls_explanation.aspx?eid=20040
- **검증: T2** (성질 테스트 — 장타 가중이 단조 증가, 표본 배수에 불변)

✅ **`失策出塁`(ROE) 항을 포함한다** (2026-08-15 정정). 소스 조사 단계에서는 「선수별 공개 소스 없음」으로 판정했으나(보고서 §3-5), **박스스코어의 타석 결과 셀(`三ゴ失`·`遊ゴ失` 등)에 인코딩되어 있음을 실측으로 확인**했다(340경기 214건). 따라서 **공식 산식을 생략 없이 계산한다.**

✅ **고의사구(`敬遠四`)도 구별 가능**하므로 `四球−故意四球`를 정확히 계산한다.

⚠**계수는 시즌마다 다르다**(1.02가 명시). 공개된 것은 위 1세트뿐이다. 그래서 **wOBA 절대치를 전면에 내세우지 않고 리그평균 대비 상대치(wRAA·wRC+)로 제시한다.**

### 3.2 wRAA

```
wRAA = (wOBA − リーグ平均wOBA) ÷ 1.24 × 打席
```

- `1.24` = wOBAscale. **확신도: 공식** — https://1point02.jp/op/gnav/glossary/gls_explanation.aspx?ecd=204&eid=20041
- `リーグ平均wOBA`는 **우리 아카이브에서 산출한다**(§4).
- **검증: T2** — 리그 평균과 같은 선수의 wRAA는 정확히 0이어야 한다.
- **분모: 타석.** 단위는 「평균 대비 득점」이며 누적값이므로 타석에 비례한다.

### 3.3 wRC+

```
wRC+ = ((wRAA ÷ 打席 + リーグ得点/打席) ÷ リーグ得点/打席) × 100
```

- **확신도: 우리 정의**(파크팩터 항을 뺀 축약형)
- **검증: T2** — 리그 전체를 넣으면 정확히 100.
- **분모: 타석.**

⚠**파크팩터를 적용하지 않는다.** 신뢰할 만한 PF를 만들려면 3~5시즌 합산이 필요한데 원시 로그가 2025년분부터만 존재한다. **적용하지 않았다는 사실을 지표 라벨에 쓴다** — 「적용 안 함」과 「적용했는데 1.0」은 다르다.

### 3.4 FIP

```
FIP = (13×被本塁打 + 3×(四球+死球) − 2×奪三振) × 3 ÷ アウト数 + cFIP
```

- **확신도: 관례**(계수 13/3/2는 널리 쓰이는 표준형)
- `cFIP`는 **우리가 산출한다**(§4).
- **검증: T2** — 리그 전체를 넣으면 리그 ERA와 정확히 일치해야 한다. 이것이 cFIP의 정의다.
- **분모: 아웃 수.**

---

## 3.5 SRC — 状況得点貢献 (Situational Run Contribution) · **v1 간판 지표**

```
SRC = Σ ( RE(타석 후 상태) − RE(타석 전 상태) + 그 타석에서 난 득점 )
```

- **확신도: 우리 정의**(RE24 계열이지만 우리가 만든 행렬을 쓰고 범위를 우리가 정했다)
- **검증: T2** — 리그 전체의 SRC 합이 0에 가까워야 한다(평균 대비 지표이므로). **실측 2026: 센트럴 +4.2 / 퍼시픽 +11.5**(타석 각 2.3만)
- **분모: 타석.** `600타석 환산`을 함께 제공한다
- 단위는 **평균 대비 득점**. 승리(wins)로 환산하지 않는다

### 무엇을 재고 무엇을 안 재는가 — **각주가 아니라 정의의 일부다**

| 잰다 | 안 잰다 |
|---|---|
| 타격이 만든 득점기대치 변화 | **수비** — 타구 좌표·존·타구속도가 공개되지 않아 입력 자체가 없다 |
| 상황(주자·아웃)의 가중 | **주루** — 도루·진루 기여는 별도 지표로 |
| | **포지션 보정·대체수준** — 그래서 WAR이 아니다 |

⚠**WAR이 아니며 WAR과 비교할 수 없다.** 이름을 다르게 쓰는 이유가 그것이다.
⚠**RE 행렬에 없는 상태는 0으로 때우지 않는다.** 계산하지 않고 「미계산 타석」으로 센다 —
때우면 그 타석이 조용히 큰 마이너스가 된다. 실측 2026 시즌 미계산 **0타석**.

### 득점기대치(RE) 행렬

**우리 아카이브에서 직접 산출한다.** 리그·시즌마다 따로 만든다.

| | 무사 | 1사 | 2사 |
|---|---|---|---|
| 주자없음 (센트럴) | 0.381 | 0.195 | 0.077 |
| 주자없음 (퍼시픽) | 0.424 | 0.217 | 0.086 |
| 만루 (센트럴) | 2.521 | 1.327 | 0.593 |

24/24 상태가 양 리그에서 관측됐다. **표본 수를 값과 함께 낸다** — 무사만루는 96개뿐이다.

타석별 득점은 보존식(`주자_전 + 1 = 주자_후 + 아웃증가 + 득점`)으로 유도하고
**라인스코어와 대조**한다. 실측: 632경기 4,557점이 라인스코어 총점과 정확히 일치.
⚠이닝 마지막 타석만은 보존식이 성립하지 않는다(잔루가 득점으로 잡힌다) — 라인스코어로 닫는다.

---

## 4. 리그 상수 — 베끼지 않고 산출한다

| 상수 | 산출 방법 |
|---|---|
| `averageWoba` | 리그 전체 타격 집계에 wOBA 산식을 그대로 적용 |
| `cFip` | `리그ERA − 리그FIP미가공값`. 정의상 리그 FIP = 리그 ERA가 된다 |
| `runsPerPa` | `리그 득점 ÷ 리그 타석` |

**왜 베끼지 않는가**
1. 계수·평균은 **시즌마다 다르다.** 남의 값을 고정으로 쓰면 조용히 틀린다.
2. 우리 아카이브로 산출하면 **「원본의 재배열이 아니라 우리 산출물」**이 되어 데이터 권리 위치가 좋아진다(설계 스펙 §7 L2).

⚠**표본이 비면 상수를 만들지 않고 예외를 던진다.** 0으로 나눈 상수를 조용히 돌려주면 그 시즌의 **전 지표가 조용히 틀린다.**
⚠**센트럴과 퍼시픽을 섞지 마라.** DH 유무만으로도 득점 환경이 다르다.

---

## 5. 자격 기준과 순위

| 항목 | 규칙 | 확신도 |
|---|---|---|
| **규정타석** | `올림(팀 시합수 × 3.1)` | 공식(NPB 규칙) |
| **규정투구회** | `팀 시합수 × 1이닝` (= 아웃 수 × 3) | 공식(NPB 규칙) |
| **동률 처리** | 같은 값은 **같은 순위**를 받고 **다음 순위를 건너뛴다**(공동 2위가 둘이면 다음은 4위) | 관례 |
| **자격 미달** | 순위는 `null`. **목록에서 빼지 않는다** | 우리 정의 |

⚠**자격 기준은 「표시 규칙」이 아니라 값의 일부다**(M3). 기준을 바꾸면 순위가 바뀐다. 그래서 코드에만 두지 않고 테스트로 고정한다.

⚠**자격 미달자를 목록에서 빼지 마라.** 성적은 있는데 순위만 없는 것이지 존재하지 않는 것이 아니다. 빼면 「데이터 없음」과 구별할 수 없어진다(M11).

---

## 5-A. 스플릿의 자격 기준 (⚠미확정 · 설계 판단 필요)

스플릿에는 **시즌 규정타석과 별개로 스플릿 자체의 최소 표본**이 필요하다.

실측으로 확인된 문제(2026-08-15): 시즌 규정타석(177)만 걸고 「좌투 상대 타율」 순위를 내면
**48타석 .364인 선수가 3위**로 올라온다. 분모를 병기해도 **순위표라는 형식 자체가
「이 사람은 좌투에 강하다」로 읽히므로** 표본이 얇으면 순위에서 빼야 한다.

| 스플릿 | 잠정 최소 표본 | 근거 |
|---|---|---|
| 좌우 상대 | 100타석 | 임의. 이 값으로 순위가 「48타석 .364」에서 「143타석 .341」로 바뀐다 |
| 홈/원정 | 미정 | |
| 주자상황 | 미정 | 득점권은 특히 얇다 |
| 월별 | 미정 | 1개월이 본질적으로 얇다 — 순위를 매기지 않는 편이 나을 수 있다 |
| 투수×타자 | 미정 | **대부분 10타석 미만이다.** 순위를 매기는 것 자체가 부적절할 수 있다 |

⚠**이 값들은 정답이 아니라 선택이다.** 증분 G(배포)에서 화면 설계와 함께 확정하고,
확정하면 **테스트로 고정**한다(M3 — 자격 기준은 표시 규칙이 아니라 값의 일부다).

⚠**데이터 계층은 이미 옳다.** 모든 스플릿이 타석 수를 함께 나르므로 임계값은 표시 계층의 선택이다.
데이터에서 임계 미만을 지우지 마라 — 「성적이 있는데 순위만 없는 것」과 「데이터가 없는 것」은 다르다.

---

## 6. 산출하지 않는 지표와 그 이유

| 지표 | 왜 안 하는가 |
|---|---|
| **WAR** | 수비 런밸류에 필요한 타구 좌표·존·타구속도가 공개 기록에 **존재하지 않는다.** 사람이 계산해도 나오지 않는다. **타 사이트 값을 빌려오지도 않는다** — 갱신 주기와 정의를 추적할 수 없다 |
| **UZR·TZR·DRS 등 수비 지표** | 동상 |
| **xFIP** | 뜬공 비율(FB%)이 필요한데 npb.jp 박스스코어에 없고, 소스 조사에서도 타구질 데이터가 확인되지 않았다 |
| **구종별 성적** | 구종·구속 데이터의 무료·약관 클린 소스가 0건이다(Yahoo가 2026-02에 앱 전용으로 이관) |

⚠이 목록은 **「나중에 하겠다」가 아니라 「입력이 없어서 불가능하다」**이다. 소스 상황이 바뀌면 이 표를 먼저 고친다.

---

## 5-B. 투수의 역할 — 선발과 구원 (2026-08-15 확정)

구현 `packages/aggregate/src/season.ts` · `packages/metrics/src/ranking.ts` · 확정 2026-08-15.

### 선발 등판을 어떻게 아는가

⚠**`pitching_line`에는 등판 순서가 없다.** 주키가 `(game_id, player_id)`라 순서를 담을 자리가 없다.
그래서 **타석 로그에서 되찾는다** — 각 이닝 절반의 **첫 타석을 던진 투수**가 그 팀의 선발이다.

실측(2026-08-15):

| 확인 | 결과 |
|---|---|
| 실시경기에서 양 팀 선발이 나오는가 | **632경기 전부 / 1264명 · 누락 0** |
| 그 투수가 `pitching_line`에 있는가 | **누락 0건** |
| 타석 로그가 없는 경기 | 31경기 — **전부 우천 중지**(투수 기록 자체가 없다). 빈틈이 아니다 |
| 결정 표기 합계 | 승 620 · 패 620 · 세이브 333 · 홀드 974 (정규시즌만 세면 618/618/331/974 — 차이는 올스타 2경기) |

### 역할 판정 규칙

**아웃 카운트가 많은 쪽**이 그 투수의 역할이다. 동수면 선발(선발 등판이 있었다는 사실을 우선).

⚠**등판 수가 아니라 아웃으로 나눈다.** 33등판 중 1선발인 투수가 실재하는데,
등판 수로 재면 흔들림이 크고 「선발 1회 6이닝 + 구원 32회 30이닝」 같은 경우에 답이 뒤집힌다.

2026 시즌 결과: 투수 356명 = **선발형 131 / 구원형 225**.
그중 **양쪽 등판이 다 있는 투수가 45명**이며, 이들에게는 선수 페이지에 「先発・救援別」 블록이 나온다.

### 자격 기준

| 부문 | 기준 | 출처 |
|---|---|---|
| 선발 | 규정투구회 = **팀 경기수 × 1이닝** | **NPB 공식** |
| 구원 | 규정투구회의 **3분의 1** = 팀 경기수 × 1/3이닝 | ⚠**우리 기준. NPB 기준이 아니다** |

⚠**구원에 공식 기준이 없는 것은 우연이 아니다.** NPB의 구원 부문 타이틀(最多セーブ·最優秀中継ぎ)은
**개수**라서 자격선 자체가 필요 없다. 방어율·WHIP 같은 **비율**을 구원끼리 줄 세우려면
우리가 선을 그을 수밖에 없고, 그러면 **그것이 우리 기준임을 화면이 말해야 한다.**
화면 문구: 「救援投手には公式の規定投球回がないため、当サイトは規定投球回の3分の1（35.7回）を基準にしています。これはNPBの基準ではありません。」

규정투구회를 그대로 걸면 구원 투수는 **한 명도 자격을 얻지 못한다.**
1/3 기준으로 구원형 23명이 자격을 얻어 선발 부문 16명과 두께가 맞는다(2026-08-15 · 팀 최다 109경기).

### 순위 부문

```
打者   wRC+ SRC OPS 打率 出塁率 長打率 wOBA wRAA 本塁打 打点 盗塁
先発   勝利 防御率 FIP WHIP K/9 BB/9 奪三振 投球回 敗戦 先発
救援   セーブ ホールド HP 防御率 FIP WHIP K/9 BB/9 奪三振 投球回 勝利 登板
```

- **HP(홀드포인트) = 홀드 + 구원승.** NPB 最優秀中継ぎ의 정의다.
- ⚠**개수 지표에도 역할 필터를 건다.** 세이브 순위에 선발을 섞으면 전원 0이 되고,
  승리 순위에 마무리를 섞으면 「7승 선발」과 「7승 구원」이 같은 줄에 선다 — 두 7승은 같은 뜻이 아니다.
- ⚠**순위의 값은 전 등판을 합친 성적**이다(선발분만 떼지 않는다). NPB 공식 타이틀이 그렇게 매기므로,
  다르게 하면 같은 이름의 값이 공표값과 어긋난다. 역할은 **어느 줄에 서는가**와
  **어떤 잣대로 색을 칠하는가**만 정한다.

---

## 6-A. 수준 등급 (5단계) — 화면의 색이 무엇을 주장하는가

구현 `packages/web/src/grade.ts` · 테스트 `packages/web/test/glossary.test.ts` · 확정 2026-08-15.

**색은 「이 리그에서 어느 정도인가」를 말한다. 절대적 좋고 나쁨이 아니다.** 그래서 임계값을 문서에 남긴다 —
규칙이 코드에만 있으면 아무도 검증할 수 없다(M3와 같은 이유).

### 기준 모집단 — ⚠**규정 도달자를 쓰면 안 된다**

| 그룹 | 인원 | 왜 이 선을 골랐나 |
|---|---|---|
| 타자 | **100타석 이상 157명** | 규정 도달자는 40명뿐이고 **전부 상위권**이다. 그 40명의 5분위로 자르면 평범한 선수가 전부 「매우 나쁨」이 된다 |
| 선발 | **30이닝 이상 85명** | 동상(규정 도달 16명) |
| 구원 | **20이닝 이상 90명** | 30이닝으로 자르면 53명까지 줄어 선발 쪽(85명)과 두께가 맞지 않는다 |

각 지표의 **20/40/60/80 백분위**를 임계값으로 삼고 읽기 쉬운 수로 반올림했다.
검산: wRC+의 5분위가 **74.9 / 95.2 / 109.9 / 133.4** — 리그 평균 100이 한가운데 오는 것이
이 모집단 선택이 맞았다는 근거다.

### ⚠선발과 구원을 하나의 잣대로 재지 않는다 (2026-08-15 확정)

2026 시즌 실측 방어율 5분위:

```
선발  2.58 / 2.86 / 3.50 / 4.63
구원  1.83 / 2.30 / 2.73 / 3.89
```

**방어율 3.20은 선발에게 중위권이고 구원에게는 하위권이다.** 실제 화면에서도 그렇게 갈린다 —
같은 3.20이 선발 페이지에서는 「ふつう」, 구원 페이지에서는 「悪い」로 칠해진다.
하나의 임계값을 쓰면 값은 맞는데 화면이 틀린 말을 한다.

이유는 편차가 아니라 **역할의 성질**이다 — 구원은 짧은 이닝을 전력으로 던지고,
주자를 남기고 내려가면 그 실점이 앞 투수의 자책점이 된다.

### 타자 임계값 (100타석 이상 157명 · 2026 시즌)

| 지표 | 20% | 40% | 60% | 80% | 방향 | 최소 표본 |
|---|---|---|---|---|---|---|
| 打率 | .225 | .240 | .255 | .275 | 높을수록 좋음 | 50 |
| 出塁率 | .280 | .300 | .320 | .345 | ↑ | 50 |
| 長打率 | .300 | .345 | .390 | .430 | ↑ | 50 |
| OPS | .590 | .650 | .700 | .760 | ↑ | 50 |
| wOBA | .275 | .300 | .315 | .340 | ↑ | 50 |
| wRC+ | 75 | 95 | 110 | 133 | ↑ | 50 |
| ISO | .065 | .100 | .135 | .175 | ↑ | 50 |
| BB% | .045 | .065 | .080 | .100 | ↑ | 50 |
| K% | .150 | .180 | .215 | .258 | **낮을수록 좋음** | 50 |

### 선발 임계값 (30이닝 이상 85명 · 최소 표본 90아웃=30회)

| 지표 | 20% | 40% | 60% | 80% | 방향 |
|---|---|---|---|---|---|
| 防御率 | 2.60 | 2.90 | 3.50 | 4.60 | **↓** |
| WHIP | 1.05 | 1.18 | 1.27 | 1.44 | **↓** |
| FIP | 2.90 | 3.18 | 3.45 | 4.10 | **↓** |
| K/9 | 6.40 | 7.35 | 8.40 | 9.30 | ↑ |
| BB/9 | 1.80 | 2.25 | 2.80 | 3.40 | **↓** |
| HR/9 | 0.65 | 0.80 | 0.93 | 1.15 | **↓** |
| 球数/アウト | 5.20 | 5.40 | 5.60 | 5.85 | **↓** |

### 구원 임계값 (20이닝 이상 90명 · 최소 표본 60아웃=20회)

| 지표 | 20% | 40% | 60% | 80% | 방향 |
|---|---|---|---|---|---|
| 防御率 | 1.85 | 2.30 | 2.75 | 3.90 | **↓** |
| WHIP | 0.97 | 1.09 | 1.20 | 1.36 | **↓** |
| FIP | 2.30 | 2.88 | 3.38 | 4.05 | **↓** |
| K/9 | 6.65 | 8.10 | 9.10 | 10.15 | ↑ |
| BB/9 | 2.15 | 2.85 | 3.45 | 4.20 | **↓** |
| HR/9 | 0.32 | 0.53 | 0.66 | 1.12 | **↓** |
| 球数/アウト | 5.05 | 5.25 | 5.50 | 5.75 | **↓** |

⚠**투수 지표는 대부분 「낮을수록 좋다」.** 이걸 놓치면 좋은 투수가 「매우 나쁨」 색으로 칠해진다 —
값은 맞는데 화면이 정반대를 말하는, 가장 나쁜 종류의 오류다. 테스트가 양 끝값으로 방향을 고정한다.

### 색을 칠하지 않는 것

| 대상 | 왜 |
|---|---|
| **BABIP** | 운의 영향이 커서 좋고 나쁨을 말할 수 없다 |
| **SRC · SRC/600** | 우리 자체 지표다. 남의 분포와 비교할 근거가 아직 없다 |
| **개수 지표**(안타·홈런·타점 등) | 출장 수에 비례하므로 등급이 뜻을 갖지 않는다 |
| **최소 표본 미만** | 10타석 .400에 「매우 좋음」을 칠하면 M2를 어긴 것과 같다 |

### 운용 규칙

- ⚠**매일 다시 계산하지 않는다.** 그러면 성적이 그대로인데 색이 바뀌는 날이 생기고
  「어제는 좋음이었는데」가 된다. **고정값을 쓰고 시즌이 끝나면 다시 뽑는다.**
- **색은 보조다.** 등급은 보이지 않는 글자(`.vh`)로도 나가고, 화면에서 끌 수 있다.
  ⚠단 **분모는 끌 수 없다**(M2). 색은 취향이고 분모는 규칙이다.
- **빨강↔초록을 쓰지 않는다.** 가장 흔한 색각 이상에서 구별되지 않는다.
  파랑(좋음)↔주황(나쁨)의 발산 배색을 쓰고 명도도 함께 벌려 흑백에서도 순서가 남게 한다.

---

## 7. 미결

| # | 항목 | 해소 시점 |
|---|---|---|
| ~~1~~ | ~~T1 대조 케이스~~ | **해소 2026-08-15.** 아래 §8 참조 |
| 2 | 간판 지표(상황가중 공격 기여도)의 산식·이름 | 증분 F |
| 3 | RE24 · WPA | 증분 F (타석 로그 필요) |
| 4 | 파크팩터 | 3~5시즌 축적 후 재검토 |

---

## 8. T1 검증 결과 (2026-08-15)

`node packages/aggregate/tools/verify-vs-npb.ts data/bb.sqlite`

원시 HTML → 파싱 → 저장 → 집계 → 지표까지 **전 경로**를 npb.jp 공표값과 대조했다.

| 대상 | 佐藤(阪神, player_id 41045153) |
|---|---|
| 출처 | `https://npb.jp/bis/2026/stats/idb1_t.html` |
| 기준일 | 2026-08-13 (우리 집계도 같은 날로 잘랐다) |
| 결과 | **17 / 17 항목 일치** |

일치한 항목: 試合 · 打席 · 打数 · 得点 · 安打 · 二塁打 · 三塁打 · 本塁打 · 打点 · 犠飛 ·
四球 · **敬遠** · 死球 · 三振 · **打率 .317** · **長打率 .622** · **出塁率 .404**

⚠**처음에는 17항목 중 5개만 맞았다.** 원인은 산식이 아니라 **올스타전을 정규시즌에 합산**한 것이었다
(2경기·7타석·2홈런). `competition` 컬럼을 만들어두고 전부 `regular`로 넣은 것이 화근이었다 —
「경기구분을 섞지 마라」는 규칙이 왜 있는지 숫자로 확인된 사례다.

⚠**기준일을 맞추지 않으면 대조는 의미가 없다.** 상대의 「◯月◯日現在」와 우리 아카이브 범위가
하루만 달라도 불일치로 보이고, 그건 버그가 아니다.

---

## wRC+ · wRAA 의 리그 합계 (2026-08-15 검증)

wRC+는 화면에서 가장 눈에 띄는 지표인데 **공표값과 대조할 소스가 없다**(npb.jp는 wRC+를 내지 않는다).
그래서 **성질로 검증했다** — 리그 상수가 맞다면 리그 전체의 편차 합이 0이어야 한다.

| | 센트럴 | 퍼시픽 |
|---|---|---|
| 타석가중 wOBA 평균 vs 리그 상수 | `.29575` vs `.29575` **완전 일치** | `.31004` vs `.31004` **완전 일치** |
| **wOBA 분모로 가중한 편차 합** | **0.00** | **0.00** |
| PA로 가중한 합 (= wRAA 리그 합계) | −21.57 | −8.12 |
| 타석가중 wRC+ 평균 | 98.995 | 99.659 |

### ⚠wRAA 합계가 0이 아닌 것은 **버그가 아니다**

분해하면 잔차 **전부**가 犠打·敬遠이다:

```
센트럴   PA로 가중한 합       −21.57
         犠打+敬遠 항         −21.58   ← 잔차 전부가 이것
```

`wRAA = (wOBA − lgwOBA) / wOBAscale × PA` 는 **PA**를 곱하는데,
wOBA의 분모는 `AB + BB − IBB + SF + HBP` 라 **犠打와 敬遠이 빠져 있다.**
그 차이가 그대로 남는다. **표준식이 그렇게 생겼으므로 우리 구현이 맞다.**

부호가 음수인 것도 설명된다 — 번트를 대는 쪽은 대개 리그 평균 아래 타자다.
센트럴이 더 큰 것도 마찬가지다: **DH가 없어 투수가 번트를 많이 댄다**(犠打 343 vs 302).

⚠**「0이 아니네」라며 고치지 마라.** PA를 wOBA 분모로 바꾸면 그 순간 표준 wRAA가 아니게 되고,
다른 사이트와 값이 달라진다. 불변식은 `packages/metrics/test/league-invariant.test.ts`가 고정한다.

### 아직 안 한 것

**외부 공표값과의 wRC+ 대조.** 1.02 등이 NPB wRC+를 내지만 별도 소스라
`data-source-scout`의 약관 판정을 거쳐야 하고, 개발 루프에서 외부 사이트를 치지 않는 규약도 있다.
위 성질 검증은 「상수와 스케일이 자기모순이 없다」는 것까지만 보증한다.
