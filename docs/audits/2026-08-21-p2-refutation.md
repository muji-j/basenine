# 감사 반증 라운드 종합 — P2 31건

## 1. 한 줄 요약

**사살 5 / 생존 22 / 판정 못 함 ~~4~~ → 0 (분모 31)** — ⚠**미판정 4건은 2026-08-22 에 돌렸다**(§7) — 생존 22건의 등급 내역은 P1 2건 · P2 13건 · P3 7건이고, **원 보고가 실제로 틀렸던 항목이 18건**(사살 2 + 생존 16)이다.

---

## 2. 사살된 것 (5/31)

| id | 원 지적 | 왜 죽었나 (실측) |
|---|---|---|
| **23** | M12 4상태가 배선되지 않아 「수집실패·시즌외」가 배포물에 한 장도 없다 | 관측 전반부(stateNote 프로덕션 호출처 0곳)만 참. 상태 자체는 `freshnessBar()`와 각 화면 문면으로 실재 — **수집실패 1,485/15,341장**(`更新が止まっています`) · **시즌외 13,856/15,341장**(`終了したシーズンです`). `未取得` 3건 vs `試合なし` 8건 구분도 log.html에 있음. `layout.test.ts` **25/25 pass**. 남는 실체는 미사용 수출 헬퍼 1개이고 이미 기지(旣知) 항목 |
| **39** | ranking.html 9장이 975KB~1.02MB · 여는 태그 28,525개로 비대 | 수치는 맞으나 **결함 증거 0**. 완전동일 `<table>` 중복 **0건**, 87표 = 2리그×3부문×지표수(§0-5가 요구하는 개수), 전 탭 hidden 인라인은 `parts.ts:475·424-430`에 문서화된 의도(무JS 폴백·해시 직링크). gzip 63,499~65,871B. 파일당 용량 계약이 리포에 **0건** |
| **40** | build.ts가 전 시즌 SiteData를 해제 안 해 RSS 587→1,466MB | **인과가 틀렸다.** GC 후 heapUsed 실측 **407MB**(시즌당 43.4MB)이고 RSS는 6번째 시즌부터 **827~829MB에서 평탄**(heap은 287→407로 계속 증가). 한도 6336MB의 **6.4%**, 130시즌 여유. 실패 모드도 침묵 아님(exit 134). 실제 벽은 Pages 20,000파일(현재 15,444) |
| **51** | CLAUDE.md가 확신도 다른 두 주장(일괄관리 vs 외판)을 한 캐비엇으로 묶었다 | 두 주장은 **확신도가 같다** — 같은 단일 출처(닛케이 2025-05-19)이고 `外販`·`日経`·`一括管理` **세 용어 모두 저장 샘플 grep 0건**. 3개 문서(metrics/README · 소스 서베이 2건)가 일관되게 하나로 묶는다. NPB+ 실측 부분은 이미 별도 괄호로 분리돼 있다 |
| **27** | 용어집 페이지가 배포물에 0장 · 툴팁이 유일 경로 | `dist-check/glossary.html` 실재(428줄), 표본 3장(index · players/01005157 · teams/g) 푸터 전부에 `用語集` 링크. 이번 라운드 기수정 목록과 정확히 일치 |

---

## 3. 생존한 것 (22/31)

### P1 (2건)

#### **[4] 아카이브 원격 사본이 하나 + `--clobber`가 매일 그 하나를 먼저 지운다** — `daily.yml:295~316`
- **증상**: 워크플로 파일 1개·태그 1개(`data-store`)뿐. 세대 보관 0. 업로드 후 크기 검증 0.
- **근거**: `gh release upload --help` 원문 — 「`--clobber` 사용 시 기존 자산을 **삭제한 뒤** 업로드한다. **업로드가 실패하면 원본 자산이 소실된다**」. 그런데 `daily.yml:303` 주석은 「업로드는 **멱등**이다(`--clobber`)」이고 **그 틀린 전제 위에 5회 재시도 루프(307~316)가 서 있다**. 올리는 물건 279,724,254 B(33,136건). `checkNotShrunk`는 `files`만 비교하고 `bytes`는 기록만 되고 **한 번도 비교되지 않는다**. `archive-guard.test.ts` **9/9 pass**(그러나 bytes를 안 잼).
- **수정**: ① 세대 이름(`archive-$(date -u +%Y%m%d).tar`)으로 바꿔 `--clobber` 제거 → 성공 확인 **뒤에** 옛 세대 `delete-asset` ② 업로드 후 `gh release view --json assets` size 대조(`|| true` 금지) ③ `checkNotShrunk`가 bytes도 보되 **임계는 최근 30일 매니페스트 이력을 재고 나서** 정한다 ④ 303행 주석을 gh 문언대로 정정 ⑤ 17행 「190MB(5시즌)」 → 실측 279.7MB·9시즌 ⑥ 로컬 사본을 「정책」으로 승격할지 결정.
- **검증**: V1 `grep -nE 'release upload[^|]*--clobber' daily.yml` → 0 / V2 `archive-$`·`delete-asset` 둘 다 히트 + **정리가 업로드 성공 판정 뒤**인지 육안 / V3 `.size` 히트≥1 & 전후에 `|| true` 없음 / V4 `archive-guard.test.ts` **tests > 9** & fail 0 (9 그대로면 시험 미첨부) + bytes 비교 제거 시 새 3본이 붉어지는지 뮤테이션 / V5 첫 실행 전 원격 릴리스 육안 확인.
- ⚠**진척(2026-08-24)**: ②·④ 는 이미 돼 있었다. **③ 을 했다** — `checkNotShrunk` 가 bytes 도 본다.
  ⚠**임계를 손으로 고르지 않았다**: `ops/archive-manifest.json` 의 git 이력 **58개 · 연속 쌍 57**
  (2026-08-15~08-24)을 재니 **`files` 감소 0/57** · **`bytes` 감소 15/57(26.3%)** ·
  **최악 −96,769 B(0.035%)** 였다 — 살아 있는 페이지를 다시 받으면 gz 가 작아진다.
  「줄면 실패」로 뒀다면 **네 번에 한 번 거짓 경보**가 났을 것이다. 실측 최악의 약 14배인 **0.5%**
  (절대 하한 1MiB)로 뒀고, 시험 **9 → 15본**(V4 충족) · **뮤테이션 확인**(bytes 분기 제거 → 2본 붉음).
  **⑤ 도 했다** — `daily.yml:17` 「190MB(5시즌)」 → **267.2 MiB(280,226,044 B · 33,207파일 · 9시즌)**.
- ⚠**①의 명세에 함정이 있다**(2026-08-24 발견 · **이 보고서가 틀린 자리다**):
  제안한 세대 이름 `archive-$(date -u +%Y%m%d).tar` 은 **이미 다른 뜻**이다 —
  복원 스텝이 `archive-*.tar` 를 「덧붙임 자산」으로 보고 **전부 전개**하고,
  `gh release download` 는 **자산을 전부** 받는다. 그대로 하면 **매일 267MiB × N 을 내려받는다.**
  → 세대는 **다른 접두사**(예: `store-YYYYMMDD.tar`)를 쓰고 복원은 `--pattern` 으로 좁혀야 한다.
  **그 재설계 없이 ① 만 하면 더 나빠진다** — 그래서 이번에도 안 했다.
- ⚠**정정**: 위치·증상 3항목은 전부 성립. 보탤 것 둘 — **(가)** 「사본이 하나」보다 나쁜 것은 **하루 한 번 그 하나를 스스로 지우는 창**이 있다는 것이고 주석이 정반대를 적고 있다. **(나)** 「계정 사고 하나로 **전량** 소실」은 과장 — 개발기에 32,398 gz(원격 대비 **97.8%**)가 있다. 단 **정책이 아니라 우연**(문서 0건·같은 머신·검증 없음·738건 뒤짐).

#### **[24] 오프시즌에 「取得に失敗している可能性があります」가 매일 나온다** — `layout.ts:117 / 413 / 625`
- **증상**: `freshnessBar()`의 stale 판정이 `pastSeason` 하나로만 갈리는데, 그 값은 `seasons[0]?.current === false`(= 그리는 시즌 번호가 plans 중 최신인가)라 **「이 시즌이 실제로 끝났는가」와 무관한 순수 구조 판정**이다.
- **근거**: `seasonIsOver`(query.ts:3243)는 `home-page.ts:235`·`player-page.ts:545`·`seasonOver` 필드에만 연결돼 있고 **PageOptions에 `seasonOver` 필드 자체가 없다**. 실행 재현: `freshness("2026-10-19","2026-11-20",…)` + `pastSeason=false` → `<div class="state stale">…最新の試合は 2026年10月19日（32日前）。取得に失敗している可能性があります`. `daily.yml`에 시즌 여부로 빌드를 거르는 조건 **grep 0건** → 매년 11월~3월 매일 재현. `season-over.test.ts` 주석이 같은 구멍을 이미 지목했으나 수정은 텍스트 쪽만 했다. 오늘(시즌 중) dist-check은 state fresh.
- **수정**: `isPastSeason(o)`를 그대로 넘기지 말고 「시즌 번호가 최신이 아니다」 **or** 「이 시즌이 끝났다」를 합쳐 넘긴다.
- **검증**: 수정 후 같은 인자로 `freshnessBar` 재실행 → `取得に失敗している可能性があります` 부재 · `終了したシーズンです`류 출현. + `node --test packages/web/test/layout.test.ts packages/web/test/seasons.test.ts packages/web/test/season-over.test.ts` 통과.
- ⚠정정: 없음(correction 필드 없음).

---

### P2 (13건)

#### **[1] `game.fetched_at` 7,805/7,805행이 「오늘 07:20」 한 값** — `load-archive.ts:60 / 284 / 415`
- **근거**: DB 실측 `{n:7805, d:1, mn=mx="2026-08-21T07:20:21.542Z"}`. 사이드카에 실측치가 **100% 있다**(`box.meta.json` · missingMeta **0/7,805**). 1:1 대조 → **mismatch 7,805/7,805 · 최대 드리프트 161.0시간(6.7일)**. 적재할 때마다 새 「오늘」로 갱신되어 **어긋남이 무한히 커진다**. `fetchedAtOf()`는 바로 이 사고의 처방인데(`meta.test.ts:1-8`에 사고 기록) `load-archive.ts`는 import조차 안 한다. `game.fetched_at`은 `pa_event`·`runner_event`·`batting_line`·`pitching_line` 전체의 **유일한 provenance 앵커**(migrations/010:19-22).
- **수정**: `fetchedAtOf()` 합류. ⚠`fetched_at`은 NOT NULL이므로 `?? nowIso` 폴백은 **결함을 되살린다** — nullable 마이그레이션(선례 014·017) 또는 세어서 건너뛰기 중 **선택을 코드에 적어라**. `nowIso`는 지우지 말 것(435·450·549·139행에서 계속 쓴다). `ON CONFLICT`의 무조건 덮어쓰기도 `upsertProbablePitcher`처럼 「더 새 판만 이긴다」로 할지 정하고 주석에 적어라. **재적재해야 값이 바뀐다.**
- **검증**: DB↔사이드카 1:1 대조에서 `mismatch===0 && sidecarMissing===0`(현재 7805/0). 보조로 시즌별 distinct 날짜 ≥6 — ⚠**distinct만으로 합격 처리 금지**(필요조건일 뿐).
- ⚠정정: 없음.
- **P1 아닌 근거**: `game.fetched_at`을 읽는 코드가 리포 `.ts` 전수에서 **0곳**(fetched_at SELECT 4곳은 전부 다른 표). 화면에 실릴 경로가 없다 — 추적성 필드의 침묵 오염.
- ⚠**고쳤다(2026-08-24)**. 독립 재측정으로 위 두 사실을 확인했다: 사이드카 **7,805/7,805 읽힘** ·
  `game.fetched_at` 소비자 **0곳**.
  - **고른 길: 세어서 건너뛰기**(감사가 제시한 두 길 중 하나). ⚠**nullable 마이그레이션을 안 골랐다** —
    `game` 은 FK 가 **4개**(batting_line·pitching_line·pa_event·runner_event) 들어오는 중앙 표라
    재구축 위험이 `probable_pitcher`(참조 0개)와 다르고, **읽는 곳이 0곳이라 그 위험을 살 이득이 없다.**
    못 읽으면 그 경기를 **건너뛰고 `failed` 로 센다**(기존 패턴 · `failed>0` 이면 종료 코드 1).
    ⚠**지금 걸리는 것은 0건**이다.
  - **`ON CONFLICT` 는 무조건 덮어쓰기로 뒀고 이유를 주석에 적었다**: 予告先発 은 **한 날짜가 여러 파일**에
    걸려 순서가 값을 갈랐지만, 경기는 **`game_id` 하나에 box 하나**라 사이드카 시각도 하나뿐이다 —
    **막을 경합이 없다.** 옛 아카이브 회귀는 `archive-guard` 가 앞에서 막는다.
  - **검증**: ⚠**감사가 경고한 지름길(distinct 만 보기)을 본체로 쓰지 않았다.**
    `packages/store/test/game-fetched-at.test.ts` 가 **DB↔사이드카 1:1 대조**를 하고,
    distinct·최빈값 쏠림·「경기일보다 앞서지 않는다」는 **보조**로 둔다.
    ⚠**뮤테이션을 합성으로 만들지 않았다** — 3일치만 재적재한 **실제 중간 상태**에서
    두 보조 단언이 붉었다(서로 다른 값 **19/7,805 = 0.2%** · 한 값에 **99.8%**).

#### **[2] 투수 7열이 `?? 0`으로 조용히 0 저장** — `derive.ts:204~210`
- **근거**: `h/hr/bb/hbp/so/runs/er`가 전부 `row.X ?? 0`. 공급원 `num()`은 빈 칸·`-`·비유한수에 null을 준다. 같은 표의 타격은 `requireNum()`으로 **던지고**, 같은 함수의 `outs`는 **격리로 뺀다** — 비대칭 실재. 방어 0: `sweep-archive.ts`·`crosscheck.ts` 둘 다 **CI에도 npm scripts에도 0줄**, `store.test.ts` 24/24 pass 중 투수 null을 재는 건 outs 전용 1본.
- **지금 틀린 값은 0건**: 아카이브 전량 7,805장 파싱 오류 0 · 투수 행 63,315건 × 7열 = **443,205칸 중 null 0건** · DB 교차대조(원정 투수진 SUM(runs) vs `home_runs` 등) **0/7,518경기 어긋남**. → 잠재 결함.
- **수정**: `outs` 가드와 **같은 모양**으로 derive 단계에서 막고, 격리 `kind`에 **어느 열인지 detail 기록**. `QuarantineRow.kind` 확장 시 `log-page.ts`도 같이(derive.ts:59-62가 요구하는 절차). `sweep-archive.ts`의 被安打 대조를 CI에 배선.
- **검증**: `node --test packages/store/test/store.test.ts`(수정 전 붉음 확인) / `pitching_line` 63,315행 유지 + quarantine 종류별 카운트 / `grep -n "sweep-archive" .github/workflows/daily.yml package.json` / `grep -n "unreadableColumn" log-page.ts derive.ts`.
- ⚠**고쳤다(2026-08-24)**. `outs` 가드와 **같은 모양**으로 derive 단계에서 막는다 —
  일곱 중 하나라도 못 읽으면 **그 등판을 적재하지 않고** `unreadablePitchingStat` 로 격리하며,
  `detail` 에 **어느 열인지** 적는다(`投手成績を読めなかった: 피안타・자책`).
  ⚠**화면은 격리 종류를 하드코딩하지 않는다**(실측) — `log-page.ts` 가 DB 의 `kind` 를 그대로 그리므로
  새 종류가 자동으로 뜬다. derive.ts:59-62 가 요구한 절차는 **확인으로 충족**됐다.
  ⚠**남은 것**: `sweep-archive.ts` 의 被安打 대조를 CI 에 배선하는 것은 **안 했다**.
  - **검증**: 시험 **9본 추가**(7열 각각 + 복수 열 + 기존) · `store.test.ts` **32/32** ·
    **뮤테이션 확인**(가드를 약화하니 2본 붉음). ⚠**지금 걸리는 것은 0건**이라 값은 안 바뀐다.
- ⚠**정정**: outs 처리는 「175~193」이 아니라 **179~194**. 보탤 실측 — `derive.ts:211`이 wp/balk에 대해 적은 「이 둘은 '없음'이 실재한다」가 **보유 데이터에 근거가 없다**(63,315행 중 `wp is null` 0건 · `balk is null` 0건). 비대칭이 측정이 아니라 믿음 위에 서 있다. ⚠단 방향은 **7필드를 null 보존 쪽으로**이지 wp/balk를 `?? 0`으로 바꾸라는 게 아니다.

#### **[5] `clock.ts:4`의 「유일한 곳」 단언이 거짓 + 강제한다던 린트가 없다**
- **근거**: 린터 설정·패키지·바이너리·스크립트·CI **전부 0건**(`node_modules/.bin` 26개 중 린터 0). `clock.ts:13`의 `// eslint-disable-next-line`은 **존재하지 않는 린터를 향한 죽은 주석**. 대체 grep 가드 시험도 **0본**. clock.ts 밖 인자 없는 시계 읽기 **8개소/7파일**(load-archive.ts:60 · load-players.ts:23 · load-starters.ts:32 · load-upcoming.ts:47 · marks.ts:30 · freshness.ts:38·314 · update.ts:108). `load-archive.ts:59` 주석이 「시계는 1회만 읽어…(M6)」로 **스스로 반증**. CLAUDE.md 318·447행도 없는 것을 있다고 적는다.
- **수정**: ⑴ 단언을 실측에 맞춘다(8개소를 clock 경유로 바꾸는 쪽은 권하지 않음 — 진입점 1회 읽기가 M6의 의도) ⑵ `scripts/test/clock-guard.test.ts` grep 가드 + 진입점 허용목록 하드코딩 ⑶ 죽은 disable 주석 삭제 ⑷ **CLAUDE.md 318·447행 정정**(안 하면 지적의 절반이 남는다).
- **검증**: `grep -c "유일한 곳" clock.ts`=0 / `grep -rc "eslint-disable" clock.ts`=0 / `node --test scripts/test/clock-guard.test.ts` exit 0 / **뮤테이션**: `query.ts`에 `Date.now()` 심고 `MUTANT_EXIT≠0` 확인 후 되돌림 — ⚠MUTANT_EXIT=0이면 그 초록은 「안 쟀음」 / CLAUDE.md 문구 grep 0.
- ⚠**정정**: 위치·수치·사유 정정 없음. 보탤 것 — ⑴ `clock.ts:13`의 죽은 억제 주석도 처리 대상 ⑵ 허위 주장이 **CLAUDE.md 318·447행에도** 있다. 완화 사실: 8개소 전부 진입점 1회 읽기라 M6의 실질(자정 이중 읽기)은 오늘 안 깨져 있다 — 깨진 것은 **단언의 사실성과 가드의 부재**.

#### **[17] 편집기가 상호작용마다 전량 재생성 — 누른 컨트롤이 사라진다** — `assets.ts:2325~2326`
- **근거**: 실행 재현 3/3 fail — ⑴ 누른 체크박스 노드 생존 **false** ⑵ **행이 0→3으로 이동** ⑶ 같은 자리에서 ↓ 두 번 누르면 **원래 순서로 되돌아온다**. 배포물 `site.js:404-405`에 동일 코드, `activeElement` **0건**. 편집기 실린 페이지 **6,208/15,341장**. `client.test.ts` **136/136 pass**이나 편집기 5본은 상태·순서만 재고 노드 동일성·포커스는 **0본**.
- **수정**: `renderEditor()` 진입 전 `activeElement`의 블록 id + 역할(cb/up/dn)을 기억해 재생성 뒤 복원. ⚠끝단 `disabled` 버튼에 focus하면 body로 다시 떨어져 **고친 게 0이 된다** — 반대 방향 버튼이나 행으로 흘려라. 체크박스 행 점프는 별개 원인(`state.order.concat(rest)`)이고 목록 순서 고정은 **설계 변경이라 사용자 확인 필요**(작업규칙 3).
- **검증**: 재현 시험 3/3 pass 전환(현재 3/3 fail) + `client.test.ts` 136/136 유지. ⚠dom-stub에 `activeElement`/`focus()`가 없으므로 **노드 동일성** 또는 focus 대상 id를 스텁에 심어 재라.
- ⚠**정정**: 위치가 어긋난다 — `assets.ts:2288`은 `.scroller` 판정 루프이고 실제는 **2325(renderEditor) / 2326(host.textContent="")**. 증상 서술은 맞고, 원 보고에 없던 두 증상(행 0→3 점프 · ↓ 두 번이 원위치)이 더 나왔다 — **이쪽이 포커스 손실보다 마우스 사용자에게 먼저 보인다**.

#### **[19] 입력칸 테두리가 비텍스트 3:1 미달 (1.51~1.69) · 면 대비 1.000**
- **근거**: LIGHT `.qbox` 1.514 · `.mfind` 1.580, DARK 1.688 · 1.557. `.mfind input{background:var(--panel)}`이 `.block{background:var(--panel)}` 위에 있어 **면 신호가 원리적으로 0(1.000)**, 1px `--hair-2` 테두리가 유일 식별 표시. 배포 `site.css` 원문 확인 · `appearance:none`/`::placeholder`/`prefers-contrast`/입력칸 `forced-colors` 전부 없음. **`class="qbox"`가 15,341/15,341장 전수**. `css-contrast.test.ts` **18/18 pass**인데 이 스위트는 이미 `.mf-dot`을 「조작 요소의 유일한 어포던스」로 3:1 검사하면서 입력칸만 그물 밖. 역방향 증거: site.css 주석이 `--hair-2` 1.51/1.69를 이미 적고 `tr.thin`만 `--tx-3`로 고쳤다.
- **수정**: 전용 토큰(`--field-line`)을 새로 두고 `--hair-2`와 분리(표 괘선은 장식이라 3:1 불필요 — 같은 토큰이면 다음 사람이 한쪽을 망친다). 대상 4규칙: `.qbox input` · `.mfind input` · `.mfind select` · `.mfind .rankmin input`. `--tx-3` 실측은 네 조합 전부 3:1 통과(4.910~5.499). 면 대비 1.000도 `var(--page)`로 완화(단 1.044라 단독으로는 불충분 — 테두리가 본체).
- **검증**: `node --test --experimental-strip-types packages/web/test/css-contrast.test.ts`(추가 전 네 케이스가 1.514/1.580/1.557/1.688로 **떨어지는지 먼저 확인**) + 배포물 `site.css`에서 세 규칙이 `--hair-2`를 안 쓰는지.
- ⚠정정: 없음.

#### **[25] `paceOf()` 부동소수 절단 — 완결 시즌에 「143試合換算」이 現在보다 1 작다** — `query.ts:2383`
- **근거**: 143 = 11×13이라 `teamGames`가 11·13의 배수일 때 정수 바로 아래로 떨어진다. 어긋나는 g **23개**, g=143에서 어긋나는 count **30개**(c≤700). **배포물 108행 중 5행이 틀렸다**: 2018 山川 HR 47→46 · 2018 山川 打点 124→123 · 2019 山口俊 奪三振 188→187 · 2023 村上 HR 31→30 · 2025 伊藤 奪三振 195→194. **바로 옆 칸이 `現在 47`인데 `143試合換算 46`** — 화면이 자기 자신과 모순. 방어 0(`home-page.test.ts`의 pace는 픽스처 주입값이라 산식을 안 탄다). 2026도 시한폭탄(110·117·121·130·132·143을 반드시 통과).
- **수정**: `Math.floor((count * regularSeasonGames(season)) / teamGames)` — 전수 402,000건 어긋남 0. `paceOf`가 모듈 내부 함수라 **export를 열어야** 시험 가능. ⚠**2020(N=120)도 같이 고정**해야 시즌별 표의 다른 가지가 덮인다.
- **검증**: `grep -n "count \* regularSeasonGames"`=1 & `"count / teamGames"`=0 → 재빌드 후 배포물 스캔이 **`bad=0 / 108`**(분모 108을 반드시 함께 읽어라) → 뮤테이션으로 5건이 붉는지.
- ⚠**정정**: 위치·증상·예시 전부 맞음. 범위만 좁게 잡았다 — 특정 값이 아니라 **11·13 배수 전체**(g 23개 · count 30개)이고 **2020(N=120)도 같은 결함**을 갖는다.

#### **[26] HTML 주석 374,740개 · 123.59 MiB가 배포물에 실린다**
- **근거**: `html.ts` 전문(80행)에 주석 제거 없음, 빌드 전체에 minify **0건**. `dist-check` **전수 15,341/15,341장**: 주석 374,740개 · 123.59 MiB · html 총량의 **12.15%**(정규식·grep 두 방법 일치). gzip 표본 301/15,341장에서 **전송량 23.58% 절감**. 클라이언트가 주석 노드를 안 읽는다(`nodeType===8`/`COMMENT_NODE`/`createComment` **0건**) → 하나도 load-bearing 아님. `tag-integrity.test.ts` 3/3 pass이나 재는 것은 「여는 태그 **안**의 주석」이지 산출 여부가 아니다. `teams-page.ts:97`이 **같은 정책과 23%라는 수치를 이미 적어 놨는데 한 함수에만 적용**됐다.
- **수정**: 10개 파일의 주석을 손으로 지우지 마라(「왜」가 사라진다) — **출력 경계 한 곳**(`html.ts` toString 또는 파일 쓰기 직전)에 올려라(M1). ⚠단순 정규식 금지 — script/style · pre/textarea · 속성값 세 자리에서 깨진다. 속성값이 이스케이프되어 안전하다는 **전제를 TS 주석에 적어라**. ⚠**Pages 20,000파일 벽에는 효과 0**.
- **검증**: `grep -roh --include='*.html' -e '<!--' dist-check | wc -l` → 0. ⚠**`dist`를 재지 마라(낡음)**, 그리고 `dist-check`를 다시 만든 뒤에 재라. 회귀 고정 시험 추가(현재 산출물 주석·크기 시험 **0/68본**).
- ⚠**정정**: **보고 수치는 낡은 `dist/`를 잰 것**이다 — `dist`의 337,577과 보고서 값이 정확히 일치. 현재 코드 산출물은 **374,740개·123.59 MiB**로 개수 37,163개·용량 16.17 MiB **과소평가**. 위치 표기도 좁다 — 원인은 `layout.ts:1`·`player-page.ts:815` 두 줄이 아니라 **출력 경계에 제거가 없다는 것**이고 `<!--`를 쓰는 소스는 **10개 파일**.

- ⚠**고쳤다(2026-08-24)**. 출력 경계 한 곳(`html.ts` 의 `toString`)에서 뗀다 — **소스의 주석은 그대로 둔다**(그게 「왜」다).
  - **떼기 전 실측**(15,341장 전수): 주석 **359,399개 · 114.62 MiB · html 총량의 11.39%**. ⚠**감사가 적은 374,740개·123.59 MiB 와 다르다** — 그 사이 빌드가 바뀌었다. **결론은 같다.**
  - **효과**: gzip 표본 300장 **3,449,862 → 2,684,348 B (22.19% 절감)** — **예측과 정확히 같았다.** 산출 용량 **1,022.4MB → 907.8MB**. ⚠**파일 수는 그대로**(Pages 벽에는 효과 0).
  - ⚠**위험한 세 자리를 운이 아니라 구조로 막았다** — script/style/pre/textarea 구역을 통째로 건너뛴다. 실측으로 그런 블록은 지금 **0개**지만 「오늘 없다」는 「내일도 없다」가 아니다.
  - **시험 13본**(`strip-comments.test.ts` 12 + 배포물 회귀 1) · **뮤테이션 2본 확인**
- ⚠**주석을 떼자 시험 둘이 붉어졌고, 둘 다 시험 쪽 결함이었다:**
  ⑴ `player-page.test.ts` 가 `佐藤<` 로 재고 있었는데 그 `<` 는 **바로 뒤 주석의 여는 괄호**였다 — 「이름 뒤에 태그가 온다」가 아니라 **「주석이 거기 있다」를 재고 있었다.** ⚠**렌더 결과는 안 바뀌었다**(주석 뒤에 이미 공백이 있었다).
  ⑵ `template-literals.test.ts` 가 **내가 새로 쓴 주석의 백틱을 잡았다** — 가드가 제대로 작동한 것이다.

#### **[29] `pitches / outs`를 `outs===0` 검사 없이 나눠 Infinity** — `query.ts:788`
- **근거**: `rankQualified`(leaderboard.ts:240-242)가 **자격 미달자에게도 `pick(item)`을 부른다**. DB 전수(regular·played, 2018~2026): 시즌 합계 `outs=0` 투수-시즌 **6건**(2018 髙橋朋 8구 / 2019 南川 20구 / 2020 藤平 7구 / 2021 斎藤佑樹 7구 / 2021 松坂 5구 / 2022 岩嵜 4구), 전부 pitches non-null → **Infinity**. `rankBy`는 `value !== null`만 걸러 통과. 배포물 피해 실측: 순위 페이지 9장에서 `pitchesPerOut` 패널의 「記録がある選手 N人」이 era/fip/whip/k9/bb9보다 **1 큰 곳이 정확히 6곳**이고 위 6명과 1:1. 실문장: 「記録がある選手 68人のうち」(참값 67).
- **틀린 수치는 렌더 안 됨**: `format.ts`의 `!Number.isFinite` 가드가 「—」로 떨어뜨리고, 순위행은 최악 rankAll을 받아 탈락 — 9장 전부 `Infinity`/`NaN` **0건** · `data-den="0"` **0건**. 손해는 **M11 위반 6건**이고, 정본 방어가 아니라 **하류 포맷터의 부수효과에 목숨이 걸려 있다**.
- **수정**: `rate(e.player.pitches, e.player.line.outs)`로 교체(`pitches === null` 분기는 M11 때문에 유지). 세 곳(788·3715·5357)이 같은 식을 세 벌로 든 것 자체가 M1 위반 → `pitchesPerOutOf()` 한 벌로.
- **검증**: 재빌드 후 9장의 패널별 「記録がある選手」 대조 스크립트가 **아무것도 출력하지 않을 것**(현재 6줄). 보조로 outs=0 6명이 그대로인지 확인.
- ⚠**정정**: 보고의 두 위치 중 **하나는 이미 방어돼 있다** — `query.ts:3658`(현 **3715**, 팀 페이지)은 `r.pitches === null || r.line.outs === 0` 명시 가드가 있고, 언급 안 된 5357(선수 페이지)은 `rate()`를 쓴다. **결함은 766(현 788) 한 곳뿐**이다. 또 「나눈다」가 틀린 수로 렌더되지는 않는다.

#### **[42] `deploy.md` 파일 수가 5시즌 시절(8,611/519MB) 그대로 — 같은 사실에 세 값**
- **근거**: deploy.md **408행**(391 아님)이 「8,611파일 / 519MB(5시즌 2022~2026) … 11시즌이면 약 19,500으로 98%」. CLAUDE.md는 「15,434파일(77%) … 11시즌 약 18,900으로 95%」. `find dist-check -type f | wc -l` = **15,444**. 같은 사실에 **8,611 / 6,816 / 15,434** 세 값, 11시즌 예측도 **19,500(98%) vs 18,900(95%)**. `doc-figures.test.ts`에서 `deploy.md`·`8,611`·`519MB` grep **0건** — 방어 시험 없음.
- **수정**: deploy.md §7-3 칸을 CLAUDE.md 최신값으로 갱신하거나 **CLAUDE.md를 단일 정본으로 삼고 인용/링크로 대체**(양쪽 하드코딩이면 다음 백필에 또 갈린다). `doc-figures.test.ts`에 dist-check 실측 대조 케이스 추가.
- **검증**: `grep -n "8,611\|6,816\|519MB" docs/operations/deploy.md` → 0건, 두 문서 수치 일치, `find dist-check -type f | wc -l`과 자릿수 일치.
- ⚠**정정**: 줄 번호 391 → **408**(직전 커밋 f86a3d5가 §4-2에 26줄 추가해 밀림). 내용 존재·수치·비교 대상은 정정 없음.

#### **[45] CLAUDE.md 2021 품질 기록 「정규 890」이 같은 문장 안에서 산수가 안 맞는다**
- **근거**: 12팀×143/2 = 858인데 같은 문장이 「정규 890」. DB 실측 2021 regular: **notPlayed 32 · played 858 = 890** — 890은 notPlayed까지 합산한 값. 2018은 notPlayed 56 · played 858인데 문서는 **858**(played만), 2020은 notPlayed 23 · played 720으로 문서의 「120경기」와 일치(played 기준). **같은 이름의 지표가 시즌마다 세는 방식이 갈린다.**
- **수정**: 「정규 858(played · notPlayed 32건 별도)」로 정정하거나, 890이 의도라면 **정의를 문서 상단에 명시하고 2018·2020도 같은 정의로 재계산**. 어느 쪽이든 파일 내에서 「정규 N」이 매 시즌 같은 것을 세야 한다.
- **검증**: 2021 regular status별 카운트 조회 후 CLAUDE.md 값과 눈 대조.
- ⚠정정: 없음.

#### **[46] 백필 예산 「16,389요청 · 149MB」가 실제의 약 절반** — `CLAUDE.md:273`
- **근거**: `GAME_PAGES`(discover.ts:117) = **4장/경기**. DB 시즌별 합 **7,805경기** × 4 = **31,220**이고 아카이브 `scores` .gz 실측이 **정확히 31,220**. 16,389는 그 **52.5%**(=경기당 2장 기준). **같은 문단 안에 모순**: 바로 다음 줄 「2024 = 3,636페이지」는 4장/경기(3,636/4 = 909 = DB 2024 경기 수). git 이력이 원인 확정 — 16,389는 `c56ccb7`(01:31), 3,636은 `e2c6080`(08:34), roster 추가 후 아랫줄만 갱신. 용량 실측 **250.8MiB(263.0MB)** → 149MB는 56.7%. 시간도 문서 실측(3.96초/장)으로 환산하면 9시즌 ≈ **36시간**으로 「13.7~22.8시간」 밖. `doc-figures.test.ts` 4/4 pass이나 이 줄은 **대상 아님**.
- **수정**: 「7,805경기 × **GAME_PAGES 4장** = 31,220요청 · 250.8MiB · 9시즌 ≈ 36시간」으로 고치고 ⚠**「4장」을 문장에 박아라**(16,389이 죽은 원인이 정확히 이것). `doc-figures.test.ts`에 **완결 8시즌만**(2018~2025 = 7,130×4 = **28,520**) 케이스 추가하되 기대값은 손으로 적지 말고 DB×`GAME_PAGES.length`로 만들 것.
- **검증**: 완결 8시즌 경기 수×4가 CLAUDE.md에 있는가 + 16,389·149MB가 사라졌는가 → 「true / false / false」(현재 「false / true / true」).
- ⚠**정정**: 감사자의 대조값 「33,136파일 / 266.8MiB」는 **분모가 다르다** — 그건 `ops/archive-manifest.json`의 아카이브 **전체**(scores 31,220 / players 980 / stats 120 / games 75 / starters 3). 같은 축의 옳은 값은 **31,220 / 250.8MiB**. 결론과 방향은 그대로. 덧: 같은 줄의 「43MB」도 2024 실측(gz 28.5MiB)과 안 맞지만 `du` 할당 크기 가능성이 있어 **원인 미확정 — 근거로 쓰지 마라**.

#### **[48] 「GitHub 126건 중 최근 15건 … 전부 스크레이퍼」가 분모를 결론에 반영 안 함** — 소스 서베이 280행
- **근거**: 원 조사의 15건 목록이 저장돼 있지 않아(파일 커밋 2건뿐·별도 로그 없음) 그날 그대로 재현 불가. 같은 질의 라이브 재현: `total_count=**12**`(126 아님 — 지수가 매일 바뀐다는 것 자체가 실측). 오늘의 12건 중에도 분류에 안 맞는 항목 다수 — `pitchlab`(synthetic-demo) · `MehdiKhouch/Data-Analyst`(다종목 포트폴리오) · `yasumorishima/npb-2021-backtest`(**FanGraphs** 사용) · `atsukoba/NPB-baseball-stats-R`(강의 과제). 같은 문서 278행(Kaggle)은 「「없다」가 아니라 …거기엔 0건」으로 표본 한계를 적는데 **280행만 「전부」로 단정** — 작업규칙 7 위반.
- **결론 영향 없음**: 스크레이퍼든 과제든 포트폴리오든 A/B/C/D를 라이선스 명시로 공급하는 소스가 아니라 판정(「불가」)은 안 바뀐다. 조사 방법론 기술의 정확성 문제.
- ⚠정정: 없음(correction 필드 없음).

#### **[49] 「외부 요청 0회로 대부분이 재현된다」가 거짓 — samples/는 저장소에 없다** — 소스 서베이 481행
- **근거**: `git check-ignore -v docs/sources/samples/102-terms.txt` → `.gitignore:25`. `git ls-files docs/sources/samples | wc -l` → **0**(추적 0/90). .gitignore 주석이 **정면으로 반대되는 재현 경로**를 적는다(「**외부 요청 필요**」). 이 워크트리엔 90파일이 있으나 **같은 머신의 공유 체크아웃 `bb-app/docs/sources/samples`는 없다** — 가설이 아니라 이미 존재하는 반례. 시간순이 원인 확정: 문서 `9292073` **22:24:29**, .gitignore `708dfd7` **23:38:09**(74분 뒤), 이후 `c3c47d7`도 이 줄을 안 고쳤다. 파급: 16행 · `docs/metrics/README.md:930`·`:1040`도 같은 성질 → 다음 조사자가 **약 70요청을 다시 쏘거나** 인용을 재대조 불가로 남긴다.
- **수정**: ⒜ 481행을 「**로컬 전용·저장소에 없다**(.gitignore 25행 · 708dfd7) · 클론한 사람에게는 0파일」로 ⒝ 484행 「결정되지 않았다」 → 「⑶으로 확정」, 489행은 가정이 아니라 현재 상태로 시제 정정 ⒞ 16행 표 ⒟ metrics README 두 자리 ⒠ war-xfip-recheck.md의 「외부 요청 0회」는 **그때의 기록이라 참 — 건드리지 마라**(부록에 한 줄만). 재발 방지: 문서가 인용하는 상대경로를 `git check-ignore`로 거르는 검사.
- **검증**: 경로가 여전히 ignore 대상인 채 두 문구가 남으면 STILL-BROKEN. 부수로 `git ls-files docs/sources/samples | wc -l`이 0인지(0이 아니면 반대 방향 수정).
- ⚠**정정**: ⑴ **「무추적 확정」이 문서보다 74분 나중이다** — 문서가 틀린 걸 쓴 게 아니라 **뒤의 결정이 문서를 낡게 만들었고 아무도 안 고쳤다** ⑵ 범위가 481행 하나가 아니다(16행 · metrics README 930·1040) ⑶ 484행 「결정되지 않았다」는 별개로 이미 거짓.

---

### P3 (7건)

#### **[13] `Retry-After` 무시 · 차단기 없음** — `fetcher.ts:45 / 131`
- **근거**: `packages/` 전체 `Retry-After` grep **0건**, 주입 시험에서 `Retry-After: 600`을 20 URL에 먹여도 한 번도 안 읽는다(읽는 헤더는 etag·last-modified 둘뿐). `archiveUrl`/`archivePlayers`/`archiveDates`/`update.ts:121-213` 어느 층에도 연속 실패 카운터 없음.
- **피해 방향이 반대**: 실코드 시뮬 `{urls:20, failed:20, requests:80, sleeps:[3000,6000,12000,24000], elapsed:900s}` → **1req/11.25초**로 평시(1req/3초)보다 **3.75배 느리다**. 45분 타임아웃 × 이 속도 ≈ **240요청**으로 평시 하루치(약 400)보다 적다. 침묵도 아님(`FAILED <url>` · exitCode=1 · 재시도 슬롯). `log.html`의 공개 문구(3초 이상·동시 1본)도 안 깨진다.
- **남는 위험 하나**: 서버가 「600초 기다려라」라고 말했는데 45초 만에 포기하고 계속 보낸다.
- **수정**: ⑴ delta-seconds와 HTTP-date **양쪽** 파싱(날짜면 주입 `clock` 기준 — `Date.now()` 금지), 파싱 실패는 조용히 0으로 흘리지 말고 지수 백오프 폴백(M11) ⑵ 상한(예 300초) 초과 시 자지 말고 **접는다**(재시도 크론 + 멱등이라 포기가 싸다) ⑶ 연속 429 카운터 차단기(성공 시 리셋) ⑷ 마지막 백오프 제거 ⑸ **차단 사실을 `PageResult.error`에 남긴다**(「받을 게 없었다」와 「우리가 멈췄다」 구별).
- **검증**: 3본 추가 — Retry-After 30 → `sleeps`에 30000(현재 3000) / 600 지속 → `calls===1` & 600000 없음(현재 calls=4) / 20 URL 429 → `calls ≤ 12`(현재 **80**). 회귀: 기존 25건 유지, ⑷ 적용 시 503 시험이 `[1000,2000]`으로 바뀌므로 **그 시험도 고치고 이유를 주석에**.
- ⚠**정정**: 「3s/6s/12s」는 재시도로 이어지는 세 번만 센 값 — 실측은 **[3000,6000,12000,24000] 네 번**이고 마지막 24초는 **요청 없이 버려지는 대기**다. 피해 방향도 뒤집힌다 — 「무례하게 난타」가 아니라 **「상대가 쉬라고 한 창 동안 평소보다 천천히, 그러나 계속 두드린다」**.

#### **[20] 호버로 연 툴팁 위로 포인터를 옮기면 사라져 넘친 내용에 못 닿는다**
- **근거**: 배포물에 `#tip{overflow-y:auto…}` · `mouseleave",hide` 실재, `#tip` 리스너 **0개**(호버 다리 없음). Chromium 149 실측: 1366×768 중앙에서 내용 555px 중 349px만 보임(**206px·37% 밖**), 1280×700 **220px·40% 밖**. 포인터를 툴팁 안으로 → `hidden===true` **12/12**(4뷰포트×3정렬), 가장자리 1·4·8·12·20px에서도 **5/5**, 휠 100px에 **12/12** 닫힘.
- **P3 근거(대체 경로 실측)**: 키보드 포커스로 열면 포인터가 들어가도 `hidden===false`이고 휠 300px에 `scrollTop=205`. `glossary.html`에 caveat 전문 + 선수 페이지 푸터 링크. 분모: 실 상자 기준 349px 초과 **1/56**(doused) · 250px 초과 8/56 · 200px 초과 17/56.
- **수정**: 셋 중 하나 — ⑴ `#tip`에 mouseenter/leave 다리(지연 hide 120ms) ⚠**터치 첫 탭 문제를 되살리지 마라(`downOpen` 토글 기준 유지)** ⑵ `#tip{pointer-events:none}` + 넘칠 때 `glossary.html#TERM_ANCHOR(key)` 링크(`glossary.ts:643`에 이미 있음) ⑶ 툴팁 분량 축소. ⚠어느 쪽이든 **잘렸다는 것을 화면이 말해야 한다**.
- **검증**: Playwright 스크립트 — `overflowPx`(현재 206) · `hiddenAfterPointerEntersTip` **True→False** · 그때 `tipScrollTop > 0`. ⑵로 갔다면 링크/「続き」 표시 존재 + `term-tip.test.ts` 6/6 유지.
- ⚠**정정**: **「마우스 클릭이 툴팁을 닫는다」는 결함이 아니다** — 의도된 토글이고 `term-tip.test.ts`가 명시적으로 고정(6/6 pass), 한 번 더 누르면 다시 열린다. 원 보고가 둘을 한 증상으로 묶은 것을 **분리해야 한다**. 툴팁 안에 링크·버튼 **0개**라 잃는 것은 **스크롤 하나**이고, **키보드로 연 경우엔 안 닫힌다** — 「닿을 수 없다」는 전칭이 아니다.

#### **[22] 등급 5색의 휘도가 단조가 아니다 (다크 vgood↔vbad CR 1.019)**
- **근거**: 배포 CSS 실측 10색. 다크 vgood↔vbad **1.019**, 라이트 1.309, good↔bad 1.142/1.101. `assets.test.ts` 38/38 pass이나 팔레트 시험은 hue만 재고 **휘도를 한 번도 안 잰다**.
- **함의는 벤치마크로 대부분 죽는다**: ColorBrewer RdBu-5 양끝 CR **1.131** · RdYlBu-5 **1.141** — **우리 라이트(1.309)가 두 표준안보다 낫다**. 비단조는 발산 배색의 정의(중앙 극값이 부호 인코딩). 완화도 배포 HTML에서 확인 — `<span class="vh">（とても悪い）</span>`로 등급명이 텍스트로 나가고 범례 다섯 견본에도 붙는다.
- **살아남는 것 둘**: ⑴ `assets.ts:508` 주석 「명도도 함께 벌려 흑백에서도 순서가 남게 한다」가 **실측으로 거짓** ⑵ 다크 1.02는 발산 배색 기준으로도 바닥(1.02 < 1.13 < 1.31).
- **수정**: 색 전체를 단조로 만들지 마라. ⑴ 주석을 실측에 맞게(필수·비용 0) ⑵ 다크 두 팔을 비대칭으로 벌린다(목표 ≥1.5, 패널 대비 3:1 유지 + hue 판정 유지해야 `assets.test.ts:81` 통과) ⑶ 휘도를 시험으로 못 박되 X=1.5면 현재 1.019가 붉어지는지 먼저 확인.
- **검증**: 배포 CSS에서 vgood↔vbad CR이 **모든 줄 1.5 이상** / `grep -n '흑백에서도 순서가 남게' assets.ts` **0건** / `assets.test.ts` 전건 통과(38, ⑶ 추가 시 39).
- ⚠**정정**: ⑴ 「V자」는 **다크에서만** 맞다 — 라이트는 **역V(Λ)**로 방향이 반대 ⑵ 「V자여서 흑백에서 순서가 안 남는다」를 결함으로 세우면 **ColorBrewer가 반박한다**(표준 CVD-safe 배색이 우리 라이트보다 나쁘다) ⑶ 실제 기준 미달은 **다크 1.02 하나**이고, 확실한 결함은 색이 아니라 **주석의 거짓 주장**.

#### **[43] CLAUDE.md 298-299 「적재가 여는 것은 box와 playbyplay 둘뿐」이 거짓**
- **근거**: `load-archive.ts`의 `html.gz` 참조 전건(분모 3) — 198 box · 345 playbyplay · **369 roster**. 실행 실측(임시 DB, 2026-08-15~16): 「성립 12건 · **명단 12장(실패 0)** · 선수 335명 · 투타 보충 544명 · 배번 보충 272명」 — **roster를 12/12장 열었다**. `git log -S"roster.html.gz"`가 커밋 1건(`6f6e525`)을 내는데, **그 발견을 고친 커밋 자체가 이 줄을 낡게 만들었고 줄이 안 고쳐졌다**. 이번 라운드 기수정 목록에 없음.
- **수정**: 「`box`·`playbyplay`·`roster` 셋(2026-08-17 roster 추가 · 6f6e525)」으로 고치고 ⚠**`index`만 안 읽는다**는 사유는 유지. `doc-figures.test.ts`에 참조 집합 대조 검사 추가(현 문장에서 붉어지는지 먼저 확인).
- **검증**: ① 참조 집합이 box/playbyplay/roster 3건이고 문서에 「둘뿐」이 없으면 exit 0 ② 회귀 — 적재 재실행 출력에 「명단 12장(실패 0)」.
- ⚠**정정**: 위치·증상 정확. 보탤 것 — 적재기는 이와 별개로 **월간 일정 페이지(`schedule_MM.html.gz`, 92행)**도 구장 조회표 때문에 연다(경기별 4장 밖이라 반례는 아님).

#### **[44] 핸드오버 문서가 존재하지 않는 헬퍼 `playerPath`를 「이미 있다」고 단언** — `docs/handover/2026-08-21-session.md:137`
- **근거**: ripgrep 워크트리 전수에서 `playerPath` 히트 **1건 = 문제의 그 줄**. `packages/web/src/`에서 **0건**, 문서 작성 커밋 시점에도 0. 실제 선수 링크는 헬퍼가 아니라 **인라인 템플릿 24곳**(`players/${`)이고, 같은 저장소 `assets.ts:3567`이 「⚠경로를 손으로 짓지 않는다(M1)」로 경고하는 바로 그 형태다. 다음 줄이 「`teamPath(code)`는 M1 단일 출처」로 이어져 **선수 쪽과 구단 쪽 상태가 같다고 오독시킨다**. 방어: `doc-figures.test.ts`가 지키는 문서는 3개뿐이고 핸드오버는 **어떤 시험도 안 본다**.
- **수정**: ⑴ 「선수 링크는 헬퍼가 없다 — 인라인 24곳」으로 바꾸고 **`playerPath` 문자열은 지워라** ⑵ 수를 얼리지 말고 **재는 명령 + 그때 값** 형식으로 ⑶ (권장) `doc-figures.test.ts`의 `DOCS`에 핸드오버 추가. ⚠헬퍼를 실제로 만드는 것은 **별개 작업**(은퇴 선수 분기·link-check 영향).
- **검증**: `grep -c playerPath` **0** + 137행이 「인라인 N곳」이고 N이 `grep -rhoE 'players/\$\{' packages/web/src/*.ts | wc -l`(현재 **24**), 구단 수가 `grep -rn teamPath packages/web/src | wc -l`(현재 **21**)과 일치.
- ⚠**정정**: 「teamPath 개수도 다르다」는 **현재 트리 기준으로만** 맞다 — 작성 커밋 시점엔 **20이 정확한 실측**이었고 이후 team-page.ts가 2→3으로 늘어 21이 됐다. 「21곳」도 날조가 아니다(작성 시점 인라인 생성 지점이 **정확히 21**, 현재 24). **수치 둘 다 작성 시점에는 실측이었고, 실제로 틀린 것은 「`playerPath`라는 헬퍼가 존재한다」는 이름·존재 주장 하나**다 — 「수치가 틀렸다」로 요약하면 원인을 놓친다.

#### **[47] BIP 판정이 「가격 30배」를 사유로 쓰는데 같은 파일이 그걸 금지한다** — 소스 서베이 84행
- **근거**: 84행 「**불가** — 가격 30배 · 개인 대상 아님 · …」 vs 365행 「⚠**「월 3만엔 = 상한의 30배」를 「현재의 사실」처럼 쓰지 마라**」. `grep -rn "30배" docs/ CLAUDE.md` **12건 중 11건이 취소선 또는 「4차 정정: 틀렸다」 표시**, **맨몸으로 사유로 쓰는 것은 84행 1건뿐**. 문서 결론부(§0-B 29 · §1-1 42 · §3-B 325)는 전부 「카탈로그에 좌표 없음」이 1차 — **84행이 고립점**. 부록 B(498~510)가 「우리 문서를 잘못 인용한 자리 셋」을 적는데 **이 자리는 거기 없다**(미인지). 배포물에는 안 나감(`grep -rl "3万円\|npbbip\|30배" dist-check/*.html assets _headers` → 0건).
- **수정**: 84행에서 가격을 사유에서 빼고 §0·§1·§3-B와 순서를 맞춘다(⑴카탈로그에 打球·座標 0건 ⑵개인 대상 아님 — 원문 `企業および研究機関向け` ⑶서비스 404). ⚠**감사 보고에 없던 자리 하나 더**: 134행 JBD 「우리 상한과 자릿수가 다르다」는 가격 비공개 상대에 대한 추측인데 **같은 표 122행이 스스로 금지**한다 — 같이 고쳐라.
- **검증**: `grep -n "30배\|자릿수가 다르다"`에서 84·134행이 사라지고 365행(금지)과 81행 주석만 남을 것.
- ⚠정정: 없음.

#### **[50] 「소스 전수 조사」인데 열거 방법이 안 적혀 있고 명단 밖 소스가 나온다**
- **근거**: 실측 — `baseball-data.jp` **200**(「プロ野球データバンク」) · `baseball-data-store.com` **403**(본문 미취득). 두 도메인은 `docs apps packages scripts` 전 파일 grep에서 **0건**(샘플 90개 포함). 문서 자신이 §2-4에서 계산값 사이트(nf3·SPAIA·Yahoo·npbstats·baseballab·NPB Scholar)를 실었으므로 「집계 사이트라 뺐다」는 자체 관행과 어긋난다. §6 한계 6줄은 다른 미측정을 전부 명시하는데(11구단 「못 찾았다」 · BIP PDF 미독 · Yahoo 재측정 안 함) **「집계 사이트 후보 목록을 어떤 질의로 만들었는지」는 0줄** → 작업규칙 7 위반.
- **수정**: ⑴ §6에 7번 행 추가 — 「§2-4 목록은 질의 기반 전수가 아니라 08-14 명단을 이어받은 것. 명단 밖에서 최소 2건. ⚠**「그 둘뿐」이 아니라 「지목된 둘을 확인했다」**」 ⑵ §2-4에 두 행 추가 + 「정하는 법: 톱페이지+robots+약관 3요청(0엔·10분), 단 계산값 사이트면 l.7로 자동 기각이라 **우선순위 낮음**」 + **하이픈 없는 `baseballdata.jp`는 별개 사이트**라는 각주. ⚠**CLAUDE.md의 「소스 20건 판정」도 같이 봐라**(건수가 늘면 낡는다).
- **검증**: §2-4·§6 양쪽 히트 + §6 행 수 6→7 + CLAUDE.md 「소스 20건」과 실제 건수 대조.
- ⚠**정정**: ⑴ `baseball-data-store.com`은 403이라 **「무료 NPB 통계 사이트」인지 확인되지 않았다**(원 보고도 미측정) ⑵ **`baseball-data.jp`와 `baseballdata.jp`는 다른 사이트**이고 후자는 `2026-08-14-feasibility-report.md:26`에 이미 판정이 있다 — 「이 부류가 통째로 미판정」은 과장 ⑶ **위치를 옮겨야 한다** — l.52 문장 자체는 행 라벨에 대한 선언이라 엄밀히 거짓이 아니고, 무너지는 것은 **제목의 「전수」와 §6 한계의 누락**이다.

---

## 4. ⚠원 보고가 틀렸던 것 (18/31 — 이 라운드 최대 산출물)

| id | 원 보고의 주장 | 실측 |
|---|---|---|
| **23**(사살) | 수집실패·시즌외가 배포물에 한 장도 없다 | 없는 건 **stateNote 문자열**뿐(0/15,341). 상태는 다른 문면으로 실재 — 수집실패 1,485 · 시즌외 13,856. 「M12 미구현」이 아니라 **「stateNote로 구현하지 않았다」**(설계 선택) |
| **40**(사살) | 해제 안 해 RSS 587→1,466MB | 인과 오류. 실보유 GC 후 **407MB**, RSS는 6번째부터 **827~829MB 평탄**. 상승분 대부분은 V8이 OS에 안 돌려준 페이지 + 로드 중 쓰레기. 「손봐야 한다」도 낡음(130시즌 여유, 벽은 Pages 파일 수) |
| **2** | outs 처리 175~193 | **179~194**. 추가: `derive.ts:211`의 wp/balk 비대칭 사유가 **63,315행에서 null 0건**이라 근거 없음(단 방향은 7필드→null 보존) |
| **4** | 단일 사본·세대 없음·검증 없음 | 전부 성립. **더 무거운 것 누락** — `--clobber`가 지운 뒤 올리고 실패 시 원본 소실인데 **주석은 「멱등」이라 적고 그 전제 위에 재시도 루프**. 「전량 소실」은 과장(개발기 우연히 97.8%) |
| **5** | (정정 없음) | 보탤 것 — `clock.ts:13` 죽은 disable 주석도 대상, **CLAUDE.md 318·447행에도 허위**. 완화: 8개소는 전부 진입점 1회라 M6 실질은 안 깨짐 |
| **13** | 백오프 3s/6s/12s | **[3000,6000,12000,24000] 네 번**이고 마지막 24초는 요청 없이 버려짐. **피해 방향이 반대** — 429 시 1req/11.25s로 평시의 1/3.75 |
| **17** | `assets.ts:2288` | **2325/2326**. 원 보고에 없던 두 증상 — 체크박스 행 0→3 점프 · ↓ 두 번이 원위치 |
| **20** | 마우스 클릭이 툴팁을 닫는다 | **결함 아님** — 의도된 토글이고 시험 6/6이 고정. 툴팁 안 링크 0개라 잃는 건 스크롤 하나. **키보드로 연 경우엔 안 닫힌다** |
| **22** | 휘도 V자 | 라이트는 **역V(Λ)**. 「V자라서 결함」은 **ColorBrewer가 반박**(RdBu-5 1.131 < 우리 라이트 1.309). 실제 미달은 **다크 1.02 하나**, 확실한 결함은 **주석의 거짓 주장** |
| **25** | (47/143)*143 사례 | 범위가 좁다 — **11·13 배수 전체**(g 23개 · count 30개), **2020(N=120)도 동일 결함** |
| **26** | 337,577개 | **낡은 `dist/`를 잼**(그 값과 정확히 일치). 현재 코드는 **374,740개·123.59 MiB**(개수 37,163 · 용량 16.17 MiB 과소). 원인은 두 줄이 아니라 **출력 경계에 제거 없음**, 소스 10개 파일 |
| **29** | 두 위치(766·3658) | **3658(현 3715)은 이미 명시 가드 있음**, 5357은 `rate()` 사용. **결함은 788 한 곳**. 틀린 수치는 렌더 안 됨(피해는 「記録がある選手」 6건) |
| **42** | deploy.md:391 | **408행**(f86a3d5가 26줄 삽입해 밀림). 내용·수치는 정정 없음 |
| **43** | (위치·증상 정확) | 보탤 것 — 월간 일정 페이지도 열지만 경기별 4장 밖이라 반례 아님 |
| **44** | teamPath 개수도 틀렸다 / 21곳 | **둘 다 작성 시점엔 실측**(teamPath 20 · 인라인 21). 한 커밋 만에 낡은 것. **실제로 틀린 건 「playerPath 헬퍼가 존재한다」 하나** |
| **46** | 대조값 33,136 / 266.8MiB | **분모가 다르다** — 그건 아카이브 전체. 같은 축의 값은 **31,220 / 250.8MiB**(52.5% · 56.7%). 「43MB」는 원인 미확정이라 **근거로 쓰지 마라** |
| **49** | 문서가 거짓을 적었다 | **74분 뒤의 결정이 문서를 낡게 만든 것**. 범위도 481행 하나가 아님(16행 · metrics 930·1040), 484행 「결정되지 않았다」도 이미 거짓 |
| **50** | 무료 NPB 통계 사이트 두 곳 미판정 | 하나는 **403이라 성격 미확인**. `baseball-data.jp` ≠ `baseballdata.jp`(후자는 08-14에 판정 있음). **위치를 l.52에서 §2-4 머리 + §6 한계로 옮겨야** 성립 |

---

## 5. ⚠재지 못한 범위 (「0건」과 「안 쟀음」의 구별)

**배포물 전수를 못 훑은 것 (타임아웃·범위 판단)**
- **1**: `dist-check/`에서 `game.fetched_at`을 찾는 검색 **안 돌렸다** — 「SELECT 코드 0곳」으로 상류를 막은 **추론**이지 15,341장 실측 아님.
- **2**: dist-check 스캔 안 함(상류 0건이 논리적으로 포함하나 **「dist-check 0건」이라고 쓰지 마라**).
- **5 · 43 · 44**: 배포물 안 뒤짐 — 배포 표면이 없다는 판단(43·44는 **「해당 없음」**).
- **19**: `.mfind`를 담는 장수 **못 쟀다**(전수 스캔 120초 타임아웃). `.qbox`만 15,341/15,341 전수, `.mfind`는 표본.
- **22**: 15,341장 전수 grep **못 함**. CSS는 1장이라 전수와 같지만 **등급 클래스가 붙은 페이지 분모는 안 셌다**(확인한 건 선수 페이지 1장).
- **25**: 15,341장 안 훑음 — index.html 9장(108행)만. **다른 페이지의 유사 「비율×상수 후 floor」는 미측정**.
- **29**: `"NaN"` 전수 grep **미완주**(`Infinity`만 완주, 히트 1건은 무관한 site.js).
- **46 · 47 · 49**: dist-check 전수 grep **타임아웃으로 미완** — 47은 루트 HTML 27개+assets+_headers 0건까지만, 49는 결과를 못 받음.
- **26**: script/style 안 주석은 **1/15,341장만** 봤다(index.html, 0건). 나머지 15,340장 미측정 — 이건 **수정 구현 방식**(정규식 가능 여부)에 걸린다.

**외부·운영 환경을 못 잰 것**
- **1**: **배포 D1(운영 DB) 안 쟀다** — 로컬 `data/bb.sqlite`만. 리포 밖(임시 SQL·D1 콘솔)에서 읽는지도 grep 범위 밖.
- **4**: **원격 릴리스 자산을 직접 안 봤다**(GitHub 요청 0회) — 「사본이 하나」는 워크플로 코드 실측이지 릴리스 조회 아님. 워크플로 미실행 · 45분 타임아웃 근접도 미측정 · 로컬 사본 무결성 미검사 · **npb.jp 롤오프 미측정**(「재취득 가능하니 괜찮다」가 그만큼 약하다).
- **13**: **npb.jp가 429를 내는가·Retry-After를 붙이는가 — 안 쟀다**(이 지적의 발현 확률 전체가 이 위에 얹혀 있다). 운용 중 429 이력도 **잴 수단이 없다** — `collection-log`가 HTTP 상태를 **0건** 담는다(「난 적 없다」가 아니라 **「났어도 기록이 안 남는다」**). 차단 승격은 **원리적 관측 불가**. 240요청은 산수이지 CI 실측 아님.
- **26**: **Cloudflare 엣지가 주석을 떼는지 라이브 미확인** — ⚠**만약 뗀다면 전송량 논거가 죽고 P3가 된다**(이 판정을 뒤집을 수 있는 유일한 자리). brotli는 4파일만(전수 아님).
- **50**: 두 사이트의 A/B/C/D 보유 · robots · 약관 **전혀 못 쟀다**. **「빠진 게 이 둘뿐인가」도 안 쟀다 — 2건은 하한이지 개수가 아니다**.
- **47**: 「月額3万円から」가 지금도 유효한 가격인지 **잴 수 없다**(404 · 웨이백 마지막 200이 2023-04-25). 외부 실접속 안 함.
- **48**: 원 조사 15건 목록이 **저장돼 있지 않아 재구성 불가** — 「4건이 정확히 어느 것인가」 미측정. GitHub total_count가 하루 만에 126→12로 변동해 **오늘 재현이 그날과 같다는 보장도 없다**.

**데이터 범위를 못 잰 것**
- **2**: **2016~2017 못 쟀다**(아카이브 2018~2026). 「投球回는 읽히는데 自責点만 안 읽힌다」 시나리오는 **재현하지 못한 추정** — 증거가 아니라 위험 서술.
- **25**: 2026이 143경기 도달했을 때의 값 **못 쟀다**(현재 tg 105~111) — 「지금 0건」은 **면역이 아니라 아직 안 걸린 것**. 143·120 외 시즌 미측정. 정렬 tie-break 영향 미측정.
- **29**: CS(17행)·일본시리즈(5행)의 outs=0은 **시즌 합계 기준으로 안 쟀다**(regular만). 2016·2017 없음. 같은 패널에 outs=0이 둘 이상 들어와 `Infinity-Infinity=NaN` 비교자가 되는 경우의 정렬 거동 **미실행**.
- **43**: 적재기를 **전 아카이브로 안 돌렸다** — 분모는 12경기. 전 시즌 roster 열기 성공률 미측정.
- **45**: CS·일본시리즈·올스타의 played/notPlayed 분리 **미확인**. 「890」이 어떤 SQL에서 나왔는지 **재현 안 함**(858+32로 역산 추정). 다른 시즌 전수 대조 안 함(2018·2020·2021 셋만).
- **46**: 9시즌 백필 **실제 벽시계 시간 없음**(36시간은 곱셈). 일정·선수·성적 페이지가 몇 건 얹히는지 **안 쟀다** — 31,220은 **하한**. 「43MB」 원인 미확정.
- **49**: 사용자가 ⑶을 실제로 승인했는지 **못 쟀다**. 다른 머신의 samples 사본 미측정(확인한 건 2곳).

**브라우저·실사용자를 못 잰 것**
- **17**: **진짜 브라우저 포커스 안 쟀다** — dom-stub에 `activeElement`/`focus()`가 없어 잰 건 **노드 동일성**이고 「포커스가 body로 떨어진다」는 **사양 근거 추론**. 스크린리더 거동 0건 측정. 6,208장은 문자열 매치이고 표본은 소스 1곳.
- **19**: **브라우저 렌더링 실측 안 함**(전부 토큰 산술 — 안티에일리어싱된 1px의 실효 대비는 더 낮을 수 있으나 그것도 안 쟀다). 저시력·색각 실사용자 0건. UA 기본 스타일 브라우저별 실측 안 함.
- **20**: **페이지 1장만** 쟀다(1/15,341). 합성 포인터이지 실물 마우스 아님. 터치 단말 미측정. 「잘렸다는 신호가 보이는가」는 픽셀로 답 못 함. 56항목 중 **27항목만** 실제 렌더 경로(나머지 29는 **하한**). 1280×600 이하 미측정. ⚠**별건 발견**: 용어집 페이지에 `**通算でのみ**`처럼 **별표가 글자 그대로 찍혀 있다** — 범위 밖이라 안 쟀다.
- **22**: **실제 그레이스케일 렌더링 눈으로 확인 안 함**(WCAG 산식만). APCA/CAM16 미사용. 전색맹 유병률·실사용자 미측정. **@media print가 어느 팔레트를 쓰는지 안 쟀다**. 배경 대비 3:1 미달 4건(라이트 good 2.39·avg 1.69·bad 2.09 · 다크 avg 1.97)은 **범위 밖이라 안 쟀다**.
- **26**: 주석 374,740개를 **전수로 읽지 않았다**(12문장만) — **시크릿 혼입 여부는 「없다」가 아니라 「못 쟀다」**. 주석 제거 후 68본 통과 여부 미측정. LCP·전송 시간 미측정.

**시험·이력을 못 잰 것**
- **2**: 격리 화면(`log-page.ts`)이 새 격리 종류를 어떻게 다루는지 안 봄.
- **5**: JST 자정 경계 런타임 실험 안 함. **git 이력 안 봄**(eslint가 있다가 지워졌는지 미확인). `.tmp-scout/`·`docs/`·`build/` 미측정. `.claude/*.json` 훅은 grep만(파일 미열람).
- **24**: plans 배선은 `buildSite` 시그니처까지만 — 빌드 스크립트 미독. **2026-11월 오프시즌 배포 로그는 존재하지 않아 실측 불가**(코드 실행 재현으로 대체).
- **42**: 다른 문서(decisions/sources)의 같은 수치는 grep만, **문서별 「최신성 요구 여부」 미확인**. `doc-figures.test.ts` 전문 미독(grep만).
- **43 · 47**: 이 줄을 지키는 **방어 시험이 존재하지 않는다**(grep 0건) — 「전부 통과」가 아니라 **「대상 0건」**.
- **44**: 부모 저장소·다른 워크트리·ts/js/md 외 확장자 미측정. 인라인 24곳이 실제 404를 내는지 미측정. 다른 핸드오버 수치 미검사.
- **13**: `cli-players.ts`·`cli-starters.ts`·`cli-stats.ts`는 **코드만 읽고 시뮬 안 함**(실측 분모는 `get()` 1경로).

---

## 5-A. 문서 정합성 묶음 — **끝났다** (2026-08-24)

`[46]`→`[42]` 다음으로 나머지 **7건을 한 배치로** 처리했다(감사가 요구한 순서).

| id | 무엇을 고쳤나 | 새 장치 |
|---|---|---|
| **43** | 「적재가 여는 것은 box·playbyplay 둘뿐」 → **셋**(roster 포함) | ✅ **소스와 대조하는 시험** — 문서가 아니라 `load-archive.ts` 가 정본 |
| **44** | 「`playerPath` 계열 21곳」 → **그 함수는 존재한 적이 없다**(grep 0) | — (수 대신 **재는 명령**을 적었다) |
| **45** | 2021 「정규 890」 → **858**(890 = 실시 858 + 미실시 32) | — |
| **47** | BIP·JBD 판정에서 **가격을 사유에서 뻐다** | — |
| **48** | 「전부 스크레이퍼」 → **본 15건이 전부**(126 중) | — |
| **49** | 「외부 요청 0회로 재현」 → **samples/ 는 저장소에 없다** | ✅ **문서가 가리키는 경로가 추적 중인지 검사** |
| **50** | 「20건 전수 조사」 → **판정한 20건 중** + §6 에 한계 7번 행 | — |

⚠**`[49]` 가 가장 실질적이었다** — `docs/sources/samples/` 가 `.gitignore` 로 **추적 0/90** 이라
「외부 요청 0회로 재현」은 **이 워크트리에서만 참**이고 **클론한 사람에겐 거짓**이다.
그걸 믿은 다음 조사자는 **약 70요청을 L1 준수로 다시 쓏거나** 인용을 재대조하지 못한다.
→ 새 시험이 **감사가 든 두 자리를 정확히 잡았고**, 감사가 안 적은 **세 번째 자리 하나를 더** 찾았다.

⚠**`[43]` 은 「고침이 문서를 낙게 하는」 모양이었다** — 바로 위 표가 지적한 결함을 고친
커밋(`6f6e525` · roster 추가)이 그 줄을 낙게 만들었고 **그 줄을 안 고쳤다.**
그래서 이번엔 **수를 적지 않고 소스를 정본으로 삼는 시험**을 붙였다.

---

## 6. 수정 순서 제안

| 순 | 대상 | 이유 |
|---|---|---|
| **1** | **[4] `--clobber` 창 제거 + 세대 보관**(daily.yml ①만이라도 즉시) | 유일한 **소급 불가 자산**을 하루 한 번 스스로 지우는 창이 열려 있다. 프로젝트 자신이 D-4로 등록해 두고 「순서와 무관하게 빠를수록 좋다」고 적었다. ①은 가장 싸고 가장 크다 |
| **2** | **[24] 오프시즌 stale 오진** | P1 · 매년 11~3월 **매일** 재현이고, 이미 지목된 구멍인데 절반만 고쳐졌다. 수정 범위가 or 조건 하나로 작다 |
| **3** | **[25] `paceOf` 절단** | **지금 화면에 틀린 숫자가 5개 떠 있다**(108행 중 5). 수정은 한 줄이고 검증 명령이 결정적 |
| **4** | **[1] `game.fetched_at`** + **[2] 투수 7열 `?? 0`** | 둘 다 적재 파이프라인 · M4/M7/M11 · 처방(`fetchedAtOf` · outs 가드)이 이미 있다. **1은 재적재까지 해야 값이 바뀐다** — 재적재 타이밍에 2도 같이 |
| **5** | **[29] `pitchesPerOut` Infinity** | 하류 포맷터의 부수효과에 목숨이 걸린 상태 해소. `pitchesPerOutOf()` 한 벌 뽑기로 M1도 같이 |
| **6** | **[5] 없는 린트 + 거짓 단언** / **[26] HTML 주석** | 5는 「강제한다던 기계 검사가 없다」라 **다른 규약의 신뢰도에 걸린다**. 26은 ⚠엣지가 주석을 떼는지 먼저 확인해야 우선순위가 확정된다 |
| **7** | **문서 정합성 묶음 — [42][43][45][46][47][49] + [44][50][48]** | 한 번에 처리해야 서로 안 어긋난다. **[46]→[42] 순**(파일 수·요청 수가 서로 참조). [49]는 다른 문서 3곳(16행 · metrics 930·1040)까지, [47]은 134행까지 같이. **[50]을 고치면 CLAUDE.md 「소스 20건」이 낡는다** |
| **8** | **[19] 입력칸 대비** / **[17] 편집기 포커스** | 19는 전 15,341장에 실리고 선례(tr.thin → `--tx-3`)가 있다. 17은 **목록 순서 고정이 설계 변경이라 사용자 확인 필요**(작업규칙 3) — 포커스 복원만 먼저 |
| **9** | **[22] 다크 팔레트 + 주석** / **[20] 툴팁** / **[13] Retry-After** | 22의 **주석 정정은 비용 0이니 즉시**, 색 조정은 나중. 13은 발현 확률 전체가 미측정 위에 있으나 **fix ⑸(로그에 HTTP 상태 남기기)는 「다음 사람도 못 잰다」를 푸는 값이라 먼저 해도 좋다** |

⚠**전 항목 공통**: 새 시험은 **작업규칙 9(고치기 전 코드에서 붉어지는가)**를 통과한 뒤에만 채택한다 — 이 라운드에서 [17](3/3 fail) · [25](5건) · [13](3본) · [26](374,740) · [19](4케이스)는 **기대 붉음값까지 실측돼 있다**.

## 7. ~~판정 못 한 것~~ → **넷 다 판정했다** (2026-08-22)

원래 여기에 **id 3 · 6 · 18 · 21** 이 「죽었다도 살았다도 아니다」로 남아 있었다. 넷을 실측으로 돌렸다.

| id | 판정 | 실측 |
|---|---|---|
| **3** | ⚠**절반 사살** | 원 지적은 5열을 한 덩어리로 봤는데 **실측이 둘로 갈랐다.** `game_id` 가 `season/MMDD/{home}-{away}-{no}` 라 **`season`·`game_date`·`game_no` 는 7,805/7,805 전건이 키로 결정된다** — 갱신 안 하는 게 **맞다.** 반면 **팀 코드는 148/7,805(1.9%)가 슬러그와 다르다**(별칭 정규화 `bs`→`b`) — 별칭 표를 고쳐도 **기존 행이 안 낫는다.** → 그 두 열만 갱신 대상에 넣고 revision 도 올린다. ⚠**지금 틀린 값은 0건**이고 이건 잠재 결함이다 |
| **6** | **생존** | 문서 **611/254/620 · 2,395** ↔ DB **618/255/627 · 2,401**. 진행 중 시즌을 인용해 **매일 낡는다.** ⚠**게다가 `doc-figures.test.ts` 머리말이 「走塁 각주에 이미 적용한 규칙」이라고 적는데 적용돼 있지 않았다** — 없는 장치를 있다고 적는 그 병이 하나 더 |
| **18** | **생존** | 선택 표시가 배경색 하나. panel 대 panel-2 **라이트 1.129 · 다크 1.100**(3:1 의 절반 미만) 이고 **hover 와 똑같이 생겼다.** 화살표를 눌러도 **낭독기에 아무 말도 안 갔다** |
| **21** | **생존** | 막대 대 바탕 3:1 미달 **라이트 4/12 · 다크 8/12 · 합집합 12/12**(감사 수치와 일치). ⚠**감사가 안 적은 것 하나 더**: 敗 막대(`--hair-2`)는 **전 구단·양 테마 미달**(1.580 / 1.557). ⚠**다만 WCAG 미달은 아니다** — 勝은 위·敗는 아래로 위치가 뜻을 나르고 옆에 「○勝○敗○分 · ○試合」이 분모까지 있다. **가독성 결함**으로 다룬다 |

**고친 것과 못 박은 것**
- `#3` → `upsertGame` 이 팀 코드를 갱신하고 revision 을 올린다 · `game-upsert.test.ts` 5본
  (⚠**셋이 고치기 전 코드에서 붉었다.** 나머지 둘은 멱등과 「셋을 왜 안 넣었나」의 회귀 방지)
- `#6` → 완결 시즌 기준으로 다시 적었다: **완결 8시즌 6,875경기 · 어긋남 0 · 도루가 있던 4,229경기** ·
  **2025 정규 도루 921 · 도루자 381**. `doc-figures.test.ts` 가 DB 로 다시 센다(⚠**분모를 둘로 낸다** —
  도루 0인 경기는 0=0 으로 자동 일치해서 「전수 어긋남 0」을 실제보다 강해 보이게 한다)
- `#18` → 왼쪽 규칙선(`--tx-3` · panel-2 위 라이트 4.541 · 다크 4.611) + **몇 번째·누구인지 낭독**
- `#21` → 두 막대에 같은 1px 테두리. ⚠**敗를 더 진하게 만들지 않았다** — 같은 화면의 `.trecent` 가
  이미 그 실수를 겪었다(진 쪽만 또렷해서 팀이 나쁘게 읽혔다)
- ⚠`.trecent` 의 근거 주석 「구단 색은 월별 막대가 이미 배경으로 말한다」가 **거짓임을 확인하고 적었다.**
  **근거만 무너지고 결론(`--up` 사용)은 그대로다**

⚠**이 넷에서 원 보고가 완전히 맞은 것은 2건(18·21)이고, 1건은 절반(3), 1건은 맞았지만 부수 발견이 더 컸다(6).**
반증 라운드 전체 경향과 같다.