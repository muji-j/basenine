# 감사 종합 보고 — bb-app (npb-foundation 워크트리)

---

## 1. 한 줄 요약

**14관점 감사 → 블라인드 반증 2회를 거쳐 확정 13건 중 P0 0건 · P1 8건 · P2 4건 · P3 1건**(사살 2건, 중복 병합 후). 별도로 **반증을 거치지 않은 P2/P3 60건**(P2 31 · P3 29)이 남아 있고, **브라우저 실행이 0회**라 a11y·성능의 기하·런타임 축은 전부 「0건」이 아니라 **「안 쟀음」**이다.

> ⚠**감사 중 워크트리가 움직였다.** 커밋 `12bf8de`(선수 76,509곳 · 구단 51,517곳 링크)가 반증 도중에 들어와 P1 2건(#8 · #9)이 **소스 수준에서 해소**됐다. 두 건은 **재수정 지시를 내리지 마라** — 남은 것은 dist 재빌드뿐이다.

---

## 2. 확정 발견사항

### [P1 / D1-security] `.github/workflows/daily.yml:435`

**증상** — S1(지인한정) 인가 경계 전체가 Cloudflare 대시보드 설정에만 있고, 그것이 유지되는지 확인하는 코드·시험·CI 게이트가 **리포에 0건**이다. 그 사이 이 워크플로는 하루 3회 새 배포 별칭을 만든다.

**근거(실측)**
- 추적 파일 365개 전수 grep → 인가 관련 히트 2건, **둘 다 주석**(`layout.ts:588`, `layout.test.ts:224`). 게이트 0건.
- 방어 시험 **0/138** 파일. `.github/` 파일은 `daily.yml` 하나뿐이고 배포 앞 게이트는 L6(리포 비공개)용 1개 — **같은 잡에 동형 게이트 선례가 이미 있다**(169행).
- 엣지 인가물 없음: `dist/_headers` 5줄(CSP·nosniff·Referrer-Policy·COOP), `_worker.js`/`_routes.json`/`functions/` 0건.
- **현재는 지켜지고 있다**: prod 302 · 실제 별칭 302 · **배포된 적 없는 `deadbeef` 별칭도 302**(와일드카드 앱이 미래 호스트까지 선점 커버).

**재현 조건** — Cloudflare 계정에 협업자 추가, Pages 프로젝트 재생성, Preview access 토글로 자동 생성 정책(`Allow Members - Cloudflare Pages`)이 다시 붙는 순간. 그 뒤 다음 크론(최대 8시간)이 새 별칭을 또 만든다. **이 고장은 2026-08-15 에 실제로 한 번 났다**(`deploy.md:180-181` — prod 302 / 별칭 200 · 내용 그대로).

**영향** — 인가 경계 · **침묵**. 문서가 스스로 「경고가 없고 상태 코드로도 드러나지 않는다」고 적는다. 설정 플립은 자기치유되지 않는다.

**수정안 — 2단 게이트**(⚠1차 제안을 2차가 갈아엎었다)
1. **앞단(매 배포 · 시크릿 0)**: 435행 **위**에 무자격 curl 게이트. 무작위 해시 별칭이 와일드카드 앱으로 302 되는지 + prod AUD 를 못 박는다.
2. **뒷단(주 1회/수동)**: Access API 로 정책 층(`Used by applications` 1+1 분열) 확인. 새 시크릿 `CF_API_TOKEN`(Access:Read) 필요.

**검증법**
```bash
curl -sL https://$(openssl rand -hex 4).bb-app-7mk.pages.dev/ | grep -q 'Log in to bb-app - Cloudflare Pages'
curl -s -o /dev/null -w '%{redirect_url}' https://bb-app-7mk.pages.dev/ \
  | grep -q 'kid=3a221a604b448a571f306d79c26fc98b76df8d4947ce8abd94242eefd40a1048'
grep -c "bb-app-7mk" .github/workflows/daily.yml   # ≥1 이고 435행보다 위여야 한다
```

**반증 이력** — 1차: 4갈래 전부 생존, 「무방비한 잠재」로 확정. 2차: **API 검증법이 이 환경에서 ERROR**(`jq` 127 · 토큰 len 0)라 미검증 → 무자격 curl 로 대체 실증. 동시에 **`deploy.md:226-228`(「curl로 검증되지 않는다」)이 과장임을 실측으로 뒤집었다** — 정책에는 맞지만 **애플리케이션(AUD·앱 이름·IdP)은 익명으로 다 보인다**.

**곁가지 필수 정정 2건** — ⑴ `deploy.md:226-228` 에 「정책은. 애플리케이션은 AUD·이름으로 검증된다」를 붙일 것(이 문장이 **게이트를 안 만드는 근거로 두 번 인용됐다**). ⑵ prod 앱은 Cloudflare OAuth + Email OTP, 와일드카드 앱은 Email OTP 만 — **비대칭 사유가 문서에 없다**(⚠IdP 가용성으로 정책을 판정하지 마라).

**분류** — 신규 확정

---

### [P1 / D2-concurrency] `packages/store/tools/load-archive.ts:433`(투수 448)

**증상** — 선수 표시명이 **아카이브 최초 등장 경기**의 이름으로 영구 고정된다. 등록명이 바뀌어도 화면은 옛 이름을 계속 낸다.

**근거(실측 · 5층 전부 재현)**
- 코드: `if (!seenPlayers.has(id)) { …; upsertPlayer(…) }` — `sortedEntries` 가 날짜 오름차순이라 첫 접촉 = 가장 오래된 경기. `upsertPlayer` 주석은 「표시명은 **최신**으로 갱신한다」고 적는데 이 가드가 무력화한다.
- 아카이브 box **7,805장 전수 재파싱**(parseErr 0): `DB == 최초 이름 1,640/1,640` · `DB == 최종 이름 1,531/1,640` → **드리프트 109명(6.6%), 전원 옛 이름**.
- **배포물**: `dist` 15,439장 중 **12,862장(83.3%)** 이 109명 중 최소 1명을 옛 이름으로 렌더(출현 137,996회 · 109/109 전원 등장).
- **사이트가 한 DOM 노드에서 모순**: `dist/players.html` → `data-name="アブレイユ"` + `data-kana="アルベルト・アブレウ"`.
- **2026 동명 충돌 2건 실물 확인**: `dist/teams/m.html` 에 `01105135">石川` 와 `61165139">石川`(NPB 는 2026 에 `石川慎`/`石川柊` 로 구별한다 — **충돌은 우리가 만들었다**). `dist/teams/h.html` 의 `山本` 2명도 같다.
- **자기치유의 반대**: `scripts/update.ts` 가 `--from` 없이 매일 전 아카이브를 재순회 → **매 실행이 2018-03-30 시점으로 재고정**한다.
- ⚠**의도된 설계가 아님이 증명된다**: `packages/store/test/store.test.ts:286` 「선수 표기는 갱신되지만 ID는 그대로다 — 등록명 변경은 정상이다」가 `assert.equal(display_name, "新名")` 로 **정반대를 고정한 채 초록**이다(33/33 pass). `seenPlayers` 는 최초 커밋에 **정당화 주석 0줄**로 들어왔고 `docs/` 의 `display_name`/`登録名` 언급 0건.

**영향** — **식별 오류**(수치 오류가 아니다 — 조인 키는 `player_id`). ⑴ 현재 이름으로 검색하면 못 찾는다 ⑵ 같은 페이지에 두 이름 공존 ⑶ 같은 팀 페이지에 다른 두 선수가 같은 이름 ⑷ 백필할수록 악화.

**⚠수정안 — 「최신이 이긴다」는 채택하지 마라**(2차 반증의 핵심 산출물)
NPB 박스 표기는 「신·구 등록명」이 아니라 **그 시점 로스터 식별 표기**다. 방향이 양쪽이다(늘어남 55 · 줄어듦 44 · 무관 10 — `松井雅→松井` · `山田哲→山田`). 드리프트 109명의 (선수 × 시즌) **620쌍** 기준:
- 지금(최초 고정) 틀리는 쌍 **298**
- 제안된 수정(최종 고정) 틀리는 쌍 **327** ← **29쌍 더 나빠진다**

→ 올바른 수정은 **표시명을 시즌(또는 경기) 스코프로 두는 것**이다(`batting_line`/`pitching_line` 이 이미 경기 단위로 근거를 갖고 있다).

**검증법** — 회귀 시험을 「최종과 같은가」가 아니라 **「그 페이지의 시즌에 박스가 쓴 이름과 같은가」**로 걸어라. 지금 코드에서 **298/620 으로 붉어진다**(작업규칙 9 충족).

**반증 이력** — 1차: 코드·DB·dist·시험 4층 생존 + 동명 충돌 2차 피해 발견. 2차: 세 검증 명령 전부 주장대로 재현(`アブレイユ 21 / アブレウ 0` 포함) + `store.test.ts:286` 발견 + **수정 방향 반전**.

**분류** — 신규 확정

---

### [P1 / D8-domain] `packages/aggregate/src/streaks.ts:106`(판정 137·138)

**증상** — 連続安打가 공인야구규칙 9.23(b)보다 **짧게** 나온다. 사사구·희생번트뿐이었던 경기가 기록을 끊는다.

**근거(실측)**
- SQL 이 `HAVING SUM(b.pa) > 0` 하나로만 거르고 137행이 `hit: d.hits > 0` 으로 판정 → 타석이 1개라도 있으면 안타 0 경기가 무조건 끊는다.
- 규칙 대상 경기(`pa>0 AND ab=0 AND sf=0`): 9시즌 정규 **3,554건**(2026 만 283건). 항등식 `pa=ab+bb+hbp+sh+sf` 가 **211,844/211,862** 행에서 성립하고, 깨진 18행은 전부 **打撃妨害**(9.23(b)가 「끊지 않는다」에 명시) → 매핑 정확.
- **실제 export 실행** = 배포 HTML 과 값·날짜 전부 일치: 辰己 `{best:9, 5/14〜5/24}` ↔ `dist/players/11015138.html` 「今季最長 9試合 5月14日〜5月24日」. 규칙대로면 **12試合(3/31〜4/15)**.
- 9시즌 전수 **4,179 선수-시즌**: best 어긋남 **90** · current 어긋남 **41** · best또는구간 **113**. 최악 −6(`dist/2019/players/63165132.html` 10→16). 2025 최악은 −5(蝦名 13→18).
- **형제 지표에도 있다**: 連続出塁의 9.23(c)(전 타석 犠打뿐이면 안 끊긴다) 미구현 → 추가 **33/4,179**.

**영향** — **침묵**. 오차가 항상 짧은 쪽 한 방향이라 그럴듯한 작은 수만 남는다. 툴팁은 「打席のなかった試合は数えません」만 말하고 그 근거로 **9.23(b) 자신의 논리**를 드는데, 정작 그 논리가 금지하는 일을 말없이 한다. NPB 공표 기록과 다른 수라 외부 대조에서 어긋난다.

**수정안** — 끊김 판정에서 `h=0 AND ab=0 AND sf=0 AND pa>0` 경기를 **건너뛴다**(빼는 게 아니다). SQL 에 `SUM(b.ab) ab, SUM(b.sf) sf` 추가. ⚠`sf=0` 조건을 빼면 안 된다(犠飛만 있는 경기는 규칙이 「중단된다」고 명시). ⚠`hitless` 에는 적용하지 마라. ⚠`docs/metrics/README.md` 에 **이 지표 항목 자체가 없다** — 규칙 출처와 함께 항을 만들 것(M3).

**검증법**
```bash
node --input-type=module -e "import {DatabaseSync} from 'node:sqlite';const db=new DatabaseSync('data/bb.sqlite',{readOnly:true});const gs=db.prepare(\"SELECT b.pa,b.ab,b.h,b.sf FROM batting_line b JOIN game g USING(game_id) WHERE b.player_id='11015138' AND g.season=2026 AND g.competition='regular' AND g.status='played' AND b.pa>0 ORDER BY g.game_date,g.game_no\").all();const run=a=>{let c=0,b=0;for(const x of a){if(x){c++;if(c>b)b=c}else c=0}return b};console.log('code',run(gs.map(r=>r.h>0)),'rule9.23b',run(gs.filter(r=>!(r.h===0&&r.ab===0&&r.sf===0)).map(r=>r.h>0)))"
# 지금: code 9 rule9.23b 12  → 고치면 둘이 같아야 한다
```

**반증 이력** — 1차: 4경로 전부 생존, 방어 시험 12/12 초록인데 이 경우를 밟는 것 0본(`seed()` 헬퍼가 (0,0)/(4,4)만 만들어 **구조적으로 표현 불가**). 2차: 규칙 매핑을 항등식으로 검증 + 실제 export 실행으로 dist 인과 확정 + 형제 지표 33건 추가 발견.

**분류** — 신규 확정

---

### [P1 / D6b-behavior] `packages/web/src/query.ts:315`(투수판 332)

**증상** — 스플릿 「走者状況」이 표준 용어 **「走者あり」를 「1루에만 주자」**라는 뜻으로 쓴다. 화면에 「走者あり 86打席」과 「得点圏 86打席」이 나란히 서고, **부분집합이 전체보다 큰 분모**를 갖는 페이지가 4,152장이다.

**근거(실측)**
- `splits.ts:50-53` 의 3분류가 배타적: `''`→empty / `%2%|%3%`→scoring / **else→onBase = `bases='1'` 뿐**.
- DB `bases` 값 집합이 정확히 8개: `''`318,069 / `1`102,437 / `2`46,939 / `12`39,866 / `13`15,617 / `3`13,839 / `23`12,223 / `123`14,843. 표준 어의의 走者あり = **245,764** 인데 화면이 그 이름으로 부르는 것은 **102,437(41.7%)**. 리그 전체로도 得点圏(143,327) > 「走者あり」(102,437) 로 뒤집혀 있다.
- 배포물: `dist/players/41445153.html` → `走者なし 262 / 走者あり 86 / 得点圏 86`(합 434 = 총타석). `dist/players/13315153.html`(투수) → `走者あり 91 / 得点圏 115`.
- 전수: `走者あり` 를 그리는 페이지 **5,304장** · `data-panelkey="base"` 보유 **5,668장** · 그중 得点圏 > 走者あり 인 것 **4,152장**.
- **같은 저장소가 정답 이름을 이미 갖고 있다**: `game-page.ts:98-107` `BASE_LABEL` 이 `bases==='1'` 을 **「一塁」**로 부른다(8키가 DB 8값과 정확히 일치). **한 제품 안에 같은 데이터의 두 이름.**
- 설명 경로 0: `走者あり` 5,304회 출현 주변에 `data-term`·`title`·`<abbr>` **0건**, 글로서리에 `onBase` 항목 없음(`scoring` 만 존재).

**영향** — **침묵**. 값도 분모도 맞아서(M2 형식 통과) 타입·린트·시험 어느 것도 못 잡는다. 유저는 표준 해석으로 **거짓 사실**을 얻고, 78.3% 의 페이지에서 화면이 스스로 모순된다. ⚠**진짜 침묵인 것은 나머지 998장**(286 같음 + 712 得点圏<走者あり) — 거기서는 모순조차 안 보인다.

**수정안** — `SPLIT_KEY_LABEL`·`PITCHER_SPLIT_KEY_LABEL` 두 곳(315·332)의 `onBase` 를 `"走者一塁のみ"`(또는 `"走者あり（得点圏以外）"`)로. 표준 뜻이 필요하면 **네 번째 막대 「走者あり（計）」**를 `onBase+scoring` 으로 추가하고 합계=총타석을 시험으로 고정. 동시에 `glossary.ts` 에 `onBase` 항목 + 스플릿 라벨의 `data-term` 승격.

**검증법** — 「得点圏 분모 > 走者あり 분모」인 페이지 수가 **4,152 → 0** 이어야 한다. ⚠**게이트에 함정이 있다**: 走者あり 막대를 지워도 0이 나온다 — **走者あり(또는 후속 라벨)를 그리는 페이지 수가 유지되는지 같이 세라.** ⚠`node --test packages/web/test/` 는 FAIL 이 아니라 **ERROR**(MODULE_NOT_FOUND) — 올바른 호출은 `node --test "packages/web/test/*.test.ts"` 이고 결함 존재 상태에서 **1034/1034 pass**다.

**반증 이력** — 1차: 4경로 생존, 진짜 침묵은 998장 쪽임을 지적. 2차: `BASE_LABEL` 내부 모순 발견, dist stale 가설 기각(`git show HEAD:` 바이트 동일), ⚠**주장의 분모 표기 오류 정정**(「4,152 / 5,304」 → 실제 **4,152 / 5,668**).

**분류** — 신규 확정

---

### [P1 / D7-consistency + D8-domain] `packages/web/src/query.ts:524`(및 551-552)

> ⚠D7(문구 일관성)과 D8(NPB 도메인)이 같은 결함을 다르게 보고했다 — **병합했다.**

**증상** — 구원 자격선이 **NPB 에 존재하지 않는 이닝 표기**로 나온다. `規定投球回の3分の1（47.7回）` — NPB 표기에서 소수 첫자리는 0·1·2 뿐이고 143아웃은 **47.2回**다.

**근거(실측)**
- `query.ts:524` `const one = (outs) => Math.round((outs / 3) * 10) / 10;` — 같은 식이 **551-552 에 손으로 한 번 더** 적혀 있다(바로 위 주석이 「값을 여기서 다시 계산하면 어느 날 한쪽만 고쳐진다」고 금지하는 그 일). 같은 파일이 214행에서 `innings()` 를 이미 import 하고 있다.
- 문장 전수 **9,381** 중 불가능한 표기 **8,481(90.4%)**: 47.7回 6,888 · 34.7〜37回 828 · 34〜36.3回 765. 페이지 기준 **1,167/15,340(7.6%)**.
- **같은 페이지가 자기 순위를 부정한다**(두 시즌 독립 실측): `dist/2025/ranking.html` 퍼시픽 — 143아웃=`47.2回` 田中 **3위** · 山田 **9위**, 140아웃=`46.2回` `—`. `dist/2024` 도 142아웃 `—` / 144아웃 순위 있음 → 경계는 143아웃. **NPB 표기로 읽으면 47.2 < 47.7 이라 순위가 있는 투수를 기준문이 실격시킨다.**
- 툴팁만이 아니다 — `dist/2025/ranking.html` 에서 **14건이 `<p class="note">` 본문**.
- 같은 페이지의 `<span class="den">` 는 정본 `innings()` 로 만들어져 소수 1자리가 3~9 인 것이 **0건**. `packages/parser/test/box.test.ts:216` 은 `inningsToOuts("6.3") === null` 을 못 박는다 — **우리 파서라면 「47.7回」을 이닝으로 읽기를 거부한다.**
- **형제 자리**: `player-page.ts:1058-1059` 는 `Math.floor(outs/3)` 로 잘라 **「未満（35回 / 35回）」**(도달치=기준치인데 「未満」)를 낸다 — 9시즌 구원 1,919 선수-시즌 중 **12건**(2026 2건).

**영향** — **침묵(Q3)**. 값(순위)은 아웃 카운트로 계산돼 안 틀리고 **화면만 틀린 말을 한다** — CLAUDE.md 가 「가장 나쁜 종류의 오류」라고 적은 형태. M3(자격 기준은 값의 일부)·M1 둘 다 위반. **자기치유 안 됨**: 143경기 완결 시즌마다 결정적으로 47.7이 나오고, 문서(`docs/metrics/README.md:1201` 「35.7回」)와 시험(`team-page.test.ts:192` `36.3回`)이 이미 틀린 표기를 고정한다.

**수정안** — `query.ts:524`·551·552 의 인라인 식 3개를 지우고 이미 import 된 `innings()` 로. `player-page.ts:1058-1059` 도 같이. ⚠**아웃→이닝 변환이 이 리포에 6벌 있다**(`metrics/lines.ts:59` · `format.ts:71` · `query.ts:2904` · `assets.ts:2817` · `parser/career.ts:364` · `crosscheck.ts:174`) — **`innings()` 한 벌로 모아라**(M1). ⚠`metrics` 의 `inningsPitched` 는 프로덕션 호출처 **0곳**인 죽은 export 다. 문서·시험 기댓값도 같이 고치되 **같은 식을 다시 쓰지 말고 `47.2` 를 손으로 적어라**.

**검증법**
```bash
# ⚠제안된 정규식은 범위 왼쪽 끝(34.7〜37回 828문장)을 놓친다 — 값 단위로 걸어라
grep -c '規定投球回の3分の1（47.2回）' dist/2024/ranking.html   # >0
# ⚠grep -c '47.2回' 는 수정 전에도 13 을 반환한다(den 열) — 판별력 없음
```

**반증 이력** — 1차: 배포물 990건/121파일 + 순위표 자기부정 실증, 방어 시험 4본 전부 초록. 2차: **현 소스로 2024 재빌드**해 dist stale 가설 기각(191/1307 일치), 전 매치 문맥 분류로 정당한 소수 `回` 0건 확인, ⚠**검증법의 구멍 2개 정정**.

**분류** — 신규 확정

---

### [P1 / D6-design] `packages/web/src/assets.ts:1385`(보고서의 1380은 앵커 드리프트)

**증상** — 「今日の試合」·日別 페이지의 殊勲選手 목록에서 **폭 3px 구단색 막대가 유일한 구단 표시**인데, 그 색이 패널과 3:1 미만이라 사라진다.

**근거(실측)**
- `.gstars li i{width:3px;…background:var(--chip,#6b7280);…}` — **링(box-shadow) 없음**. 같은 파일이 `:666`·`:1692` 에서 `box-shadow:inset 0 0 0 1px var(--tx-2)` 를 **쓸 줄 알면서 여기만 안 썼다**.
- 바탕은 `.block{background:var(--panel)}`(라이트 `#ffffff` · 다크 `#1c1e23`). 12구단 대비: **다크 8/12 미달**(b 1.08 · m 1.10 · d 1.35 · l 1.46 · e 1.74 · f 2.40 · db 2.54 · c 2.83) · **라이트 4/12**(t 1.61 · h 1.64 · s 2.72 · g 2.79).
- 배포물: gstars 보유 **1,431장** · 별선수 행 **21,812건**.
- 방어 시험 **0건**: `grep -rn "gstars\|gsl\|gsd\|殊勲" packages/*/test scripts` → 출력 0줄. `css-contrast.test.ts` 의 `TEAM_MARKS` 는 타입이 `prop: "color" | "stroke"` 라 **background 가 구조적으로 배제**되고, `:102` 주석이 **「⚠장식(배경·테두리·그림)에는 써도 된다」**고 명시 — 면제가 의도된 것이다.

**⚠심각도 수치 3곳을 정정한다**(2차 반증)
| 원 주장 | 실측 정정 |
|---|---|
| 「어느 구단인지 알 수 없다」(12중 1) | **카드 안 두 팀 중 어느 쪽인지 못 가른다**(2중 1) — 카드 **7,210/7,210** 이 두 팀 이름을 텍스트로 담는다 |
| 21,787/21,787 | 상호배타적 두 테마의 OR = 팔레트가 강제하는 **항진명제**. 실제 노출률 **다크 11,250/21,812(51.6%) · 라이트 6,745/21,812(30.9%)** |
| 「66쌍 중 15쌍 1.5:1 미만」 | 휘도비를 색상 구별에 오용. 지각적으로 가까운 쌍은 **5/66**(ΔE00<12), 해당 행 **883/21,812(4.0%)** |

**⚠별건 P1 로 분리할 것 — 색과 무관한 a11y 결함**
`<i>` 의 실물 형태가 dist 전체에서 **단 1종 = `<i></i>`**, **21,812/21,812 가 내용·aria-label·title 전무**. `.gsd`(勝/敗/S/H, 4,475행 = 20.5%)를 인정해도 **17,307/21,812(79.3%)** 가 스크린리더에 소속팀을 전혀 전달하지 않는다. **색을 고쳐도 안 고쳐진다.**

**수정안** — ⑴ 즉효: `box-shadow:inset 0 0 0 1px var(--tx-2)`(이 파일이 이미 쓰는 3:1 경계 방식). ⑵ 제대로: 구단 약칭을 **보이는 글자로**(`.vh` 는 안 된다 — 저시력 사용자가 못 본다). ⑶ `css-contrast.test.ts` 의 `TEAM_MARKS` 에 `background` 축 추가 + 증식 가드에 `background:\s*var\(--(team|chip)` 추가.

**⚠검증법 교체 필수** — 원 제안 명령은 **CSS 를 한 글자도 안 읽고** `<li style="--chip:#hex">` 의 hex 만 본다. 실증: 실제 fix 3종(CSS 링 · aria-label · 구단명 텍스트)에서 **여전히 exit 1(거짓 FAIL)**, 무의미 변경 2종(li 속성 추가 · 따옴표 변경)에서 **exit 0(거짓 PASS)**. 대체 게이트는 **입력 hex 가 아니라 출력 채널**을 세라 — 각 `<li>` 가 색 이외 채널(구단명 텍스트 / aria-label / `.vh`)을 하나 이상 갖는가.

**반증 이력** — 1차: 4갈래 생존 + `<i>` 텍스트 대체 전무 보강. 2차: 명령이 주장대로 출력하지만 **수용 게이트로 쓸 수 없음을 5케이스로 실증**, 심각도 수치 3곳 정정, SR 결함을 별건으로 승격.

**분류** — 신규 확정

---

### [P1 / D5-a11y + D6-design] `packages/web/src/assets.ts:1356`(보고서의 1351은 드리프트)

> ⚠D5(P1)와 D6(P2)가 같은 `.gcard.off` 를 보고했다 — **병합했다.**

**증상** — 중지·미개최 경기 카드(`.gcard.off{opacity:.62}`)의 **모든 글자**가 AA(4.5:1) 미달. 구장명·양 팀명·「中止」가 전부 여기 있다.

**근거(실측 · 독립 구현 2벌로 재계산 · 기준값 대조 통과)**
```
              bg=panel        bg=page
.gvenue 10.5px  2.470 FAIL     2.419 FAIL
.gt     13.5px  2.787 FAIL     2.739 FAIL   (light)
.gnone  11.5px  2.470 FAIL     2.419 FAIL
.gvenue 10.5px  2.770 FAIL     2.878 FAIL
.gt     13.5px  3.409 FAIL     3.535 FAIL   (dark)
.gnone  11.5px  2.770 FAIL     2.878 FAIL
```
바탕을 어느 쪽으로 잡아도 **12/12 FAIL**. opacity 를 벗기면 4.91~7.23 로 전부 통과 — 원인은 토큰이 아니라 그 한 줄이다. 글자 크기 10.5/11.5/13.5px · weight 400 → **대문자 예외 없음**.
- 배포물(Node 직접 순회 — ⚠`rg` 는 `.gitignore` 에 `dist/` 가 있어 87파일/118건으로 **과소집계**된다): **208/15,340장(1.36%) · 284카드**. `.gnone` 어휘가 中止 269 + ノーゲーム 15 = **284 로 정확히 일치**.
- 실물: `dist/2022/days/2022-09-18.html` · `dist/2018/days/2018-09-04.html`. ⚠**원 보고서가 인용한 `dist/days/2026-03-27.html` 에는 off 카드가 0개**다(gcard 6건 전부 `tapcard`).
- **가드가 공회전**: `css-contrast.test.ts` 16/16 pass. 그 시험은 한 규칙에 `color:` **와** `opacity:` 가 같이 있을 것을 요구하는데, CSS 의 `opacity:0.x` 규칙 **13건 중 같은 규칙에 `color:` 가 있는 것 0건** — **오늘 잡을 수 있는 규칙이 0건인 채로 초록**이다.
- **한 번 고친 결함의 재발**: `.daystep.off{opacity:.5}`(1.99/2.31)는 `dist/assets/site.css:1548` 에서 `color:var(--tx-3);border-style:dashed` 로 이미 고쳐졌다. **`.gcard.off` 는 그 청소에서 살아남은 마지막 한 건.**

**⚠심각도 근거 양방향 정정**
- **올리는 쪽**: 중지 경기는 승자가 없어 `gside w` 가 **0개** → 「이긴 팀 굵은 글자만 통과」가 아니라 **카드 안의 모든 글자가 미달**이다(보고서가 오히려 과소평가).
- **내리는 쪽**: 중지 「건수」는 머리글 `.sub`(6.61/6.67 AA 통과)가 이미 온전한 대비로 말한다. 흐린 카드만이 가진 정보는 **어느 경기인가**(구장명 + 양 팀명).

**수정안** — `opacity` 를 걷어내고 `.daystep.off` 방식으로(`border-style:dashed` + 자손 색 명시 · `--tx-3` 는 4.91/5.50 통과). 「中止」는 오히려 `--tx` 로 **올린다**. 그리고 `css-contrast.test.ts` 를 **자손 상속까지** 보게 넓혀라(`opacity:0.x` 규칙의 서브트리에서 나올 수 있는 글자색을 합성해 4.5 측정) — 안 넓히면 **세 번째 `.off` 가 또 통과한다**.

**검증법** — `.gcard.off` 에 `opacity` 가 없으면 PASS. 지금은 4/4(또는 바탕 양쪽 12/12) FAIL.

**반증 이력** — 1차: 4갈래 생존, 가드 공회전 실증. 2차: 독립 구현으로 재계산 + `rg` 과소집계 함정 발견 + **인용 증거 3곳 정정**(HTML 파일 · 소스 라인 · 「굵은 글자만 통과」).

**분류** — 신규 확정

---

### [P1 / D3b + D6b] `packages/web/src/game-page.ts:202` — ✅ **소스 수정 완료(`12bf8de`) · dist 재빌드 대기**

> ⚠D3b(P1)와 D6b(P2 `game-page.ts:97`)가 같은 결함을 보고했다 — **병합했다.** 분모는 D6b 전수가 정확하다.

**증상** — 경기 페이지 타석 로그의 「対 <상대 투수>」가 링크 없는 순수 텍스트. 같은 화면 타순표 이름은 링크라 **같은 이름이 자리에 따라 눌리거나 안 눌린다**.

**근거(실측)**
- 배포물 전수: 게임 페이지 **7,502/7,502 장 전부**에 `<s>対 …</s>` **81,150건**, 링크 **0/81,150**.
- 그중 **37,148건(45.8%)** 은 그 이름이 그 페이지 어디에도 링크로 없다(distinct 733명). 「전부 다른 데서라도 눌린다」는 페이지는 **984/7,502(13.1%)**.
- ⚠원 보고서의 「현재 시즌 641파일 6,788건」은 정확하지만 **전수의 8%**다. handover 의 6장 표본 추정(약 35,000)은 **2.3배 과소**였다(개막 근처 표본 편향).
- 「링크하면 죽은 링크가 된다」 반론 **기각**: `ref()` 가 `nameOf` null 이면 `PlayerRef` 를 null 로 만들어 `対` 자체를 안 그린다 → **이름이 보이면 선수 페이지가 반드시 존재**. 2018(은퇴 선수 최다) 재빌드 실측 `{linked:10162, distinctHrefs:329, deadTargets:0}`.

**현재 상태 — 재수정 지시 금지**
- 시작 HEAD `d871b9c` 는 무링크였다(확정). 반증 도중 `12bf8de` 로 커밋됨.
- 커밋본으로 재빌드: 2026 `{plain:0, linked:6788}` · 2018 `{plain:0, linked:10162, dead:0}` · link-check 「깨진 것 없음」. 시험 **23/23 pass**(19→23).
- **남은 것은 dist 재빌드 하나**(`dist` 는 04:01 판이라 여전히 81,150곳이 안 눌린다).

**남은 확인 2건**
1. 2026·2018 외 **7시즌은 「죽은 링크 0」이 아니라 「안 쟀음」** — 9시즌 전체 빌드에서 link-check 통과를 한 번 봐야 한다.
2. 같은 커밋이 구단 링크도 넓게 붙였는데, **편집 도중 상태에서 올스타 코드(`cl`/`pl`)로 배포 게이트가 걸리는 것을 실제로 봤다**(커밋본에서는 해소). 구단 링크 확장 시 재발 지점.

**반증 이력** — 1차: 4갈래 생존 + 대체 경로 census. 2차: 검증법 그대로 실행해 등식 성립 확인(재빌드 후 6,788 = 재빌드 전 6,788), 2018 로 죽은 링크 가설 출력으로 기각, 병행 커밋 포착.

**분류** — 기지(재확인) → **해소(dist 대기)**

---

### [P2 / D3b-correctness] `packages/web/src/team-page.ts:813·856` — ✅ **소스 수정 완료(`12bf8de`) · dist 재빌드 대기**

**증상** — 구단 페이지 「連続記録」·「記録に近づいている選手」 표의 선수명이 링크 아님.

**근거(실측)** — dist 전수 **108장**(9시즌 × 12구단), 명칭 셀 **923개 중 링크 0개**(연속 59 + 근접 864). 시즌별로도 새어나가는 곳 0. 런타임 구제 없음(`site.js` 에 `tstreak`/`tmile`/`closest("tr")`/`data-href`/`location.href` 전부 0건).

**⚠분자 표기 오류 2건(작업규칙 7 위반 · 별건 P3)**
- 원 지적 「**1,547**개 중 0개」 → 실제 **923**(1.68배 부풀림)
- 커밋 `12bf8de` 의 새 시험 주석 「링크 아닌 선수명이 **833곳**」 → 실제 **923**(90곳 적게)
→ **인수인계에 그대로 남기면 다음 대조가 어긋난다.**

**심각도 조정 근거** — P1 → **P2**: ⑴ 값·분모는 정확하고 이름이 안 눌릴 뿐(내비게이션 결함) ⑵ 막다른 길 아님 — **923/923 이 같은 페이지의 `b-teambat`/`b-teampit` 표에서 이미 링크로 존재**(resolved 923/923 · fileMissing 0).

**⚠뿌리는 안 없어졌다** — M1 위반(같은 `HomeStreak`/`HomeMilestone` 을 `home-page.ts` 와 `team-page.ts` 가 **따로 렌더**). 링크만 맞췄지 부품은 여전히 두 벌이라 **다음에 이 표가 바뀌면 또 한쪽만 바뀐다.**

**현재 상태** — 시험 61/61 · `tsc --noEmit` EXIT=0 · 새 링크 404 위험 0/923. dist 는 04:01 판.

**분류** — 신규 확정 → 해소(dist 대기)

---

### [P2 / D9-figures] `docs/decisions/2026-08-16-public-release-assessment.md:32`(및 30·54·65·80)

**증상** — 「공개 배포 가부 검토」의 규모 수치가 전부 1시즌 시절 값이라 **L3·L5·B5·L-1 네 칸이 어긋난다.** L5 는 부호가 반대다.

**근거(실측)**
| 문서 | 실측 | 배율 |
|---|---|---|
| L30 출처 명기 「3,213장 전부」 | dist HTML **15,340장**(결손 0/15,340 — 판정 ✅ 는 유효) | 4.8배 |
| L32 「생년월일 695명 · 체격 695명」 | `birth_date` **0** · `physique` **980** · `player` 1,640 | 부호 반대 / 1.41배 |
| L54(B5) 「주석 9,646개 · 1.59MB」 | **337,577개 · 107.42MiB**(dist HTML 바이트의 **10.81%**) | 35.0배 / **70.8배** |
| L65(L-1) 「858명」 | `player` **1,640** · dist 고유 선수 페이지 **1,641** | 1.9배 |
| L80 「playbyplay는 2025년분부터만」 | 9시즌 전부 존재(2018 67,352 … 2026 47,795) | CLAUDE.md §2-2 가 명시 철회 |

- L5 원인은 `migrations/016-birth-year.sql`(2026-08-18): 「⚠**되돌릴 수 없다**」 + `UPDATE player SET birth_date = NULL`.
- ⚠**부모 전제의 「54.89MiB」 자체가 과소치다** — 정확히 1.957배 차이는 한국어 텍스트를 UTF-16 코드유닛(`String.length`)으로 센 비율이다. 실제 **107.42MiB**.
- **B5 는 실제로 실려 나간다**: 주석 제거·minify 구현 리포 전역 **0건**, 워크플로가 `dist` 를 그대로 올린다.
- **가드 0건**: `grep -rn "public-release-assessment"` → **0건**. `doc-figures.test.ts` 는 3본만 읽고 **4 pass / 0 fail** — 문서가 틀린 채 초록. `git log` 단일 커밋, 대체 문서 없음.

**심각도 조정 근거** — P1 → **P2**: ⑴ **판정(✅/❌)이 하나도 안 뒤집힌다**(L3 ✅ 유지 · L5 ✅ 유지 — 전체 생년월일 렌더 0/698 · B5 는 **이미 블로커 표에** · L-1 은 **이미 「가장 미해결」**). 실효 결론 「아직 공개하면 안 된다」는 그대로. ⑵ 런타임·데이터·유저 노출 영향 0. ⑶ 리포 선례상 동종(CLAUDE.md 낡은 소급 범위)이 **P2** 였다.

**⚠닫지는 마라** — 넷 중 **셋이 같은 방향(위험이 실제보다 작아 보이는 쪽)**이다(B5 70.8배 · L-1 1.9배 · L5-체격 1.4배). 안전측인 것은 L5 생년월일 절반뿐이라 상쇄되지 않는다.

**수정안** — 규모 4칸 실측 갱신 · L5 를 「birth_date 0건 · birth_year 980명 — 마이그레이션 016 이 일 단위 전량 삭제」로(저감 사실을 남긴다) · L80 삭제 · **이 파일을 `doc-figures.test.ts` 의 `DOCS` 에 넣어라**.

**검증법** — `total 1640 / bd 0 / ph 980` · `find dist -name '*.html' | wc -l` → 15340.

**분류** — 신규 확정

---

### [P3 / D10-sources] `CLAUDE.md:115`

**증상** — 「타구 4분류를 정당하게 주는 소스는 **20건 전수 조사에서 0건**」인데, 그 근거인 08-20 판정표에 우리 자신의 08-14 보고서가 「가격 전부 미확인」으로 남긴 **상용 벤더가 한 줄도 없다**.

**근거(실측)**
- 08-20 문서 grep: `sportradar` **0** · `goalserve` **0** · `api-sports` **0** · `data sports group` **0**.
- 08-14 보고서에는 **5줄**: L133 「DSG … NPB 실제 입도 미검증」 · L134 「Sportradar / API-Sports / Goalserve … **가격 전부 미확인**」 · L320 「**DSG는 무료 트라이얼 CTA 존재 → 트라이얼로 NPB 실제 입도부터 검증**」 ← **0엔 지렛대가 §5 「다음에 볼 것」 7행 어디에도 없다.**
- 08-20 §2 범례가 「⚠**「미확인」은 없다**」고 선언하는데, 넷은 판정칸이 아니라 **표에 없어 선언이 공허하게 참**이 된다. §2-2 「라이선스 사업자(B2B)」 칸에 국내 3곳(JBD·데이터스타디움·1.02)만 있다 — **스코프 밖이 아니라 같은 분류 안의 구멍**.
- 「몰랐다」로 못 죽인다: 08-20 문서 L505 가 08-14 §2-C 를 **직접 인용**하고 L133·134 는 그 두 줄 아래다.
- ⚠**같은 결함이 하루 전 같은 파일에서 이미 고쳐졌다**: L278 「⚠**「없다」가 아니라 「`search=NPB` 한 질의의 전수를 봤고 거기엔 0건」이다**(2026-08-21 정정) … **정하는 법**: 질의 3~4개 추가(0엔·10분)」 — **수선 패턴이 ⑱ 에만 적용되고 §2-2 와 CLAUDE.md:115 에는 안 갔다.**
- 방어 시험 0건(이 주장을 방어하는 시험 파일 자체가 없다 — FAIL 이 아니라 **대상 없음**).

**심각도 조정 근거** — P1 → **P3**: ⑴ 배포물 영향 **0**(dist 15,443 전수 — `Sportradar` 0장 · `전수` 1장은 site.js 개발자 주석 · `20件` 66장은 전부 `120件` 부분문자열) ⑵ 결론이 안 움직인다(§3-B 권리 사슬이 제3자 재판매업자를 전부 이미 판정된 JBD 하류에 놓는다) ⑶ 침묵이 아니다(08-14 §2-C 에 살아 있고 CLAUDE.md:334 가 링크).
**P3 밑으로는 못 내린다**: ⑴ §0-11 판정 기준 실제 미달 ⑵ 범례 선언이 공허하게 참 ⑶ **자기치유 아님 + 부분수선 흔적**(「연 1회 재조사」가 이 넷을 명시 안 함).

**수정안(0엔 · 문서만 · 08-21 선례 복사)**
- CLAUDE.md:115 → 「판정한 20건 중 0건 · ⚠08-14 §2-C 의 상용 벤더 4건은 승계 안 됨 = **「안 쟀음」**」
- 08-20 §2-2 에 「⑦′ 해외 상용 재판매 — **미판정 · 08-14 §2-C 승계** · **정하는 법: DSG 무료 트라이얼**」 한 행
- `docs/metrics/README.md` §6 의 「전수로 쟀다. **0건이다.**」에도 같은 한정

**검증법** — `grep -c "Sportradar" docs/sources/2026-08-20-blocked-metrics-source-survey.md` ≠ 0 · `grep -n "전수 조사에서 0건" CLAUDE.md` 가 **분모와 미판정 목록을 같은 줄에** 담을 것(⚠분모 「20건」은 **이미 있다** — 빠진 것은 미판정 목록뿐).

**분류** — 신규 확정

---

## 3. 사살된 지적 — **다시 올리지 마라**

### ❌ `packages/metrics/src/ranking.ts:105` — 「같은 값에 다른 순위」(D8, P1 주장)
`dist/2025/ranking.html` 에 `.348` 이 6·7·8위로 나란히 서는 **관측은 재현됐다**. 그러나 **같은 값이 아니다** — `avg3` 는 round-half-up 이라 `.348` 구간은 [0.3475, 0.3485) 이고 분모마다 가능한 정수 분자가 정확히 하나다: 近本 222/638=0.34796 · 桑原 152/437=0.34783 · 岡林 220/633=0.34755. **크기 순서가 6·7·8위와 정확히 일치.** 105행은 표시 문자열이 아니라 무반올림 `rate.value` 를 비교하고, NaN 도달 경로가 없다(`rate()` 가 denominator 0 → null, eligible 필터가 value !== null 요구). 배포물 9장 전수 26,168 자격행: **진짜 동률 3,991건은 툴팁대로 같은 순위**, 순위가 다른 315건은 전부 반올림 표시 사례. 시험 13/13 pass.
→ **남는 것은 문구뿐**(별건 저등급): 툴팁이 「表示は小数3桁に丸めています・順位は丸める前の値で決めます」를 말하지 않는다.

### ❌ `packages/web/src/assets.ts` `.mf-ax:focus-visible` — 「선택 축에 초점 표시 0」(D5, P1 주장)
주장된 명령은 주장된 출력을 냈지만, **그 명령이 규칙 블록의 바이트 오프셋을 잰다** — CSS 캐스케이드는 **선언 단위**로 해소된다. 선언 단위 해석기(대조군으로 건전성 검증 완료)로 재면 델타가 0이 아니라 **2**다:
- `.mf-spoke` 의 `fill` → `<line>` 은 면적 0이라 무효 **(지적이 맞다)**
- **`.mf-lab` 의 `stroke:var(--tx)` 는 무경쟁으로 살아남는다** — `.mf-ax.on .mf-lab` 은 `fill`·`font-weight` 만 선언한다. `stroke-width` 초깃값 1 이고 **11px 한자 라벨**(打率/出塁/長打/選球/接触)에 붙으므로 「픽셀 하나 다르지 않다」는 **거짓**.
- 둘째 다리는 **방향이 반대**: `select(i)` 가 `.on` 을 옮기고 화살표 핸들러가 `select(next); el.focus()` → **`.on` 연출 전체(dot scale 2배 + 구단색, lab `--tx`+700, spoke `--tx-2`)가 초점과 함께 이동**한다.

---

## 4. 미검증(P2/P3) — **⚠전부 반증을 거치지 않았다**

**총 60건**(P2 31 · P3 29 · 중복 7건 병합 후). `refuteSkipped` 는 전 관점 0 이지만, **반증은 P1 만 대상이었다.** 아래는 각 관점의 1차 관측이며 **확정이 아니다.**

### 4-1. 파이프라인 · 정합성 (D2 · D3)

| # | 등급 | 위치 | 증상 요약 |
|---|---|---|---|
| 1 | P2 | `store/tools/load-archive.ts:284·415` | **`game.fetched_at` 이 취득 시각이 아니라 적재 시각.** 7,805행 distinct **1**(2018 경기도 「08-18에 받았다」). `player.last_seen_at` 도 distinct 1. ⚠**이미 두 번 고친 결함의 세 번째 자리** — `meta.ts:fetchedAtOf()` 를 이 파일만 안 부른다 *(D2·D3 병합)* |
| 2 | P2 | `store/src/derive.ts:204` | 투수 행 7칸(被安打·被本塁打·四球·死球·三振·失点·自責点) 파싱 실패 시 **격리·로그·종료코드 없이 0 저장** → 그 등판이 「무피안타·무실점」. 같은 파일 175~193 은 `outs` 에 대해 **정반대**로 처리(격리 + 미적재) |
| 3 | P2 | `store/src/load.ts:175` | `upsertGame` ON CONFLICT 가 `away_code`·`home_code`·`season`·`game_date`·`game_no` 를 **한 번도 갱신 안 함** + `revision` 도 안 오름(실행 대조 완료) |
| 4 | P2 | `.github/workflows/daily.yml:309` | **소급 불가 아카이브의 원격 사본이 정확히 하나.** `--clobber` 고정 이름, 세대 보관 0곳, 업로드 후 크기 검증 0 |
| 5 | P2 | `archiver/src/clock.ts:4` | **M6 를 강제한다는 린트가 존재하지 않는다**(eslint 설정 0 · lint 스크립트 0 · CI 단계 0). `clock.ts` 의 「유일한 곳」 단언이 거짓 — 실제 **9행** |
| 6 | P2 | `CLAUDE.md:75` | 走塁 대조 수치가 진행 시즌이라 이미 거짓: 문서 611/254/620 ↔ DB **618/255/627**, 2,395 ↔ **2,401**. ⚠**`steal.ts` 가 루별 주석은 완결 시즌으로 옮기며 규칙을 적어 놓고 머리말(:10-11)·SQL 주석(:85)에는 적용 안 함** *(D3·D9 병합)* |
| 7 | P3 | `aggregate/src/run-expectancy.ts:118` | RE 행렬이 **끝내기 절단 하프이닝**을 정상 이닝과 동일 취급. 2025 센트럴 50개 제외 시 `123\|0` Δ+0.026. **선택이 어디에도 안 적혀 있다** |
| 8 | P3 | `store/src/align.ts:144` | 라인스코어 파싱 실패 시 **그 경기 전 타석 득점이 0으로 덮인다**(현재 사례 0건). `pbpEvents` 대입이 `deriveRuns` 앞에 있는 것이 원인 |
| 9 | P3 | `store/migrations/001-initial.sql:33` | **M4 의 `as_of` 가 11개 테이블 어디에도 없다** → CLAUDE.md §0 제약 10 판정 기준이 현재 **거짓** |
| 10 | P3 | `archiver/src/archive.ts:124` | 「받았는데 안 바뀌었다」를 경기·일정·予告先発에서 **기록 안 함**(`checkedAt` 은 선수 페이지만). `etag`/`lastModified` 둘 다 null 이라 **매번 200 + sha 비교가 상시 경로** |
| 11 | P3 | `.github/workflows/daily.yml:134` | 재시도 슬롯이 「직전 실행 실패?」를 물을 때 **브랜치·이벤트 미필터** ⚠`gh` 인증 없어 **실행 이력 미확인** |
| 12 | P3 | `store/tools/load-archive.ts:270` | `inProgress` 경기가 **`game` 에도 `upcoming_game` 에도 없는 상태**가 생긴다 ⚠발생 빈도 미측정 |

### 4-2. 보안 · 공급망 (D1)

| # | 등급 | 위치 | 증상 요약 |
|---|---|---|---|
| 13 | P2 | `archiver/src/fetcher.ts:45` | **429 에서 `Retry-After` 무시** + 3s/6s/12s 재시도, 차단기 0 → 전면 429 시 페이지 수 × 4 요청 |
| 14 | P3 | `archiver/src/players.ts:16` | `playerKey()` 가 ID 형태 미검사 → `../` 로 **아카이브 루트 밖 쓰기**(실행 대조 완료). `web/src/site.ts:238` 은 같은 값에 정규식 + throw |
| 15 | P3 | `daily.yml:86` | `permissions:` 가 워크플로 레벨만 → 읽기만 하는 `decide` 잡도 `contents: write` |
| 16 | P3 | `daily.yml:167` | `npm ci` 가 checkout 자격증명 위에서 서드파티 install 스크립트 실행(third-party 101 · 실제 실행 2건: esbuild·workerd). *(기지 · 워크플로 주석이 이미 인정)* |

### 4-3. 화면 · 접근성 · 디자인 (D2c · D3b · D5 · D6 · D6b · D6c)

| # | 등급 | 위치 | 증상 요약 |
|---|---|---|---|
| 17 | P2 | `assets.ts:2288` | 블록 편집기 `renderEditor()` 가 상호작용마다 `host.textContent=""` 로 전체 재생성 → **방금 누른 컨트롤이 통째로 교체**. 같은 파일 표 정렬(2648)은 노드 재사용. 선수 페이지 6,207장 ⚠실브라우저 focus 미관측 |
| 18 | P2 | `assets.ts:135` | 검색 드롭다운 선택 표시가 **배경색 하나**, 대비 라이트 **1.13** · 다크 **1.10**. 포커스는 입력칸에 머물고 `say()` 도 몇 번째인지 말 안 함 |
| 19 | P2 | `assets.ts:125` | 입력칸 **테두리가 유일한 식별 표시인데 1.51~1.69:1**. `.mfind input` 은 채움이 감싸는 블록과 **완전히 같음**(면 대비 1.000) → 4자리 × 2테마 **8/8 FAIL** |
| 20 | P2 | `assets.ts:598` | **호버로 연 툴팁 위로 포인터를 옮기면 사라진다**(`#tip` 에 mouseenter 0건 · `gap=8` 빈틈). 마우스 클릭은 열지 않고 **닫는다**. `#tip` 은 `overflow-y:auto` 스크롤 상자 |
| 21 | P2 | `assets.ts:1433` | 「月別 勝-敗-分」 막대에서 勝(구단색)\|敗(`--hair-2`) 대비 — 라이트 4/12 · 다크 8/12 미달, **합집합 12/12**. 108장 806막대. ⚠`.trecent` 의 근거 주석(「월별 막대가 이미 배경으로 말한다」)이 다크 롯데 1.10 · 오릭스 1.08 에서 **거짓** |
| 22 | P2 | `assets.ts:510` | 등급 5색의 불변식(「흑백에서도 순서가 남게」)이 **실측으로 거짓** — 휘도가 단조가 아니라 **V자**. 다크 vgood\|vbad **1.02:1**. 3/5 가 비텍스트 3:1 미달. 등급 글자는 `.vh` 라 안 보임. 3,298장 29,006개 |
| 23 | P2 | `layout.ts:23` | **M12 를 그리는 `stateNote()`/`DataState` 의 프로덕션 호출처 0곳** → 「수집실패」·「시즌외」가 dist **0/15,340**. 시험이 죽은 코드를 초록 통과 |
| 24 | P2 | `layout.ts:117` | 오프시즌에 「시즌 종료」를 말할 수 없다 — `lagDays>3` 만 보고 **「取得に失敗している可能性があります」**(붉은 경고). 현행 시즌 **1,484/15,340장** *(기지)* |
| 25 | P2 | `query.ts:2358` | `143試合換算` 이 부동소수 + `Math.floor` 로 **1 작게**: `(47/143)*143 = 46.99999…` → 46. 換算 108행 중 **5행**이 「現在 47 … 換算 46」. 시험은 `pace` 를 픽스처에 손으로 박아 못 잡음 |
| 26 | P2 | `layout.ts:1` / `player-page.ts:815` | **`html\`\`\`` 템플릿 안 HTML 주석이 배포물에 그대로**: **337,577개 · 107.42MiB · 15,340/15,340장**(한국어 개발 주석 · 감사 이력 · 미수정 결함 서술). 제거 단계 0건. `source-figures.test.ts` 는 「주석은 화면에 안 나간다」 전제라 구조적으로 못 봄 *(D6b·D9 병합 · 확정 #10 B5 와 동일 뿌리)* |
| 27 | P2 | `glossary.ts:42` | 용어 정의 **56항목의 도달 경로가 툴팁 하나뿐**. 용어집·도움말 페이지 dist **0장**, 한 화면 최대 노출 28개. 스플릿 막대에는 `data-term` **0건**. 火消し率 툴팁은 555px 라 320×568(가용 480)에 **원리적으로 안 들어감** *(기지)* |
| 28 | P2 | `player-page.ts:1058` | 구원 자격 문구 **「未満（35回 / 35回）」** — 도달치=기준치인데 「未満」. `Math.floor(outs/3)` 이 원인. 9시즌 구원 1,919 중 **12건** *(확정 #5 와 같은 뿌리 · 함께 고칠 것)* |
| 29 | P3 | `query.ts:766`·3658 | `pitchesPerOut` 이 `rate()` 를 안 거치고 **outs===0 체크 없이 나눔**. `pitching_line` 63,315행 중 outs=0 & pitches>0 이 **773건**(경기 단위) ⚠시즌 집계 잔존 여부 미측정 |
| 30 | P3 | `assets.ts:249` | `.mf-grid` 1.580/1.557 · `.mf-spoke` 1.336/1.212 — 값은 안 움직였으나 **전제가 바뀌었다**(도형·꼭짓점이 `--tx-2` 로 이동, 6.61/6.67 통과). 고치지 말고 **전제를 시험 옆에 못 박을 것** *(기지)* |
| 31 | P3 | `assets.ts:678` | `.sortable i{opacity:.3}` → 정렬 가능 표시 `↕` 가 **1.554/1.765**. 정렬된 뒤에는 문제 없음. ⚠`css-contrast.test.ts` 가 **이것도 못 잡는다**(규칙에 `color:` 없음) |
| 32 | P3 | `assets.ts:596` | 고정 바 4겹인데 `scroll-padding-top` 사다리에 **`.pickbar` 만 빠짐**(규칙 3개 전수 확인). 같은 파일 주석은 셋이라고 적음. ⚠실제 가림은 브라우저 필요 |
| 33 | P3 | `assets.ts:659` | 같은 「구단 색 사각형」이 **9벌 따로**, 크기 5종 · 테두리 3종. **대비 보증(링)이 붙은 것은 36장, 안 붙은 것이 8,955장(58.4%)**. 그 자리 주석은 「모든 표가 같은 한 벌을 쓴다(M1의 정신)」 |
| 34 | P3 | `assets.ts:653` | **forced-colors 대응 규칙 1,848줄에 0개**. 상태 규칙 25개 중 **6개**가 background/box-shadow/border 외 채널 없음 |
| 35 | P3 | `player-page.ts:1431` | 球場別 안내문이 **접기가 없어도 무조건** 「まとめています」 — 문장 5,668회 vs 실제 접힘 3,893회 = **1,775회(31.3%)가 근거 없는 주장** |
| 36 | P3 | `assets.ts:1679` | `.chip` 에 `transition:all`(실제 변화는 color·border-color·background·font-weight). 배포 CSS `transition:all` 1건 |
| 37 | P3 | `assets.ts:1701` | `.roster a:hover{padding-left:4px}` 를 `transition:padding-left` 로 — 레이아웃 속성 애니메이트. `<season>/players.html` 8장 |
| 38 | P3 | `assets.ts:238` | transition 31건 중 **29건(93.5%)**이 비합성 속성. box-shadow 2곳. *(CLAUDE.md 가 「합성 전용」을 제약으로 못 박은 바 없음 — 우선순위 낮음)* |

### 4-4. 성능 (D4)

| # | 등급 | 위치 | 증상 요약 |
|---|---|---|---|
| 39 | P2 | `web/src/pages.ts:540` | 순위표 9장이 **HTML 981KB~1.02MB · 오프닝태그 28,525개**(`<td>` 14,701 · `<tr>` 3,004 · 선수 링크 2,897)를 한 테이블에. brotli 40.7KB 이므로 **네트워크가 아니라 파싱·DOM 비용** ⚠TBT/INP 미측정 |
| 40 | P2 | `web/tools/build.ts:86` | `loaded` 가 전 9시즌 `SiteData` 를 동시 보유, 마지막 시즌 쓰기까지 해제 0. **`loadSite` 만 9회 = RSS 587MB → 1,466MB**(렌더·쓰기 제외한 **바닥값**) *(기지)* |
| 41 | P3 | `web/tools/build.ts:42` | Pages 파일 상한 **15,443/20,000 = 77.2%**. 시즌당 1,559~1,795(2020 단축) → **여유 완결 시즌 2.5~2.6개분** *(기지)* |

### 4-5. 문서 · 소스 조사 (D7 · D9 · D10)

| # | 등급 | 위치 | 증상 요약 |
|---|---|---|---|
| 42 | P2 | `docs/operations/deploy.md:391` | Pages 상한 칸이 5시즌 시절 값 — 문서 **8,611파일 / 43%** ↔ 실측 **15,443 / 77.2%**. 같은 사실에 **네 수**(deploy.md 8,611 · CLAUDE.md 15,434 · handover 15,464 · 실측 15,443). ⚠**CLAUDE.md:222-224 가 경고한 그 실패가 이웃 파일에서 재발** *(D7·D9 병합)* |
| 43 | P2 | `CLAUDE.md:299` | 「적재가 여는 것은 `box` 와 `playbyplay` **둘뿐**」 — `load-archive.ts:370` 이 `roster.html.gz` 도 연다. **같은 절의 표가 반대를 적어 문서가 자기 모순** |
| 44 | P2 | `docs/handover/2026-08-21-session.md:137` | 「`playerPath` 계열 21곳」 — **`playerPath` 라는 함수가 존재하지 않는다**(web grep 0건). TS **21곳 손조립** + 클라 JS **5곳**. `teamPath` 도 20 이 아니라 13(주석 제외 11) |
| 45 | P2 | `CLAUDE.md:236` | 2021 품질 기록 「정규 **890**」 ↔ DB played **858**(890 = 858 + 미실시 32). 같은 문장이 앞에서 「12팀 전부 143경기」(=858). 인접 2018 줄은 858 로 **다른 정의** |
| 46 | P2 | `CLAUDE.md:273` | 백필 예산이 절반 — 「9시즌 ≈ 16,389요청 · 13.7~22.8시간 · 149MB」 ↔ 매니페스트 **33,136파일 / 266.8MiB**(→ 27.6~46.0시간). **바로 다음 줄의 시즌당 실측(3,636 × 9)과 자기 모순**. 원인: 경기당 2장 시절 견적 |
| 47 | P2 | survey `:84` | NPB BIP 판정 첫 사유가 **「가격 30배」** — 같은 문서 §5 와 CLAUDE.md 가 **금지한** 「가격을 현재의 사실로 쓰기」. 1차 사실은 「카탈로그에 그 데이터가 없다」이고 가격은 「물어보지 않았다」 |
| 48 | P2 | survey `:280` | GitHub 행이 **126건 중 15건**만 보고 「**전부** 스크레이퍼」 전칭. 그 15건 중 **4건은 NPB 무관**(오탐)이라 **자기 표본에 대해서도 거짓**. ⚠**바로 위 줄(L278)은 08-21 에 같은 규칙으로 정정됐다** |
| 49 | P2 | survey `:481` | 「재조사 시 먼저 여기를 열어라 — **외부 요청 0회로 재현**」 ↔ `.gitignore:25` 가 `docs/sources/samples/` 를 무추적 확정(`git ls-files` **0**). **결정이 71분 먼저 났는데 문서가 안 따라갔다** |
| 50 | P2 | survey `:52` | 「⚠**「미확인」은 없다**」 선언인데 **표에 아예 없는** 무료 NPB 통계 사이트 2곳(`baseball-data-store.com` · `baseball-data.jp`) — docs 전수 grep **0건**. robots 200 / 본문 403·SPA 미도달 ⚠**「A 를 준다」고 쓰지 마라 — 본문 못 봄** |
| 51 | P2 | `CLAUDE.md:201` | 「12구단 일괄관리 + 외판 모색」이 **확신도가 다른 두 주장을 한 라벨로** 묶음. 앞쪽은 **KONAMI 2025-10-10 공식 보도자료 1차 확인**(「一括してシステム管理するDMPを運用」), 뒤쪽(外販)은 여전히 근거 0. 같은 원문에 「外販」 **0건** + 「테스트 2025-10-10 · 정식 2026」이 우리 문서에 누락 |
| 52 | P3 | `query.ts:2904` | 아웃→이닝이 **6벌**, `metrics` 정본 `inningsPitched` 는 **프로덕션 호출 0곳인 죽은 export**, `query.ts` 는 import 한 `innings()` 두고 인라인 재구현 *(확정 #5 와 같은 뿌리)* |
| 53 | P3 | `scripts/freshness.ts:20` | 「古い」의 뜻이 두 벌 — `log.html` 임계 **2일** + 4감시 OR vs 나머지 15,339장 임계 **3일** + 경기 지연 하나. ⚠아직 한 번도 안 갈림(`stale:true` 0/36) |
| 54 | P3 | `query.ts:3277` / `calendar.ts:206` | 주석이 「2022~2025 4시즌 48개 전부 143」 — 보유는 9시즌이고 **2020은 120**. 코드는 `regularSeasonGames(season)` 로 맞고 **주석만 낡음** |
| 55 | P3 | `glossary.ts:101` | wRC+ 툴팁 「**当サイトはまだ球場係数を出していません**」 — PF 는 산출돼 문서 14값 + 시험에 박혀 있다. ⚠「もう反映しています」로 바꾸지 마라(배선 0곳) |
| 56 | P3 | `aggregate/src/bunt.ts:197` | 「打順一巡」이 타순 순회가 아니라 **그 투수 × 그 타자 대전 횟수**(키가 `batterId`). 2025 「1巡目」 38,435 중 **773(2.0%)**이 9타자 이상 뒤. 3巡目 이상 **542타석(4.8%) 과소**. ⚠**정의서에 항목 자체가 없다**(글로서리 56 중 8이 정의서 부재) |
| 57 | P3 | `docs/metrics/README.md:1188` | 「2026 투수 **356명**」 — DB **358**, 날짜 라벨 없는 현재형인데 **바로 위 표는 라벨 있음** |
| 58 | P3 | `web/tools/marks.ts:156` | `source-figures.test.ts` 스캔 루트가 `packages/web/src` **한 겹(비재귀)** → tools/ 와 6개 패키지 전부 검사 밖. 그 밖에 화면 문자열 「**695人**ぶんと…」 실재(DB 1,640) |
| 59 | P3 | survey `:174`·`:369` | ⑩이 「**「못 찾았다」**」라고 정확히 적고 같은 항목에서 **「12구단이 전부 공개해도 A 는 열리지 않는다」** 전칭 — 근거는 **야쿠르트 1구단**. 같은 항목이 **야쿠르트 자신에게 위음성이 난다**고 적음 |
| 60 | P3 | survey `:371` | 「**연 1회 재조사** — 판정을 영구화하지 않기 위한 최소 장치」가 **어디에도 구현 안 됨**(.yml/.ts/.json **0건**, .md 4파일뿐). ⚠이 저장소는 `doc-figures.test.ts` 로 **할 줄 안다** |

---

## 5. ⚠재지 못한 범위 — 「0건」과 「안 쟀음」의 구별

### 5-1. 반증 상한
- **P1 확정 11건은 2회 반증을 거쳤다**(`refuteSkipped` 전 관점 **0**).
- ⚠**위 P2/P3 60건은 반증 0회다.** 블라인드 반증 대상이 P1 이었고, 그 절차에서 **P1 2건이 사살**되고 **6건의 수치·검증법·수정방향이 정정**됐다(#2 수정방향 반전 · #5 게이트 정규식 · #6 게이트 무효 · #7 분모 · #8 인용증거 3곳 · #1 검증법 전면 교체). **같은 비율이면 60건 중 상당수의 세부가 틀렸다고 가정하라.**

### 5-2. 브라우저 — **0회**
`node_modules/.bin` 에 playwright/puppeteer **0건**(실측), `npm run preview` 금지. 따라서 아래는 전부 **「안 쟀음」**이다:
- **WCAG 2.5.8 Target Size**(`.sortable` ≈25.5px · 표 안 `td a` ≈18.6px — CSS 산수로는 간격 예외 통과할 것 같지만 **실측 아님**)
- **WCAG 2.4.11 Focus Not Obscured**(#32 pickbar 는 CSS 사다리의 구멍이지 **관측된 가림이 아님**)
- **INP / TBT / LCP / long task**(#39 순위표는 정적 크기만 쟀다), site.js 런타임 프로파일
- `renderEditor()` 재생성 시 `activeElement` 실제 리셋(#17) — 테스트 DOM 스텁이 focus 를 모델링 안 함(`activeElement` grep 0건)
- `insertBefore` 이동이 `rise` 애니메이션을 재생시키는지
- **낭독기 실기(NVDA/VoiceOver) 0회** · **색각 시뮬레이션 0회** · **forced-colors 실렌더 0회**(선언 유무만 셌다) · **320px 실렌더 0회** · **인쇄 실출력 0회**
- **Firefox·Safari·하이브리드 입력기기 0회**(handover 가 「구조적으로 못 쟀다」고 적은 그대로)
- **클라이언트가 조립하는 화면의 런타임 DOM**(`compare`·`matchup`·`days` 각 9장) — 정적 HTML 만 봤다. 그 화면의 표 구조·빈 상태·紋 겹침·분모 가드·NaN 여부는 **이번 분모에 없다**

### 5-3. 데이터 · 배포물의 분모
- 로컬 `data/bb.sqlite` 는 **2026-08-18 스냅샷**(최신 경기일 2026-08-16). CI 정본(`ops/collection-log.jsonl` 08-20)은 **18경기 앞선다** → 진행 시즌이 걸린 어긋남(#6 走塁 · #57)은 **CI 에서 더 크다**.
- ⚠**배포물 분모가 셋 다 다르다**: CLAUDE.md **15,434** · 로컬 실측 **15,443**(html 15,340) · handover **15,464**. **왜 다른지 안 쟀다.**
- **CI 배포물 자체를 못 봤다**(Cloudflare Access 뒤 · 작업규칙 11). D1 2차의 302 실측이 유일한 외부 접촉.
- **2016~2017 시즌은 아카이브에 없다.** CLAUDE.md 가 경고한 「옛 시즌 투수 귀속이 얇다」(2016 표본 1경기 34/88)는 이번에 안 쟀다 → `pitcher_id IS NULL` **0건은 보유 9시즌 한정**.
- **2군·오픈전 행이 DB 에 0** → 「섞이지 않는다」가 아니라 **없어서 못 섞인다**.
- **더블헤더·서스펜디드·재편성 중복 0건** → 식별자가 그것을 감당하는지 **시험된 적이 없다**.
- `career_*` 커버리지는 **현재 등록 선수 980명뿐** → 독립 대조(3,432/1,857/1,290 선수-시즌)의 분모는 **리그 전량이 아니다**.
- **콜드게임·시간제한 절단 하프이닝 미측정**(#7 의 50개는 **끝내기만** — 그 수는 **하한**).

### 5-4. 실행하지 않은 검증
- **`npm test` 를 돌린 관점은 D9 하나뿐**(tests **1,777** · pass 1,777 · fail 0 · exit 0 · 205.6초). ⚠**확정 P1 8건이 전부 이 초록 안에 있다.** 다른 관점의 「시험이 잡는가」는 「0건」이 아니라 **「안 쟀음」**. (handover 는 1,775 로 적는다 · +2)
- **라이브 뮤테이션은 138 테스트 파일 중 2개만**(`standings.ts` 3/3 FAIL 재현 · `draw.ts` 2건). 나머지 **136 파일 미실행**. 기존 뮤테이션 스크립트 4건(`park`·`venue`·`runner-seq`·`tip-marks`)은 **공유 워크트리 오염 위험**으로 생략(draw.ts 가 실제로 타임아웃으로 뮤테이션 상태로 남았다가 즉시 복원).
- **`npm audit` / CVE 대조 미실행**(네트워크 차단) → 「알려진 취약 의존 0건」이라고 읽지 마라.
- **`gh` 인증 없음** → 워크플로 실행 이력·릴리스 자산의 실물(개수·크기·업로드 이력)을 **한 번도 못 봤다**(#4·#11 미검증).
- **동시 실행 미재현** · **SQLite 잠금 경합 미측정**(`openDb` 에 `busy_timeout`·WAL 없음은 읽었으나 SQLITE_BUSY 가 조용한지 안 쟀다).
- **wOBA 계수·wRC+·SRC·RE 행렬·파크팩터 값 미재현**(폴백 감지 시험 존재만 확인).
- **`packages/aggregate` 의 쓰기 경로**(D2 관점 밖 — 「결함 0건」이 아니라 **「대상 밖」**) · `packages/parser`/`archiver`/`store` 내부 중복 구현 미조사.
- `query.ts` **5,691줄 전체를 라인 단위로 못 읽음**(발췌·grep 위주).
- **정렬·동률 처리(M3) 스캔은 5열 표만** — 열 수가 다른 표(구단 순위·팀 페이지 내부 순위)는 **안 쟀다** → 315쌍은 **하한**.

### 5-5. 소스 조사(D10)의 미도달
- **본문 미도달 3곳**: `baseball-data-store.com`(403) · `baseball-data.jp`(SPA) · `fangraphs.com/leaders/international/npb`(403) → **「A 를 준다/안 준다」를 판정할 수 없다. 「판정이 없다」만 확정.**
- **실접속 0회**: Data Sports Group · Sportradar · API-Sports · Goalserve(확정 #11 은 「그들이 A 를 준다」가 아니라 **「우리 표에 판정이 0건」**이다).
- **질의 미확대**: Kaggle 추가 0회 · HuggingFace 1질의만 · **GitHub 111/126 여전히 미확인**.
- **재접속 0회**: 1.02 · Yahoo · NPB+ · 데이터스타디움 · npbstats · npbbasement · 12구단 — 저장 원문 재독만.
- `docs` **20개 중 4~6개만 대조**. `docs/decisions/2026-08-14-paid-track-verified.md`(580줄) · `docs/superpowers/**` · `docs/mockups/*` 는 **수치를 뽑기만 하고 대조 안 함**.
- CLAUDE.md DB 파생 32그룹 중 **6그룹 미재현**(xFIP 극단 시뮬 · wOBA 계수 이행 459명 · 2016 표본 34/88 · 2018 오릭스 148경기 · roster 858/858 · 배번 782 중 10).
- `docs/metrics/README.md` 의 **파크팩터 3표 · 火消し 계열 7수 · SRP 2,215 · GIDP 3,177 미대조**.
- HTML 주석 **337,577개 중 개별 값은 3건만** 확인.
- `docs/operations.md:58`(D1 쓰기 예산 70.8%)이 낡은 것은 확인했으나 **D1 이 지금도 쓰이는지 확인 못 해 심각도 미부여**.

---

## 6. 수정 순서 제안 — 의존 관계 반영

### 0단계 — 즉시(다른 것과 무관 · 이미 끝난 일 마무리)
1. **dist 재빌드 1회** → 확정 #8(game-page 対 링크 81,150곳) · #9(team-page 923곳) 이 배포물에서 해소된다. ⚠**9시즌 전체 빌드에서 link-check 통과를 반드시 볼 것**(2026·2018 외 7시즌은 「안 쟀음」).
2. 인수인계·커밋 주석의 **분자 3건 정정**: 1,547 / 833 → **923**, handover 의 「35,000곳」 → **81,150곳**, 「`playerPath` 21곳」 → **존재하지 않음**(#44).

### 1단계 — 침묵하는 인가·데이터 경계(자기치유 없음 · 잠재 손실 최대)
3. **확정 #1** Access 2단 게이트(앞단 무자격 curl 은 시크릿 0 · 배포 전에 돈다) + `deploy.md:226-228` 정정.
4. **#1·#2(P2)** `fetched_at` / `derive.ts` 0 덮기 — 둘 다 **M4·M7 의 뿌리**이고, 고치면 #3(ON CONFLICT)·#8(align.ts)의 검증 게이트를 공유한다. 배포 게이트에 **「투수 실점 합 = 상대 팀 득점」**(현재 7,518/7,518 통과)을 9번째로 걸면 #2·#8 이 동시에 잡힌다.
5. **#4(P2)** 아카이브 원격 사본 회전 + 업로드 크기 검증 — **소급 불가 자산**이라 다른 어떤 것보다 되돌릴 수 없다.

### 2단계 — 틀린 수 · 틀린 뜻(값이 화면에 나가고 있다)
6. **확정 #3** `streaks.ts` 9.23(b) + 형제 9.23(c) 33건 — 정의서 항 신설(M3)이 동반이라 문서 작업과 묶어라.
7. **확정 #4** `走者あり` 라벨 — `BASE_LABEL` 이 정답을 갖고 있으므로 **거기서 이름을 빌려라**(M1). 글로서리 `onBase` 항목 + `data-term` 승격이 동반.
8. **확정 #5 + #28 + #52** 아웃→이닝 **6벌을 `innings()` 한 벌로**. ⚠**한쪽만 고치면 선수 페이지와 순위표가 서로 다른 수를 말한다** — 반드시 함께. 문서(`README.md:1201`)·시험(`team-page.test.ts:192`) 기댓값도 같이.
9. **#25(P2)** `143試合換算` 정수 곱 — 5행짜리 한 줄 수정이지만 **같은 행 안의 모순**이라 눈에 띈다.

### 3단계 — 표시명(수정 방향 결정이 먼저 필요)
10. **확정 #2** — ⚠**「최신이 이긴다」로 고치지 마라**(620쌍 중 298 → 327 로 악화). **시즌 스코프 표시명**이 올바른 방향이고, 이건 **스키마·렌더 양쪽에 걸리는 결정**이라 사용자 확인이 필요하다(작업규칙 3). 회귀 시험은 「그 페이지의 시즌에 박스가 쓴 이름과 같은가」로.

### 4단계 — 접근성·디자인(가드를 먼저 넓혀라)
11. **`css-contrast.test.ts` 를 세 축으로 확장**(이 하나가 확정 #6·#7 과 미검증 #21·#22·#31·#33 을 동시에 덮는다):
    - **자손 상속**: `opacity<1` 규칙의 서브트리 글자색 합성 → 확정 #7(`.gcard.off`) · #31(`.sortable i`)
    - **`background` 축**: `TEAM_MARKS` 의 `prop` 에 추가 → 확정 #6(`.gstars li i`) · #21(`.tbar`) · #33(칩 9벌)
    - **등급 5단**: 패널 3:1 + 이웃 단계 구별 → #22
    ⚠**넓히지 않으면 세 번째 `.off` 가 또 통과한다**(지금 그 시험은 오늘 잡을 수 있는 규칙이 **0건**인 채 초록이다).
12. 확정 #7 → #6(색) → **#6-별건(SR `<i></i>` 17,307/21,812)** → #21 → #22 → #19 → #18 → #20 순. ⚠확정 #6 의 **검증법을 먼저 교체**하라(입력 hex 가 아니라 출력 채널).

### 5단계 — 문서 · 조사(런타임 영향 0 이지만 판단의 입력)
13. **#26(P2) 주석 제거**를 먼저 — 확정 #10 의 B5 와 미검증 #26 이 **한 번에 닫히고** 배포물이 107MiB 줄어든다. ⚠`assets.ts` 는 파일 전체가 템플릿 리터럴이라 **가장 위험한 파일**(`\d` 함정과 같은 자리).
14. **#42** Pages 상한 칸을 실측으로 → 그 다음 **#41** 완화책(소급 시즌 경기 페이지 통합) 착수. ⚠**2시즌만 더 넣으면 벽**이다.
15. 확정 #10 · #11 + #6·#43~#51·#57~#60 문서 정정. **가드를 같이 넣어라** — `doc-figures.test.ts` 의 `DOCS` 에 `public-release-assessment.md`·`deploy.md` 추가(#57 대상), `source-figures.test.ts` 의 `SRC_DIR` 재귀화(#58 · 넓혀도 새 히트 1건뿐), **조사 기준일 만료 시험**(#60 · 365일 · 외부 요청 0회).

### 6단계 — 구조(급하지 않지만 재발 방지)
16. **#5(P2) M6 린트** — 지금 「린트로 강제한다」가 CLAUDE.md 에만 있고 실행되는 것이 0이다.
17. **#23(P2) M12 배선 또는 삭제** — ⚠**시험만 남기는 선택지는 없다**. #24(오프시즌 술어)와 같은 작업이므로 함께.
18. **#27(P2) 용어집 페이지 1장** — #4(글로서리 `onBase`)·#20(툴팁 555px)·#56(打順一巡 정의 부재)이 전부 여기로 수렴한다. Pages 파일 +1 장.
19. **#44 `playerPath` 헬퍼 신설**(TS 21곳 + 클라 5곳) · **#9 의 뿌리**(`HomeStreak`/`HomeMilestone` 이중 렌더) 통합 — 링크를 더 넓히기 **전에**.

---

## 부록 — 병합 이력(같은 결함을 다르게 말한 것)

| 병합 후 | 원 보고 |
|---|---|
| 확정 #5 (`query.ts:524` 47.7回) | D7-P1 + D8-P2(`query.ts:524`) |
| 확정 #7 (`.gcard.off`) | D5-P1(assets.ts:1351) + D6-P2(assets.ts:1351) |
| 확정 #8 (`対` 링크) | D3b-P1(game-page.ts:202) + D6b-P2(game-page.ts:97) |
| 미검증 #1 (`fetched_at`) | D2-P2(load-archive.ts:415) + D3-P2(load-archive.ts:284) |
| 미검증 #6 (走塁 수치) | D3-P2 + D9-P2(둘 다 `CLAUDE.md:75`) |
| 미검증 #26 (HTML 주석) | D6b-P2(layout.ts:1) + D9-P3(player-page.ts:815) · 확정 #10 의 B5 와 동일 뿌리 |
| 미검증 #42 (Pages 상한 문서) | D7-P2 + D9-P2(둘 다 `deploy.md:391`) · D4-P3(build.ts:42)와 연동 |

**라인 앵커 드리프트 3건**(반증에서 실측 정정): 확정 #6 → `assets.ts:1385`(1380 아님) · 확정 #7 → `assets.ts:1356`(1351 아님) · 확정 #10 → `store/migrations/016-birth-year.sql` 이 원인(문서가 지목한 곳 아님).